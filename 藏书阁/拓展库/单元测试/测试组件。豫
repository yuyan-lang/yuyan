

寻观「标准库」之书。

「成功数」乃「引用类」于「整数」也。
「成功数」者「新引用」于「零」也。

「失败数」乃「引用类」于「整数」也。
「失败数」者「新引用」于「零」也。

「打印当前成功失败数」乃化「有」而「有」也。
「打印当前成功失败数」者会「无」而
    「打印行」于（「串组合并」于（「缀」于『当前成功数：』于（「缀」于（「表示」于（「读引用」于「成功数」））于（「缀」于『    当前失败数：』于（「缀」于（「表示」于（「读引用」于「失败数」））于「罄」）））））也。

「断言为阳」乃化「爻」而「有」也。
「断言为阳」者会「结果」而
    虑「操作」者若「结果」
    则（「写引用」于「成功数」于（「加」于（「读引用」于「成功数」）于「一」））
    否则（「写引用」于「失败数」于（「加」于（「读引用」于「失败数」）于「一」））
    而（「行」于「打印当前成功失败数」）也。

「断言串相等」乃化「字符串」而化「字符串」而「有」也。
「断言串相等」者会「期待」而会「实际」而
    虑「结果」者「串相等」于「期待」于「实际」而
    虑「操作」者若「结果」
    则（「写引用」于「成功数」于（「加」于（「读引用」于「成功数」）于「一」））
    否则（「写引用」于「失败数」于（「加」于（「读引用」于「失败数」）于「一」））
    而（（「打印当前成功失败数」于「元」）；
    若「结果」则「元」否则（「打印行」于（「串组合并」于（「缀」于『期待：』于（「缀」于「期待」于（「缀」于『，实际：』于（「缀」于「实际」于「罄」）））））））也。

「断言」乃化「爻」而「有」也。
「断言」者「断言为阳」也。
