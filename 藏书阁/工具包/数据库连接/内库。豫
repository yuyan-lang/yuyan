寻观「标准库」之书。
寻观「标准库」之「操作系统」之「子进程」之书。
寻观「库应」之书。

「：
内库者 SQLite 也。其库内嵌于程，无独立之服。每问乃启一程，程毕而连绝。
白话：内库指 SQLite。当前每次查询都启动一个 sqlite3 进程，不保持长连接。
：」

「造内库问参」乃化「字符串」而化「字符串」而（「列」于「字符串」）也。
「造内库问参」者会「库址」而会「问辞」而
    「列」【
        『-batch』，
        『-noheader』，
        『-separator』，
        「字节转字符串」于「九」，
        『-nullvalue』，
        『NULL』，
        『--』，
        「库址」，
        「问辞」
    】也。

「以内库程问」乃化「字符串」而化「字符串」而化「字符串」而「库应」也。
「以内库程问」者会「程名」而会「库址」而会「问辞」而
    「获取输出」于「程名」于（「造内库问参」于「库址」于「问辞」）也。

「内库问」乃化「字符串」而化「字符串」而「库应」也。
「内库问」者会「库址」而会「问辞」而
    「以内库程问」于『sqlite3』于「库址」于「问辞」也。

「内库可通乎」乃化「字符串」而「库应」也。
「内库可通乎」者会「库址」而「内库问」于「库址」于『select 1;』也。
