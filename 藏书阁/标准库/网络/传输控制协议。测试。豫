寻观「标准库」之书。
寻观「标准库」之「网络」之「传输控制协议」之书。
寻观「拓展库」之「单元测试」之「测试组件」之书。


「必须成功」乃承「甲」而化（「操作结果」于「甲」）而「甲」也。
「必须成功」者受「甲」而会「结果」而
    鉴「结果」而
    有「操作成功」于「值」则「值」
    或有「暂需等待」于「值」则（「甲」也（「发生事故」于『传输操作仍需等待』））
    或有「连接已关闭」则（「甲」也（「发生事故」于『连接提前关闭』））
    或有「操作失败」于「状态」则（「甲」也（「发生事故」于（「错误消息」于「状态」）））
也。

「必须等待」乃承「甲」而化（「操作结果」于「甲」）而「有」也。
「必须等待」者受「甲」而会「结果」而
    鉴「结果」而
    有「暂需等待」于「值」则「元」
    或有「操作成功」于「值」则「发生事故」于『传输操作本应暂需等待』
    或有「连接已关闭」则「发生事故」于『连接意外关闭』
    或有「操作失败」于「状态」则「发生事故」于（「错误消息」于「状态」）
也。

「等待连接完成」乃化「传输连接」而「传输连接」也。
「等待连接完成」者会「连接值」而
    虑「事件」者「必须成功」授以「整数」于（「等待可写」于「连接值」于「一零零零」）而
    虑「无」者「断言」于（「事件可写吗」于「事件」）而
    「必须成功」授以「传输连接」于（「完成连接」于「连接值」）
也。

「获取客户端套接字」乃化（「操作结果」于「传输连接」）而「传输连接」也。
「获取客户端套接字」者会「结果」而
    鉴「结果」而
    有「操作成功」于「连接值」则「连接值」
    或有「暂需等待」于「连接值」则「等待连接完成」于「连接值」
    或有「连接已关闭」则（「传输连接」也（「发生事故」于『连接提前关闭』））
    或有「操作失败」于「状态」则（「传输连接」也（「发生事故」于（「错误消息」于「状态」）））
也。


虑「监听套接字」者「必须成功」授以「传输连接」于（「监听」于『127.0.0.1』于「零」于「一六」）而
虑「无」者「必须等待」授以「传输连接」于（「接受」于「监听套接字」）而
虑「端口」者「必须成功」授以「整数」于（「获取本地端口」于「监听套接字」）而
虑「客户端套接字」者「获取客户端套接字」于（「开始连接」于『127.0.0.1』于「端口」）而
虑「监听事件」者「必须成功」授以「整数」于（「等待可读」于「监听套接字」于「一零零零」）而
虑「无」者「断言」于（「事件可读吗」于「监听事件」）而
虑「服务端套接字」者「必须成功」授以「传输连接」于（「接受」于「监听套接字」）而
虑「无」者「必须等待」授以「字符串」于（「读取」于「服务端套接字」于「一零二四」）而
虑「写入字节数」者「必须成功」授以「整数」于（「写入」于「客户端套接字」于『你好』）而
虑「无」者「断言」于（「等于」于「写入字节数」于「六」）而
虑「读取事件」者「必须成功」授以「整数」于（「等待可读」于「服务端套接字」于「一零零零」）而
虑「无」者「断言」于（「事件可读吗」于「读取事件」）而
虑「收到内容」者「必须成功」授以「字符串」于（「读取」于「服务端套接字」于「一零二四」）而
虑「无」者「断言」于（「字符串相等吗」于「收到内容」于『你好』）而
虑「无」者「必须成功」授以「有」于（「关闭」于「客户端套接字」）而
虑「无」者「必须成功」授以「有」于（「关闭」于「服务端套接字」）而
「必须成功」授以「有」于（「关闭」于「监听套接字」）。
