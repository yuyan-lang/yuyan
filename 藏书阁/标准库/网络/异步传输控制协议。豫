寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「引用」之书。
寻观「标准库」之「控制」之「异步」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「标准库」之「网络」之「传输控制协议」之书。

「：详细使用方法见标准库/控制目录下的《异步使用说明.md》文件。：」


「操作结果转异步步骤」乃承「甲」而
    化「传输连接」而化「整数」而化（「操作结果」于「甲」）而
    （「异步步骤」于（「操作结果」于「甲」））
也。
「操作结果转异步步骤」者受「甲」而会「连接值」而会「关注事件」而会「结果」而
    鉴「结果」而
    有（「操作成功」于「值」）则「异步步骤完成」于（「操作成功」于「值」）
    或有（「暂需等待」于「值」）则
        （「异步步骤等待输入输出」授以（「操作结果」于「甲」））于「连接值」于「关注事件」
    或有「连接已关闭」则
        「异步步骤完成」于（「连接已关闭」授以「甲」）
    或有（「操作失败」于「状态」）则
        「异步步骤完成」于（（「操作失败」授以「甲」）于「状态」）
也。


「异步连接状态」立「元类型」也。
「尚未开始连接」立「异步连接状态」也。
「正在建立连接」立化「传输连接」而「异步连接状态」也。

「异步开始连接」乃化「字符串」而化「整数」而（「异步任务」于（「操作结果」于「传输连接」））也。
「异步开始连接」者会「主机」而会「端口」而
    虑「状态引用」者「新引用」于「尚未开始连接」而
    「派生可等待」授以（「操作结果」于「传输连接」）于（会「无」而
        鉴「读引用」于「状态引用」而
        有「尚未开始连接」则（
            鉴「开始连接」于「主机」于「端口」而
            有（「暂需等待」于「连接值」）则（
                虑「无」者「写引用」于「状态引用」于（「正在建立连接」于「连接值」）而
                （「异步步骤等待输入输出」授以（「操作结果」于「传输连接」））于「连接值」于「关注可写」）
            或有（「操作成功」于「连接值」）则「异步步骤完成」于（「操作成功」于「连接值」）
            或有「连接已关闭」则「异步步骤完成」于（「连接已关闭」授以「传输连接」）
            或有（「操作失败」于「错误状态」）则
                「异步步骤完成」于（（「操作失败」授以「传输连接」）于「错误状态」））
        或有（「正在建立连接」于「连接值」）则
            「操作结果转异步步骤」授以「传输连接」于「连接值」于「关注可写」于（「完成连接」于「连接值」）
    ）
也。

「异步接受」乃化「传输连接」而（「异步任务」于（「操作结果」于「传输连接」））也。
「异步接受」者会「监听套接字」而
    「派生可等待」授以（「操作结果」于「传输连接」）于（会「无」而
        「操作结果转异步步骤」授以「传输连接」于「监听套接字」于「关注可读」于（「接受」于「监听套接字」））
也。

「异步读取」乃化「传输连接」而化「整数」而（「异步任务」于（「操作结果」于「字符串」））也。
「异步读取」者会「连接值」而会「最大字节数」而
    「派生可等待」授以（「操作结果」于「字符串」）于（会「无」而
        「操作结果转异步步骤」授以「字符串」于「连接值」于「关注可读」于（「读取」于「连接值」于「最大字节数」））
也。

「异步读取字节串」乃化「传输连接」而化「整数」而（「异步任务」于（「操作结果」于「字节串」））也。
「异步读取字节串」者会「连接值」而会「最大字节数」而
    「派生可等待」授以（「操作结果」于「字节串」）于（会「无」而
        「操作结果转异步步骤」授以「字节串」于「连接值」于「关注可读」于（「读取字节串」于「连接值」于「最大字节数」））
也。

「异步从字节序数写入」乃
    化「传输连接」而化「字符串」而化「整数」而
    （「异步任务」于（「操作结果」于「整数」））
也。
「异步从字节序数写入」者会「连接值」而会「内容」而会「起始字节序数」而
    「派生可等待」授以（「操作结果」于「整数」）于（会「无」而
        「操作结果转异步步骤」授以「整数」于「连接值」于「关注可写」于
            （「从字节序数写入」于「连接值」于「内容」于「起始字节序数」））
也。

「异步写入」乃化「传输连接」而化「字符串」而（「异步任务」于（「操作结果」于「整数」））也。
「异步写入」者会「连接值」而会「内容」而
    「异步从字节序数写入」于「连接值」于「内容」于「零」
也。

「异步从字节序数写入字节串」乃
    化「传输连接」而化「字节串」而化「整数」而
    （「异步任务」于（「操作结果」于「整数」））
也。
「异步从字节序数写入字节串」者会「连接值」而会「内容」而会「起始字节序数」而
    「派生可等待」授以（「操作结果」于「整数」）于（会「无」而
        「操作结果转异步步骤」授以「整数」于「连接值」于「关注可写」于
            （「从字节序数写入字节串」于「连接值」于「内容」于「起始字节序数」））
也。

「异步写入字节串」乃化「传输连接」而化「字节串」而（「异步任务」于（「操作结果」于「整数」））也。
「异步写入字节串」者会「连接值」而会「内容」而
    「异步从字节序数写入字节串」于「连接值」于「内容」于「零」
也。
