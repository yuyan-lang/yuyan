「：这是我尝试加速文字处理的程序，非常具有实验性质：」



寻观整数操作之书。

寻观标准库之语言核心之内建类型之书。
寻观标准库之语言核心之异常之书。
寻观标准库之数学运算之数组转换之书。
「： 字符流立元类型也。 ：」
可索引字符串即字符串也。
「：可索引字符流在JS平台上可以是buffer：」
字符流即可索引字符串合整数「：末尾结尾0在字符串中的序数：」
            合整数「：当前的序数，指向下个待读的值：」也。「：整数是UTF8中当前指针位置：」

字节长度乃化可索引字符串而整数也。
字节长度者会串而《《C调用》》名『yyStringByteArrayGetLength』传串也。

创建字符流乃化字符串而字符流也。
创建字符流者会串而
    虑索引串者（可索引字符串也《《C调用》》名『yy_豫言字符串获取字节数组』传串）而
    索引串与（字节长度于索引串）与零也。


「：在当前位置匹配下个字符串：」
字符流获取剩余字符串乃化字符流而字符串也。
字符流获取剩余字符串者会流而
    虑被匹配者流中零而
    虑当前指针者流中二而
    《《C调用》》名『yy_豫言子字符串从字节序数开始』传被匹配与当前指针也。

「：在当前位置匹配下个字符串：」
字符流匹配字符串乃化字符流而化字符串而爻也。
字符流匹配字符串者会流而会串而
    虑被匹配者流中零而
    虑当前指针者流中二而
    《《C调用》》名『yy_豫言字符串匹配』传被匹配与当前指针与串也。

「：在当前位置匹配下个字符串：」
字符流前进字符串乃化字符流而化字符串而字符流也。
字符流前进字符串者会流而会串而
    虑新流者（流中零）与（流中一）与（（流中二）加（字节长度于串））
    而新流也。

「：处于效率考虑，仅比较当前指针是否相等：」
字符流位置相等乃化字符流而化字符流而爻也。
字符流位置相等者会流甲而会流乙而（流甲中二）等于（流乙中二）也。

字符流已经结束乃化字符流而爻也。
字符流已经结束者会流而（流中一）等于（流中二）也。

字符流获取下个字符乃化字符流而字符串合字符流也。
字符流获取下个字符者会流而
    虑被匹配者流中零而
    虑当前指针者流中二而
    虑下个字符者（字符串也（《《C调用》》名『yy_豫言字符串获取字节序数当前字符』传被匹配与当前指针））而
    虑下个字符字节长度者（字节长度于下个字符）而
    虑无者（若下个字符字节长度等于零则发生事故于『无法获取下个字符』否则元）而
    虑新流者（流中零）与（流中一）与（（流中二）加（下个字符字节长度））
    而下个字符与新流也。

「：字符流的下个字必须是"，返回对应的"前的字符串以及新的字符流，
新字符流的下一个字是"的后一个字：」
字符流获取癸象字符串乃化字符流而字符串合字符流合有也。
字符流获取癸象字符串者会流而
    虑当前串者流中零而
    虑当前指针者流中二而
    虑获取串及前进数者（字符串合整数也（数组转换之数组转对子于（《《C调用》》名『yy_豫言字符串获取JSON字符串』传当前串与当前指针）））而
    虑新流者（流中零）与（流中一）与（（流中二）加（获取串及前进数中一））而
    虑新串者（字符串也（获取串及前进数中零））而
    新串与新流与元也。