「：这是我尝试加速文字处理的程序，非常具有实验性质：」



寻观「整数操作」之书。

寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数学运算」之「数组转换」之书。
「可索引字符串」即「字符串」也。
「：可索引字符流在JS平台上可以是buffer：」
「字符流」即「可索引字符串」合「整数」「：末尾结尾0在字符串中的序数：」
            合「整数」「：当前的序数，指向下个待读的值：」也。「：整数是UTF8中当前指针位置：」

「字节长度」乃化「可索引字符串」而「整数」也。
「字节长度」者会「串」而《《C调用》》名『豫言_字符串字节数组长度』传「串」也。

「创建字符流」乃化「字符串」而「字符流」也。
「创建字符流」者会「串」而
    虑「索引串」者（「可索引字符串」也《《C调用》》名『豫言_字符串获取字节数组』传「串」）而
    「索引串」与（「字节长度」于「索引串」）与「零」也。


「：在当前位置匹配下个字符串：」
「字符流获取剩余字符串」乃化「字符流」而「字符串」也。
「字符流获取剩余字符串」者会「流」而
    虑「被匹配」者「流」中「零」而
    虑「当前指针」者「流」中「二」而
    《《C调用》》名『豫言_子字符串从字节序数开始』传「被匹配」与「当前指针」也。

「：在当前位置匹配下个字符串：」
「字符流匹配字符串」乃化「字符流」而化「字符串」而「爻」也。
「字符流匹配字符串」者会「流」而会「串」而
    虑「被匹配」者「流」中「零」而
    虑「当前指针」者「流」中「二」而
    《《C调用》》名『豫言_字符串匹配』传「被匹配」与「当前指针」与「串」也。

「：在当前位置匹配下个字符串：」
「字符流前进字符串」乃化「字符流」而化「字符串」而「字符流」也。
「字符流前进字符串」者会「流」而会「串」而
    虑「新流」者（「流」中「零」）与（「流」中「一」）与（「加」于（「流」中「二」）于（「字节长度」于「串」））
    而「新流」也。

「：处于效率考虑，仅比较当前指针是否相等：」
「字符流位置相等」乃化「字符流」而化「字符流」而「爻」也。
「字符流位置相等」者会「流甲」而会「流乙」而「等」于（「流甲」中「二」）于（「流乙」中「二」）也。

「字符流已经结束」乃化「字符流」而「爻」也。
「字符流已经结束」者会「流」而「等」于（「流」中「一」）于（「流」中「二」）也。

「字符流获取下个字符」乃化「字符流」而「字符串」合「字符流」也。
「字符流获取下个字符」者会「流」而
    虑「被匹配」者「流」中「零」而
    虑「当前指针」者「流」中「二」而
    虑「下个字符」者（「字符串」也（《《C调用》》名『豫言_字符串获取字节序数当前字符』传「被匹配」与「当前指针」））而
    虑「下个字符字节长度」者（「字节长度」于「下个字符」）而
    虑「无」者（若「等」于「下个字符字节长度」于「零」则「发生事故」于『无法获取下个字符』否则「元」）而
    虑「新流」者（「流」中「零」）与（「流」中「一」）与（「加」于（「流」中「二」）于（「下个字符字节长度」））
    而「下个字符」与「新流」也。

「：字符流的下个字必须是"，返回对应的"前的字符串以及新的字符流，
新字符流的下一个字是"的后一个字：」
「字符流获取癸象字符串」乃化「字符流」而「字符串」合「字符流」合「有」也。
「字符流获取癸象字符串」者会「流」而
    虑「当前串」者「流」中「零」而
    虑「当前指针」者「流」中「二」而
    虑「获取串及前进数」者（「字符串」合「整数」也（「数组转换」之「数组转对子」于（《《C调用》》名『豫言_字符串获取JSON字符串』传「当前串」与「当前指针」）））而
    虑「新流」者（「流」中「零」）与（「流」中「一」）与（「加」于（「流」中「二」）于（「获取串及前进数」中「一」））而
    虑「新串」者（「字符串」也（「获取串及前进数」中「零」））而
    「新串」与「新流」与「元」也。
