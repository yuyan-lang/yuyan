寻观「标准库」之书。
寻观「标准库」之「控制」之「异步」之书。
寻观「标准库」之「操作系统」之「异步子进程」之书。

「：文言：事各有所待，毕而后启；败则止启，候在途者毕。汉语：依赖成功后启动任务，失败后停止启动并回收在途任务。：」
「构建任务」即「字符串」合（「列」于「字符串」）合
    （化「有」而（「异步任务」于「子进程结果」））合
    （化「子进程结果」而「有」）合（「引用类」于「整数」）合
    （「引用类」于（「异步任务」于「子进程结果」））也。

「新构建任务」乃化「字符串」而化（「列」于「字符串」）而
    化（化「有」而（「异步任务」于「子进程结果」））而
    化（化「子进程结果」而「有」）而「构建任务」也。
「新构建任务」者会「名称」而会「依赖」而会「启动」而会「完成」而
    「名称」与「依赖」与「启动」与「完成」与（「新引用」授以「整数」于「零」）与
    （「新引用」于（「立即完成」授以「子进程结果」于（「零」与「零」与『』与『』）））
也。

「任务已成功」乃化（「列」于「构建任务」）而化「字符串」而「爻」也。
「任务已成功」者会「任务们」而会「名称」而
    鉴「任务们」而
    有「罄」则「阴」
    或有（「缀」于「任务」于「余」）则
        若「串相等」于（「任务」中「零」）于「名称」
        则「等于」于（「读引用」授以「整数」于（「任务」中「四」））于「二」
        否则「任务已成功」于「余」于「名称」
也。

「任务依赖已成」乃化（「列」于「构建任务」）而化（「列」于「字符串」）而「爻」也。
「任务依赖已成」者会「任务们」而会「依赖们」而
    鉴「依赖们」而
    有「罄」则「阳」
    或有（「缀」于「首」于「余」）则
        若「任务已成功」于「任务们」于「首」
        则「任务依赖已成」于「任务们」于「余」否则「阴」
也。

「执行任务图并记录」乃化（「引用类」于（「列」于「构建任务」））而化「整数」而
    化（化「字符串」而化「子进程结果」而「有」）而「整数」也。
「执行任务图并记录」者会「图」而会「上限」而会「记录结果」而
    虑「在途」者「新引用」授以「整数」于「零」而
    虑「失败码」者「新引用」授以「整数」于「零」而
    递归虑「收取」其化（「列」于「构建任务」）而「有」者会「任务们」而
        鉴「任务们」而
        有「罄」则「元」
        或有（「缀」于「任务」于「余」）则（
            虑「无」者（若「等于」于（「读引用」授以「整数」于（「任务」中「四」））于「一」则（
                虑「异步值」者「读引用」授以（「异步任务」于「子进程结果」）于（「任务」中「五」）而
                若「完成了吗」授以「子进程结果」于「异步值」则（
                    虑「无」者「写引用」授以「整数」于「在途」于（「减」于（「读引用」授以「整数」于「在途」）于「一」）而
                    「尝试运行」授以「有」于（会「无」而
                        虑「结果」者「等待」授以「子进程结果」于「异步值」而
                        虑「无」者「记录结果」于（「任务」中「零」）于「结果」而
                        若「等于」于（「结果」中「一」）于「零」则（
                            虑「无」者（「任务」中「三」）于「结果」而
                            「写引用」授以「整数」于（「任务」中「四」）于「二」）
                        否则（
                            虑「无」者「标准错误打印行」于（『构建任务失败：』附（「任务」中「零」）附『「：换行：」』附（「结果」中「二」）附（「结果」中「三」））而
                            虑「无」者（若「等于」于（「读引用」授以「整数」于「失败码」）于「零」则「写引用」授以「整数」于「失败码」于（「结果」中「一」）否则「元」）而
                            「写引用」授以「整数」于（「任务」中「四」）于「三」））
                    于（会「错误」而
                        虑「无」者「标准错误打印行」于「错误」而
                        虑「无」者「写引用」授以「整数」于「失败码」于「一」而
                        「写引用」授以「整数」于（「任务」中「四」）于「三」））
                否则「元」）否则「元」）而
            「收取」于「余」）
    而
    递归虑「派发」其化（「列」于「构建任务」）而「有」者会「任务们」而
        鉴「任务们」而
        有「罄」则「元」
        或有（「缀」于「任务」于「余」）则（
            虑「无」者（若「不等于」于（「读引用」授以「整数」于「失败码」）于「零」则「元」否则若「小于」于（「读引用」授以「整数」于「在途」）于「上限」则
                若「等于」于（「读引用」授以「整数」于（「任务」中「四」））于「零」则
                    若「任务依赖已成」于（「读引用」授以（「列」于「构建任务」）于「图」）于（「任务」中「一」）则「尝试运行」授以「有」于（会「无」而
                        虑「无」者「写引用」授以（「异步任务」于「子进程结果」）于（「任务」中「五」）于（（「任务」中「二」）于「元」）而
                        虑「无」者「写引用」授以「整数」于（「任务」中「四」）于「一」而
                        「写引用」授以「整数」于「在途」于（「加」于（「读引用」授以「整数」于「在途」）于「一」））
                    于（会「错误」而
                        虑「无」者「标准错误打印行」于（（「任务」中「零」）附『：』附「错误」）而
                        虑「无」者「写引用」授以「整数」于「失败码」于「一」而
                        「写引用」授以「整数」于（「任务」中「四」）于「三」）
                    否则「元」
                否则「元」
            否则「元」）而
            「派发」于「余」）
    而
    递归虑「循环」其化「有」而「整数」者会「无」而
        虑「无」者「收取」于（「读引用」授以（「列」于「构建任务」）于「图」）而
        虑「无」者（若「等于」于（「读引用」授以「整数」于「失败码」）于「零」
            则「派发」于（「读引用」授以（「列」于「构建任务」）于「图」）否则「元」）而
        若「大于」于（「读引用」授以「整数」于「在途」）于「零」
        则（虑「无」者「让出」于「元」而「循环」于「元」）
        否则若「不等于」于（「读引用」授以「整数」于「失败码」）于「零」则「读引用」授以「整数」于「失败码」
        否则鉴（「滤」授以「构建任务」于（会「任务」而「等于」于（「读引用」授以「整数」于（「任务」中「四」））于「零」）于（「读引用」授以（「列」于「构建任务」）于「图」））而
            有「罄」则「零」
            或有「未完成」则（
                虑「无」者「标准错误打印行」于（『构建任务依赖成环或缺失：』附（「间合」于『，』于（「态射」授以「构建任务」授以「字符串」于「未完成」于（会「任务」而「任务」中「零」））））而「一」）
    而
    若「小于」于「上限」于「一」则「发生事故」授以「整数」于『并发数必须大于零』否则「循环」于「元」
也。

「执行任务图」乃化（「引用类」于（「列」于「构建任务」））而化「整数」而「整数」也。
「执行任务图」者会「图」而会「上限」而
    「执行任务图并记录」于「图」于「上限」于（会「名称」而会「结果」而「元」）
也。
