# LOCALAGENTS 验收记录

测试环境：2026-09-23，`qwen3.8:27b`（Ollama），`think=xhigh`，`num_ctx=262144`；编译器为当前仓库的 `yy3_bs`。每次模型回答前给指南全文及题目，模型不可查阅其他材料。回答写入源码文件后才由验收端运行编译器。原始请求、回答、编译日志和代码保存在 `/tmp/豫言本地指南/轮次/`。

| 轮次 | 题型 | 首次答案 | 验证 | 处理 |
| --- | --- | --- | --- | --- |
| 第一轮 | 回文字符串 | 有完整源码；10830 输出 token | 类型检查、编译、两个样例和四个补充边界均通过 | 保留 |
| 第一轮 | 有序整数列表去重 | 16384 输出 token 全耗于思考，未给最终源码 | 从思考中提取的草稿报 `〇于〇 与 〇授以〇`；给空列表参数加括号后类型检查和样例通过 | 在指南加入实参括号规则、错误诊断和提交时限提醒；重新测试 |
| 修订后重测 | 有序整数列表去重 | 24576 输出 token 全耗于重复自检，未给最终源码 | 无可验收的首次答案 | 保持 `xhigh`、256K，调整采样温度和重复惩罚再次测试 |
| 采样调整重测 | 有序整数列表去重 | 16384 输出 token 全耗于思考，未给最终源码 | 无可验收的首次答案 | 记录为本地模型的提交失败，继续测不同题型 |
| 第一轮 | 爬楼梯动态规划 | 有完整源码；6621 输出 token | 类型检查、编译、样例及 `n=0,1,2,5,10` 均通过 | 保留 |
| 第一轮 | 二叉树最大深度 | 有完整源码；4860 输出 token | 最后一条顶层打印语句少一个全角右括号，首次无法解析；补括号后类型检查通过 | 指南加入拆分中间值和逐一配对括号规则，待重测 |
| 修订后重测 | 二叉树最大深度 | 16384 输出 token 全耗于思考，无最终源码 | 没有可验收的首次答案 | 把原思考保留在同一会话并要求结束分析，得到完整源码；该续答类型检查、编译及空树、单节点、深树测试通过，不能计为首次通过 |
| 对照实验 | 有序整数列表去重，关闭思考 | 232 token 给出源码 | 使用了无效的裸 `【…】` 列表字面量，无法解析；算法还会读取空列表首项 | 不计入 `xhigh` 验收；表明简单关闭思考不能解决正确性 |
| 连续思考续答 | 有序整数列表去重 | 保留第一次 24576 token、第二次 8192 token 的思考，第三次生成 921 token 完整源码 | 首次提交的完整源码未经修改即通过类型检查、编译、样例及空列表、单元素、全重复、负数边界 | 一次源码提交通过，但用了三次模型请求，不能称为单次请求通过 |
| 第二轮 | 两数之和 | 保留三段各 8192 token 思考，第四次请求才提交完整源码 | 首次源码有多处不成对的 `」`，无法解析 | 指南增加定界符核对与已编译的两层列表扫描模板；该题未通过 |
| 修订回归 | 两数之和，样例换为 `[3,2,4]` | 首请求思考 8192 token，第二次请求提交完整源码 | 首次提交的源码未经修改即类型检查、编译、样例及另一个样例、重复值、无解测试通过 | 回归通过；用了两次模型请求，模板与题型接近，尚不能证明对未见题型的泛化 |
| 第三轮留出题 | 合并两个有序整数列表 | 首请求思考 8192 token，第二次请求提交完整源码 | 首次提交的源码未经修改即类型检查、编译、样例及双空列表、单边为空、重复值测试通过 | 留出题通过；用了两次模型请求 |

“首次答案通过”只表示该题和列出的测试通过，不能推断所有 LeetCode 题都能一次通过。
