# 分区重复读取与排序分段

本轮没有新增读取复用编译器变换。原型在当前迭代的整数比较后，复用已经读取的扫描值；不跨写入、调用或循环回边。最初临时脚本以不存在的后继函数名划定边界，误改了其他函数，校验和 4000004 被立即拒绝。修正为括号配对确定完整函数边界后，只修改 rdq756 三处展开副本；全部正式统计校验和为 6000002。错误原型未纳入测量结论。

五进程各六轮、轮换版本：形态版首轮 57.764 ms / 热轮 45.117；读复用原型 56.487 / 44.708。热轮差不足 1%，没有稳定收益证据。

另用仅指定 print-wasm-code-function-index=808 的诊断运行提取 rdq756，不计入性能统计。Liftoff 从 1188 字节、68 条静态加载指令降为 1048 字节、56 条；TurboFan 两版均为 888 字节、27 条加载指令。逐指令比较，19 项编码差异均为代码末段运行时桩 bl 调用的地址重定位，其余编码完全相同。这里加载计数包含栈加载，不是动态数组访问次数。该证据说明热层已消除该重复读取，当前不为这一个收益不足的场景增加新分析。

分段诊断在完整模块增加计时导入与独立调用入口，保留原填充和排序函数，每轮重新分配四百万元素数组，五进程各六轮，校验和一致。未执行原 _start，因而结果用于定位，不能代替完整程序验收，也不能把不同阶段的中位数严格相加。

| 阶段 | 首轮 ms | 热轮 ms |
|---|---:|---:|
| 分配 |2.590750|1.037625|
| 填充 |6.424333|1.461875|
| 排序主体 |48.533041|42.289208|

当前剩余成本主要在排序主体，单独消除数组分配不足以填平与 C 的差距。下一步应针对实际热循环、条件交换与边界证明展开调查；不得因此取消原有检查或引入持久私有缓冲。当前编译器保留递归形态策略，读取复用仅存为实验。整体目标未完成。
