--- /tmp/yy共同递进前生成。豫	2026-09-20 08:20:35
+++ 库/编译器核心/编译步骤/代码生成/网页汇编栈数值生成。豫	2026-09-20 08:20:36
@@ -50,6 +50,7 @@
  虑「独址参」者「新引用」授以（「列」于「整数」）于「罄」而
  虑「步活」者「新引用」授以（「数组引用类」于「爻」）于（「新数组引用」授以「爻」于「阳」于（「函」中「四」））而
  虑「独址态」者「新引用」授以「爻」于「阴」而
+ 虑「步扫描」者「新引用」授以「整数」于（「减」于「零」于「一」）而
  虑「批终点」者「新引用」授以「字符串」于『』而
  虑「是独址」者（化「整数」而「爻」也会「号」而「栈表含号」于（「读引用」于「独址参」）于「号」）而
  虑「写值」者（化「正则原子」而「字符串」也会「值」而
@@ -86,7 +87,7 @@
       若「且」于「尾令」于（鉴「终」而有（「正则产出」于（「正则局部」于「果号」））则「等于」于「号」于「果号」或有「其他」则「阴」）则
        （虑「自调」者「串相等」于「名」于（「函」中「零」）而
        虑「写位」者「滤」授以「整数」于（会「位」而若「且」于「自调」于（「读引用」于「单步态」）则（鉴（「第N个」授以「正则原子」于「位」于「参」）而有（「正则局部」于「实号」）则「非」于（「等于」于「实号」于（「第N个」授以「整数」于「位」于（「函」中「二」）））或有「其他」则「阳」）否则「阳」）于（「整数范围」于「零」于（「长度」授以「正则原子」于「参」））而
-       虑「写址位」者「滤」授以「整数」于（会「位」而若「且」于「自调」于（「读引用」于「单步态」）则「是址参」于（「第N个」授以「整数」于「位」于（「函」中「二」））否则「阴」）于「写位」而
+       虑「写址位」者「滤」授以「整数」于（会「位」而若「且」于「自调」于（「读引用」于「单步态」）则「且」于（「是址参」于（「第N个」授以「整数」于「位」于（「函」中「二」）））于（「非」于（「等于」于（「第N个」授以「整数」于「位」于（「函」中「二」））于（「读引用」于「步扫描」）））否则「阴」）于「写位」而
        虑「写数位」者「滤」授以「整数」于（会「位」而若「且」于「自调」于（「读引用」于「独址态」）则「非」于（「是独址」于（「第N个」授以「整数」于「位」于（「函」中「二」）））否则「阳」）于「写位」而
        「栈接」于（「列」【「栈接」于（「态射」授以「整数」授以「字符串」于「写数位」于（会「位」而「写值」于（「第N个」授以「正则原子」于「位」于「参」））），
         「栈接」于（「态射」授以「整数」授以「字符串」于「写址位」于（会「位」而「新址」于（「第N个」授以「正则原子」于「位」于「参」））），
@@ -104,13 +105,14 @@
    虑「资格」者「求栈独址资格」于「函」于（「进证」中「一」）于「址证」于「界号」而
    虑「无」者「写引用」于「独址参」于（「资格」中「零」）而
    虑「无」者「写引用」于「步活」于（「资格」中「一」）而
+   虑「无」者「写引用」于「步扫描」于（若「是独址」于（「进参」中「一」）则「进参」中「一」否则「减」于「零」于「一」）而
    虑「无」者「写引用」于「独址态」于「阳」而
    虑「无」者「写引用」于「单步态」于「阳」而
    虑「步文」者「写段」于「阳」于（「进证」中「一」）而
    虑「无」者「写引用」于「单步态」于「阴」而
    若「大于」于（「串长度」于「步文」）于「四零九六」则「可无」否则
    虑「比较号」者「栈局部名」于（「进证」中「零」）而
-   虑「单步」者「栈接」于（「列」【『(block $step i64.const 0 local.set 』附「比较号」，「步文」，『drop)』】）而
+   虑「单步」者「栈接」于（「列」【『(block $step i64.const 0 local.set 』附「比较号」，「步文」，『drop)』，若「小于」于（「读引用」于「步扫描」）于「零」则『』否则『local.get 』附（「址名」于（「读引用」于「步扫描」））附『 i32.const 8 i32.add local.set 』附（「址名」于（「读引用」于「步扫描」））】）而
    虑「上」者「写栈原子」于（「正则局部」于（「进参」中「零」））而
    虑「扫」者「写栈原子」于（「正则局部」于（「进参」中「一」））而
    虑「无」者若「是独址」于（「进参」中「一」）则「写引用」于「批终点」于（「栈接」于（「列」【『(local $pend i32)』，「写栈原子」于（「正则局部」于（「址证」中「零」）），「上」，『i32.wrap_i64 i32.const 3 i32.shl i32.add local.set $pend』】））否则「元」而
@@ -128,6 +130,7 @@
   「读引用」于「批终点」，
   「栈接」于（「态射」授以「整数」授以「字符串」于「址参」于（会「号」而（「重算址」于（「正则局部」于「号」））附『 local.set 』附（「址名」于「号」））），「文」】）或有「可无」则「栈接」于（「列」【『(loop $self (result i64)』，「写段」于「阳」于（「函」中「三」），『)』】）），『)』】）））也。
 
+「：文言：诸歧皆进一，合其址递于步后。汉语：仅对进度证明中的独址扫描参数，将各尾分支的地址递进合并到单步块之后；其他参数先按旧扫描值并行求值与赋值，异常仍在原操作处发生。数值扫描参数不使用此变换。：」
 「：文言：合步须续行之证，先去奇数一步，余者两步而行。汉语：批次入口只供范围守卫后的快体使用；进度证明保证每步正常结束时扫描恰好加一且上界不变。异常不跨步重排，比较局部在步内为假、结束时为真，尾参数仍并行赋值。：」
 「生成栈数值体以界」乃化（化「字符串」而「栈表示签」）而化（化「字符串」而「字符串」）而化「栈表示签」而化「整数」而化「正则函数」而化（「列」于「整数」）而「或可有」于「字符串」也。
 「生成栈数值体以界」者会「查签」而会「取名」而会「本签」而会「根号」而会「函」而会「界号」而「生成栈数值体批次」于「查签」于「取名」于「本签」于「根号」于「函」于「界号」于「可无」也。
