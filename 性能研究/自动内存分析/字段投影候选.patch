--- /tmp/yy字段投影前常量内联。豫	2026-09-20 12:44:09
+++ 库/编译器核心/编译步骤/优化/跨文件优化/常量内联。豫	2026-09-20 12:44:09
@@ -73,6 +73,12 @@
   或有「其他」则「阴」）
  或有「其他」则「阴」也。
 
+「：文言：独参唯取其一域者，可展薄门，显其所指。汉语：只新增单参数直接字段投影；不复制分支、递归、调用或字段修改。沿用原有 beta 变换保存实参求值次数和次序，使后续分析直接看到容器读取。：」
+「单参字段投影」乃化「表达式」而「爻」也。
+「单参字段投影」者会「式」而鉴「式」而
+ 有（「式节点」于（「拉姆达抽象」于（「多参数形」于「一」））于（「列」【（「绑定」于「参名」于（「式节点」于（「元组解构节点」于「位」）于（「列」【（「绑定变量」于「一」）】）））】））则「非」于（「小于」于「位」于「零」）
+ 或有「其他」则「阴」也。
+
 「原语内联候选」乃化「文件定义列」而「文件定义列」也。
 「原语内联候选」者会「定义们」而
  「滤态射」授以（「字符串」合「爻」合「表达式」）授以（「字符串」合「爻」合「表达式」）于（会「项」而
@@ -83,6 +89,7 @@
   若「且」于（「薄函体」于「式」）于（「小于」于（「计算表达式复杂度」于「式」）于（「读引用」于「优化内联复杂度」））则「阳」否则
   若「且」于（「数组构造薄函」于「式」）于（「小于」于（「计算表达式复杂度」于「式」）于「六四」）则「阳」否则
   若「静态单参转交」于「式」则「阳」否则
+  若「单参字段投影」于「式」则「阳」否则
   若「至少二参小函」于「式」则（若「小于」于（「计算表达式复杂度」于「式」）于「六四」则「直线小函」于「式」否则「阴」）否则「阴」
  或有「其他」则「阴」而
  若「可用」则「可有」于（（「项」中「零」）与（「项」中「一」）与「式」）否则「可无」）于「定义们」也。
