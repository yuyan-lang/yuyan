# 既有输入规模补充复测

本轮未改变编译器。恢复后的原生三、四代哈希现场核对均为 `d388568e49f86da0100a81145f1ea78bd645a657738cb526b8d3c4c400c1b1d4`。本轮前一次进展是覆盖证明实现、正确性验证和否决性实测，并非无进展等待。

核对 `性能研究/运行内核/最终复测` 的文件清单，既有六个程序各包含两个规模：斐波那契 38/40，素数筛 2000000/10000000，矩阵 300/600，排序 1000000/4000000，数组 2000000/10000000，树 18/20。近期主要记录了大规模及额外的树深度 22，不能用这些记录代替完整的既有规模验收。

本次补测七组，豫言使用恢复后的 `yy容器图栈<程序>.wasm`，C 使用原 `yy内核C.wasm`。每种实现各五个新进程，每进程六轮，交替顺序，无并发构建。首轮单列，后五轮合并取中位数；逐进程中位数和全部结果值另存，不进行择优。所有结果值一致。见 [完整数据](既有规模补充复测.json)。

| 程序与规模 | 豫言热轮 ms | C/Wasm 热轮 ms |
|---|---:|---:|
| 斐波那契 38 | 39.940 | 49.886250 |
| 素数筛 2000000 | 2.964 | 5.637125 |
| 矩阵乘法 300 | 18.727 | 22.994375 |
| 数组基准 2000000 | 0.511 | 2.161334 |
| 快速排序 1000000 | 7.921 | 7.714583 |
| 树基准 18 | 0.001 | 4.347833 |
| 树基准 20 | 0.001 | 17.392583 |

百万项排序在本组约慢 2.68%。不能把四百万项曾测得约 0.1% 的差距推广到所有规模；也不能凭五个进程的描述统计判定统计等效。树的工作已被自动造消优化大幅消除，微秒级结果不能解释成一般分配器的速度。

已向用户提出需要决定的验收问题：是否接受预先固定的 2% 统计容差，或要求无容差、统计上证明不慢于 C。提问源于用户要求关键取舍停止并询问；容差关系到目标完成判定，尚未得到答复。无论如何，既有程序与规模、公开 ABI、整数和数组语义、完整冷自举不超过 30 秒的要求不变。即使采用 2%，当前百万项排序的描述结果也不构成通过证据。正式验收须在标准确定后独立复测，不能沿用探索样本直接宣布完成。

其余尚需审计的最终测量条件包括 C 构建选项与豫言实际使用的 Wasm 特性是否可比，以及各独立进程的统计单位。当前 C 文件 SHA256 为 `d47d789da9410190f2d1726c8c0a80c020763e0b00bb24c340c9f050f07d5b64`；此前构建记录为 WASI SDK clang `-O3`、16 MiB 线性栈。不可把当前有限对照解释为所有 C 编译策略或整门语言的商业成熟度。
