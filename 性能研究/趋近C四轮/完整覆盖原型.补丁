--- yy条件数组基准.wasm.wat	2026-09-19 16:09:12
+++ yy组完整覆盖.wat	2026-09-19 16:53:49
@@ -103286,7 +103286,7 @@
 end return
 ) )
 (func $c771
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $a1 (ref null eq))
 (result (ref null eq))
 i32.const 1
@@ -103306,7 +103306,7 @@
 end
 )
 (func $d771
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $i1 i64)
 (result (ref null eq)) (local $u (ref null eq)) (local $j i64)
 
@@ -103329,6 +103329,7 @@
 
 
 
+(local $len i32) (local $index64 i64) global.get $arena_len local.set $len
 (loop $self (result (ref null eq))
 local.get $i1
 i64.const 0
@@ -103343,11 +103344,13 @@
 local.set $i3
 local.get $a0
 
-ref.cast (ref $ints)
+
 local.get $i3
-local.tee $j i64.const 4294967295 i64.gt_u if unreachable end local.get $j i32.wrap_i64
+local.set $index64 drop
+local.get $index64 local.get $len i64.extend_i32_u i64.ge_u if unreachable end
+local.get $index64 i32.wrap_i64 i32.const 3 i32.shl
 local.get $i1
-array.set $ints ref.null eq
+i64.store offset=8 ref.null eq
 local.set $a4
 local.get $i1
 i64.const 1
@@ -103355,7 +103358,7 @@
 
 local.set $i5
 local.get $a0
- ref.cast (ref $ints)
+ 
 local.get $i5
 
 local.set $i1
@@ -103367,7 +103370,7 @@
 end return
 ) )
 (func $e771
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $a1 (ref null eq))
 (result (ref null eq)) (local $u (ref null eq)) (local $j i64)
 (local $a2 (ref null eq))
@@ -103388,6 +103391,7 @@
 
 
 
+(local $len i32) (local $index64 i64) global.get $arena_len local.set $len
 (loop $self (result (ref null eq))
 local.get $a1
 
@@ -103406,13 +103410,15 @@
 local.set $i3
 local.get $a0
 
-ref.cast (ref $ints)
+
 local.get $i3
-local.tee $j i64.const 4294967295 i64.gt_u if unreachable end local.get $j i32.wrap_i64
+local.set $index64 drop
+local.get $index64 local.get $len i64.extend_i32_u i64.ge_u if unreachable end
+local.get $index64 i32.wrap_i64 i32.const 3 i32.shl
 local.get $a1
 
 local.tee $u ref.test (ref i31) if (result i64) local.get $u ref.cast (ref i31) i31.get_s i64.extend_i32_s else local.get $u ref.cast (ref $big) struct.get $big 0 end
-array.set $ints ref.null eq
+i64.store offset=8 ref.null eq
 local.set $a4
 local.get $a1
 
@@ -103422,7 +103428,7 @@
 
 local.set $i5
 local.get $a0
- ref.cast (ref $ints)
+ 
 local.get $i5
 
 return_call $d771
@@ -103666,7 +103672,7 @@
 end return
 ) )
 (func $c774
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $a1 (ref null eq))
 (param $a2 (ref null eq))
 (result (ref null eq))
@@ -103692,7 +103698,7 @@
 end
 )
 (func $d774
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $i1 i64)
 (param $i2 i64)
 (result (ref null eq)) (local $u (ref null eq)) (local $j i64)
@@ -103720,6 +103726,7 @@
 
 
 
+(local $len i32) (local $index64 i64) global.get $arena_len local.set $len
 (loop $self (result (ref null eq))
 local.get $i1
 i64.const 0
@@ -103739,10 +103746,12 @@
 local.set $i5
 local.get $a0
 
-ref.cast (ref $ints)
+
 local.get $i5
-local.tee $j i64.const 4294967295 i64.gt_u if unreachable end local.get $j i32.wrap_i64
-array.get $ints
+local.set $index64 drop
+local.get $index64 local.get $len i64.extend_i32_u i64.ge_u if unreachable end
+local.get $index64 i32.wrap_i64 i32.const 3 i32.shl
+i64.load offset=8
 local.set $i6
 local.get $i2
 local.get $i6
@@ -103750,7 +103759,7 @@
 
 local.set $i7
 local.get $a0
- ref.cast (ref $ints)
+ 
 local.get $i4
 local.get $i7
 
@@ -103763,7 +103772,7 @@
 end return
 ) )
 (func $e774
-(param $a0 (ref $ints))
+(param $a0 i32)
 (param $a1 (ref null eq))
 (param $a2 (ref null eq))
 (result (ref null eq)) (local $u (ref null eq)) (local $j i64)
@@ -103788,6 +103797,7 @@
 
 
 
+(local $len i32) (local $index64 i64) global.get $arena_len local.set $len
 (loop $self (result (ref null eq))
 local.get $a1
 
@@ -103813,10 +103823,12 @@
 local.set $i5
 local.get $a0
 
-ref.cast (ref $ints)
+
 local.get $i5
-local.tee $j i64.const 4294967295 i64.gt_u if unreachable end local.get $j i32.wrap_i64
-array.get $ints
+local.set $index64 drop
+local.get $index64 local.get $len i64.extend_i32_u i64.ge_u if unreachable end
+local.get $index64 i32.wrap_i64 i32.const 3 i32.shl
+i64.load offset=8
 local.set $i6
 local.get $a2
 
@@ -103826,7 +103838,7 @@
 
 local.set $i7
 local.get $a0
- ref.cast (ref $ints)
+ 
 local.get $i4
 local.get $i7
 
@@ -103949,7 +103961,7 @@
 (result (ref null eq)) (local $u (ref null eq)) (local $j i64)
 
 
-(local $a2 (ref null eq))
+(local $a2 i32)
 (local $a3 (ref null eq))
 (local $a4 (ref null eq))
 
@@ -103965,17 +103977,17 @@
 
 local.tee $u ref.test (ref i31) if (result i64) local.get $u ref.cast (ref i31) i31.get_s i64.extend_i32_s else local.get $u ref.cast (ref $big) struct.get $big 0 end
 local.tee $j i64.const 4294967295 i64.gt_u if unreachable end local.get $j i32.wrap_i64
-array.new $ints
+call $arena_new
 local.set $a2
 local.get $a2
- ref.cast (ref $ints)
+ 
 local.get $a1
 
 
 call $c771
 local.set $a3
 local.get $a2
- ref.cast (ref $ints)
+ 
 local.get $a1
 
 i32.const 0 ref.i31
@@ -105108,4 +105120,14 @@
 call $f779 drop
 
 )
+;; 文言：仅供有界全写消融，未入编译器。汉语：固定单缓冲、128 MiB 上限；该基准先覆盖全部元素再读取，故省去显式清零。尚未实现通用调用图验证与超限回退。
+(memory $scratch 1 2048)
+(global $arena_len (mut i32) (i32.const 0))
+(func $arena_new (param $v i64) (param $n i32) (result i32) (local $pages i32)
+ local.get $n i32.const 16777215 i32.gt_u if unreachable end
+ local.get $n global.set $arena_len
+ local.get $n i32.const 3 i32.shl i32.const 65543 i32.add i32.const 16 i32.shr_u memory.size i32.sub local.tee $pages i32.const 0 i32.gt_s
+ if local.get $pages memory.grow i32.const -1 i32.eq if unreachable end end
+ i32.const 0)
+
 )
\ No newline at end of file
