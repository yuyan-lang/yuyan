
# 快速开始

准备好仓库根目录下的稳定自举编译器 `yy_bs_stable` 后，可以直接运行程序：

```sh
./yy_bs_stable filename.yuyan
```

只编译而不运行：

```sh
./yy_bs_stable filename.yuyan --compile-only -o output
```

显示帮助

```sh
./yy_bs_stable --help
```
