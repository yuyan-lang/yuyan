{
  "name": "yuyan-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 测试/*.mjs",
    "检查配置": "WRANGLER_LOG_PATH=/tmp/yy-playground-wrangler.log wrangler deploy --config 云端配置.jsonc --dry-run --containers-rollout=none",
    "部署": "wrangler deploy --config 云端配置.jsonc",
    "开发": "wrangler dev --config 云端配置.jsonc"
  },
  "dependencies": { "@cloudflare/containers": "0.3.7" },
  "devDependencies": { "wrangler": "4.129.0" }
}
