寻观「底层库」之「基础」之书。

「：文言：取命令行参数之块，各以零终，依次相连，书于（缓冲址，缓冲长）；出址书（总长，个数）共八字节；缓冲不足则返六十一，并书所需总长。汉语：取得命令行参数块，各参数以零结尾依次相连，写入（缓冲址，缓冲长）；出址写（总长，个数）共 8 字节；缓冲不足返回 61，并写入所需总长。：」
「取参数块」乃化「整数」而化「整数」而化「整数」而「整数」也。

「：文言：取环境变量之块，每项曰「名=值」，各以零终，依次相连；余同取参数块。汉语：取得环境变量块，每项形如「名=值」，各以零结尾依次相连；其余同取参数块。：」
「取环境块」乃化「整数」而化「整数」而化「整数」而「整数」也。

「：文言：以码终本进程，不返。汉语：以给定的退出码结束本进程，不返回。：」
「退出」乃化「整数」而「有」也。

「：文言：读而清中断之标（Ctrl-C），零或一书于出址。汉语：读取并清除中断标志（Ctrl-C），结果 0 或 1 写入出址。：」
「查询中断」乃化「整数」而「整数」也。

「：文言：取平台之号，书于出址；一为裸机，二为 Linux，三为 macOS，四为 Windows。汉语：取得平台编号，写入出址；1 裸机，2 Linux，3 macOS，4 Windows。：」
「取平台」乃化「整数」而「整数」也。

「：文言：取在线处理器之数，书于出址。汉语：取得在线处理器的数量，写入出址。：」
「取处理器数」乃化「整数」而「整数」也。
