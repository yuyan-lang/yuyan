寻标准库之语言核心之内建类型之书。
观标准库之语言核心之内建类型之书。

寻多态列之书。
观多态列之书。

寻可选值之书。
观可选值之书。

寻列表搜索之书。
观列表搜索之书。

寻字符串术之书。
观字符串术之书。

「：最好的情况是(K, ORD K)，但目前的类型系统不支持表达这个类型，
所以KEY暂定为STRING：」
「：字典Map最好是用二叉搜索树来实现，但目前先用列表实现：」

串典立化元类型而元类型也。

构造串典立（承元类型者甲而化（字符串合甲）列而串典于甲）也。

空串典乃（承元类型者甲而串典于甲）也。
空串典者受甲而构造串典于【】也。

串典插入乃承甲而化（串典于甲）而化字符串而化甲而串典于甲也。
串典插入者受甲而会串典而会键而会物而
    鉴串典而
    有构造串典于「乙」则构造串典于（（字符串合甲也键与物）衔「乙」）也。

串典查找乃承甲而化（串典于甲）而化字符串而或可有甲也。
串典查找者会串典而会串而
    鉴串典而
    有构造串典于「乙」则「列表查找」于「乙」于串也。

串典读取乃承甲而化（串典于甲）而化字符串而甲也。
串典读取者会串典而会串而
    鉴串典而
    有构造串典于「乙」则「列表读取」于「乙」于串也。

存在键乃承甲而化（串典于甲）而化字符串而爻也。
存在键者会串典而会串而
    鉴（串典查找于串典于串）而
        有（可有物）则阳
        或有（可无）则阴也。

「：TODO：要做：操作将报错如果键未找到：」
串典操作乃承甲而化（串典于甲）而化字符串而化（化甲而甲）而串典于甲也。
串典操作者受甲而会串典而会键而会操作而
    鉴串典而
    有构造串典于乙则
        构造串典于（态射授以（字符串合甲）授以（字符串合甲）「：要做：使用约束解析：」
                于（遇字符串合甲者串物而
            若串相等于（串物中零）于键
                则（串物中零与（操作于（串物中一）））
                否则串物
        ）于乙）
    也。


「：如存在则更新，不存在则插入：」
串典更新乃承甲而化（串典于甲）而化字符串而化甲而串典于甲也。
串典更新者会串典而会键而会物而
        若存在键于串典于键
        则（串典操作于串典于键于（会旧物而物））
        否则（串典插入于串典于键于物）
    也。