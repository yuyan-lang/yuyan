寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「数据结构」之「整数操作」之书。
寻观「标准库」之「数学运算」之「数组转换」之书。

「：非阻塞的可靠字节流传输接口。：」


「传输连接」即「整数」也。

「操作结果」立化「元类型」而「元类型」也。
「操作成功」立（承「甲」而化「甲」而「操作结果」于「甲」）也。
「暂需等待」立（承「甲」而化「甲」而「操作结果」于「甲」）也。
「连接已关闭」立承「甲」而「操作结果」于「甲」也。
「操作失败」立（承「甲」而化「整数」而「操作结果」于「甲」）也。

「关注可读」者「一」也。
「关注可写」者「二」也。
「关注可读写」者「三」也。

「事件可读」者「一」也。
「事件可写」者「二」也。
「事件错误」者「四」也。
「事件挂断」者「八」也。

「事件包含」乃化「整数」而化「整数」而「爻」也。
「事件包含」者会「事件们」而会「待查事件」而
    「不等于」于
        （「减」于
            （「除以」于「事件们」于「待查事件」）
            于（「乘」于（「除以」于「事件们」于（「乘」于「待查事件」于「二」））于「二」））
        于「零」
也。

「事件可读吗」乃化「整数」而「爻」也。
「事件可读吗」者会「事件们」而「事件包含」于「事件们」于「事件可读」也。

「事件可写吗」乃化「整数」而「爻」也。
「事件可写吗」者会「事件们」而「事件包含」于「事件们」于「事件可写」也。

「事件错误吗」乃化「整数」而「爻」也。
「事件错误吗」者会「事件们」而「事件包含」于「事件们」于「事件错误」也。

「事件挂断吗」乃化「整数」而「爻」也。
「事件挂断吗」者会「事件们」而「事件包含」于「事件们」于「事件挂断」也。


「转换操作结果」乃承「甲」而化（「整数」合「甲」）而（「操作结果」于「甲」）也。
「转换操作结果」者受「甲」而会「原始结果」而
    虑「状态」者「原始结果」中「零」而
    虑「值」者「原始结果」中「一」而
    若「等于」于「状态」于「零」
    则（「操作成功」于「值」）
    否则（若「等于」于「状态」于「一」
        则（「暂需等待」于「值」）
        否则（若「等于」于「状态」于「二」
            则（「连接已关闭」授以「甲」）
            否则（（「操作失败」授以「甲」）于「状态」）））
也。


「监听」乃化「字符串」而化「整数」而化「整数」而（「操作结果」于「传输连接」）也。
「监听」者会「地址」而会「端口」而会「等待队列长度」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_监听』传「地址」与「端口」与「等待队列长度」））
也。

「开始连接」乃化「字符串」而化「整数」而（「操作结果」于「传输连接」）也。
「开始连接」者会「主机」而会「端口」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_开始连接』传「主机」与「端口」））
也。

「完成连接」乃化「传输连接」而（「操作结果」于「传输连接」）也。
「完成连接」者会「连接值」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_完成连接』传「连接值」））
也。

「接受」乃化「传输连接」而（「操作结果」于「传输连接」）也。
「接受」者会「监听套接字」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_接受』传「监听套接字」））
也。

「读取」乃化「传输连接」而化「整数」而（「操作结果」于「字符串」）也。
「读取」者会「连接值」而会「最大字节数」而
    「转换操作结果」授以「字符串」于
        （「数组转换」之「数组转对子」授以「整数」授以「字符串」于
            （《《C调用》》名『豫言_传输控制协议_读取』传「连接值」与「最大字节数」））
也。

「从字节序数写入」乃化「传输连接」而化「字符串」而化「整数」而（「操作结果」于「整数」）也。
「从字节序数写入」者会「连接值」而会「内容」而会「起始字节序数」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_从字节序数写入』传「连接值」与「内容」与「起始字节序数」））
也。

「写入」乃化「传输连接」而化「字符串」而（「操作结果」于「整数」）也。
「写入」者会「连接值」而会「内容」而「从字节序数写入」于「连接值」于「内容」于「零」也。

「等待」乃化「传输连接」而化「整数」而化「整数」而（「操作结果」于「整数」）也。
「等待」者会「连接值」而会「关注事件」而会「超时毫秒数」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_等待』传「连接值」与「关注事件」与「超时毫秒数」））
也。

「等待可读」乃化「传输连接」而化「整数」而（「操作结果」于「整数」）也。
「等待可读」者会「连接值」而会「超时毫秒数」而
    「等待」于「连接值」于「关注可读」于「超时毫秒数」
也。

「等待可写」乃化「传输连接」而化「整数」而（「操作结果」于「整数」）也。
「等待可写」者会「连接值」而会「超时毫秒数」而
    「等待」于「连接值」于「关注可写」于「超时毫秒数」
也。

「获取本地端口」乃化「传输连接」而（「操作结果」于「整数」）也。
「获取本地端口」者会「连接值」而
    「转换操作结果」授以「整数」于
        （「数组转换」之「数组转对子」授以「整数」授以「整数」于
            （《《C调用》》名『豫言_传输控制协议_获取本地端口』传「连接值」））
也。

「设置无延迟」乃化「传输连接」而化「爻」而（「操作结果」于「有」）也。
「设置无延迟」者会「连接值」而会「是否启用」而
    「转换操作结果」授以「有」于
        （「数组转换」之「数组转对子」授以「整数」授以「有」于
            （《《C调用》》名『豫言_传输控制协议_设置无延迟』传「连接值」与「是否启用」））
也。

「关闭写入」乃化「传输连接」而（「操作结果」于「有」）也。
「关闭写入」者会「连接值」而
    「转换操作结果」授以「有」于
        （「数组转换」之「数组转对子」授以「整数」授以「有」于
            （《《C调用》》名『豫言_传输控制协议_关闭写入』传「连接值」））
也。

「关闭」乃化「传输连接」而（「操作结果」于「有」）也。
「关闭」者会「连接值」而
    「转换操作结果」授以「有」于
        （「数组转换」之「数组转对子」授以「整数」授以「有」于
            （《《C调用》》名『豫言_传输控制协议_关闭』传「连接值」））
也。

「错误消息」乃化「整数」而「字符串」也。
「错误消息」者会「状态」而《《C调用》》名『豫言_传输控制协议_错误消息』传「状态」也。
