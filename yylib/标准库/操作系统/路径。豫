
寻系统名称之书。
观系统名称之书。

寻豫言标准内核之书。
观豫言标准内核之书。

路径分隔符者
    若行于当前系统是微软Windows
    则『\』
    否则『/』
也。

获取路径名乃化字符串而字符串也。
获取路径名者会路径而
    鉴串从右搜索单个于路径分隔符于路径而
    有「可无」则『』
    或有（可有下标）则获取子字符串于零于下标于路径也。
    
路径合并乃化字符串而化字符串而字符串也。
路径合并者会甲而会乙而
    甲附路径分隔符附乙也。