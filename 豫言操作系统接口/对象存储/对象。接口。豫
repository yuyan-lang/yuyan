寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「标准库」之「数据结构」之「多态列」之书。

「：文言：问一对象之元；无则返字面 null，有则返 JSON：大小、etag、httpEtag、上传毫秒、httpMetadata、customMetadata。汉语：读取对象元数据，不读取正文；对象不存在时返回字面量 null，否则返回 {"size":字节数,"etag":"…","httpEtag":"\"…\"","uploaded":上传时刻毫秒,"httpMetadata":{…},"customMetadata":{…}}。：」
「取对象元数据」乃化「字符串」而化「字符串」而「字符串」也。
「：文言：读对象之文，严守 UTF-8，逾限不读；无则字面 null，逾限返 tooLarge，否则返 JSON：文、大小、httpEtag、customMetadata。汉语：读取对象正文为字符串；对象大于字节上限（1 至 2097152）时不读取正文，返回 {"tooLarge":true,"size":n}；不存在返回 null；正文不是严格合法的 UTF-8 时抛出豫言异常；成功返回 {"text":"…","size":n,"httpEtag":"…","customMetadata":{…}}。：」
「读取对象文字」乃化「字符串」而化「字符串」而化「整数」而「字符串」也。
「：文言：读对象之全部字节，无则示阴，逾限则告事故；上限至多十六 MiB。汉语：读取对象正文为字节串，返回（爻 合 字节串）：对象不存在得（阴，空字节串）；存在且大小不超过字节上限得（阳，正文字节）；字节上限须在 1 至 16777216 之间，越界抛出豫言异常；对象大于字节上限时不读取正文并抛出豫言异常（需区分时先用取对象元数据查大小）。字节经宿主值桥直接传递，不经 JSON 转义。：」
「读取对象字节」乃化「字符串」而化「字符串」而化「整数」而（「爻」合「字节串」）也。
「：文言：写一对象之字节，可附条件、摘要与元数据；条件不合则返 written 为阴，不为过。汉语：写入对象字节；选项 JSON 可含 ifNoneMatch、ifMatch、sha256、contentType、cacheControl、customMetadata；成功返回 {"written":true,"size":n,"etag":"…","httpEtag":"…"}，前置条件不满足返回 {"written":false}（不抛异常）；摘要不符、超限、平台故障抛出豫言异常。：」
「写入对象」乃化「字符串」而化「字符串」而化「字符串」而化「字节串」而「字符串」也。
「：文言：写入对象文字，其字节即文之 UTF-8，余同写入对象。汉语：以字符串的 UTF-8 字节写入对象，选项与返回同写入对象。：」
「写入对象文字」乃化「字符串」而化「字符串」而化「字符串」而化「字符串」而「字符串」也。
「：文言：一次去诸键，至多一千；无者不为过。汉语：删除至多 1000 个对象；不存在的键不报错。：」
「删除对象」乃化「字符串」而化（「列」于「字符串」）而「有」也。
「：文言：按前缀、限额、续标与分隔符列诸对象，返 JSON。汉语：列举对象；选项 JSON 可含 prefix、limit（至多 1000）、cursor、delimiter、include（仅 customMetadata）；返回 {"objects":[…],"truncated":真假,"cursor":续标或 null,"delimitedPrefixes":[…]}。：」
「列举对象」乃化「字符串」而化「字符串」而「字符串」也。
「：文言：以对象之流径为当前入站之终答，体不入客器；无对象则返阴且不占终答。汉语：把对象存储对象的正文流直通为当前入站请求的最终响应，自动带 Content-Length 与 ETag，并叠加附加标头；HEAD 请求只读元数据、无正文；对象不存在返回阴且不占用最终响应。：」
「回应入站以对象存储对象」乃化「字符串」而化「字符串」而化「整数」而化（「列」于（「字符串」合「字符串」））而「爻」也。
