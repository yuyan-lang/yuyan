# 启动接口规范 0.1.0

此包定客所具之入口。客依此包，必导出逻辑接口模块「程序入口」之「启动程序」，型为有至有；宿主调用之，不得以同名宿主术充之。客包自定入口源文件之名，绑定者于其文中惟一求得同名同型之实现，映于逻辑模块，不以源文件名定公约。

宿主先核包版、接诸导入、设授权，而后调用。每运行实例惟调用一次；其术可候，返则此轮事毕，返值不为进程退码。浏览器或节点宿主可异其触发时机，不得异其型。若需久候输入，则客于入口内候之；若需逐次承网页请求，则别依网页服务包。
