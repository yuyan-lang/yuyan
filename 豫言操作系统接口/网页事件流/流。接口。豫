寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「数据结构」之「多态列」之书。

「：文言：即提响应头，程序续行，返流柄，柄惟此事可用。状态二百至二百九十九而非二零四、二零五；内容种类恒为 text/event-stream; charset=utf-8，Cache-Control 缺则置 no-store；附加标头严校，违则抛豫言之异。汉语：以服务端事件流回应当前入站请求：立即提交响应头，返回仅在本次事件内有效的不透明流柄，程序继续运行。状态须为 200 至 299 且不是 204、205；内容类型固定，Cache-Control 默认 no-store 可被覆盖；附加标头（至多 32 项，名为 RFC 9110 令牌，值仅可见 ASCII、空格、制表，禁用 Content-Length、Transfer-Encoding、Connection、Keep-Alive、Upgrade、TE、Trailer、Content-Type、Content-Encoding、Set-Cookie 及重复名）违规则抛豫言异常。：」
「开始入站事件流回应」乃化「整数」而化（「列」于（「字符串」合「字符串」））而「字符串」也。
「：文言：以 UTF-8 即书其文，至多六万五千五百三十六字节；零已书，一客已断，二流已终；缓冲满则阻至有隙或客断；客断之后应用可续行。汉语：把文字按 UTF-8 立即写入事件流，每次最多 65536 字节，超限抛豫言异常；返回 0 已写入、1 客户端已断开、2 流已由应用结束或中止；缓冲满时阻塞直到有空位或客户端断开；客户端断开之后应用仍可继续运行，此后写入只返回 1。：」
「写入入站事件流文字」乃化「字符串」而化「字符串」而「整数」也。
「：文言：察客已断否，不阻；既见断，恒为阳。汉语：返回是否已观察到客户端断开，不阻塞；一旦为阳恒为阳（此后结束或中止亦不改）。：」
「入站事件流已断开」乃化「字符串」而「爻」也。
「：文言：正终事件流，幂等。汉语：正常结束事件流，客户端读到正常结束；已结束、已中止或已断开的流不报错。：」
「结束入站事件流」乃化「字符串」而「有」也。
「：文言：异终事件流，用于上游败；因不告客；幂等。汉语：异常终止事件流，用于上游失败：宿主使响应体流以错误终止；原因（至多 1024 字节）只供宿主诊断，不发给客户端；幂等。：」
「中止入站事件流」乃化「字符串」而化「字符串」而「有」也。
