寻观「标准库」之「语言核心」之「内建类型」之书。

「：文言：以获授之邮件绑定，自发件址寄一纯文之信于一收件址；成败以返值表之：成则（阳，空文），败则（阴，简短之因），其因不含址与令牌。汉语：参数依次为邮件绑定名、发件地址、收件地址、主题、正文。先在豫言里校验（收件与发件各一个地址、至多 254 字节、无空白与 CR/LF；主题至多 200 字节且无 CR/LF；正文至多 1 MiB），再交给绑定的 send。成功返回（阳，空字符串）；参数无效、绑定未配置、平台同步或异步拒绝都返回（阴，简短原因），不抛异常；原因是固定的中文说明，不含地址、主题、正文或令牌。未授权的绑定名属部署错误，宿主直接中止当前请求。：」
「发送文字邮件」乃化「字符串」而化「字符串」而化「字符串」而化「字符串」而化「字符串」而（「爻」合「字符串」）也。
