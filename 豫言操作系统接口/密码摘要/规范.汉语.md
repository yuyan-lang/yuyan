# 密码摘要接口规范 0.1.0

`摘要SHA256十六进制` 对输入字符串的原始 UTF-8 字节计算 SHA-256，返回恰好 64 个 ASCII 小写十六进制字符，不加前缀、盐或换行。相同输入在浏览器、CF Worker、Node.js 和原生适配中必须得到相同结果。空字符串有效；输入处理或宿主密码原语失败时调用失败。本包只提供摘要计算，不宣称密码存储或消息认证安全语义；使用者仍须另行处理盐、口令派生和凭据寿命。
