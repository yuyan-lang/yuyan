# 前端补全计划：让云控制台前端用豫言浏览器应用重写

状态：2026-09-26 起草，由“前端接口”代理（F1）维护；“前端应用”代理（F2）据此写代码。本文分五部分：一、范围与结论；二、缺口表（前端用到的每一个浏览器能力，对照现有 OS 接口）；三、新增与升级的接口一览（签名速查）；四、进度表（F2 只使用“规范已提交”的接口；能构建、能验收的以“适配已提交／验证通过”为准）；五、F2 使用须知与需求留言。

## 一、范围与结论

**范围**：云仓 `应用/豫言云工作台/网页/{工作台,控制台,智能输入,命令总线,本地编译,行差异}.mjs` 与 `工具/包管理服务/网页/阅读状态.js`。代码浏览器（`应用/豫言代码浏览`）已从云仓删除，不在范围内。

**审计结论**（逐文件通读并对照当前的 `豫言操作系统接口/`、`豫言操作系统/适配/`、`库/拓展库/浏览器宿主/`、`宿主/浏览器/宿主.mjs`）：

1. 调研 `浏览器前端.md` 里最重的四个结构性阻塞——统一事件队列、句柄表压力、无 id 元素的事件委托、同步 `preventDefault`——**已由 `网页事件` 0.1.0、`网页界面` 0.2.0、`网页文树` 0.2.0、`网页消息` 0.2.0 解决**（含键规则 `仅选区为空/仅目标标识/仅当属性`、`data-yy` 操作键、一次建树、整树释放、每类型背压）；浏览器宿主实现已落库（提交 8aafbddb），92 例一致性验证通过。**不重做。**
2. 仍缺的是三类：**(甲)** 统一事件队列里“预留”的四类事件源——定时、事件流（SSE）、请求（非阻塞 fetch）、编译——还没有对应的 OS 接口包；另有储存、导航与历史、页面环境（时区、饼）、启动另一个豫言应用。**(乙)** 已有的通用 OS 接口（时间、随机数、网址定位、网址查询、日志、文字规整）只有“云工”适配，浏览器构建绑定不到。**(丙)** 现有接口的几处小缺口：按选择器过滤的同步拦截、事件载荷带目标属性与矩形、程序性点击、属性移除与读回、节点矩形与位置、选择器查询与节点读回、受限超文本装入（阅读状态与源码高亮用）。
3. 处置：新增 8 个接口包（网页定时、网页储存、网页导航、网页环境、网页请求、网页事件源、网页编译、网页应用），升级 3 个（网页事件 0.2.0、网页界面 0.3.0、网页文树 0.3.0），补 6 个浏览器适配（时间、随机数、网址定位、网址查询、日志、文字规整，目录名带“浏览器”，构建器在浏览器模式下优先选它）。宿主 JS 只在 `豫言操作系统/宿主/浏览器/宿主.mjs`；新增逻辑写成可脱离 Wasm 单测的工厂函数，业务规则一处写成、多处校验。
4. **批次**：第一批（控制台 `工作台/控制台/智能输入/命令总线/本地编译`）＝定时、储存、导航、环境、请求、事件源、编译、应用、网页事件 0.2.0、网页界面 0.3.0、四个浏览器适配（时间、随机数、网址定位、网址查询）；第二批（阅读状态）＝网页文树 0.3.0 与日志、文字规整的浏览器适配。

## 二、缺口表

“✓”＝现有接口已覆盖，F2 直接用；“新”＝本计划新增；“升”＝升级已有接口。

### 事件与输入

| 前端用到的能力（出处） | 现有覆盖 | 处置 |
| --- | --- | --- |
| click/input/change/submit 委托，动态无 id 按钮（工作台、控制台） | ✓ 网页事件 `订阅界面事件`（委托＋`data-yy` 操作键） | 无需新增 |
| keydown 同步拦截：Ctrl+L、无选区的 Ctrl+C、目标为命令框时的 ↑↓、候选框可见时的 ↑↓ Tab Enter Esc 空格、`?`（工作台、智能输入） | ✓ 键规则（`仅选区为空`、`仅目标标识`、`仅当属性`、`含组字`） | 无需新增 |
| compositionstart/end、`isComposing`（工作台、智能输入） | ✓ 事件载荷 `组字中` | 无需新增 |
| 捕获阶段 document click 拦截：只截共用导航的语言钮、菜单钮与 `a[href]`，并读被点链接的 `href`（工作台） | ✗ 订阅只能按全部后代拦截，载荷无被点元素的属性 | **升 网页事件 0.2.0**：策略加 `选择器`（最近匹配祖先才产生事件与动作）、`带属性`、`带矩形` |
| 程序性 `.click()` 回放语言钮（工作台 `界面.语言`） | ✗ | **升 网页界面 0.3.0**：`点击节点` |
| mousedown 阻止默认，免候选按钮抢焦点（智能输入） | ✓ 订阅 `mousedown`＋`阻止默认` | 无需新增 |
| dialog `cancel` 阻止默认、`close`（工作台） | ✓ 网页界面 `打开/关闭模态框`＋订阅 | 无需新增 |
| online/offline、visibilitychange、popstate、hashchange（工作台、阅读状态） | ✓ 类型 联机/可见性/历史 | 无需新增 |
| mouseover/mouseout/focusin/focusout＋`closest('.类型引用[title]')` 悬停提示（阅读状态） | ✗ 无选择器过滤，载荷无 `title` 与矩形 | 同上 网页事件 0.2.0（`选择器`＋`带属性`＋`带矩形`） |
| scroll（窗口捕获）、details `toggle`（阅读状态） | ✓ | 无需新增 |
| `pagehide` 时同步写会话储存（阅读状态） | ✗ 卸载时豫言事件循环不会再运行，任何 Wasm 应用都做不到 | 不提供。阅读状态每次 `toggle` 已即时记录，舍去 `pagehide` 备份 |
| document 上的自定义事件 `豫言语言改变`（detail 为 `"han"`/`"wen"`）、`豫言高亮请求/完成`（工作台） | ✓ 网页消息（订阅／发布，前缀“豫言”，详情为 JSON） | 无需新增 |

### 界面读写与节点树

| 前端用到的能力 | 现有覆盖 | 处置 |
| --- | --- | --- |
| `textContent`、`hidden/disabled/readOnly`、`className`、`classList.toggle`、`data-*`、`aria-*`、`role`、`title`、`tabIndex`、`href/target/rel`（全部文件） | ✓ 网页界面 0.2.0 / 网页文树 0.2.0 白册 | 无需新增 |
| `removeAttribute(...)`、`delete el.dataset.x`（`aria-busy`、`aria-activedescendant`、`data-sidebar`…约 7 处） | ✗ 只能设值不能移除 | 升 网页界面 0.3.0：`移除节点属性` |
| 读回属性与文字：`getAttribute`、`dataset`、`textContent`（含根元素 `data-lang`）（工作台、控制台） | 部分：仅语言标签、控件值 | 升 网页界面 0.3.0：`读取节点属性`（保留字 `文档根`、`页体`）、`读取节点文字` |
| `value` 读写、`selectionStart`、`setSelectionRange`、`getSelection().toString()`、`FormData`（逐控件读） | ✓ | 无需新增 |
| `focus()`、`document.activeElement`、`contains(activeElement)` | ✓ | 无需新增 |
| `scrollTop/scrollHeight/clientHeight` | ✓ `读取滚动度量`、`设置滚动顶` | 无需新增 |
| `getComputedStyle(el).display` | ✓ `读取节点可见` | 无需新增 |
| `getBoundingClientRect`、`style.left/top` 定位浮层（阅读状态） | ✗（`style` 属性按规范一律拒绝） | 升 网页界面 0.3.0：`读取节点矩形`、`设置节点位置`（只放两个整数，不开放 `style`） |
| `iframe.src`（工作台站点预览） | ✓ `设置页面框架地址` | 无需新增 |
| `document.title` | ✓ | 无需新增 |
| createElement＋append＋prepend＋before/after＋insertBefore＋replaceChildren＋remove（约 60 处建节点；日志行、差异、列表、表格、菜单） | ✓ 一次建树、插入、替换、移除、释放（句柄表 4096 有整树释放） | 无需新增 |
| `new Option()`、`<select>`、`<table>`、`<details>` | ✓ 白册含 select option table details summary | 无需新增 |
| fetch 同源模板＋DOMParser＋importNode 装入 AI 市场（控制台） | ✓ `装入页面模板`（模板里被选元素须有 id；F2 可给模板加 id） | 无需新增 |
| 读同源 JSON 目录（`/products/目录.json`）、`拼音字表.json`（控制台、智能输入） | ✓ `读取页面资源文字`（同源 GET，≤2 MiB） | 无需新增（字表 145 KB，用 `癸象解析`） |
| 动态 `import('/AI市场应用/入口.mjs')` 并 `启动豫言浏览器应用()`；`/cloud/用量应用/入口.mjs`（控制台） | ✗ | **新 网页应用**：`启动页面应用` |
| `innerHTML = 高亮成色`（源码视图；阅读状态悬停提示） | ✗ | 升 网页文树 0.3.0：`设置页面节点超文本`（白册清洗，第二批） |
| `querySelector(All)`、`closest`、`matches`、`parentElement`、`children`、`firstChild`、`TreeWalker`、`Range.cloneContents`（阅读状态对服务端渲染页的后加工） | ✗ | 升 网页文树 0.3.0：查询、读回、树导出、替换、移除属性（第二批，见第三部分） |
| 从 DOM 数据库统计（事件行 `dataset`）、`form.reset()` | 不是 OS 能力 | F2 改用数据模型；`form.reset()` 用 `设置节点值` 清空 |

### 定时、储存、导航、网络、事件源

| 前端用到的能力 | 现有覆盖 | 处置 |
| --- | --- | --- |
| `setTimeout/clearTimeout`（80 ms 同步防抖、100 ms 任务状态合并、5 s SSE 宽限、120 ms 日志冲刷、300 ms 重试）、`setInterval`（3 s 轮询、1 s 活动栏）（工作台、本地编译） | 宿主有低层原语（事件类型“定时”），无 OS 接口 | **新 网页定时** |
| `sessionStorage`（草稿，按账户＋项目分键）、`localStorage`（编译位置偏好；阅读状态的显赋、展开、定位）；写入配额失败要能感知 | `拓展库/浏览器宿主/储存` 有，无 OS 接口 | **新 网页储存** |
| `location.assign/replace/reload`、`history.pushState/replaceState` 只改地址不带状态 | `拓展库/浏览器宿主/导航与饼` 有，无 OS 接口 | **新 网页导航**（popstate 用 网页事件订阅“窗口”） |
| `location.href/search/pathname` 读取，`new URL(…, base)`、`URLSearchParams` 构造与解析、`encodeURIComponent` | 读地址 ✓（`读取页面网址`、`读取页面查询参数`）；`网址` 编码 ✓（浏览器适配）；`网址定位`、`网址查询` 只有云工适配 | **新 适配** 网址定位浏览器、网址查询浏览器 |
| `document.cookie` 读语言 `yuyan_lang` | ✗ | **新 网页环境**：`读取页面饼` |
| `Date` 本地时间：`toLocaleString('zh-CN',{month,day,hour,minute})`、`toLocaleTimeString` | ✗ 无时区 | **新 网页环境**：`读取本地时区偏移分钟`（格式化用纯豫言，固定 zh-CN 数字格式） |
| `Date.now()`、`Date.parse(ISO)`、`toISOString` | 时间接口有 `读取当前Unix毫秒`、`Unix毫秒转ISO文字`，只有云工适配 | **新 适配** 时间浏览器；ISO 解析用纯豫言（云台公共库） |
| `crypto.randomUUID()`×5（工作台、命令总线） | 随机数接口有，只有云工适配 | **新 适配** 随机数浏览器 |
| `fetch`：GET/POST JSON、`cache:'no-store'`、并发多个请求、读 `status/ok/json()`、`AbortController`（工作台 `api()`） | 网络 0.1.0 只是规范，无浏览器适配且为阻塞式 | **新 网页请求**：非阻塞发起＋事件＋取消，另附阻塞变体 |
| `EventSource('/cloud/api/projects/{id}/events')`，`onopen/onerror/onmessage`，自动重连并带 `Last-Event-ID`，连接时回放全部历史（可达上千条） | `拓展库/浏览器宿主/事件源` 是逐源阻塞等待，不进统一队列，无背压 | **新 网页事件源**：进统一队列，批事件，宿主按队列余量读流（不丢事件） |
| `console.error`（工作台一处） | 日志接口只有 Worker 适配 | 新 适配 日志浏览器（第二批） |
| 文字规整（trim、小写、UTF-16 长度） | 接口有，只有云工适配 | 新 适配 文字规整浏览器（第二批；F2 若用云台文本库则不需要） |
| `WebAssembly.promising` 探测、Worker＋Wasm 编译器（`本地编译.mjs` → `编译/客户端.mjs`） | ✗ | **新 网页编译**：`本地编译可用/启动本地编译/停止本地编译`，进度与结果进统一队列 |

### 明确不属 OS 的部分（F2 用纯豫言写）

正则（约 55 处，需手写扫描器）、Markdown 子集渲染、命令预览的未闭合 JSON 扫描、任务概况统计、行差异、命令总线状态机、SSE 帧解析（有了 `网页事件源` 后不再需要自解析）、ISO 时间解析、UTF-16 长度与 UTF-8 字节数、拼音字表反查（pinyin-pro 不迁）。

## 三、新增与升级的接口一览（签名速查）

完整语义、限额、失败与一致性样例以各包 `规范.汉语.md` 为准；此处只列签名与要点。类型记法同规范：`有`＝单位值；`爻`＝布尔；`（甲 合 乙）`＝二元组；JSON 文字都是 UTF-8 字符串。**所有新事件源都进同一个统一事件队列，用 `等待网页事件` 取（见网页事件规范）**，事件 JSON 里 `类型` 与 `订阅号` 区分来源。

### 网页定时 0.1.0（`定时。接口。豫`）

- `启动一次定时 : 整数(毫秒) → 字符串(标记) → 整数(定时号)`
- `启动重复定时 : 整数(间隔毫秒) → 字符串(标记) → 整数(定时号)`
- `取消定时 : 整数 → 爻`（曾存在且未到期为阳；同时清掉该定时号尚未取走的事件）

事件：类型 `定时`，`订阅号`＝定时号，`名称` 为“定时”，`详情` 为 `{"种类":"一次"或"重复","标记":文字,"时刻":Unix毫秒}`。同时至多 64 个定时器；周期定时器在上一个事件被取走前不重复投递。

### 网页储存 0.1.0（`储存。接口。豫`）

- `读取网页储存 : 字符串(区域) → 字符串(键) → （爻 合 字符串）`（存在与否，值）
- `写入网页储存 : 字符串(区域) → 字符串(键) → 字符串(值) → 爻`（配额满、隐私模式等失败返回阴，不抛事故）
- `删除网页储存 : 字符串(区域) → 字符串(键) → 有`

区域只有 `会话`（sessionStorage）与 `本地`（localStorage）；键不加前缀，原样使用；键至多 512、值至多 1 MiB（UTF-16 码元）。

### 网页导航 0.1.0（`导航。接口。豫`）

- `前往网址 : 字符串 → 有`（`location.assign`，解析后只允许 http/https）
- `替换当前网址 : 字符串 → 有`（`location.replace`）
- `重载页面 : 有 → 有`
- `压入历史 : 字符串 → 有`、`替换历史 : 字符串 → 有`（`pushState/replaceState(null,'',url)`，只允许同源）
- `移动历史 : 整数 → 有`（`history.go`）

回退与前进产生的 `popstate`、`hashchange` 用网页事件包订阅“窗口”取得（类型 `历史`）。

### 网页环境 0.1.0（`环境。接口。豫`）

- `读取本地时区偏移分钟 : 整数(Unix毫秒) → 整数`（东为正：北京时间为 480；按该时刻的夏令时计）
- `读取页面饼 : 字符串(名) → （爻 合 字符串）`（只读当前页面可见的饼，原样返回）

### 网页请求 0.1.0（`请求。接口。豫`）

- `发起网页请求 : 字符串(请求 JSON) → 整数(请求号)`：非阻塞；结果作为类型 `请求` 的事件投递，`订阅号`＝请求号，`名称` 为“完成”或“失败”，`详情` 为响应对象。
- `取消网页请求 : 整数 → 爻`：取消后保证不再收到该请求号的事件。
- `请求网页文字 : 字符串(请求 JSON) → 字符串(响应 JSON)`：阻塞变体（挂起当前调用，其余事件照常排队）。

请求 JSON：`{"方法":"GET","网址":"/cloud/api/session","标头":{"Content-Type":"application/json"},"正文":文字,"缓存":"不缓存","超时毫秒":60000,"正文上限":8388608,"标记":文字}`（除网址外皆可省）。只允许同源。响应对象：成功 `{"成":true,"状态":200,"状态文":"OK","标头":{小写名:值},"正文":文字,"标记":文字}`；失败 `{"成":false,"原因":"网络错误|超时|正文过大|正文不是有效UTF-8","说明":文字,"标记":文字}`。任何 HTTP 状态码（含 4xx、5xx）都算“完成”。并发至多 32。

### 网页事件源 0.1.0（`事件源。接口。豫`）

- `打开事件源 : 字符串(网址) → 字符串(选项 JSON) → 整数(事件源号)`
- `关闭事件源 : 整数 → 爻`

选项 JSON：`{"起始事件号":"…","重连毫秒":3000}`（可省）。事件：类型 `事件流`，`订阅号`＝事件源号，`名称`：`打开`；`消息`（详情 `{"批":[{"事件号":"12","事件名":"message","数据":"…"},…]}`，一批 1 至 256 项，宿主在队列有积压时不再读流，**不丢事件**）；`错误`（详情 `{"状态":"重连中"或"已关闭","原因":文字}`）。断线自动带 `Last-Event-ID` 重连（`重连中` 之后可再收到 `打开`）；HTTP 非 200、内容类型不对属致命，`已关闭` 后不再有事件。仅同源；同时至多 8 个。

### 网页编译 0.1.0（`编译。接口。豫`）

- `本地编译可用 : 有 → 爻`（JSPI、Worker、DecompressionStream 齐备）
- `启动本地编译 : 字符串(文件 JSON {"路径":"内容",…}) → 整数(编译号)`
- `停止本地编译 : 有 → 爻`

事件：类型 `编译`，`订阅号`＝编译号；`名称` 为“进度”（详情 `{"批":[{"类":"输出|诊断|阶段","流":"stdout|stderr","文字":…,"标签":…},…]}`）或“完成”（详情 `{"成":爻,"标准输出":文字,"标准错误":文字,"错误":文字,"阶段":文字,"产物摘要":文字}`）。同一时刻只有一个编译；编译客户端默认从页面同目录 `编译/客户端.mjs` 加载。

### 网页应用 0.1.0（`应用。接口。豫`）

- `启动页面应用 : 字符串(同源入口路径，如 /AI市场应用/入口.mjs) → 有`：加载并启动另一个豫言浏览器应用（其入口模块导出 `启动豫言浏览器应用`），挂起到它就绪；子应用此后独立运行。路径须以 `/` 开头、以 `/入口.mjs` 结尾、不含 `..`、查询与反斜杠。

### 网页事件 0.2.0（升级；新增策略字段，旧函数与旧策略不变）

`订阅界面事件` 的策略 JSON 新增：`选择器`（CSS 选择器，≤512 字符，订阅时校验）、`带属性`（≤8 个属性名，事件载荷里带 `属性` 对象）、`带矩形`（载荷带 `矩形` `{"左","上","右","下"}`）。有 `选择器` 时：只有事件目标的“最近匹配祖先（止于订阅边界）”存在才产生事件并执行阻止默认等动作；载荷的 `标识`、`操作键`、`属性`、`矩形` 都取该匹配元素，另加 `目标标识`（原始目标的 ID）。

### 网页界面 0.3.0（升级；追加函数）

- `读取节点属性 : 字符串(标识) → 字符串(名) → （爻 合 字符串）`（保留字 `文档根`、`页体` 指 `<html>`、`<body>`；布尔属性存在时值为空文字）
- `移除节点属性 : 字符串 → 字符串 → 有`
- `读取节点文字 : 字符串 → 字符串`（`textContent`）
- `点击节点 : 字符串 → 有`（程序性 `click()`）
- `设置节点位置 : 字符串 → 整数 → 整数 → 有`（`left/top` 像素，−100000 至 100000）
- `读取节点矩形 : 字符串 → 字符串`（JSON `{"左","上","右","下"}`，视口坐标，取整）

### 网页文树 0.3.0（升级，第二批；签名以第二批规范为准，此处为规划）

`查询页面节点`（选择器→句柄列表）、`读取页面节点标签/属性/文字/父级/矩形`、`查找最近页面祖先`、`页面节点匹配选择器`、`读取页面节点树`（导出为 `构建页面节点树` 的描述形状，供豫言侧改写后重建）、`替换页面节点`、`移除页面节点属性`、`设置页面节点超文本`（白册清洗）。

### 浏览器适配（既有接口，新目录）

`适配/时间浏览器`（0.2.0）、`适配/随机数浏览器`（0.1.0）、`适配/网址定位浏览器`（0.2.0）、`适配/网址查询浏览器`（0.1.0）；第二批：`适配/日志浏览器`（0.1.0）、`适配/文字规整浏览器`（0.2.0）。接口包本身不动。

## 四、进度表

阶段：**规划**＝仅本文；**规范**＝`。接口。豫` 与双语规范已提交（F2 可依此写代码，但需适配才能构建）；**适配**＝豫言适配与宿主已提交，可构建；**验证**＝一致性验证通过。

| 项 | 批次 | 状态 | 提交 |
| --- | --- | --- | --- |
| 审计与本计划 | 一 | 规划 | 本提交 |
| 网页定时 0.1.0 | 一 | 规划 | |
| 网页储存 0.1.0 | 一 | 规划 | |
| 网页导航 0.1.0 | 一 | 规划 | |
| 网页环境 0.1.0 | 一 | 规划 | |
| 网页请求 0.1.0 | 一 | 规划 | |
| 网页事件源 0.1.0 | 一 | 规划 | |
| 网页编译 0.1.0 | 一 | 规划 | |
| 网页应用 0.1.0 | 一 | 规划 | |
| 网页事件 0.2.0（选择器、带属性、带矩形） | 一 | 规划 | |
| 网页界面 0.3.0 | 一 | 规划 | |
| 适配 时间浏览器、随机数浏览器、网址定位浏览器、网址查询浏览器 | 一 | 规划 | |
| 网页文树 0.3.0 | 二 | 规划 | |
| 适配 日志浏览器、文字规整浏览器 | 二 | 规划 | |
| 登记文本（总纲表行与文言段、总览段） | 末 | 规划 | 交主代理 |

## 五、F2 使用须知与需求留言

**事件循环形态**：应用只有一个阻塞点 `等待网页事件`，返回的 JSON 里 `类型` 取 界面／消息／定时／事件流／请求／编译／可见性／联机／历史／关闭。请求号、定时号、事件源号、编译号、界面订阅号都出现在 `订阅号` 字段；应用自己保存“号 → 处理”的对应表。旧的 `等待界面事件`、`等待网页消息` 可与它混用，但同一个事件只被取走一次。

**限额速记**：句柄表 4096（用一次建树后立即释放）；界面订阅 ≤256；定时 ≤64；并发请求 ≤32；事件源 ≤8；每类型事件队列默认 1024（`设置网页事件队列上限` 可调到 65536）。

**同步决定必须预先声明**：阻止默认与停止传播只能在订阅时写进策略；要按运行状态改变，请取消并重新订阅，或用键规则的 `仅当属性`。程序性 `点击节点` 会经过页面上的所有订阅：回放被拦截的点击时，须先 `取消订阅界面事件` 再 `点击节点`，随后重订。

**时间与区域**：`读取当前Unix毫秒`（时间接口）加 `读取本地时区偏移分钟` 得本地时间，格式化固定为 zh-CN 数字样式，用纯豫言写；旧代码的 `toLocaleString` 输出会有细微差异，需在报告里说明。

**需求留言**（F2 只在本节末尾追加一行“日期｜需求｜原因”，一次 Edit）：

