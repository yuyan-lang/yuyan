# 剪贴板接口规范 0.1.0

此包是独立的必需能力组；应用声明依赖后，宿主必须提供完整实现。`写剪贴板文字` 把一条豫言字符串作为纯文本写入当前用户的系统剪贴板，成功返回仅表示写入操作已完成，不保证后续仍是同一内容。函数可挂起。宿主不得把内容当 HTML、命令或程序执行。

浏览器可要求安全上下文、焦点或用户手势；其他宿主可按本机策略要求授权。权限不足、剪贴板不可用或操作失败时须报告调用失败，不得假称成功，也不得在失败时改写其他持久数据。宿主不具备写入能力时不声明此包。首版不含读取、图像、多格式和剪贴板变化监听。
