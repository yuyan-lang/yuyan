寻观「标准库」之「语言核心」之「内建类型」之书。

「：文言：以 JSON 文入原子事务，回调循当前执行入口。汉语：在当前持久仓原子执行豫言事务处理，参数和结果均为有效 JSON 文字。：」
「执行持久事务」乃化「字符串」而「字符串」也。
「：文言：仅事务处理入口可取其参数。汉语：只在事务处理入口读取本次事务参数。：」
「读取持久事务参数」乃化「有」而「字符串」也。
「：文言：缺键以阴辨之，真值可为 JSON null。汉语：读取本事务内的键，返回存在标记和 JSON 文字，缺键与 JSON null 不混淆。：」
「读取持久事务键值」乃化「字符串」而（「爻」合「字符串」）也。
「：文言：写入当前事务之键，事务败则撤。汉语：在当前事务内写入有效 JSON 文字，失败时随事务回滚。：」
「写入持久事务键值」乃化「字符串」而化「字符串」而「有」也。
「：文言：以 JSON 文定事务之果，一次惟一。汉语：将有效 JSON 文字设置为当前事务的唯一结果。：」
「完成持久事务」乃化「字符串」而「有」也。

「：文言：删事务内之键，事务败则撤。汉语：在当前事务内删除一个键；键不存在时无事发生，事务失败时随之回滚。键按 UTF-8 计须为 1 至 2048 字节。：」
「删除持久事务键」乃化「字符串」而「有」也。
「：文言：按选项列事务内诸键，见本事务未提交之写。汉语：在当前事务内按选项列举键值，返回 JSON 数组文字 [[键,值],…]（值为存入的 JSON 值本身），并能看到本事务尚未提交的写入；选项为 JSON 对象文字，字段 prefix、start、startAfter、end、limit、reverse 均可省，空字符串视同未指定。：」
「列举持久事务键值」乃化「字符串」而「字符串」也。
「：文言：弃本事务诸写，此后不得再读写本事务之键。汉语：放弃当前事务已作的全部写入；此后本事务的读写会被平台拒绝，处理入口仍须调用一次完成持久事务。：」
「回滚持久事务」乃化「有」而「有」也。
「：文言：仓外直读一键，缺与 JSON null 不混。汉语：在持久对象的普通事件里直接读取一个键，返回存在标记和 JSON 值文字；缺键为阴与空字符串，已存 JSON null 为阳与 null。不可在事务处理入口内使用。：」
「读取持久仓键值」乃化「字符串」而（「爻」合「字符串」）也。
「：文言：仓外直写一键，值须为有效 JSON 文且不逾二兆。汉语：在持久对象的普通事件里直接写入一个 JSON 值文字（不超过 2 MiB，不含宿主保留的单键对象），失败抛可捕获的豫言异常；不可在事务处理入口内使用。：」
「写入持久仓键值」乃化「字符串」而化「字符串」而「有」也。
「：文言：仓外直删一键，返其原有与否。汉语：在持久对象的普通事件里直接删除一个键，键原先存在返回阳，否则返回阴。：」
「删除持久仓键」乃化「字符串」而「爻」也。
「：文言：仓外按选项列诸键，结果同事务内之列举。汉语：在持久对象的普通事件里按选项列举键值，选项与结果格式同列举持久事务键值；结果文字超过 8 MiB 时失败，应用应以 limit 分页。：」
「列举持久仓键值」乃化「字符串」而「字符串」也。
「：文言：尽清本对象之仓。汉语：清空当前持久对象的全部持久数据（键值；在 SQLite 存储上还含 SQL 表，平台按兼容日期决定是否连告警一并清除）；不可在事务处理入口内使用。：」
「清空持久仓」乃化「有」而「有」也。
