寻观「标准库」之「语言核心」之「内建类型」之书。

「：文言：依同源入口之径载而启另一豫言浏览器应用，候其就绪而返；径须以斜杠首、以“/入口.mjs”终，无上行、查询与反斜杠；同径不得再启，同时至多八个；败则事故。汉语：启动另一个同源的豫言浏览器应用：参数是同源入口模块的站内路径（例如 /AI市场应用/入口.mjs）。宿主动态加载该模块并调用它导出的 启动豫言浏览器应用，挂起当前调用直到子应用就绪（它第一次进入事件等待或已结束），再返回；子应用此后独立运行，直到页面结束或父应用结束（宿主关闭时子应用一并关闭）。路径须以 / 开头且不以 // 开头，以 /入口.mjs 结尾，不含 ..、查询、片段与反斜杠，至多 512 个 UTF-16 码元；同一路径不能重复启动，同时至多启动 8 个子应用；模块不存在、未导出启动函数、子应用启动失败或 30 秒内未就绪时抛事故。：」
「启动页面应用」乃化「字符串」而「有」也。
