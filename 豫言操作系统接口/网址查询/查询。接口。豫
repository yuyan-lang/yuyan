寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「数据结构」之「多态列」之书。

「：文言：以有序之名值列成表单编码之查询串：空格作「+」，余字依 UTF-8 作百分号编码，同名之项守其序；至多一零二四项，总字节不逾 1 MiB。汉语：把有序的名称值列表编码成 application/x-www-form-urlencoded 查询串（URLSearchParams 规则）：空格写作加号，除字母、数字与 *-._ 外一律按 UTF-8 百分号编码（含 !'()~），同名项按原序保留；最多 1024 项、总字节不超过 1 MiB。：」
「编码查询串」乃化（「列」于（「字符串」合「字符串」））而「字符串」也。
「：文言：析表单编码之文：加号为空格，坏 UTF-8 代以 U+FFFD，首之问号先去一个，返守序之 [[名,值],…] JSON 数组文；所入不逾 1 MiB。汉语：按 WHATWG application/x-www-form-urlencoded 规则解析（加号为空格，百分号解码，非法 UTF-8 以 U+FFFD 替换，首字符 ? 先去掉一个，与 URLSearchParams 构造器一致），返回保持原序的 [[名,值],…] JSON 数组文字；输入不超过 1 MiB。：」
「解析表单键值」乃化「字符串」而「字符串」也。
「：文言：依 URL 与 searchParams.set 之义改网址之一查询参，旧同名者尽易为一，余参皆留；绝对 HTTP(S) 址返全 href，相对址以固定内部基址析之，惟返径、查询与片段，且不得易源。汉语：按 URL 与 searchParams.set 语义设置网址的一个查询参数（同名旧值全部替换为一项，其余参数保留并按表单规则重新序列化）；绝对 http(s) 网址返回完整 href，相对网址以固定内部基址解析后只返回 pathname、search 与 hash，且不得借此改变来源。：」
「设置网址查询参数」乃化「字符串」而化「字符串」而化「字符串」而「字符串」也。
