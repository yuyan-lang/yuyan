# 网址定位之约 0.1.0

`解析绝对网址部件` 受绝对 `http:` 或 `https:` 址；`根据根址解析网址部件` 受待析文与绝对 HTTP(S) 根址。二术循 WHATWG URL 之规析之、规其径与源，返 JSON 物文，定四字段：`href`、`origin`、`pathname`、`search`，皆为文。`search` 无片段，有查询则含首 `?`，无则空。坏址、非 HTTP(S) 或根非绝对，则败。

此包惟析址，不授所得址之联网权；客用其果前，仍须验源、根径与业务号。今 `_start` 以 JSON 文传部件，后可易豫言记录型而不改 URL 之义。
