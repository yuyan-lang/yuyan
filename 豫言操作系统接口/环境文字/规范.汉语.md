# 环境文字接口规范 0.1.0

`读取授权环境文字` 接受环境绑定名，返回该绑定的文字值；缺失时返回空字符串。宿主资源许可控制可读名称，未授权、值不是文字或读取失败时调用失败。空字符串与缺失不可区分，应用需要区分时应在自己的配置契约中另设标记。

此包不枚举环境、不写入变量，也不隐式允许读取密钥。密钥与普通文字配置应由部署许可分别授权。浏览器、Node.js、CF Worker 与原生宿主可依各自环境实现。
