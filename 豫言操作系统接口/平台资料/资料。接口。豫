寻观「标准库」之「语言核心」之「内建类型」之书。

「：
文言：列某资料集之键，以前缀择之，序以 UTF-8 字节。
汉语：列出平台资料集里键以「前缀」起首的全部键，返回 JSON 数组文字（字符串数组，按 UTF-8 字节序升序；前缀为空串则列出全部）。资料集名是平台定义的名字（云工：标准库），1 至 64 字节、不含控制字符；前缀至多 512 字节；结果至多 4096 个键、JSON 文字至多 1 MiB。资料集未知、未授权或资料不可用时抛出可捕获的豫言异常（消息「<集名>资料不可用」或「未知的平台资料集：<集名>」）。
：」
「列出平台资料键」乃化「字符串」而化「字符串」而「字符串」也。
「：
文言：读某资料集之一键之文；无则阴。
汉语：读取平台资料集里一个键的文字：键存在返回（阳，文字），不存在返回（阴，空串）。键 1 至 512 字节；文字至多 2 MiB（更大的条目视为不存在，且不会被列出）。资料集的问题同 列出平台资料键（抛出可捕获的豫言异常）。
：」
「读取平台资料文字」乃化「字符串」而化「字符串」而（「爻」合「字符串」）也。
