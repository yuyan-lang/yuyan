寻观「标准库」之「语言核心」之「内建类型」之书。

「：文言：开独占之区，以名与 JSON 参授处理入口，区内他事皆候，毕而得 JSON 果。汉语：在当前持久对象内开启并发独占区并重入应用的 durable-block 处理入口：区内可等待外部输入输出，其他事件排队等候；参数和结果均为有效 JSON 文字（各不超过 8 MiB）；区内失败抛可捕获的豫言异常，并且不重置对象。：」
「执行持久独占」乃化「字符串」而化「字符串」而「字符串」也。
「：文言：独占区之处理入口取本次操作名。汉语：只在 durable-block 处理入口读取本次操作名。：」
「读取持久独占名」乃化「有」而「字符串」也。
「：文言：独占区之处理入口取本次 JSON 参。汉语：只在 durable-block 处理入口读取本次参数 JSON 文字，与调用者传入的文字一致。：」
「读取持久独占参数」乃化「有」而「字符串」也。
「：文言：以 JSON 文定独占区之果，一次惟一。汉语：把有效 JSON 文字设置为当前独占区的唯一结果，每次独占区必须且只能成功调用一次。：」
「完成持久独占」乃化「字符串」而「有」也。
