# 关系查询公约 0.2.0

`查询关系库首行` 受获授权库名与预备 SQL，返首行 UTF-8 JSON 之文；无行则字面 `null`。宿主核库名之许可；未授、库错或所得不可序为 JSON，则此请败。SQL 于所授之库行之，此约不称惟读；授此能即许客向该库陈 SQL。客可依其类型析文，公约包惟依标准库。

`查询关系库首行带文字参数` 更受诸文参，依次缚于 SQL 占位，不以拼文代缚参。空列与无参之术同义；参量或其型不合，则由库报败。初版惟缚文参，无整参、字节参、事务及流式诸行。今云工适配接 D1，别的平台可接同类关系库。此包惟司查询，HTTP 答另取能力包。
