寻观「标准库」之「语言核心」之「内建类型」之书。

「：文言：去文首尾之空白，其义同 ECMAScript 之 String.prototype.trim（含 U+3000、U+00A0、U+FEFF、U+2028、U+2029 等）；输入至多一 MiB。汉语：去掉字符串首尾的空白和行终止符，语义与 JS 的 trim 完全相同（含全角空格 U+3000、NBSP U+00A0、字节序标记 U+FEFF、U+2028、U+2029 等）；输入不超过 1 MiB（UTF-8），超出则抛出豫言异常。：」
「去首尾空白」乃化「字符串」而「字符串」也。
「：文言：依完整 Unicode 映射转小写，其义同 String.prototype.toLowerCase（土耳其 İ 化为 i 加 U+0307，希腊 Σ 循词尾之规）；输入至多一 MiB。汉语：按完整 Unicode 映射转小写，语义与 JS 的 toLowerCase 完全相同（例如 İ 变成 i 加组合点 U+0307，希腊大写 Σ 按词尾规则变 ς 或 σ）；输入不超过 1 MiB，超出则抛出豫言异常。：」
「转小写」乃化「字符串」而「字符串」也。
「：文言：返文之 UTF-16 码元数，其义同 JS 字符串之 length（增补平面之字计二）；输入至多一 MiB。汉语：返回字符串的 UTF-16 码元数，语义与 JS 的 length 相同（增补平面字符如 emoji 计为 2）；输入不超过 1 MiB，超出则抛出豫言异常。：」
「UTF16长度」乃化「字符串」而「整数」也。
「：文言：文非空，且诸字皆 Unicode 之字母（\p{L}）或数字（\p{N}），则阳；其义同 /^[\p{L}\p{N}]+$/u.test，所用 Unicode 之版由宿主引擎定；输入至多一 MiB。汉语：字符串非空，且每个码点都属 Unicode 通用类别的字母（L）或数字（N）时为阳，语义与 JS 的 /^[\p{L}\p{N}]+$/u.test 相同（含汉字、假名、带音标字母与各种文字的数字；不含下划线、连字符、空白、标点、符号与 emoji）；空串为阴；所用 Unicode 版本由宿主的 JavaScript 引擎决定；输入不超过 1 MiB，超出则抛出豫言异常。：」
「全为字母或数字吗」乃化「字符串」而「爻」也。
