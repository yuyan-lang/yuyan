# 网页消息接口

此能力组让豫言应用订阅、等待和发布当前文档的应用消息。名称必须以“豫言”开头；detail 以 UTF-8 JSON 文字传递，最多 1 MiB。订阅的消息进入本应用实例事件队列，关闭时等待函数以“关闭”为名称返回。发布后的事件在当前文档同步派发，应用须自行用请求编号配对响应。

浏览器适配只传递事件名称和 JSON 值，不能执行全局回调或把 JavaScript 对象句柄交给应用。此接口不承诺跨窗口、跨 Worker 或持久消息；需要这些语义时另建能力包。当前宿主必须有文档事件队列，没有文档的平台不提供本组。
