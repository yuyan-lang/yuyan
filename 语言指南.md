# 豫言语法指南 — 内建操作符与关键字

保持裸字（不用「」）的内建符号。其余所有标识符加「」。

## 核心语法
```
虑 者 而                 -- let绑定
递归虑 其 者 而           -- 表达式级递归let绑定
也                      -- 类型标注 / 语句终止
于                      -- 函数调用
授以                    -- 隐式函数调用
之                      -- 结构投影
；                      -- 顺序执行
鉴 有 则 或             -- 模式匹配
若 否则                  -- 条件分支
```

## 类型
```
化〇而〇                -- 函数类型
承〇而〇 承〇者□而〇      -- 多态类型
会□而〇                -- 函数参数
受〇而会□而〇           -- 隐式类型参数
与 合 中 其实            -- 对子
```

## 声明
```
立 乃 者 即 也
术 交 无序 盖谓        -- 操作符声明（将移除）
寻 观 诵 导入 打开 导出 并打开 之 书
```

## 内置类型/值
```
元类型 爻 有 整数 小数 字符串 动态分类值
阳 阴 元
```

## 操作符声明中用到的特殊标记
```
〇 -- 操作数占位符
□ -- 绑定名占位符
```

## 字面量
```
『字符串』 《《内建类型：整数》》 《《内建爻：阳》》
「一二三四五六七八九」 「三点一四」
```

## 注释
```
「：注释：」
```
