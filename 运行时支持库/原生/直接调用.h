#ifndef 豫言直接调用_H
#define 豫言直接调用_H
#include "垃圾回收器.h"
#include "影子根栈.h"
#include <setjmp.h>
uint64_t 获取构造器序数(豫言值 参数);

/* 文言：同形尾调，随模块定其参。汉语：生成模块按实际最大元数定义签名；运行时独立编译采用默认声明，不调用此类型。 */
#ifndef 豫言直接参数
#define 豫言直接参数 豫言值 参数0, 豫言值 参数1, 豫言值 参数2, 豫言值 参数3, 豫言值 参数4, 豫言值 参数5, 豫言值 参数6, 豫言值 参数7, 豫言值 参数8, 豫言值 参数9, 豫言值 参数10, 豫言值 参数11, 豫言值 参数12, 豫言值 参数13, 豫言值 参数14, 豫言值 参数15
#endif
typedef 豫言值 (*豫言直接函数)(豫言直接参数);

typedef struct 豫言直接退出域 {
    jmp_buf 跳转;
    豫言影子根帧 *根顶;
    struct 豫言直接退出域 *前域;
    volatile 豫言值 *结果槽;
    uint64_t 激活号;
} 豫言直接退出域;
extern 豫言直接退出域 *豫言_直接退出顶;
豫言值 豫言_直接进入退出域(豫言直接退出域 *域, volatile 豫言值 *结果槽);
_Noreturn void 豫言_直接提前退出(豫言值 令牌, 豫言值 结果);

static inline 豫言值 豫言_直接函数值(豫言直接函数 函数) {
    return (豫言值)(uintptr_t)函数 | ((豫言值)函数指针类型标记 << 64);
}
static inline 豫言直接函数 豫言_直接取函数(豫言值 值) {
    return (豫言直接函数)(uintptr_t)值;
}
static inline 豫言值 豫言_直接造标签(uint64_t 序, uint64_t 长, 豫言值 载荷) {
    设置豫言值类型(&载荷, 13);
    设置豫言值子类型(&载荷, 长);
    设置豫言值原始长度(&载荷, 序);
    return 载荷;
}
static inline 豫言值 豫言_直接取载荷(豫言值 值) {
    return 原始元组转豫言值(获取构造器元组长度(值), (豫言值 *)(uintptr_t)值);
}
#endif
