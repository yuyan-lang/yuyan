#include "公共包含.h"

豫言值 豫言_获取命令行程序名(){
    return 静态字符串转豫言值(全局参数值[0]);
}


豫言值 豫言_获取命令行参数(){

    if(全局参数数量 < 2){
        豫言值 元素[] = {};
        return 数组转同构列(0, 元素);
    }

    豫言值 参数字符串[全局参数数量 - 1];

    for (int 序数 = 0; 序数 < 全局参数数量 - 1; 序数 ++){
        参数字符串[序数] = 静态字符串转豫言值(全局参数值[序数 + 1]);
    }

    豫言值 结果 = 数组转同构列(全局参数数量 - 1, 参数字符串);
    return 结果;
}
