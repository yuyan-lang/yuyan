#ifndef 豫言宿主协议
#define 豫言宿主协议
/* 文言：界上惟传整数与字节，不传客内指针之义。汉语：协议版本固定；地址均为 guest 线性内存偏移，整数按小端编码。 */
enum { 宿主启动 = 1, 宿主等待, 宿主结果信息, 宿主读输出, 宿主释放,
       宿主核数, 宿主存上下文, 宿主多路等待, 宿主真实路径 };
/* 文言：捕获、弃流、继承输出、尽承标准流。汉语：与原生子进程 API 的四种标准流模式对应。 */
enum { 宿主捕获, 宿主弃流, 宿主继承输出, 宿主继承标准流 };
#endif
