# 持久独占适配一致性验证

持久事务、持久告警、持久独占三个适配共用一套验证，放在 `豫言操作系统/宿主/云工/一致性验证/`：那里的试验应用同时依赖三个接口，Node 测试用真实豫言 Wasm 逐个覆盖函数、失败情形与限额，另有对本地 workerd 的差分比对。复跑方法（构建、环境变量、命令）见该目录的 `说明.汉语.md`。

本适配相关的用例（`持久对象壳.test.mjs`）：名称与参数往返、区内仓与事务、区内失败与缺结果与重复结果与坏结果不重置对象、入参校验与限额、4 MiB 大参数与大结果、并发独占区不交错、区内到达的普通事件被挡到区毕、独占区时限。
