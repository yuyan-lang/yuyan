寻观「标准库」之书。

「：
文言：集名、前缀、键之限，皆本公约。
汉语：接口规范里的限额：资料集名 1 至 64 字节（不含控制字符），前缀至多 512 字节，键 1 至 512 字节。
：」
「集名上限」乃「整数」也。
「集名上限」者「六四」也。
「前缀上限」乃「整数」也。
「前缀上限」者「五一二」也。
「键上限」乃「整数」也。
「键上限」者「五一二」也。
「参数无效文」乃「字符串」也。
「参数无效文」者『平台资料参数无效』也。

「：
文言：名中不得有控制之字（零至三十一及一百二十七）。
汉语：按字节检查字符串里没有控制字符（0 至 31 与 127）；用于资料集名。
：」
「无控制字符」乃化「字符串」而化「整数」而「爻」也。
「无控制字符」者会「文」而会「位」而
 若「非」于（「小于」于「位」于（「字节长度」于「文」））则「阳」否则
 虑「字节」者「字符转整数」于「文」于「位」而
 若「或者」于（「小于」于「字节」于「三二」）于（「等于」于「字节」于「一二七」）则「阴」否则「无控制字符」于「文」于（「加」于「位」于「一」）也。
「验集名」乃化「字符串」而「有」也。
「验集名」者会「集」而
 若「或者」于（「等于」于（「字节长度」于「集」）于「零」）于（「或者」于（「大于」于（「字节长度」于「集」）于「集名上限」）于（「非」于（「无控制字符」于「集」于「零」）））
 则「发生事故」授以「有」于「参数无效文」否则「元」也。

「：
文言：宿主之术二：列键与读文，皆以值返其败，不令 JS 异越界。
汉语：宿主原语（由云工宿主的 平台资料.mjs 提供，能力表里登记）：列键安全 返回（是否成功，JSON 数组文字或错误文）；读文字安全 返回（状态，文字或错误文），状态 0 键存在、1 键不存在、2 失败。宿主已核对集名、授权与资料，并按字节序排序、按前缀筛键。
：」
「平台资料列键宿主」乃化「字符串」而化「字符串」而（「爻」合「字符串」）也。
「平台资料列键宿主」者会「集」而会「前缀」而《《C调用》》名『豫言_云工_平台资料列键安全』传「集」与「前缀」也。
「平台资料读文字宿主」乃化「字符串」而化「字符串」而（「整数」合「字符串」）也。
「平台资料读文字宿主」者会「集」而会「键」而《《C调用》》名『豫言_云工_平台资料读文字安全』传「集」与「键」也。

「：
文言：列某集之键，先验其参，后问宿主；败则告异。
汉语：实现 列出平台资料键：先检查集名与前缀的限额（违规抛「平台资料参数无效」），再调宿主原语；宿主失败（未知集、未授权、资料不可用）抛出以宿主消息为文的豫言异常，成功返回 JSON 字符串数组文字。
：」
「列出平台资料键」乃化「字符串」而化「字符串」而「字符串」也。
「列出平台资料键」者会「集」而会「前缀」而
 虑「无」者「验集名」于「集」而
 若「大于」于（「字节长度」于「前缀」）于「前缀上限」则「发生事故」授以「字符串」于「参数无效文」否则
 虑「果」者「平台资料列键宿主」于「集」于「前缀」而
 若「果」中「零」则（「果」中「一」）否则「发生事故」授以「字符串」于（「果」中「一」）也。

「：
文言：读某集某键之文；键在则（阳，文），无则（阴，空），败则告异。
汉语：实现 读取平台资料文字：先检查集名与键的限额（违规抛「平台资料参数无效」），再调宿主原语；状态 0 得（阳，文字），状态 1 得（阴，空串），状态 2 抛出以宿主消息为文的豫言异常。
：」
「读取平台资料文字」乃化「字符串」而化「字符串」而（「爻」合「字符串」）也。
「读取平台资料文字」者会「集」而会「键」而
 虑「无」者「验集名」于「集」而
 若「或者」于（「等于」于（「字节长度」于「键」）于「零」）于（「大于」于（「字节长度」于「键」）于「键上限」）则「发生事故」授以（「爻」合「字符串」）于「参数无效文」否则
 虑「果」者「平台资料读文字宿主」于「集」于「键」而
 若「等于」于（「果」中「零」）于「零」则（「阳」与（「果」中「一」））否则
 若「等于」于（「果」中「零」）于「一」则（「阴」与『』）否则
 「发生事故」授以（「爻」合「字符串」）于（「果」中「一」）也。
