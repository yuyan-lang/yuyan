寻观「标准库」之书。
寻观「拓展库」之「癸象工具」之「总集」之书。
寻观「拓展库」之「癸象工具」之「癸象访问」之书。
寻观「拓展库」之「浏览器宿主」之「核心」之书。
寻观「豫言操作系统网页事件」之「事件」之书。
寻观「豫言操作系统网页界面」之「页面」之书。
寻观「豫言操作系统网页文树」之「节点」之书。
寻观「豫言操作系统网页消息」之「消息」之书。

「：文言：参数取自癸象数组，越界则空。汉语：从请求的参数数组里取第几个值，越界返回癸象空。：」
「参之」乃化（「列」于「癸象」）而化「整数」而「癸象」也。
「参之」者会「诸」而会「序」而
 若「小于」于「序」于（「长度」授以「癸象」于「诸」）则（「第N个」授以「癸象」于「序」于「诸」）否则「癸象空」也。
「串参」乃化（「列」于「癸象」）而化「整数」而「字符串」也。
「串参」者会「诸」而会「序」而「癸象取字符串」于（「参之」于「诸」于「序」）于『』也。
「整参」乃化（「列」于「癸象」）而化「整数」而「整数」也。
「整参」者会「诸」而会「序」而「癸象取整数」于（「参之」于「诸」于「序」）于「零」也。
「爻参」乃化（「列」于「癸象」）而化「整数」而「爻」也。
「爻参」者会「诸」而会「序」而「癸象取爻」于（「参之」于「诸」于「序」）于「阴」也。
「列参」乃化（「列」于「癸象」）而化「整数」而（「列」于「字符串」）也。
「列参」者会「诸」而会「序」而
 （「态射」授以「癸象」授以「字符串」）于（「癸象取数组」于（「参之」于「诸」于「序」））于（会「项」而「癸象取字符串」于「项」于『』）也。
「爻文」乃化「爻」而「字符串」也。
「爻文」者会「值」而若「值」则『true』否则『false』也。

「分派宿主」乃化「字符串」而化（「列」于「癸象」）而「字符串」也。
「分派宿主」者会「操作」而会「参」而
 若「串相等」于「操作」于『等待浏览器事件』则「等待浏览器事件」于「元」否则
 若「串相等」于「操作」于『等待网页消息』则
  （虑「果」者「等待网页消息」于「元」而（「果」中「零」）附『|』附（「果」中「一」））否则
 若「串相等」于「操作」于『定时一次』则「浏览器定时一次」于（「整参」于「参」于「零」）于（「串参」于「参」于「一」）否则
 若「串相等」于「操作」于『定时重复』则「浏览器定时重复」于（「整参」于「参」于「零」）于（「串参」于「参」于「一」）否则
 若「串相等」于「操作」于『取消定时』则「爻文」于（「浏览器取消定时」于（「串参」于「参」于「零」））否则
 「发生事故」授以「字符串」于（『未知探针操作：』附「操作」）也。

「分派文树」乃化「字符串」而化（「列」于「癸象」）而「字符串」也。
「分派文树」者会「操作」而会「参」而
 若「串相等」于「操作」于『取得页面节点』则「取得页面节点」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『新建页面节点』则「新建页面节点」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）否则
 若「串相等」于「操作」于『设置页面节点文字』则（「设置页面节点文字」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置页面节点属性』则（「设置页面节点属性」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『添加页面子节点』则（「添加页面子节点」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『清空页面子节点』则（「清空页面子节点」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『设置页面标题』则（「设置页面标题」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『读取页面查询参数』则「读取页面查询参数」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『读取页面资源文字』则「读取页面资源文字」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『构建页面节点树』则「构建页面节点树」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『插入页面子节点前』则（「插入页面子节点前」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『替换页面子节点』则（「替换页面子节点」于（「串参」于「参」于「零」）于（「列参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『移除页面节点』则（「移除页面节点」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『释放页面节点』则（「释放页面节点」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『追加页面节点文字』则（「追加页面节点文字」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置页面框架地址』则（「设置页面框架地址」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『装入页面模板』则（「装入页面模板」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『读取页面网址』则「读取页面网址」于「元」否则
 「分派宿主」于「操作」于「参」也。

「分派界面」乃化「字符串」而化（「列」于「癸象」）而「字符串」也。
「分派界面」者会「操作」而会「参」而
 若「串相等」于「操作」于『读取节点值』则「读取节点值」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『设置节点值』则（「设置节点值」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点显示』则（「设置节点显示」于（「串参」于「参」于「零」）于（「爻参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点禁用』则（「设置节点禁用」于（「串参」于「参」于「零」）于（「爻参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点只读』则（「设置节点只读」于（「串参」于「参」于「零」）于（「爻参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点类名』则（「设置节点类名」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点类标记』则（「设置节点类标记」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「爻参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『聚焦节点』则（「聚焦节点」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『读取聚焦标识』则「读取聚焦标识」于「元」否则
 若「串相等」于「操作」于『节点包含焦点』则「爻文」于（「节点包含焦点」于（「串参」于「参」于「零」））否则
 若「串相等」于「操作」于『设置输入光标』则（「设置输入光标」于（「串参」于「参」于「零」）于（「整参」于「参」于「一」）于（「整参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『读取输入光标』则
  （虑「果」者「读取输入光标」于（「串参」于「参」于「零」）而（「整数表示」于（「果」中「零」））附『,』附（「整数表示」于（「果」中「一」）））否则
 若「串相等」于「操作」于『读取选区文字』则「读取选区文字」于「元」否则
 若「串相等」于「操作」于『读取滚动度量』则「读取滚动度量」于（「串参」于「参」于「零」）否则
 若「串相等」于「操作」于『设置滚动顶』则（「设置滚动顶」于（「串参」于「参」于「零」）于（「整参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『打开模态框』则（「打开模态框」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『关闭模态框』则（「关闭模态框」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『读取页面状态』则「读取页面状态」于「元」否则
 若「串相等」于「操作」于『读取节点可见』则「爻文」于（「读取节点可见」于（「串参」于「参」于「零」））否则
 「分派文树」于「操作」于「参」也。

「分派消息」乃化「字符串」而化（「列」于「癸象」）而「字符串」也。
「分派消息」者会「操作」而会「参」而
 若「串相等」于「操作」于『订阅网页消息』则（「订阅网页消息」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『取消订阅网页消息』则（「取消订阅网页消息」于（「串参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『发布网页消息』则（「发布网页消息」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『读取页面语言』则「读取页面语言」于「元」否则
 若「串相等」于「操作」于『设置节点文字』则（「设置节点文字」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）；『』）否则
 若「串相等」于「操作」于『设置节点属性』则（「设置节点属性」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）；『』）否则
 若「串相等」于「操作」于『等待界面事件』则
  （虑「果」者「等待界面事件」于「元」而（「果」中「零」）附『|』附（「果」中「一」））否则
 「分派界面」于「操作」于「参」也。

「分派事件」乃化「字符串」而化（「列」于「癸象」）而「字符串」也。
「分派事件」者会「操作」而会「参」而
 若「串相等」于「操作」于『等待网页事件』则「等待网页事件」于「元」否则
 若「串相等」于「操作」于『订阅界面事件』则「整数表示」于（「订阅界面事件」于（「串参」于「参」于「零」）于（「串参」于「参」于「一」）于（「串参」于「参」于「二」）） 否则
 若「串相等」于「操作」于『取消订阅界面事件』则（「取消订阅界面事件」于（「整参」于「参」于「零」）；『』）否则
 若「串相等」于「操作」于『读取网页事件丢弃数』则「整数表示」于（「读取网页事件丢弃数」于「元」）否则
 若「串相等」于「操作」于『读取网页事件类型丢弃数』则「整数表示」于（「读取网页事件类型丢弃数」于（「串参」于「参」于「零」））否则
 若「串相等」于「操作」于『设置网页事件队列上限』则（「设置网页事件队列上限」于（「串参」于「参」于「零」）于（「整参」于「参」于「一」）；『』）否则
 「分派消息」于「操作」于「参」也。

「回复」乃化「整数」而化「爻」而化「字符串」而「有」也。
「回复」者会「号」而会「成」而会「文」而
 「发布网页消息」于『豫言测试回复』于（「癸象表示」于（「癸象对象」于（「列」【『号』与（「癸象整数」于「号」），『成』与（「癸象爻」于「成」），『果』与（「癸象字符串」于「文」）】）））也。

「处理」乃化「字符串」而「有」也。
「处理」者会「详情」而
 虑「请求」者「癸象解析」于「详情」而
 虑「号」者「癸象字段整数」于「请求」于『号』于「零」而
 虑「操作」者「癸象字段字符串」于「请求」于『操作』于『』而
 虑「参」者「癸象字段数组」于「请求」于『参』而
 虑「果」者「尝试运行」授以（「爻」合「字符串」）于（会「无」而（「阳」与（「分派事件」于「操作」于「参」）））于（会「信息」而（「阴」与「信息」））而
 「回复」于「号」于（「果」中「零」）于（「果」中「一」）也。

「循环」乃化「有」而「有」也。
「循环」者会「无」而
 虑「事」者「等待网页消息」于「元」而
 若「串相等」于（「事」中「零」）于『关闭』则「元」否则
 （「处理」于（「事」中「一」）；「循环」于「元」）也。

虑「无」者「订阅网页消息」于『豫言测试命令』而
「循环」于「元」。
