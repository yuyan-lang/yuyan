# 持久告警适配一致性验证

持久事务、持久告警、持久独占三适配共用一套验证，置于 `豫言操作系统/宿主/云工/一致性验证/`：彼处试验应用同依三接口，Node 测试以真豫言 Wasm 逐一覆盖诸术、败之情形与限，又有对本地 workerd 之差分比对。复跑之法（构建、环境变量、命令）见彼目录之 `说明.文言.md`。

此适配相关之用例（`持久对象壳.test.mjs`）：读设换删与非法时刻、事务内告警与事务同命运、告警事件的重试数与失败重试、告警内重设与删除、告警入口内开独占区与事务、非持久对象事件的失败、每事件时限（告警放宽到 5000 毫秒成功、250 毫秒失败）。
