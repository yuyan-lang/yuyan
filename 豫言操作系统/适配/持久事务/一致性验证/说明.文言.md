# 持久事务适配一致性验证

持久事务、持久告警、持久独占三适配共用一套验证，置于 `豫言操作系统/宿主/云工/一致性验证/`：彼处试验应用同依三接口，Node 测试以真豫言 Wasm 逐一覆盖诸术、败之情形与限，又有对本地 workerd 之差分比对。复跑之法（私有暂存之构建、环境变量、命令）见彼目录之 `说明.文言.md`。

此适配相关之用例（`持久对象壳.test.mjs`）：直接仓（读写删、null 与缺键、非法写入、列举选项、列举超限、清空）与持久事务（读写删列、回滚、失败不提交、事务内列举、事务函数的使用位置、事务运行期间他事排队、回滚后复用）以及额度应用回归。
