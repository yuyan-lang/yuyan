# 浏览器网页界面适配

`页面。豫` 实现[豫言操作系统网页界面接口](../../../豫言操作系统接口/网页界面/规范.汉语.md)，把已有浏览器宿主的文树、属性和事件队列收束为四个中文函数。应用只见元素 ID、纯文本、受限属性以及事件名称和目标 ID，不见原生 DOM 句柄。`实现接口。豫` 固定公共包身份与版本，接口检查器须在构建前核对四项签名。

此包是当前编译器的过渡适配；纯 `。接口。豫` 仍须通过后续绑定器进入普通编译链。节点创建、选择器、默认动作、剪贴板、网络等能力不属于本包。
