# 浏览器网页界面适配

`页面。豫` 承[网页界面之约](../../../豫言操作系统接口/网页界面/规范.文言.md)，以旧浏览器宿主之文树与事件，出四项中文公术。客惟见标识、纯文、受限属性、事名与目标标识，不得原生文树之柄。`实现接口。豫` 固其包籍与版本；构前当核四术之签名。

此乃旧编译器之过渡适配；纯 `。接口。豫` 犹待绑定入常构。造节点、选择符、默认动作、剪贴、网络，不属此包。
