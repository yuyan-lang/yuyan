寻观「标准库」之书。
寻观「标准库」之「数据结构」之「字符串术」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「拓展库」之「癸象工具」之「总集」之书。
寻观「拓展库」之「云工宿主」之「核心」之书。

「：文言：诸限之数，平台以十进千字节计之（一 KB 为一千字节）。消息体以规范化 JSON 之 UTF-8 字节计。汉语：单条消息体上限 128 KB = 128000 字节；一批各体合计上限 256 KB = 256000 字节（平台按十进制千字节计，本地 workerd 实测单条恰 128000 字节可投、128001 字节被拒）；一批条数上限 100；延迟上限 12 小时；批量入参文字上限 1 MiB（只用来限制适配的工作量）。：」
「消息体字节上限」乃「整数」也。
「消息体字节上限」者「一二八零零零」也。
「批量字节上限」乃「整数」也。
「批量字节上限」者「二五六零零零」也。
「批量条数上限」乃「整数」也。
「批量条数上限」者「一零零」也。
「延迟秒上限」乃「整数」也。
「延迟秒上限」者「四三二零零」也。
「批量输入字节上限」乃「整数」也。
「批量输入字节上限」者「一零四八五七六」也。
「：文言：消息体之嵌套至多二十四层：宿主桥自身限三十二层，此限使投递之参数必不逾之。汉语：JSON 嵌套层数上限（数组或对象各算一层）；宿主桥对参数和结果的嵌套限制是 32 层，取 24 层使投递参数不会触及它。：」
「消息体嵌套上限」乃「整数」也。
「消息体嵌套上限」者「二四」也。

「：文言：以原字节交宿主，由 TextDecoder 还为 JS 字符串，得其 JSON 字面量；不经豫言之 JSON 转义，故任何字符无损。汉语：把字符串的 UTF-8 字节交给宿主，用 TextDecoder（ignoreBOM: true）还原成 JS 字符串，返回宿主用 JSON.stringify 写出的字面量；不经过豫言的 JSON 编码器，因此控制字符也不会出错。：」
「宿主字面」乃化「字符串」而「字符串」也。
「宿主字面」者会「文」而
 虑「字节号」者「云工字节成句柄」于（「字符串转字节串」于「文」）而
 虑「解码器号」者「取云工句柄」于（「云工构造对象」于『TextDecoder』于『["utf-8",{"ignoreBOM":true}]』）而
 虑「字面」者「云工调用方法」于「解码器号」于『decode』于（『[』附（「云工句柄引用文」于「字节号」）附『]』）而
 虑「无」者「云工释放句柄」于「字节号」而
 虑「无」者「云工释放句柄」于「解码器号」而
 「字面」也。

「：文言：反其道：宿主之 JSON 字面量经 TextEncoder 编为字节而取回，豫言不析转义。汉语：把宿主返回的 JSON 字符串字面量交给 TextEncoder 编成 UTF-8 字节并取回还原，豫言一侧完全不解析 JSON 转义。：」
「字面还原为文」乃化「字符串」而「字符串」也。
「字面还原为文」者会「字面」而
 虑「编码器号」者「取云工句柄」于（「云工构造对象」于『TextEncoder』于『[]』）而
 虑「字节号」者「取云工句柄」于（「云工调用方法」于「编码器号」于『encode』于（『[』附「字面」附『]』））而
 虑「字节」者「云工句柄取字节」于「字节号」而
 虑「无」者「云工释放句柄」于「编码器号」而
 虑「无」者「云工释放句柄」于「字节号」而
 「云工解码文字」于『utf-8』于「阴」于「阳」于「字节」也。

「：文言：仅由可见 ASCII 而成、且无引号与反斜线者，可径以引号裹之。汉语：文字全部是 0x20 至 0x7E 的可见 ASCII 且不含双引号和反斜线时，可以直接加引号成为 JSON 字面量。：」
「普通字面可用」乃化「字符串」而「爻」也。
「普通字面可用」者会「文」而
 虑「字节」者「字符串转字节串」于「文」而
 虑「长」者「字节串长度」于「字节」而
 递归虑「查」其化「整数」而「爻」者会「位」而
  若「等于」于「位」于「长」则「阳」否则
  虑「码」者「字节串取字节」于「字节」于「位」而
  若「或者」于（「小于」于「码」于「三二」）于（「或者」于（「大于」于「码」于「一二六」）于（「或者」于（「等于」于「码」于「三四」）于（「等于」于「码」于「九二」）））则「阴」否则「查」于（「加」于「位」于「一」）
 而「查」于「零」也。

「安全字面」乃化「字符串」而「字符串」也。
「安全字面」者会「文」而
 若「普通字面可用」于「文」则『"』附「文」附『"』否则「宿主字面」于「文」也。

「：文言：宿主对象之记号（$句柄、$未定义、$大整数、$数字）不属 JSON；规范化之 JSON 中，记号恒为首键，故以「{"记号":」求之，既不漏亦不误。汉语：句柄桥用单键对象 {"$句柄":…} 等表示宿主值；宿主生成的规范化 JSON 里它们只会以“{"记号":”的形式出现（字符串里的引号都带反斜线），所以子串检查既不漏报也不会把字符串内容误判。：」
「含宿主记号」乃化「字符串」而「爻」也。
「含宿主记号」者会「文」而
 「或者」于（「或者」于（「是子字符串吗」于『{"$句柄":』于「文」）于（「是子字符串吗」于『{"$未定义":』于「文」））
  于（「或者」于（「是子字符串吗」于『{"$大整数":』于「文」）于（「是子字符串吗」于『{"$数字":』于「文」））也。

「：文言：自宿主之误 JSON 取其辞；辞之字面经宿主还原，控制之字亦无损。汉语：错误 JSON 形如 {"名称":…,"消息":…}；取出消息的字面量并经宿主还原为字符串，取不到时返回整个错误文。：」
「错误消息」乃化「字符串」而「字符串」也。
「错误消息」者会「错误文」而
 若「非」于（「是子字符串吗」于『,"消息":』于「错误文」）则「错误文」否则
 鉴（「串从左搜索单个」于『,"消息":』于「错误文」）而
  有（「可有」于「位」）则（
   虑「起」者「加」于「位」于（「串长度」于『,"消息":』）而
   虑「止」者「减」于（「串长度」于「错误文」）于「一」而
   「字面还原为文」于（「获取子字符串」于「起」于「止」于「错误文」））
  或有「可无」则「错误文」也。

「：文言：调宿主之法而不使 JS 之误越桥；败则以平台原辞为豫言之异。汉语：用 云工调用方法安全 调用，把同步异常和 Promise 拒绝转成豫言异常，异常消息是给定前缀加平台报告的原文。：」
「安全调用」乃化「字符串」而化「字符串」而化「字符串」而化「字符串」而「字符串」也。
「安全调用」者会「句柄」而会「方法」而会「参数」而会「前缀」而
 虑「结果」者「云工调用方法安全」于「句柄」于「方法」于「参数」而
 若「结果」中「零」则（「结果」中「一」）否则「发生事故」授以「字符串」于（「前缀」附（「错误消息」于（「结果」中「一」）））也。

「：文言：数规范化 JSON 之最大嵌套层数（数组或对象各一层），串内之字不计。汉语：扫描规范化 JSON 文字，返回括号的最大嵌套深度；字符串内部（含转义）不当结构。：」
「最大嵌套深度」乃化「字符串」而「整数」也。
「最大嵌套深度」者会「文」而
 递归虑「走」其化（「列」于「字符串」）而化「爻」而化「爻」而化「整数」而化「整数」而「整数」者
  会「余」而会「在串」而会「转义」而会「深」而会「最大」而
  鉴「余」而
   有「罄」则「最大」
   或有（「缀」于「字」于「尾」）则
    虑「码」者「字符转整数」于「字」于「零」而
    若「转义」则「走」于「尾」于「在串」于「阴」于「深」于「最大」否则
    若「在串」则
     （若「等于」于「码」于「九二」则「走」于「尾」于「阳」于「阳」于「深」于「最大」否则
      若「等于」于「码」于「三四」则「走」于「尾」于「阴」于「阴」于「深」于「最大」否则
      「走」于「尾」于「阳」于「阴」于「深」于「最大」）否则
    若「等于」于「码」于「三四」则「走」于「尾」于「阳」于「阴」于「深」于「最大」否则
    若「或者」于（「等于」于「码」于「九一」）于（「等于」于「码」于「一二三」）则
     （虑「新深」者「加」于「深」于「一」而「走」于「尾」于「阴」于「阴」于「新深」于（「整数最大值」于「最大」于「新深」））否则
    若「或者」于（「等于」于「码」于「九三」）于（「等于」于「码」于「一二五」）则「走」于「尾」于「阴」于「阴」于（「减」于「深」于「一」）于「最大」否则
    「走」于「尾」于「阴」于「阴」于「深」于「最大」
  而「走」于（「获取字符」于「文」）于「阴」于「阴」于「零」于「零」也。

「：文言：先交宿主 JSON.parse 而复 JSON.stringify：验其为 JSON，去其冗白，正其转义，并使数字循 ECMAScript 之义；又拒宿主记号，使消息体不得冒宿主对象。汉语：让宿主执行 JSON.parse 再 JSON.stringify，得到规范化文字，同时验证语法与嵌套层数上限（批量时数组与项对象另计两层）；规范化文字里若出现句柄桥的记号（如 {"$句柄":…}，包括借 \u0024 转义拼出的键、以及 1e999 变成的无穷大）则拒绝，防止消息体伪造宿主对象。数字按 IEEE 754 双精度取值，与 JS 一致。：」
「规范化JSON」乃化「字符串」而化「字符串」而化「整数」而「字符串」也。
「规范化JSON」者会「文」而会「名」而会「深度上限」而
 虑「JSON号」者「云工全局句柄」于『JSON』而
 虑「结果」者「云工调用方法安全」于「JSON号」于『parse』于（『[』附（「宿主字面」于「文」）附『]』）而
 若「非」于（「结果」中「零」）则
  （若「是子字符串吗」于『嵌套过深』于（「结果」中「一」）则「发生事故」授以「字符串」于（「名」附『嵌套超过 24 层』）
   否则「发生事故」授以「字符串」于（「名」附『不是有效的 JSON 文字』））否则
 虑「规范」者「结果」中「一」而
 若「含宿主记号」于「规范」则「发生事故」授以「字符串」于（「名」附『含有不属于 JSON 的值或宿主专用记号』）否则
 若「大于」于（「最大嵌套深度」于「规范」）于「深度上限」则「发生事故」授以「字符串」于（「名」附『嵌套超过 24 层』）否则「规范」也。

「验绑定名」乃化「字符串」而「有」也。
「验绑定名」者会「绑定名」而
 若「串相等」于「绑定名」于『』则「发生事故」授以「有」于『队列绑定名不得为空』否则「元」也。

「验延迟」乃化「整数」而「有」也。
「验延迟」者会「秒」而
 若「或者」于（「小于」于「秒」于「零」）于（「大于」于「秒」于「延迟秒上限」）则「发生事故」授以「有」于『延迟秒数须在 0 至 43200 之间』否则「元」也。

「：文言：投递选项：恒以 json 存之；延迟为零则不书。汉语：send 的选项对象：内容类型固定为 json；延迟为 0 时不写 delaySeconds。：」
「发送选项」乃化「整数」而「字符串」也。
「发送选项」者会「秒」而
 若「等于」于「秒」于「零」则『{"contentType":"json"}』
 否则『{"contentType":"json","delaySeconds":』附（「整数表示」于「秒」）附『}』也。

「：文言：投一书。正文验为 JSON、去宿主记号、不逾一二八 KB（一二八零零零字节），乃交 send。汉语：向授权队列投递一条消息；规范化后的 JSON 直接放进 send 的参数文字，不经豫言 JSON 库。：」
「发送队列消息」乃化「字符串」而化「字符串」而化「整数」而「有」也。
「发送队列消息」者会「绑定名」而会「正文」而会「延迟」而
 虑「无」者「验绑定名」于「绑定名」而
 虑「无」者「验延迟」于「延迟」而
 若「大于」于（「字节长度」于「正文」）于「消息体字节上限」则「发生事故」授以「有」于『消息体超过 128000 字节』否则
 虑「规范」者「规范化JSON」于「正文」于『消息体』于「消息体嵌套上限」而
 若「大于」于（「字节长度」于「规范」）于「消息体字节上限」则「发生事故」授以「有」于『消息体超过 128000 字节』否则
 虑「队列号」者「云工队列绑定」于「绑定名」而
 虑「参数」者『[』附「规范」附『,』附（「发送选项」于「延迟」）附『]』而
 虑「无」者「安全调用」于「队列号」于『send』于「参数」于『队列发送失败：』而
 「元」也。

「：文言：收一元：字列反者，合而为文，非空则入积。汉语：把反向累积的字符列合并成一个元素文字，非空时放到反向的元素列表头部。：」
「收元素」乃化（「列」于「字符串」）而化（「列」于「字符串」）而（「列」于「字符串」）也。
「收元素」者会「现」而会「积」而
 虑「元」者「串组合并」于（「反转」授以「字符串」于「现」）而
 若「等于」于（「字节长度」于「元」）于「零」则「积」否则（「缀」授以「字符串」于「元」于「积」）也。

「：文言：拆一 JSON 容器之顶层诸元。入者为容器首字之后之字列；遇顶层逗号则断，遇顶层闭括则止；串内之字（含转义）不作结构。规范化 JSON 无空白，故元文即其原文。汉语：把一个 JSON 容器（数组或对象）拆成顶层元素的文字：输入是去掉开头括号后的字符列；在括号深度为 0 的逗号处分隔，遇到深度 0 的闭括号结束；字符串内部（含转义）不当结构。输入须是规范化 JSON，元素文字就是原文。：」
「拆顶层」乃化（「列」于「字符串」）而（「列」于「字符串」）也。
「拆顶层」者会「诸字」而
 递归虑「走」其化（「列」于「字符串」）而化「整数」而化「爻」而化「爻」而化（「列」于「字符串」）而化（「列」于「字符串」）而（「列」于「字符串」）者
  会「余」而会「深」而会「在串」而会「转义」而会「现」而会「积」而
  鉴「余」而
   有「罄」则「反转」授以「字符串」于「积」
   或有（「缀」于「字」于「尾」）则
    虑「码」者「字符转整数」于「字」于「零」而
    若「转义」则「走」于「尾」于「深」于「在串」于「阴」于（「缀」于「字」于「现」）于「积」否则
    若「在串」则
     （若「等于」于「码」于「九二」则「走」于「尾」于「深」于「阳」于「阳」于（「缀」于「字」于「现」）于「积」否则
      若「等于」于「码」于「三四」则「走」于「尾」于「深」于「阴」于「阴」于（「缀」于「字」于「现」）于「积」否则
      「走」于「尾」于「深」于「阳」于「阴」于（「缀」于「字」于「现」）于「积」）否则
    若「等于」于「码」于「三四」则「走」于「尾」于「深」于「阳」于「阴」于（「缀」于「字」于「现」）于「积」否则
    若「或者」于（「等于」于「码」于「九一」）于（「等于」于「码」于「一二三」）则「走」于「尾」于（「加」于「深」于「一」）于「阴」于「阴」于（「缀」于「字」于「现」）于「积」否则
    若「或者」于（「等于」于「码」于「九三」）于（「等于」于「码」于「一二五」）则
     （若「大于」于「深」于「零」则「走」于「尾」于（「减」于「深」于「一」）于「阴」于「阴」于（「缀」于「字」于「现」）于「积」
      否则「反转」授以「字符串」于（「收元素」于「现」于「积」））否则
    若「且」于（「等于」于「码」于「四四」）于（「等于」于「深」于「零」）则「走」于「尾」于「深」于「阴」于「阴」于（「罄」授以「字符串」）于（「收元素」于「现」于「积」）否则
    「走」于「尾」于「深」于「阴」于「阴」于（「缀」于「字」于「现」）于「积」
  而「走」于「诸字」于「零」于「阴」于「阴」于（「罄」授以「字符串」）于（「罄」授以「字符串」）也。

「：文言：自顶层元中取以某前缀起者之值文；无则空文。汉语：在顶层键值对文字列表里找以给定前缀（如 "body":）开头的第一项，返回前缀之后的值文字；没有则返回空字符串。：」
「取对值」乃化（「列」于「字符串」）而化「字符串」而「字符串」也。
「取对值」者会「诸对」而会「前缀」而鉴「诸对」而
 有「罄」则『』
 或有（「缀」于「对」于「余」）则（若「串开头是它吗」于「前缀」于「对」则「去除串开头」于「前缀」于「对」否则「取对值」于「余」于「前缀」）也。

「是数字码」乃化「整数」而「爻」也。
「是数字码」者会「码」而「且」于（「非」于（「小于」于「码」于「四八」））于（「小于」于「码」于「五八」）也。

「：文言：批中延迟之文：空则无；否则须为一至五位数字且不逾四万三千二百。汉语：解析批量项里的 delaySeconds 文字；空文字表示未给出（0）；否则必须是不超过 5 位的十进制整数且不大于 43200。：」
「解析批延迟」乃化「字符串」而「整数」也。
「解析批延迟」者会「文」而
 若「串相等」于「文」于『』则「零」否则
 虑「字节」者「字符串转字节串」于「文」而
 虑「长」者「字节串长度」于「字节」而
 虑「全数字」者（若「大于」于「长」于「五」则「阴」否则
  递归虑「查」其化「整数」而「爻」者会「位」而
   若「等于」于「位」于「长」则「阳」否则
   若「是数字码」于（「字节串取字节」于「字节」于「位」）则「查」于（「加」于「位」于「一」）否则「阴」
  而「查」于「零」）而
 若「非」于「全数字」则「发生事故」授以「整数」于『批量消息项的 delaySeconds 须为 0 至 43200 的整数』否则
 虑「值」者「理解整数」于「文」而
 若「大于」于「值」于「延迟秒上限」则「发生事故」授以「整数」于『批量消息项的 delaySeconds 须为 0 至 43200 的整数』否则「值」也。

「：文言：解一批项：必为对象，唯许 body 与 delaySeconds，body 必有且不逾一二八 KB（一二八零零零字节）；返（投递项文，体之字节数）。汉语：解析规范化 JSON 数组里的一项：必须是对象，只允许 body 与 delaySeconds 两个键，body 必须存在且不超过 128 KB（128000 字节）；返回（交给 sendBatch 的项文字，body 的字节数）。项文字固定加上 contentType:json。：」
「解析批项」乃化「字符串」而（「字符串」合「整数」）也。
「解析批项」者会「项文」而
 若「非」于（「且」于（「串开头是它吗」于『{』于「项文」）于（「串结尾是它吗」于『}』于「项文」））则「发生事故」授以（「字符串」合「整数」）于『批量消息的每一项须为 JSON 对象』否则
 虑「诸对」者「拆顶层」于（「放」授以「字符串」于「一」于（「获取字符」于「项文」））而
 虑「体文」者「取对值」于「诸对」于『"body":』而
 虑「延迟文」者「取对值」于「诸对」于『"delaySeconds":』而
 虑「已知对数」者（「加」于（若「串相等」于「体文」于『』则「零」否则「一」）于（若「串相等」于「延迟文」于『』则「零」否则「一」））而
 若「串相等」于「体文」于『』则「发生事故」授以（「字符串」合「整数」）于『批量消息项缺少 body』否则
 若「不等于」于（「长度」授以「字符串」于「诸对」）于「已知对数」则「发生事故」授以（「字符串」合「整数」）于『批量消息项只允许 body 与 delaySeconds 两个字段』否则
 虑「延迟」者「解析批延迟」于「延迟文」而
 虑「体长」者「字节长度」于「体文」而
 若「大于」于「体长」于「消息体字节上限」则「发生事故」授以（「字符串」合「整数」）于『批量消息体超过 128000 字节』否则
 （『{"body":』附「体文」附『,"contentType":"json"』附（若「等于」于「延迟」于「零」则『』否则『,"delaySeconds":』附（「整数表示」于「延迟」））附『}』）与「体长」也。

「：文言：一次投至多百书。全数验过方投；任一不合则一书不投。汉语：批量投递：先在豫言里完整校验整个数组（1 至 100 项，各项形状、延迟、各体不超过 128 KB（128000 字节）、合计不超过 256 KB（256000 字节）），全部合格才调用 sendBatch；任何一项不合格则不投递任何消息。：」
「批量发送队列消息」乃化「字符串」而化「字符串」而「有」也。
「批量发送队列消息」者会「绑定名」而会「批文」而
 虑「无」者「验绑定名」于「绑定名」而
 若「大于」于（「字节长度」于「批文」）于「批量输入字节上限」则「发生事故」授以「有」于『批量消息文字超过 1 MiB』否则
 虑「规范」者「规范化JSON」于「批文」于『批量消息』于（「加」于「消息体嵌套上限」于「二」）而
 若「非」于（「串开头是它吗」于『[』于「规范」）则「发生事故」授以「有」于『批量消息须为 JSON 数组』否则
 虑「诸项文」者「拆顶层」于（「放」授以「字符串」于「一」于（「获取字符」于「规范」））而
 虑「条数」者「长度」授以「字符串」于「诸项文」而
 若「等于」于「条数」于「零」则「发生事故」授以「有」于『批量消息不得为空』否则
 若「大于」于「条数」于「批量条数上限」则「发生事故」授以「有」于『批量消息不得超过 100 条』否则
 虑「诸解析」者「态射」授以「字符串」授以（「字符串」合「整数」）于「诸项文」于「解析批项」而
 虑「总长」者「从左折叠」授以（「字符串」合「整数」）授以「整数」于（会「项」而会「积」而「加」于「积」于（「项」中「一」））于「零」于「诸解析」而
 若「大于」于「总长」于「批量字节上限」则「发生事故」授以「有」于『批量消息体合计超过 256000 字节』否则
 虑「诸项」者「态射」授以（「字符串」合「整数」）授以「字符串」于「诸解析」于（会「项」而「项」中「零」）而
 虑「队列号」者「云工队列绑定」于「绑定名」而
 虑「无」者「安全调用」于「队列号」于『sendBatch』于（『[[』附（「间合」于『,』于「诸项」）附『]]』）于『队列批量发送失败：』而
 「元」也。

「：文言：非队列之事而用队列之术则败，使误为可捕之豫言异。汉语：先核对事件种类，使“当前事件不是队列事件”成为可捕获的豫言异常，而不是宿主 JS 错误。：」
「验队列事件」乃化「有」而「有」也。
「验队列事件」者会「无」而
 若「串相等」于（「云工事件种类」于「元」）于『queue』则「元」否则「发生事故」授以「有」于『当前事件不是队列事件』也。

「：文言：序号自零起，须在本批之内。汉语：消息序号从 0 起，必须小于本批消息数；越界抛出豫言异常（宿主对越界的 JS 错误不可捕获，故先在这里核对）。：」
「验消息序号」乃化「整数」而「有」也。
「验消息序号」者会「序」而
 虑「数」者「云工队列消息数」于「元」而
 若「或者」于（「小于」于「序」于「零」）于（「非」于（「小于」于「序」于「数」））则「发生事故」授以「有」于『队列消息序号越界』否则「元」也。

「：文言：取消息之时，或为日期物，或为数；日期则取其毫秒。汉语：读取消息的 timestamp：平台给出 Date 对象（经句柄取 getTime），测试替身也可以直接给毫秒整数。：」
「消息毫秒」乃化「整数」而「整数」也。
「消息毫秒」者会「序」而
 虑「消息号」者「云工队列消息句柄」于「序」而
 虑「值文」者「云工读取属性」于「消息号」于『timestamp』而
 若「串开头是它吗」于『{"$句柄"』于「值文」则
  （虑「日期号」者「取云工句柄」于「值文」而
   虑「毫」者「云工调用方法」于「日期号」于『getTime』于『[]』而
   虑「无」者「云工释放句柄」于「日期号」而
   虑「无」者「云工释放句柄」于「消息号」而
   「理解整数」于「毫」）
 否则（虑「无」者「云工释放句柄」于「消息号」而「理解整数」于「值文」）也。

「：文言：一书之 JSON 文；体为宿主所出之规范文，含宿主记号者非 JSON 值，则败。汉语：把一条消息写成 {"id":…,"attempts":…,"timestamp":…,"body":…}；body 是宿主输出的规范 JSON 文字，含句柄桥记号（Date、Map、二进制、undefined、BigInt、NaN 等非 JSON 值）时抛出豫言异常。：」
「批消息文」乃化「整数」而「字符串」也。
「批消息文」者会「序」而
 虑「标识」者「云工队列消息标识」于「序」而
 虑「尝试」者「云工队列消息尝试数」于「序」而
 虑「毫」者「消息毫秒」于「序」而
 虑「正文」者「云工队列消息正文」于「序」而
 若「含宿主记号」于「正文」则「发生事故」授以「字符串」于（『队列消息正文不是 JSON 值：序号 』附（「整数表示」于「序」））否则
 『{"id":』附（「安全字面」于「标识」）附『,"attempts":』附（「整数表示」于「尝试」）附『,"timestamp":』附（「整数表示」于「毫」）附『,"body":』附「正文」附『}』也。

「批消息们」乃化「整数」而化「整数」而化（「列」于「字符串」）而（「列」于「字符串」）也。
「批消息们」者会「序」而会「数」而会「积」而
 若「非」于（「小于」于「序」于「数」）则「反转」授以「字符串」于「积」否则
 「批消息们」于（「加」于「序」于「一」）于「数」于（「缀」授以「字符串」于（「批消息文」于「序」）于「积」）也。

「：文言：返当前批次之 JSON 文。汉语：返回 {"queue":队列名,"messages":[…]}；消息按平台给出的顺序排列，序号从 0 起。：」
「读取队列批次」乃化「有」而「字符串」也。
「读取队列批次」者会「无」而
 虑「无」者「验队列事件」于「元」而
 虑「名」者「云工队列名称」于「元」而
 虑「数」者「云工队列消息数」于「元」而
 虑「诸文」者「批消息们」于「零」于「数」于（「罄」授以「字符串」）而
 『{"queue":』附（「安全字面」于「名」）附『,"messages":[』附（「间合」于『,』于「诸文」）附『]}』也。

「：文言：显式确认第序号书。汉语：确认当前批次的第 序号 条消息。：」
「确认队列消息」乃化「整数」而「有」也。
「确认队列消息」者会「序」而
 虑「无」者「验队列事件」于「元」而
 虑「无」者「验消息序号」于「序」而
 「云工队列消息确认」于「序」也。

「：文言：使第序号书重投；延迟零则从平台之默认。汉语：请求重新投递第 序号 条消息；延迟秒数为 0 时使用平台默认延迟，否则按给定秒数延后。：」
「重试队列消息」乃化「整数」而化「整数」而「有」也。
「重试队列消息」者会「序」而会「秒」而
 虑「无」者「验队列事件」于「元」而
 虑「无」者「验消息序号」于「序」而
 虑「无」者「验延迟」于「秒」而
 若「等于」于「秒」于「零」则「云工队列消息重试」于「序」否则「云工队列消息延后重试」于「序」于「秒」也。
