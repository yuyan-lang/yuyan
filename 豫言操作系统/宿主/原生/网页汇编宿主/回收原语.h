extern 豫言值 豫言_源码数字名(豫言值);
/* 文言：主线新原语亦通宿主。汉语：支持阶段序列化所需的精确小数及源码文本格式化。 */
extern 豫言值 豫言_小数精确表示(豫言值);
extern 豫言值 豫言_源码可用名(豫言值);
extern 豫言值 豫言_源码字符串表示(豫言值);
extern 豫言值 豫言_运行于Windows(void);
extern 豫言值 豫言_运行于MacOS(void);
extern 豫言值 豫言_运行于Linux(void);
extern 豫言值 豫言_获取当前本地日期时间字符串(void);
extern 豫言值 豫言_格式化当前本地日期时间(豫言值);
/* 文言：逐名列其原语，未知者拒之。汉语：显式系统原语白名单；原生原语只用于运行时接口。 */
extern 豫言值 豫言_字符串相等(豫言值,豫言值);
extern 豫言值 豫言_字符串拼接(豫言值,豫言值);
extern 豫言值 豫言_取得真实路径(豫言值);
extern 豫言值 豫言_路径为符号链接(豫言值);
extern 豫言值 豫言_存放包上下文(豫言值);
extern 豫言值 豫言_打印通用值(豫言值,豫言值);
extern 豫言值 豫言_字符串按字节在前(豫言值,豫言值);
extern 豫言值 豫言_可绘监视面板(void);
extern 豫言值 豫言_绘监视面板(豫言值);
extern 豫言值 豫言_异步_输入输出多路等待(豫言值,豫言值);
extern 豫言值 豫言_获取命令行程序名(void);
extern 豫言值 豫言_获取命令行参数(void);
extern 豫言值 豫言_同步运行子进程并获取输出(豫言值,豫言值);
extern 豫言值 豫言_同步运行子进程(豫言值,豫言值);
extern 豫言值 豫言_同步运行子进程并传递输出(豫言值,豫言值);
extern 豫言值 豫言_同步运行子进程并继承标准流(豫言值,豫言值);
extern 豫言值 豫言_尝试收取异步子进程(豫言值);
extern 豫言值 豫言_启动异步子进程(豫言值,豫言值);
extern 豫言值 豫言_在线处理器数量(豫言值);
extern 豫言值 豫言_同步读取文件(豫言值);
extern 豫言值 豫言_同步删除文件(豫言值);
extern 豫言值 豫言_同步写入文件(豫言值,豫言值);
extern 豫言值 豫言_同步读取文件字节串(豫言值);
extern 豫言值 豫言_同步写入文件字节串(豫言值,豫言值);
extern 豫言值 豫言_同步列出文件夹(豫言值);
extern 豫言值 豫言_路径是文件夹(豫言值);
extern 豫言值 豫言_路径是普通文件(豫言值);
extern 豫言值 豫言_路径存在(豫言值);
extern 豫言值 豫言_获取文件修改时间(豫言值);
extern 豫言值 豫言_获取当前工作目录(void);
extern 豫言值 豫言_切换当前工作目录(豫言值);
extern 豫言值 豫言_路径可执行(豫言值);
extern 豫言值 豫言_查找可执行程序(豫言值);
extern 豫言值 豫言_获取当前纳秒时间(void);
extern 豫言值 豫言_获取环境变量(豫言值);
extern 豫言值 豫言_退出进程(豫言值);
extern 豫言值 豫言_整数转小数(豫言值);
extern 豫言值 豫言_小数转整数(豫言值);
extern 豫言值 豫言_获取随机整数(豫言值);
extern 豫言值 豫言_获取随机小数(void);
extern 豫言值 豫言_获取通字符(豫言值);
extern 豫言值 豫言_通字符拼接(豫言值);
extern 豫言值 豫言_是子字符串(豫言值,豫言值);
extern 豫言值 豫言_字符串字节长度(豫言值);
extern 豫言值 豫言_字符转整数(豫言值,豫言值);
extern 豫言值 豫言_字节转字符串(豫言值);
extern 豫言值 豫言_字符串转无调拼音(豫言值);
extern 豫言值 豫言_字符串转拼音首字母(豫言值);
extern 豫言值 豫言_字符串字节数组长度(豫言值);
extern 豫言值 豫言_字符串获取字节数组(豫言值);
extern 豫言值 豫言_子字符串从字节序数开始(豫言值,豫言值);
extern 豫言值 豫言_字符串匹配(豫言值,豫言值,豫言值);
extern 豫言值 豫言_字符串获取字节序数当前字符(豫言值,豫言值);
extern 豫言值 豫言_字符串获取JSON字符串(豫言值,豫言值);
extern 豫言值 豫言_字节串_空(void);
extern 豫言值 豫言_字节串_长度(豫言值);
extern 豫言值 豫言_字节串_取字节(豫言值,豫言值);
extern 豫言值 豫言_字节串_从字符串(豫言值);
extern 豫言值 豫言_字节串_单字节(豫言值);
extern 豫言值 豫言_字节串_拼接(豫言值,豫言值);
extern 豫言值 豫言_小数加(豫言值,豫言值);
extern 豫言值 豫言_小数乘(豫言值,豫言值);
extern 豫言值 豫言_小数除(豫言值,豫言值);
extern 豫言值 豫言_小数减(豫言值,豫言值);
extern 豫言值 豫言_小数转字符串(豫言值);
extern 豫言值 豫言_字符串转小数(豫言值);
extern 豫言值 豫言_整数加(豫言值,豫言值);
extern 豫言值 豫言_整数乘(豫言值,豫言值);
extern 豫言值 豫言_整数除(豫言值,豫言值);
extern 豫言值 豫言_整数转字符串(豫言值);
extern 豫言值 豫言_字符串转整数(豫言值);
extern 豫言值 豫言_传输控制协议_监听(豫言值,豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_开始连接(豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_完成连接(豫言值);
extern 豫言值 豫言_传输控制协议_接受(豫言值);
extern 豫言值 豫言_传输控制协议_读取(豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_读取字节串(豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_从字节序数写入(豫言值,豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_从字节序数写入字节串(豫言值,豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_等待(豫言值,豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_获取本地端口(豫言值);
extern 豫言值 豫言_传输控制协议_设置无延迟(豫言值,豫言值);
extern 豫言值 豫言_传输控制协议_关闭写入(豫言值);
extern 豫言值 豫言_传输控制协议_关闭(豫言值);
extern 豫言值 豫言_传输控制协议_错误消息(豫言值);
extern 豫言值 豫言_不安全转换(豫言值);
extern 豫言值 豫言_新建引用(豫言值);
extern 豫言值 豫言_读取引用(豫言值);
extern 豫言值 豫言_写入引用(豫言值,豫言值);
extern 豫言值 豫言_新建引用数组(豫言值,豫言值);
extern 豫言值 豫言_新建未初始化引用数组(豫言值);
extern 豫言值 豫言_读取引用数组(豫言值,豫言值);
extern 豫言值 豫言_写入引用数组(豫言值,豫言值,豫言值);
extern 豫言值 豫言_打印字符串(豫言值);
extern 豫言值 豫言_打印行(豫言值);
extern 豫言值 豫言_标准错误打印行(豫言值);
extern 豫言值 豫言_尝试读取标准输入行(void);
extern 豫言值 豫言_标准输入是终端(void);
extern 豫言值 豫言_标准输出是终端(void);
extern 豫言值 豫言_进入终端原始输入模式(void);
extern 豫言值 豫言_退出终端原始输入模式(void);
extern 豫言值 豫言_读取终端按键(void);
static 豫言值 调原语(回收转换 *桥, const char *名, 豫言值 *参, size_t 数) {
    if (!strcmp(名, "豫言_源码数字名")) { if (数 != 1) 桥报错(桥, "豫言_源码数字名 参数数目不符"); return 豫言_源码数字名(参[0]); }
    if (!strcmp(名, "豫言_小数精确表示")) { if (数 != 1) 桥报错(桥, "豫言_小数精确表示 参数数目不符"); return 豫言_小数精确表示(参[0]); }
    if (!strcmp(名, "豫言_源码可用名")) { if (数 != 1) 桥报错(桥, "豫言_源码可用名 参数数目不符"); return 豫言_源码可用名(参[0]); }
    if (!strcmp(名, "豫言_源码字符串表示")) { if (数 != 1) 桥报错(桥, "豫言_源码字符串表示 参数数目不符"); return 豫言_源码字符串表示(参[0]); }
    if (!strcmp(名, "豫言_字符串相等")) { if (数 != 2) 桥报错(桥, "豫言_字符串相等 参数数目不符"); return 豫言_字符串相等(参[0],参[1]); }
    if (!strcmp(名, "豫言_字符串拼接")) { if (数 != 2) 桥报错(桥, "豫言_字符串拼接 参数数目不符"); return 豫言_字符串拼接(参[0],参[1]); }
    if (!strcmp(名, "豫言_取得真实路径")) { if (数 != 1) 桥报错(桥, "豫言_取得真实路径 参数数目不符"); return 豫言_取得真实路径(参[0]); }
    if (!strcmp(名, "豫言_路径为符号链接")) { if (数 != 1) 桥报错(桥, "豫言_路径为符号链接 参数数目不符"); return 豫言_路径为符号链接(参[0]); }
    if (!strcmp(名, "豫言_存放包上下文")) { if (数 != 1) 桥报错(桥, "豫言_存放包上下文 参数数目不符"); return 豫言_存放包上下文(参[0]); }
    if (!strcmp(名, "豫言_打印通用值")) { if (数 != 2) 桥报错(桥, "豫言_打印通用值 参数数目不符"); return 豫言_打印通用值(参[0],参[1]); }
    if (!strcmp(名, "豫言_字符串按字节在前")) { if (数 != 2) 桥报错(桥, "豫言_字符串按字节在前 参数数目不符"); return 豫言_字符串按字节在前(参[0],参[1]); }
    if (!strcmp(名, "豫言_可绘监视面板")) { if (数 != 0) 桥报错(桥, "豫言_可绘监视面板 参数数目不符"); return 豫言_可绘监视面板(); }
    if (!strcmp(名, "豫言_绘监视面板")) { if (数 != 1) 桥报错(桥, "豫言_绘监视面板 参数数目不符"); return 豫言_绘监视面板(参[0]); }
    if (!strcmp(名, "豫言_异步_输入输出多路等待")) { if (数 != 2) 桥报错(桥, "豫言_异步_输入输出多路等待 参数数目不符"); return 豫言_异步_输入输出多路等待(参[0],参[1]); }
    if (!strcmp(名, "豫言_获取命令行程序名")) { if (数 != 0) 桥报错(桥, "豫言_获取命令行程序名 参数数目不符"); return 豫言_获取命令行程序名(); }
    if (!strcmp(名, "豫言_获取命令行参数")) { if (数 != 0) 桥报错(桥, "豫言_获取命令行参数 参数数目不符"); return 豫言_获取命令行参数(); }
    if (!strcmp(名, "豫言_同步运行子进程并获取输出")) { if (数 != 2) 桥报错(桥, "豫言_同步运行子进程并获取输出 参数数目不符"); return 豫言_同步运行子进程并获取输出(参[0],参[1]); }
    if (!strcmp(名, "豫言_同步运行子进程")) { if (数 != 2) 桥报错(桥, "豫言_同步运行子进程 参数数目不符"); return 豫言_同步运行子进程(参[0],参[1]); }
    if (!strcmp(名, "豫言_同步运行子进程并传递输出")) { if (数 != 2) 桥报错(桥, "豫言_同步运行子进程并传递输出 参数数目不符"); return 豫言_同步运行子进程并传递输出(参[0],参[1]); }
    if (!strcmp(名, "豫言_同步运行子进程并继承标准流")) { if (数 != 2) 桥报错(桥, "豫言_同步运行子进程并继承标准流 参数数目不符"); return 豫言_同步运行子进程并继承标准流(参[0],参[1]); }
    if (!strcmp(名, "豫言_尝试收取异步子进程")) { if (数 != 1) 桥报错(桥, "豫言_尝试收取异步子进程 参数数目不符"); return 豫言_尝试收取异步子进程(参[0]); }
    if (!strcmp(名, "豫言_启动异步子进程")) { if (数 != 2) 桥报错(桥, "豫言_启动异步子进程 参数数目不符"); return 豫言_启动异步子进程(参[0],参[1]); }
    if (!strcmp(名, "豫言_在线处理器数量")) { if (数 != 1) 桥报错(桥, "豫言_在线处理器数量 参数数目不符"); return 豫言_在线处理器数量(参[0]); }
    if (!strcmp(名, "豫言_同步读取文件")) { if (数 != 1) 桥报错(桥, "豫言_同步读取文件 参数数目不符"); return 豫言_同步读取文件(参[0]); }
    if (!strcmp(名, "豫言_同步删除文件")) { if (数 != 1) 桥报错(桥, "豫言_同步删除文件 参数数目不符"); return 豫言_同步删除文件(参[0]); }
    if (!strcmp(名, "豫言_同步写入文件")) { if (数 != 2) 桥报错(桥, "豫言_同步写入文件 参数数目不符"); return 豫言_同步写入文件(参[0],参[1]); }
    if (!strcmp(名, "豫言_同步读取文件字节串")) { if (数 != 1) 桥报错(桥, "豫言_同步读取文件字节串 参数数目不符"); return 豫言_同步读取文件字节串(参[0]); }
    if (!strcmp(名, "豫言_同步写入文件字节串")) { if (数 != 2) 桥报错(桥, "豫言_同步写入文件字节串 参数数目不符"); return 豫言_同步写入文件字节串(参[0],参[1]); }
    if (!strcmp(名, "豫言_同步列出文件夹")) { if (数 != 1) 桥报错(桥, "豫言_同步列出文件夹 参数数目不符"); return 豫言_同步列出文件夹(参[0]); }
    if (!strcmp(名, "豫言_路径是文件夹")) { if (数 != 1) 桥报错(桥, "豫言_路径是文件夹 参数数目不符"); return 豫言_路径是文件夹(参[0]); }
    if (!strcmp(名, "豫言_路径是普通文件")) { if (数 != 1) 桥报错(桥, "豫言_路径是普通文件 参数数目不符"); return 豫言_路径是普通文件(参[0]); }
    if (!strcmp(名, "豫言_路径存在")) { if (数 != 1) 桥报错(桥, "豫言_路径存在 参数数目不符"); return 豫言_路径存在(参[0]); }
    if (!strcmp(名, "豫言_获取文件修改时间")) { if (数 != 1) 桥报错(桥, "豫言_获取文件修改时间 参数数目不符"); return 豫言_获取文件修改时间(参[0]); }
    if (!strcmp(名, "豫言_获取当前工作目录")) { if (数 != 0) 桥报错(桥, "豫言_获取当前工作目录 参数数目不符"); return 豫言_获取当前工作目录(); }
    if (!strcmp(名, "豫言_切换当前工作目录")) { if (数 != 1) 桥报错(桥, "豫言_切换当前工作目录 参数数目不符"); return 豫言_切换当前工作目录(参[0]); }
    if (!strcmp(名, "豫言_路径可执行")) { if (数 != 1) 桥报错(桥, "豫言_路径可执行 参数数目不符"); return 豫言_路径可执行(参[0]); }
    if (!strcmp(名, "豫言_查找可执行程序")) { if (数 != 1) 桥报错(桥, "豫言_查找可执行程序 参数数目不符"); return 豫言_查找可执行程序(参[0]); }
    if (!strcmp(名, "豫言_获取当前纳秒时间")) { if (数 != 0) 桥报错(桥, "豫言_获取当前纳秒时间 参数数目不符"); return 豫言_获取当前纳秒时间(); }
    if (!strcmp(名, "豫言_获取环境变量")) { if (数 != 1) 桥报错(桥, "豫言_获取环境变量 参数数目不符"); return 豫言_获取环境变量(参[0]); }
    if (!strcmp(名, "豫言_退出进程")) { if (数 != 1) 桥报错(桥, "豫言_退出进程 参数数目不符"); return 豫言_退出进程(参[0]); }
    if (!strcmp(名, "豫言_整数转小数")) { if (数 != 1) 桥报错(桥, "豫言_整数转小数 参数数目不符"); return 豫言_整数转小数(参[0]); }
    if (!strcmp(名, "豫言_小数转整数")) { if (数 != 1) 桥报错(桥, "豫言_小数转整数 参数数目不符"); return 豫言_小数转整数(参[0]); }
    if (!strcmp(名, "豫言_获取随机整数")) { if (数 != 1) 桥报错(桥, "豫言_获取随机整数 参数数目不符"); return 豫言_获取随机整数(参[0]); }
    if (!strcmp(名, "豫言_获取随机小数")) { if (数 != 0) 桥报错(桥, "豫言_获取随机小数 参数数目不符"); return 豫言_获取随机小数(); }
    if (!strcmp(名, "豫言_获取通字符")) { if (数 != 1) 桥报错(桥, "豫言_获取通字符 参数数目不符"); return 豫言_获取通字符(参[0]); }
    if (!strcmp(名, "豫言_通字符拼接")) { if (数 != 1) 桥报错(桥, "豫言_通字符拼接 参数数目不符"); return 豫言_通字符拼接(参[0]); }
    if (!strcmp(名, "豫言_是子字符串")) { if (数 != 2) 桥报错(桥, "豫言_是子字符串 参数数目不符"); return 豫言_是子字符串(参[0],参[1]); }
    if (!strcmp(名, "豫言_字符串字节长度")) { if (数 != 1) 桥报错(桥, "豫言_字符串字节长度 参数数目不符"); return 豫言_字符串字节长度(参[0]); }
    if (!strcmp(名, "豫言_字符转整数")) { if (数 != 2) 桥报错(桥, "豫言_字符转整数 参数数目不符"); return 豫言_字符转整数(参[0],参[1]); }
    if (!strcmp(名, "豫言_字节转字符串")) { if (数 != 1) 桥报错(桥, "豫言_字节转字符串 参数数目不符"); return 豫言_字节转字符串(参[0]); }
    if (!strcmp(名, "豫言_字符串转无调拼音")) { if (数 != 1) 桥报错(桥, "豫言_字符串转无调拼音 参数数目不符"); return 豫言_字符串转无调拼音(参[0]); }
    if (!strcmp(名, "豫言_字符串转拼音首字母")) { if (数 != 1) 桥报错(桥, "豫言_字符串转拼音首字母 参数数目不符"); return 豫言_字符串转拼音首字母(参[0]); }
    if (!strcmp(名, "豫言_字符串字节数组长度")) { if (数 != 1) 桥报错(桥, "豫言_字符串字节数组长度 参数数目不符"); return 豫言_字符串字节数组长度(参[0]); }
    if (!strcmp(名, "豫言_字符串获取字节数组")) { if (数 != 1) 桥报错(桥, "豫言_字符串获取字节数组 参数数目不符"); return 豫言_字符串获取字节数组(参[0]); }
    if (!strcmp(名, "豫言_子字符串从字节序数开始")) { if (数 != 2) 桥报错(桥, "豫言_子字符串从字节序数开始 参数数目不符"); return 豫言_子字符串从字节序数开始(参[0],参[1]); }
    if (!strcmp(名, "豫言_字符串匹配")) { if (数 != 3) 桥报错(桥, "豫言_字符串匹配 参数数目不符"); return 豫言_字符串匹配(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_字符串获取字节序数当前字符")) { if (数 != 2) 桥报错(桥, "豫言_字符串获取字节序数当前字符 参数数目不符"); return 豫言_字符串获取字节序数当前字符(参[0],参[1]); }
    if (!strcmp(名, "豫言_字符串获取JSON字符串")) { if (数 != 2) 桥报错(桥, "豫言_字符串获取JSON字符串 参数数目不符"); return 豫言_字符串获取JSON字符串(参[0],参[1]); }
    if (!strcmp(名, "豫言_字节串_空")) { if (数 != 0) 桥报错(桥, "豫言_字节串_空 参数数目不符"); return 豫言_字节串_空(); }
    if (!strcmp(名, "豫言_字节串_长度")) { if (数 != 1) 桥报错(桥, "豫言_字节串_长度 参数数目不符"); return 豫言_字节串_长度(参[0]); }
    if (!strcmp(名, "豫言_字节串_取字节")) { if (数 != 2) 桥报错(桥, "豫言_字节串_取字节 参数数目不符"); return 豫言_字节串_取字节(参[0],参[1]); }
    if (!strcmp(名, "豫言_字节串_从字符串")) { if (数 != 1) 桥报错(桥, "豫言_字节串_从字符串 参数数目不符"); return 豫言_字节串_从字符串(参[0]); }
    if (!strcmp(名, "豫言_字节串_单字节")) { if (数 != 1) 桥报错(桥, "豫言_字节串_单字节 参数数目不符"); return 豫言_字节串_单字节(参[0]); }
    if (!strcmp(名, "豫言_字节串_拼接")) { if (数 != 2) 桥报错(桥, "豫言_字节串_拼接 参数数目不符"); return 豫言_字节串_拼接(参[0],参[1]); }
    if (!strcmp(名, "豫言_小数加")) { if (数 != 2) 桥报错(桥, "豫言_小数加 参数数目不符"); return 豫言_小数加(参[0],参[1]); }
    if (!strcmp(名, "豫言_小数乘")) { if (数 != 2) 桥报错(桥, "豫言_小数乘 参数数目不符"); return 豫言_小数乘(参[0],参[1]); }
    if (!strcmp(名, "豫言_小数除")) { if (数 != 2) 桥报错(桥, "豫言_小数除 参数数目不符"); return 豫言_小数除(参[0],参[1]); }
    if (!strcmp(名, "豫言_小数减")) { if (数 != 2) 桥报错(桥, "豫言_小数减 参数数目不符"); return 豫言_小数减(参[0],参[1]); }
    if (!strcmp(名, "豫言_小数转字符串")) { if (数 != 1) 桥报错(桥, "豫言_小数转字符串 参数数目不符"); return 豫言_小数转字符串(参[0]); }
    if (!strcmp(名, "豫言_字符串转小数")) { if (数 != 1) 桥报错(桥, "豫言_字符串转小数 参数数目不符"); return 豫言_字符串转小数(参[0]); }
    if (!strcmp(名, "豫言_整数加")) { if (数 != 2) 桥报错(桥, "豫言_整数加 参数数目不符"); return 豫言_整数加(参[0],参[1]); }
    if (!strcmp(名, "豫言_整数乘")) { if (数 != 2) 桥报错(桥, "豫言_整数乘 参数数目不符"); return 豫言_整数乘(参[0],参[1]); }
    if (!strcmp(名, "豫言_整数除")) { if (数 != 2) 桥报错(桥, "豫言_整数除 参数数目不符"); return 豫言_整数除(参[0],参[1]); }
    if (!strcmp(名, "豫言_整数转字符串")) { if (数 != 1) 桥报错(桥, "豫言_整数转字符串 参数数目不符"); return 豫言_整数转字符串(参[0]); }
    if (!strcmp(名, "豫言_字符串转整数")) { if (数 != 1) 桥报错(桥, "豫言_字符串转整数 参数数目不符"); return 豫言_字符串转整数(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_监听")) { if (数 != 3) 桥报错(桥, "豫言_传输控制协议_监听 参数数目不符"); return 豫言_传输控制协议_监听(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_传输控制协议_开始连接")) { if (数 != 2) 桥报错(桥, "豫言_传输控制协议_开始连接 参数数目不符"); return 豫言_传输控制协议_开始连接(参[0],参[1]); }
    if (!strcmp(名, "豫言_传输控制协议_完成连接")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_完成连接 参数数目不符"); return 豫言_传输控制协议_完成连接(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_接受")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_接受 参数数目不符"); return 豫言_传输控制协议_接受(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_读取")) { if (数 != 2) 桥报错(桥, "豫言_传输控制协议_读取 参数数目不符"); return 豫言_传输控制协议_读取(参[0],参[1]); }
    if (!strcmp(名, "豫言_传输控制协议_读取字节串")) { if (数 != 2) 桥报错(桥, "豫言_传输控制协议_读取字节串 参数数目不符"); return 豫言_传输控制协议_读取字节串(参[0],参[1]); }
    if (!strcmp(名, "豫言_传输控制协议_从字节序数写入")) { if (数 != 3) 桥报错(桥, "豫言_传输控制协议_从字节序数写入 参数数目不符"); return 豫言_传输控制协议_从字节序数写入(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_传输控制协议_从字节序数写入字节串")) { if (数 != 3) 桥报错(桥, "豫言_传输控制协议_从字节序数写入字节串 参数数目不符"); return 豫言_传输控制协议_从字节序数写入字节串(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_传输控制协议_等待")) { if (数 != 3) 桥报错(桥, "豫言_传输控制协议_等待 参数数目不符"); return 豫言_传输控制协议_等待(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_传输控制协议_获取本地端口")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_获取本地端口 参数数目不符"); return 豫言_传输控制协议_获取本地端口(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_设置无延迟")) { if (数 != 2) 桥报错(桥, "豫言_传输控制协议_设置无延迟 参数数目不符"); return 豫言_传输控制协议_设置无延迟(参[0],参[1]); }
    if (!strcmp(名, "豫言_传输控制协议_关闭写入")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_关闭写入 参数数目不符"); return 豫言_传输控制协议_关闭写入(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_关闭")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_关闭 参数数目不符"); return 豫言_传输控制协议_关闭(参[0]); }
    if (!strcmp(名, "豫言_传输控制协议_错误消息")) { if (数 != 1) 桥报错(桥, "豫言_传输控制协议_错误消息 参数数目不符"); return 豫言_传输控制协议_错误消息(参[0]); }
    if (!strcmp(名, "豫言_不安全转换")) { if (数 != 1) 桥报错(桥, "豫言_不安全转换 参数数目不符"); return 豫言_不安全转换(参[0]); }
    if (!strcmp(名, "豫言_新建引用")) { if (数 != 1) 桥报错(桥, "豫言_新建引用 参数数目不符"); return 豫言_新建引用(参[0]); }
    if (!strcmp(名, "豫言_读取引用")) { if (数 != 1) 桥报错(桥, "豫言_读取引用 参数数目不符"); return 豫言_读取引用(参[0]); }
    if (!strcmp(名, "豫言_写入引用")) { if (数 != 2) 桥报错(桥, "豫言_写入引用 参数数目不符"); return 豫言_写入引用(参[0],参[1]); }
    if (!strcmp(名, "豫言_新建引用数组")) { if (数 != 2) 桥报错(桥, "豫言_新建引用数组 参数数目不符"); return 豫言_新建引用数组(参[0],参[1]); }
    if (!strcmp(名, "豫言_新建未初始化引用数组")) { if (数 != 1) 桥报错(桥, "豫言_新建未初始化引用数组 参数数目不符"); return 豫言_新建未初始化引用数组(参[0]); }
    if (!strcmp(名, "豫言_读取引用数组")) { if (数 != 2) 桥报错(桥, "豫言_读取引用数组 参数数目不符"); return 豫言_读取引用数组(参[0],参[1]); }
    if (!strcmp(名, "豫言_写入引用数组")) { if (数 != 3) 桥报错(桥, "豫言_写入引用数组 参数数目不符"); return 豫言_写入引用数组(参[0],参[1],参[2]); }
    if (!strcmp(名, "豫言_打印字符串")) { if (数 != 1) 桥报错(桥, "豫言_打印字符串 参数数目不符"); return 豫言_打印字符串(参[0]); }
    if (!strcmp(名, "豫言_打印行")) { if (数 != 1) 桥报错(桥, "豫言_打印行 参数数目不符"); return 豫言_打印行(参[0]); }
    if (!strcmp(名, "豫言_标准错误打印行")) { if (数 != 1) 桥报错(桥, "豫言_标准错误打印行 参数数目不符"); return 豫言_标准错误打印行(参[0]); }
    if (!strcmp(名, "豫言_尝试读取标准输入行")) { if (数 != 0) 桥报错(桥, "豫言_尝试读取标准输入行 参数数目不符"); return 豫言_尝试读取标准输入行(); }
    if (!strcmp(名, "豫言_标准输入是终端")) { if (数 != 0) 桥报错(桥, "豫言_标准输入是终端 参数数目不符"); return 豫言_标准输入是终端(); }
    if (!strcmp(名, "豫言_标准输出是终端")) { if (数 != 0) 桥报错(桥, "豫言_标准输出是终端 参数数目不符"); return 豫言_标准输出是终端(); }
    if (!strcmp(名, "豫言_进入终端原始输入模式")) { if (数 != 0) 桥报错(桥, "豫言_进入终端原始输入模式 参数数目不符"); return 豫言_进入终端原始输入模式(); }
    if (!strcmp(名, "豫言_退出终端原始输入模式")) { if (数 != 0) 桥报错(桥, "豫言_退出终端原始输入模式 参数数目不符"); return 豫言_退出终端原始输入模式(); }
    if (!strcmp(名, "豫言_读取终端按键")) { if (数 != 0) 桥报错(桥, "豫言_读取终端按键 参数数目不符"); return 豫言_读取终端按键(); }
    if (!strcmp(名, "豫言_运行于Windows")) return 豫言_运行于Windows();
    if (!strcmp(名, "豫言_运行于MacOS")) return 豫言_运行于MacOS();
    if (!strcmp(名, "豫言_运行于Linux")) return 豫言_运行于Linux();
    if (!strcmp(名, "豫言_获取当前本地日期时间字符串")) return 豫言_获取当前本地日期时间字符串();
    if (!strcmp(名, "豫言_格式化当前本地日期时间")) return 豫言_格式化当前本地日期时间(参[0]);
    桥报错(桥, 名); return 0;
}
