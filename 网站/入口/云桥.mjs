// 古曰：一门通旧府，来路不可改其所往。今释：固定代理 GitHub Pages 构建产物，不接受自定义上游或转发身份凭据。
export function 原站地址(请求地址){const url=new URL(请求地址);const p=url.pathname.startsWith('/yuyan/')?url.pathname.slice(6):url.pathname;return new URL('/yuyan'+(p==='/'?'/':p),'https://yuyan-lang.github.io');}
export default {async fetch(请求){if(!['GET','HEAD'].includes(请求.method))return new Response('仅支持读取网页',{status:405,headers:{Allow:'GET, HEAD'}});const url=new URL(请求.url),origin=原站地址(url);const headers=new Headers();for(const key of ['accept','if-none-match','if-modified-since','range'])if(请求.headers.has(key))headers.set(key,请求.headers.get(key));const 回=await fetch(origin,{method:请求.method,headers,redirect:'manual'});const 出=new Headers(回.headers);出.set('X-Content-Type-Options','nosniff');出.set('Referrer-Policy','strict-origin-when-cross-origin');if(出.has('Location')){const to=new URL(出.get('Location'),origin);if(to.origin===origin.origin&&to.pathname.startsWith('/yuyan')){to.protocol=url.protocol;to.host=url.host;to.pathname=to.pathname.slice(6)||'/';出.set('Location',to.href);}}const 应=new Response(回.body,{status:回.status,headers:出});if(!出.get('Content-Type')?.includes('text/html'))return 应;return new HTMLRewriter().on('[href],[src],[data-han-page],[data-wen-page],[data-home]',{element(e){for(const a of ['href','src','data-han-page','data-wen-page','data-home']){const v=e.getAttribute(a);if(v?.startsWith('/yuyan/'))e.setAttribute(a,v.slice(6));}}}).transform(应);}};
