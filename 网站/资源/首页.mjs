// 古曰：择例而观，欲试则往。今释：静态输出明确标注，真实执行交给试写间。
import {示例} from '../共用/示例.mjs';
const 域=document.getElementById('首页示例');if(域){let 当前='你好';for(const b of document.querySelectorAll('[data-example]'))b.addEventListener('click',()=>{当前=b.dataset.example;const e=示例[当前];域.textContent=e.code;document.getElementById('示例输出').textContent=e.output;const p=document.getElementById('示例解释');p.dataset.han=e.han;p.dataset.wen=e.wen;p.textContent=window.豫言界面?.语言==='wen'?e.wen:e.han;document.getElementById('运行示例').href='https://playground.yuyan-lang.org/?example='+encodeURIComponent(当前);document.querySelectorAll('[data-example]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));});document.getElementById('复制示例').addEventListener('click',async e=>{try{await navigator.clipboard.writeText(示例[当前].code);e.target.textContent=window.豫言界面?.译('已复制')||'已复制';}catch{e.target.textContent=window.豫言界面?.译('复制失败，请手动选择代码。')||'复制失败，请手动选择代码。';}});}
