寻观「标准库」之书。

「按四个阈值为输入整数生成分层说明」乃
  化「整数」而
  化「整数」而
  化「整数」而
  化「整数」而
  化「整数」而
  「字符串」
也。
「按四个阈值为输入整数生成分层说明」者
  会「当前输入整数」而
  会「最低危险阈值」而
  会「需要留意的阈值」而
  会「正常范围的阈值」而
  会「超过范围的阈值」而
  若「小于」于「当前输入整数」于「最低危险阈值」则
    『数值低于最低危险阈值：』附（「整数表示」于「当前输入整数」）
  否则若「小于」于「当前输入整数」于「需要留意的阈值」则
    『数值已经进入需要留意的范围：』附（「整数表示」于「当前输入整数」）
  否则若「小于」于「当前输入整数」于「正常范围的阈值」则
    『数值处于普通范围：』附（「整数表示」于「当前输入整数」）
  否则若「小于」于「当前输入整数」于「超过范围的阈值」则
    『数值接近上限：』附（「整数表示」于「当前输入整数」）
  否则
    『数值已经超过允许范围：』附（「整数表示」于「当前输入整数」）
也。
