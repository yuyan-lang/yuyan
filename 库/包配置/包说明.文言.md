# 包配置

以 parser 解析静态包配置并验证依赖图。

## 籍与用

所有者曰豫言，版本暂定 0.1.0。此包之版本，非编译器命令所报之版本。

此包可供他包所依，观书须先书包名，继书包内路径。无默认程序入口，试例须明指其书。

在仓库之根行 `./yy豫构 检查 包配置` 以验其籍。

所依与原生组成皆见[包籍](包配置。包。豫)。变接口则修说明，变身份与组成则修包籍。说明不作模块。
