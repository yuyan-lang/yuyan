寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数据结构」之「整数操作」之书。
寻观「标准库」之「数据结构」之「可选值」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「拓展库」之「安全外壳」之「二进制编码」之书。

「：文言：一号断开报有非零原因数、UTF-8 说明及语言标。汉语：编码 SSH_MSG_DISCONNECT；文本从豫言字符串转换为 UTF-8 字节。：」
「编码断开载荷」乃化「整数」而化「字符串」而化「字符串」而「字节串」也。
「编码断开载荷」者会「原因」而会「说明」而会「语言」而
    若「小于」于「原因」于「一」则「发生事故」授以「字节串」于『断开原因码须非零』
    否则「拼接字节串」于（「单字节串」于「一」）于（「拼接字节串」于（「编码四字节无符号数」于「原因」）于（「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于「说明」））于（「编码长度字节串」于（「字符串转字节串」于「语言」））））
也。

「：文言：析一号断开报，核原因非零与二串尾界。汉语：严格解析 DISCONNECT，返回原因码、说明原字节及语言标记原字节。：」
「读取断开载荷」乃化「字节串」而（「或可有」于（「整数」合「字节串」合「字节串」））也。
「读取断开载荷」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「一三」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「一」则「可无」
    否则鉴「读取四字节无符号数」于「载荷」于「一」而
        有（「可有」于「原因与位」）则（
            若「等于」于（「原因与位」中「零」）于「零」则「可无」
            否则鉴「读取长度字节串」于「载荷」于（「原因与位」中「一」）而
                有（「可有」于「说明与位」）则（
                    鉴「读取长度字节串」于「载荷」于（「说明与位」中「一」）而
                        有（「可有」于「语言与位」）则（
                            若「不等于」于（「语言与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                            否则「可有」于（（「原因与位」中「零」）与（「说明与位」中「零」）与（「语言与位」中「零」）））
                        或有「可无」则「可无」）
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。

「：文言：二号忽略报载任意字节串。汉语：编码 SSH_MSG_IGNORE，允许空数据。：」
「编码忽略载荷」乃化「字节串」而「字节串」也。
「编码忽略载荷」者会「内容」而「拼接字节串」于（「单字节串」于「二」）于（「编码长度字节串」于「内容」）也。

「：文言：读二号忽略报，字节虽任意，尾不得余。汉语：严格解析 IGNORE，返回原始数据。：」
「读取忽略载荷」乃化「字节串」而（「或可有」于「字节串」）也。
「读取忽略载荷」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「五」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「二」则「可无」
    否则鉴「读取长度字节串」于「载荷」于「一」而
        有（「可有」于「内容与位」）则（
            若「不等于」于（「内容与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
            否则「可有」于（「内容与位」中「零」））
        或有「可无」则「可无」
也。

「：文言：三号未实之报指原报序号。汉语：编码 SSH_MSG_UNIMPLEMENTED 与被拒报文的 uint32 序号。：」
「编码未实现载荷」乃化「整数」而「字节串」也。
「编码未实现载荷」者会「序号」而「拼接字节串」于（「单字节串」于「三」）于（「编码四字节无符号数」于「序号」）也。

「读取未实现载荷」乃化「字节串」而（「或可有」于「整数」）也。
「读取未实现载荷」者会「载荷」而
    若「不等于」于（「字节串长度」于「载荷」）于「五」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「三」则「可无」
    否则鉴「读取四字节无符号数」于「载荷」于「一」而
        有（「可有」于「序与位」）则「可有」于（「序与位」中「零」）
        或有「可无」则「可无」
也。

「：文言：四号调试报以布尔、UTF-8 文及语言标为序。汉语：编码 SSH_MSG_DEBUG，always_display 严格编码为 0 或 1。：」
「编码调试载荷」乃化「爻」而化「字符串」而化「字符串」而「字节串」也。
「编码调试载荷」者会「常显」而会「说明」而会「语言」而
    「拼接字节串」于（「单字节串」于「四」）于（「拼接字节串」于（「单字节串」于（若「常显」则「一」否则「零」））于（「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于「说明」））于（「编码长度字节串」于（「字符串转字节串」于「语言」））））
也。

「：文言：析四号调试报，布尔惟零一，二串须尽报。汉语：严格解析 DEBUG，返回 always_display、说明及语言原字节。：」
「读取调试载荷」乃化「字节串」而（「或可有」于（「爻」合「字节串」合「字节串」））也。
「读取调试载荷」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「一零」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「四」则「可无」
    否则虑「布尔」者「字节串取字节」于「载荷」于「一」而
        若「大于」于「布尔」于「一」则「可无」
        否则鉴「读取长度字节串」于「载荷」于「二」而
            有（「可有」于「说明与位」）则（
                鉴「读取长度字节串」于「载荷」于（「说明与位」中「一」）而
                    有（「可有」于「语言与位」）则（
                        若「不等于」于（「语言与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                        否则「可有」于（（若「等于」于「布尔」于「一」则「阳」否则「阴」）与（「说明与位」中「零」）与（「语言与位」中「零」）））
                    或有「可无」则「可无」）
            或有「可无」则「可无」
也。
