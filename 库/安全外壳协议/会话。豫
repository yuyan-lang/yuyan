寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数据结构」之「整数操作」之书。
寻观「标准库」之「数据结构」之「爻术」之书。
寻观「标准库」之「数据结构」之「可选值」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「拓展库」之「安全外壳」之「二进制编码」之书。
寻观「安全外壳协议」之「密钥交换」之书。

「终端模式续验」乃化「字节串」而化「整数」而「爻」也。
「终端模式续验」者会「模式」而会「位」而
    若「大于」于（「加」于「位」于「一」）于（「字节串长度」于「模式」）则「阴」
    否则虑「操作码」者「字节串取字节」于「模式」于「位」而
        若「等于」于「操作码」于「零」则「等于」于（「加」于「位」于「一」）于（「字节串长度」于「模式」）
        否则若「大于」于「操作码」于「一五九」则「阳」
        否则鉴「读取四字节无符号数」于「模式」于（「加」于「位」于「一」）而
            有（「可有」于「值与位」）则「终端模式续验」于「模式」于（「值与位」中「一」）
            或有「可无」则「阴」
也。

「：文言：已知模式须以零码止，每前码一至一五九后随四字节数；扩展码一六零以上止析。汉语：验证 RFC 4254 终端模式流；遇未知扩展操作码按规范停止解析。：」
「终端模式可用吗」乃化「字节串」而「爻」也。
「终端模式可用吗」者会「模式」而「终端模式续验」于「模式」于「零」也。

「：文言：九十八号通道请求共头含彼号、名及待答位。汉语：编码 CHANNEL_REQUEST 通用头部。：」
「编码通道请求共头」乃化「整数」而化「字节串」而化「爻」而「字节串」也。
「编码通道请求共头」者会「彼号」而会「名」而会「待答」而
    若「等于」于（「字节串长度」于「名」）于「零」则「发生事故」授以「字节串」于『通道请求名不可为空』
    否则「拼接字节串」于（「单字节串」于「九八」）于（
        「拼接字节串」于（「编码四字节无符号数」于「彼号」）于（
        「拼接字节串」于（「编码长度字节串」于「名」）于（「单字节串」于（若「待答」则「一」否则「零」））））
也。

「：文言：读九十八号共头，布尔须零或一。汉语：解析 CHANNEL_REQUEST 的 recipient、请求名、want_reply 和附加数据偏移。：」
「读取通道请求共头」乃化「字节串」而（「或可有」于（「整数」合「字节串」合「爻」合「整数」））也。
「读取通道请求共头」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「一」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「九八」则「可无」
    否则鉴「读取四字节无符号数」于「载荷」于「一」而
        有（「可有」于「号与位」）则（
            鉴「读取长度字节串」于「载荷」于（「号与位」中「一」）而
                有（「可有」于「名与位」）则（
                    若「等于」于（「字节串长度」于（「名与位」中「零」））于「零」则「可无」
                    否则若「大于」于（「加」于（「名与位」中「一」）于「一」）于（「字节串长度」于「载荷」）则「可无」
                    否则虑「待答位」者「字节串取字节」于「载荷」于（「名与位」中「一」）而
                        若「等于」于「待答位」于「零」
                        则「可有」于（（「号与位」中「零」）与（「名与位」中「零」）与「阴」与（「加」于（「名与位」中「一」）于「一」））
                        否则若「等于」于「待答位」于「一」
                        则「可有」于（（「号与位」中「零」）与（「名与位」中「零」）与「阳」与（「加」于（「名与位」中「一」）于「一」））
                        否则「可无」）
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。

「：文言：终端宽高及像素宽高均四字节数。汉语：编码 PTY 或窗口变化请求的四个尺寸。：」
「编码终端四尺寸」乃化「整数」而化「整数」而化「整数」而化「整数」而「字节串」也。
「编码终端四尺寸」者会「列」而会「行」而会「像列」而会「像行」而
    「拼接字节串」于（「编码四字节无符号数」于「列」）于（
        「拼接字节串」于（「编码四字节无符号数」于「行」）于（
        「拼接字节串」于（「编码四字节无符号数」于「像列」）于（「编码四字节无符号数」于「像行」）））
也。

「读取终端四尺寸」乃化「字节串」而化「整数」而（「或可有」于（「整数」合「整数」合「整数」合「整数」合「整数」））也。
「读取终端四尺寸」者会「载荷」而会「位」而
    鉴「读取四字节无符号数」于「载荷」于「位」而
        有（「可有」于「列与位」）则（
            鉴「读取四字节无符号数」于「载荷」于（「列与位」中「一」）而
                有（「可有」于「行与位」）则（
                    鉴「读取四字节无符号数」于「载荷」于（「行与位」中「一」）而
                        有（「可有」于「像列与位」）则（
                            鉴「读取四字节无符号数」于「载荷」于（「像列与位」中「一」）而
                                有（「可有」于「像行与位」）则「可有」于（（「列与位」中「零」）与（「行与位」中「零」）与（「像列与位」中「零」）与（「像行与位」中「零」）与（「像行与位」中「一」））
                                或有「可无」则「可无」）
                        或有「可无」则「可无」）
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。

「：文言：请配 PTY，传 TERM、尺寸及模式，模式须合法。汉语：编码 pty-req，want_reply 可配置；TERM 不可为空。：」
「编码PTY请求」乃化「整数」而化「爻」而化「字节串」而化「整数」而化「整数」而化「整数」而化「整数」而化「字节串」而「字节串」也。
「编码PTY请求」者会「彼号」而会「待答」而会「终端名」而会「列」而会「行」而会「像列」而会「像行」而会「模式」而
    若「等于」于（「字节串长度」于「终端名」）于「零」则「发生事故」授以「字节串」于『TERM 不可为空』
    否则若「终端模式可用吗」于「模式」
    则「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『pty-req』）于「待答」）于（
        「拼接字节串」于（「编码长度字节串」于「终端名」）于（
        「拼接字节串」于（「编码终端四尺寸」于「列」于「行」于「像列」于「像行」）于（「编码长度字节串」于「模式」）））
    否则「发生事故」授以「字节串」于『终端模式流非法』
也。

「：文言：服务者析 PTY 请求，核尺寸与模式及尾界。汉语：解析 pty-req，返回通道号、want_reply、TERM、四尺寸和模式。：」
「读取PTY请求」乃化「字节串」而（「或可有」于（「整数」合「爻」合「字节串」合「整数」合「整数」合「整数」合「整数」合「字节串」））也。
「读取PTY请求」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
        有（「可有」于「头」）则（
            若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『pty-req』）
            则（鉴「读取长度字节串」于「载荷」于（「头」中「三」）而
                有（「可有」于「终端与位」）则（
                    若「等于」于（「字节串长度」于（「终端与位」中「零」））于「零」则「可无」
                    否则鉴「读取终端四尺寸」于「载荷」于（「终端与位」中「一」）而
                        有（「可有」于「尺寸」）则（
                            鉴「读取长度字节串」于「载荷」于（「尺寸」中「四」）而
                                有（「可有」于「模式与位」）则（
                                    若「不等于」于（「模式与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                                    否则若「终端模式可用吗」于（「模式与位」中「零」）
                                        则「可有」于（（「头」中「零」）与（「头」中「二」）与（「终端与位」中「零」）与（「尺寸」中「零」）与（「尺寸」中「一」）与（「尺寸」中「二」）与（「尺寸」中「三」）与（「模式与位」中「零」））
                                        否则「可无」）
                                或有「可无」则「可无」）
                        或有「可无」则「可无」）
                或有「可无」则「可无」）
            否则「可无」）
        或有「可无」则「可无」
也。

「：文言：窗变更消息不待答，附四尺寸。汉语：编码 window-change 请求，want_reply 固定为 false。：」
「编码终端窗口变化」乃化「整数」而化「整数」而化「整数」而化「整数」而化「整数」而「字节串」也。
「编码终端窗口变化」者会「彼号」而会「列」而会「行」而会「像列」而会「像行」而
    「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『window-change』）于「阴」）于（「编码终端四尺寸」于「列」于「行」于「像列」于「像行」）
也。

「：文言：shell 请求惟共头，无后物。汉语：编码 shell 请求；每个通道只能成功启动一次程序。：」
「编码Shell请求」乃化「整数」而化「爻」而「字节串」也。
「编码Shell请求」者会「彼号」而会「待答」而
    「编码通道请求共头」于「彼号」于（「字符串转字节串」于『shell』）于「待答」
也。

「：文言：exec 请求后附命令串；执行许可由服务者另核。汉语：编码 exec 请求，服务端仍须独立授权命令。：」
「编码Exec请求」乃化「整数」而化「爻」而化「字节串」而「字节串」也。
「编码Exec请求」者会「彼号」而会「待答」而会「命令」而
    「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『exec』）于「待答」）于（「编码长度字节串」于「命令」）
也。

「：文言：读窗变，待答须阴且四尺寸后无余字。汉语：服务端严格解析 window-change，返回通道号与四个尺寸。：」
「读取终端窗口变化」乃化「字节串」而（「或可有」于（「整数」合「整数」合「整数」合「整数」合「整数」））也。
「读取终端窗口变化」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
        有（「可有」于「头」）则（
            若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『window-change』）
            则（若「头」中「二」则「可无」
                否则鉴「读取终端四尺寸」于「载荷」于（「头」中「三」）而
                    有（「可有」于「尺寸」）则（
                        若「不等于」于（「尺寸」中「四」）于（「字节串长度」于「载荷」）则「可无」
                        否则「可有」于（（「头」中「零」）与（「尺寸」中「零」）与（「尺寸」中「一」）与（「尺寸」中「二」）与（「尺寸」中「三」）））
                    或有「可无」则「可无」）
            否则「可无」）
        或有「可无」则「可无」
也。

「：文言：shell 请求无后物。汉语：服务端解析 shell 请求，返回通道号和 want_reply。：」
「读取Shell请求」乃化「字节串」而（「或可有」于（「整数」合「爻」））也。
「读取Shell请求」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
    有（「可有」于「头」）则（
        若「不等于」于（「头」中「三」）于（「字节串长度」于「载荷」）则「可无」
        否则若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『shell』）
            则「可有」于（（「头」中「零」）与（「头」中「二」））
            否则「可无」）
    或有「可无」则「可无」
也。

「：文言：exec 请求后惟一命令串。汉语：服务端解析 exec 请求，返回通道号、want_reply 和命令原始字节。：」
「读取Exec请求」乃化「字节串」而（「或可有」于（「整数」合「爻」合「字节串」））也。
「读取Exec请求」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
    有（「可有」于「头」）则（
        若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『exec』）
        则（鉴「读取长度字节串」于「载荷」于（「头」中「三」）而
            有（「可有」于「命令与位」）则（
                若「不等于」于（「命令与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                否则「可有」于（（「头」中「零」）与（「头」中「二」）与（「命令与位」中「零」）））
            或有「可无」则「可无」）
        否则「可无」）
    或有「可无」则「可无」
也。

「：文言：信号请不待答，名去 SIG 前缀；不施进程。汉语：编码 RFC 4254 的 signal 通道请求，want_reply 固定为 false；发送权限由调用方负责。：」
「编码会话信号」乃化「整数」而化「字节串」而「字节串」也。
「编码会话信号」者会「彼号」而会「信号名」而
    若「等于」于（「字节串长度」于「信号名」）于「零」则「发生事故」授以「字节串」于『信号名不可为空』
    否则若「大于」于（「字节串长度」于「信号名」）于「六四」则「发生事故」授以「字节串」于『信号名过长』
    否则「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『signal』）于「阴」）于（「编码长度字节串」于「信号名」）
也。

「：文言：信号请惟容阴待答及一名串，无余字。汉语：严格解析 signal 请求，返回通道号和去掉 SIG 前缀的原始信号名；拒绝 want_reply=true、空名、超长名及尾随数据。：」
「读取会话信号」乃化「字节串」而（「或可有」于（「整数」合「字节串」））也。
「读取会话信号」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
        有（「可有」于「头」）则（
            若「非」于（「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『signal』））则「可无」
            否则若「头」中「二」则「可无」
            否则鉴「读取长度字节串」于「载荷」于（「头」中「三」）而
                有（「可有」于「信号与位」）则（
                    若「等于」于（「字节串长度」于（「信号与位」中「零」））于「零」则「可无」
                    否则若「大于」于（「字节串长度」于（「信号与位」中「零」））于「六四」则「可无」
                    否则若「不等于」于（「信号与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                    否则「可有」于（（「头」中「零」）与（「信号与位」中「零」）））
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。

「：文言：env 请附名与值二串，名不得空；此术惟编报而不施环境。汉语：编码 env 通道请求；环境变量是否被允许和应用，由服务端另行决定。：」
「编码环境请求」乃化「整数」而化「爻」而化「字节串」而化「字节串」而「字节串」也。
「编码环境请求」者会「彼号」而会「待答」而会「名称」而会「值」而
    若「等于」于（「字节串长度」于「名称」）于「零」则「发生事故」授以「字节串」于『环境变量名不可为空』
    否则「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『env』）于「待答」）于
        （「拼接字节串」于（「编码长度字节串」于「名称」）于（「编码长度字节串」于「值」））
也。

「：文言：严析 env 之名值二串，余字或空名皆拒；不更进程环境。汉语：解析完整 env 通道请求，返回接收通道号、want_reply、原始变量名与值；不设置环境。：」
「读取环境请求」乃化「字节串」而（「或可有」于（「整数」合「爻」合「字节串」合「字节串」））也。
「读取环境请求」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
    有（「可有」于「头」）则（
        若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『env』）
        则（鉴「读取长度字节串」于「载荷」于（「头」中「三」）而
            有（「可有」于「名与位」）则（
                若「等于」于（「字节串长度」于（「名与位」中「零」））于「零」则「可无」
                否则鉴「读取长度字节串」于「载荷」于（「名与位」中「一」）而
                    有（「可有」于「值与位」）则（
                        若「不等于」于（「值与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                        否则「可有」于（（「头」中「零」）与（「头」中「二」）与（「名与位」中「零」）与（「值与位」中「零」）））
                    或有「可无」则「可无」）
            或有「可无」则「可无」）
        否则「可无」）
    或有「可无」则「可无」
也。

「：文言：请求应答九十九号成，百号败。汉语：编码 CHANNEL_SUCCESS 或 CHANNEL_FAILURE，按 recipient channel 指向请求方。：」
「编码通道请求应答」乃化「整数」而化「爻」而「字节串」也。
「编码通道请求应答」者会「彼号」而会「成功」而
    「拼接字节串」于（「单字节串」于（若「成功」则「九九」否则「一零零」））于（「编码四字节无符号数」于「彼号」）
也。

「：文言：应答恰五字，返彼号及成败。汉语：解析 CHANNEL_SUCCESS 或 CHANNEL_FAILURE，拒绝尾随数据。：」
「读取通道请求应答」乃化「字节串」而（「或可有」于（「整数」合「爻」））也。
「读取通道请求应答」者会「载荷」而
    若「不等于」于（「字节串长度」于「载荷」）于「五」则「可无」
    否则虑「号」者「字节串取字节」于「载荷」于「零」而
        若「等于」于「号」于「九九」
        则（鉴「读取四字节无符号数」于「载荷」于「一」而
            有（「可有」于「彼号与位」）则「可有」于（（「彼号与位」中「零」）与「阳」）
            或有「可无」则「可无」）
        否则若「等于」于「号」于「一零零」
        则（鉴「读取四字节无符号数」于「载荷」于「一」而
            有（「可有」于「彼号与位」）则「可有」于（（「彼号与位」中「零」）与「阴」）
            或有「可无」则「可无」）
        否则「可无」
也。

「：文言：程序既毕，以九十八号 exit-status 告彼通道，勿求应答。汉语：编码 exit-status 通道请求，供服务端报告远程命令退出码。：」
「编码通道退出状态」乃化「整数」而化「整数」而「字节串」也。
「编码通道退出状态」者会「彼号」而会「状态」而
    「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『exit-status』）于「阴」）于（「编码四字节无符号数」于「状态」）
也。

「：文言：读 exit-status，须无待答且状态恰四字。汉语：严格解析 exit-status，返回接收通道号和退出码。：」
「读取通道退出状态」乃化「字节串」而（「或可有」于（「整数」合「整数」））也。
「读取通道退出状态」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
        有（「可有」于「头」）则（
            若「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『exit-status』）
            则（若「头」中「二」则「可无」
                否则鉴「读取四字节无符号数」于「载荷」于（「头」中「三」）而
                    有（「可有」于「状态与位」）则（
                        若「不等于」于（「状态与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                        否则「可有」于（（「头」中「零」）与（「状态与位」中「零」）））
                    或有「可无」则「可无」）
            否则「可无」）
        或有「可无」则「可无」
也。

「：文言：子进程因信号终，九十八号 exit-signal 请不待答，列名不冠 SIG。汉语：编码 RFC 4254 exit-signal；信号名不含 SIG 前缀，另带 core dumped、错误文本和语言标签。：」
「编码通道退出信号」乃化「整数」而化「字节串」而化「爻」而化「字节串」而化「字节串」而「字节串」也。
「编码通道退出信号」者会「彼号」而会「信号名」而会「转储」而会「错误文」而会「语言」而
    若「等于」于（「字节串长度」于「信号名」）于「零」则「发生事故」授以「字节串」于『退出信号名不可为空』
    否则若「大于」于（「字节串长度」于「信号名」）于「六四」则「发生事故」授以「字节串」于『退出信号名过长』
    否则若「大于」于（「字节串长度」于「错误文」）于「四零九六」则「发生事故」授以「字节串」于『退出信号错误文过长』
    否则若「大于」于（「字节串长度」于「语言」）于「六四」则「发生事故」授以「字节串」于『退出信号语言过长』
    否则「拼接字节串」于（「编码通道请求共头」于「彼号」于（「字符串转字节串」于『exit-signal』）于「阴」）于
        （「拼接字节串」于（「编码长度字节串」于「信号名」）于
            （「拼接字节串」于（「单字节串」于（若「转储」则「一」否则「零」））于
                （「拼接字节串」于（「编码长度字节串」于「错误文」）于（「编码长度字节串」于「语言」））））
也。

「：文言：读 exit-signal，核阴待答、布尔、四字段及尾界。汉语：严格解析 exit-signal，返回通道号、信号名、core dumped、错误文本和语言标签。：」
「读取通道退出信号」乃化「字节串」而（「或可有」于（「整数」合「字节串」合「爻」合「字节串」合「字节串」））也。
「读取通道退出信号」者会「载荷」而
    鉴「读取通道请求共头」于「载荷」而
        有（「可有」于「头」）则（
            若「非」于（「换钥字节同」于（「头」中「一」）于（「字符串转字节串」于『exit-signal』））则「可无」
            否则若「头」中「二」则「可无」
            否则鉴「读取长度字节串」于「载荷」于（「头」中「三」）而
                有（「可有」于「信号与位」）则（
                    若「等于」于（「字节串长度」于（「信号与位」中「零」））于「零」则「可无」
                    否则若「大于」于（「字节串长度」于（「信号与位」中「零」））于「六四」则「可无」
                    否则若「大于」于（「加」于（「信号与位」中「一」）于「一」）于（「字节串长度」于「载荷」）则「可无」
                    否则虑「转储字」者「字节串取字节」于「载荷」于（「信号与位」中「一」）而
                        若「大于」于「转储字」于「一」则「可无」
                        否则鉴「读取长度字节串」于「载荷」于（「加」于（「信号与位」中「一」）于「一」）而
                            有（「可有」于「错误与位」）则（
                                若「大于」于（「字节串长度」于（「错误与位」中「零」））于「四零九六」则「可无」
                                否则鉴「读取长度字节串」于「载荷」于（「错误与位」中「一」）而
                                    有（「可有」于「语言与位」）则（
                                        若「大于」于（「字节串长度」于（「语言与位」中「零」））于「六四」则「可无」
                                        否则若「不等于」于（「语言与位」中「一」）于（「字节串长度」于「载荷」）则「可无」
                                        否则「可有」于（（「头」中「零」）与（「信号与位」中「零」）与（若「等于」于「转储字」于「一」则「阳」否则「阴」）与（「错误与位」中「零」）与（「语言与位」中「零」）））
                                    或有「可无」则「可无」）
                            或有「可无」则「可无」）
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。
