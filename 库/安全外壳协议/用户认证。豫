寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数据结构」之「整数操作」之书。
寻观「标准库」之「数据结构」之「爻术」之书。
寻观「标准库」之「数据结构」之「可选值」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「拓展库」之「安全外壳」之「二进制编码」之书。
寻观「安全外壳密码」之「原语」之书。
寻观「安全外壳协议」之「协商」之书。
寻观「安全外壳协议」之「密钥交换」之书。

「：文言：认证共头为五十号、用户、服务及方法三串。汉语：编码 SSH_MSG_USERAUTH_REQUEST 的通用前缀，拒绝空用户名或服务名。：」
「编码认证共头」乃化「字节串」而化「字节串」而化「字节串」而「字节串」也。
「编码认证共头」者会「用户」而会「服务名」而会「方法」而
    若「等于」于（「字节串长度」于「用户」）于「零」则「发生事故」授以「字节串」于『认证用户名不可为空』
    否则若「等于」于（「字节串长度」于「服务名」）于「零」则「发生事故」授以「字节串」于『认证服务名不可为空』
    否则「拼接字节串」于（「单字节串」于「五零」）于（「拼接字节串」于（「编码长度字节串」于「用户」）于（「拼接字节串」于（「编码长度字节串」于「服务名」）于（「编码长度字节串」于「方法」）））
也。

「：文言：无认证法惟供探问可续之法。汉语：编码 RFC 4252 的 none 认证请求，用于获取服务端允许的方法。：」
「编码无认证请求」乃化「字节串」而化「字节串」而「字节串」也。
「编码无认证请求」者会「用户」而会「服务名」而
    「编码认证共头」于「用户」于「服务名」于（「字符串转字节串」于『none』）
也。

「：文言：原公钥三十二字节，纳之 SSH 载物。汉语：将 32 字节 Ed25519 公钥编码为 SSH 公钥 blob。：」
「编码Ed25519原公钥载物」乃化「字节串」而「字节串」也。
「编码Ed25519原公钥载物」者会「公钥」而
    若「不等于」于（「字节串长度」于「公钥」）于「三二」则「发生事故」授以「字节串」于『Ed25519 公钥须三十二字节』
    否则「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于『ssh-ed25519』））于（「编码长度字节串」于「公钥」）
也。

「：文言：以三十二字节私种得 Ed25519 公钥载物。汉语：从 Ed25519 种子编码 SSH 公钥 blob。：」
「编码Ed25519公钥载物」乃化「字节串」而「字节串」也。
「编码Ed25519公钥载物」者会「私种」而
    虑「公钥」者「求Ed25519公钥」于「私种」而
    「编码Ed25519原公钥载物」于「公钥」
也。

「：文言：公钥试探不署签，惟求服务者示可签否。汉语：编码 unsigned publickey 认证试探，服务端不得据此授予登录。：」
「编码Ed25519公钥试探请求」乃化「字节串」而化「字节串」而化「字节串」而「字节串」也。
「编码Ed25519公钥试探请求」者会「用户」而会「服务名」而会「公钥」而
    「拼接字节串」于（「编码认证共头」于「用户」于「服务名」于（「字符串转字节串」于『publickey』））于（「拼接字节串」于（「单字节串」于「零」）于（「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于『ssh-ed25519』））于（「编码长度字节串」于（「编码Ed25519原公钥载物」于「公钥」））））
也。

「编码Ed25519认证签名载物」乃化「字节串」而化「字节串」而「字节串」也。
「编码Ed25519认证签名载物」者会「私种」而会「被签文」而
    「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于『ssh-ed25519』））于（「编码长度字节串」于（「签Ed25519」于「私种」于「被签文」））
也。

「：文言：私种签首会话号之 SSH 串与五十号请求前缀，后附签名载物。汉语：编码 Ed25519 公钥认证请求；签名覆盖 string session_id 与完整请求前缀。：」
「编码Ed25519认证请求」乃化「字节串」而化「字节串」而化「字节串」而化「字节串」而「字节串」也。
「编码Ed25519认证请求」者会「用户」而会「服务名」而会「会话号」而会「私种」而
    若「不等于」于（「字节串长度」于「会话号」）于「三二」则「发生事故」授以「字节串」于『认证会话号须三十二字节』
    否则若「不等于」于（「字节串长度」于「私种」）于「三二」则「发生事故」授以「字节串」于『Ed25519 私种须三十二字节』
    否则虑「共头」者「编码认证共头」于「用户」于「服务名」于（「字符串转字节串」于『publickey』）而
        虑「公钥载物」者「编码Ed25519公钥载物」于「私种」而
        虑「前缀」者「拼接字节串」于「共头」于（「拼接字节串」于（「单字节串」于「一」）于（「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于『ssh-ed25519』））于（「编码长度字节串」于「公钥载物」）））而
        虑「被签文」者「拼接字节串」于（「编码长度字节串」于「会话号」）于「前缀」而
        「拼接字节串」于「前缀」于（「编码长度字节串」于（「编码Ed25519认证签名载物」于「私种」于「被签文」））
也。

「读取认证共头」乃化「字节串」而（「或可有」于（「字节串」合「字节串」合「字节串」合「整数」））也。
「读取认证共头」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「一」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「五零」则「可无」
    否则鉴「读取长度字节串」于「载荷」于「一」而
        有（「可有」于「用户与位」）则（
            若「等于」于（「字节串长度」于（「用户与位」中「零」））于「零」则「可无」
            否则鉴「读取长度字节串」于「载荷」于（「用户与位」中「一」）而
                有（「可有」于「服务与位」）则（
                    若「等于」于（「字节串长度」于（「服务与位」中「零」））于「零」则「可无」
                    否则鉴「读取长度字节串」于「载荷」于（「服务与位」中「一」）而
                        有（「可有」于「方法与位」）则「可有」于（（「用户与位」中「零」）与（「服务与位」中「零」）与（「方法与位」中「零」）与（「方法与位」中「一」））
                        或有「可无」则「可无」）
                或有「可无」则「可无」）
        或有「可无」则「可无」
也。

「：文言：取认证报所呈 Ed25519 公钥，供查授权簿；其余签与尾界仍须后验。汉语：从 publickey 请求提取候选公钥，绑定用户和服务；授权、签名和完整报文仍须由后续严格验证。：」
「读取Ed25519认证所呈钥」乃化「字节串」而化「字节串」而化「字节串」而（「或可有」于「字节串」）也。
「读取Ed25519认证所呈钥」者会「载荷」而会「用户」而会「服务名」而
    鉴「读取认证共头」于「载荷」而
    有（「可有」于「共头」）则（
        若「非」于（「换钥字节同」于（「共头」中「零」）于「用户」）则「可无」
        否则若「非」于（「换钥字节同」于（「共头」中「一」）于「服务名」）则「可无」
        否则若「非」于（「换钥字节同」于（「共头」中「二」）于（「字符串转字节串」于『publickey』））则「可无」
        否则若「大于」于（「加」于（「共头」中「三」）于「一」）于（「字节串长度」于「载荷」）则「可无」
        否则虑「签位」者「字节串取字节」于「载荷」于（「共头」中「三」）而
            若「大于」于「签位」于「一」则「可无」
            否则鉴「读取长度字节串」于「载荷」于（「加」于（「共头」中「三」）于「一」）而
                有（「可有」于「算法与位」）则（
                    若「非」于（「换钥字节同」于（「算法与位」中「零」）于（「字符串转字节串」于『ssh-ed25519』））则「可无」
                    否则鉴「读取长度字节串」于「载荷」于（「算法与位」中「一」）而
                        有（「可有」于「公钥与位」）则「读取Ed25519主钥」于（「公钥与位」中「零」）
                        或有「可无」则「可无」）
                或有「可无」则「可无」）
    或有「可无」则「可无」
也。

「：文言：验无认证请求共头与尾界。汉语：服务端解析 none 请求，核用户与目标服务并拒绝尾随字段。：」
「无认证请求可用吗」乃化「字节串」而化「字节串」而化「字节串」而「爻」也。
「无认证请求可用吗」者会「载荷」而会「用户」而会「服务名」而
    鉴「读取认证共头」于「载荷」而
        有（「可有」于「共头」）则（
            若「不等于」于（「共头」中「三」）于（「字节串长度」于「载荷」）则「阴」
            否则若「换钥字节同」于（「共头」中「零」）于「用户」
                则（若「换钥字节同」于（「共头」中「一」）于「服务名」
                    则「换钥字节同」于（「共头」中「二」）于（「字符串转字节串」于『none』）
                    否则「阴」）
                否则「阴」）
        或有「可无」则「阴」
也。

「：文言：试探者须合用户、服务、获准之钥，且无签名。汉语：服务端严格验证未签名公钥试探的授权公钥和完整字段。：」
「Ed25519公钥试探可用吗」乃化「字节串」而化「字节串」而化「字节串」而化「字节串」而「爻」也。
「Ed25519公钥试探可用吗」者会「载荷」而会「用户」而会「服务名」而会「获准公钥」而
    若「不等于」于（「字节串长度」于「获准公钥」）于「三二」则「阴」
    否则鉴「读取认证共头」于「载荷」而
        有（「可有」于「共头」）则（
            若「非」于（「换钥字节同」于（「共头」中「零」）于「用户」）则「阴」
            否则若「非」于（「换钥字节同」于（「共头」中「一」）于「服务名」）则「阴」
            否则若「非」于（「换钥字节同」于（「共头」中「二」）于（「字符串转字节串」于『publickey』））则「阴」
            否则若「大于」于（「加」于（「共头」中「三」）于「一」）于（「字节串长度」于「载荷」）则「阴」
            否则若「不等于」于（「字节串取字节」于「载荷」于（「共头」中「三」））于「零」则「阴」
            否则鉴「读取长度字节串」于「载荷」于（「加」于（「共头」中「三」）于「一」）而
                有（「可有」于「算法与位」）则（
                    若「换钥字节同」于（「算法与位」中「零」）于（「字符串转字节串」于『ssh-ed25519』）
                    则鉴「读取长度字节串」于「载荷」于（「算法与位」中「一」）而
                        有（「可有」于「公钥与位」）则（
                            若「不等于」于（「公钥与位」中「一」）于（「字节串长度」于「载荷」）则「阴」
                            否则鉴「读取Ed25519主钥」于（「公钥与位」中「零」）而
                                有（「可有」于「公钥」）则「换钥字节同」于「公钥」于「获准公钥」
                                或有「可无」则「阴」）
                        或有「可无」则「阴」
                    否则「阴」）
                或有「可无」则「阴」）
        或有「可无」则「阴」
也。

「：文言：六十号回显获准公钥，令客户署签。汉语：编码 USERAUTH_PK_OK，回显 Ed25519 算法和公钥 blob。：」
「编码Ed25519公钥待签载荷」乃化「字节串」而「字节串」也。
「编码Ed25519公钥待签载荷」者会「公钥」而
    「拼接字节串」于（「单字节串」于「六零」）于（「拼接字节串」于（「编码长度字节串」于（「字符串转字节串」于『ssh-ed25519』））于（「编码长度字节串」于（「编码Ed25519原公钥载物」于「公钥」）））
也。

「验Ed25519认证签」乃化「字节串」而化「字节串」而化「字节串」而化「整数」而「爻」也。
「验Ed25519认证签」者会「载荷」而会「会话号」而会「公钥」而会「签位」而
    鉴「读取长度字节串」于「载荷」于「签位」而
        有（「可有」于「签载与位」）则（
            若「不等于」于（「签载与位」中「一」）于（「字节串长度」于「载荷」）则「阴」
            否则鉴「读取Ed25519签名」于（「签载与位」中「零」）而
                有（「可有」于「签名」）则（
                    虑「被签文」者「拼接字节串」于（「编码长度字节串」于「会话号」）于（「截取字节串」于「载荷」于「零」于「签位」）而
                    「验Ed25519」于「公钥」于「被签文」于「签名」）
                或有「可无」则「阴」）
        或有「可无」则「阴」
也。

「验Ed25519认证续」乃化「字节串」而化「字节串」而化「字节串」而化「整数」而「爻」也。
「验Ed25519认证续」者会「载荷」而会「会话号」而会「获准公钥」而会「位」而
    若「大于」于（「加」于「位」于「一」）于（「字节串长度」于「载荷」）则「阴」
    否则若「不等于」于（「字节串取字节」于「载荷」于「位」）于「一」则「阴」
    否则鉴「读取长度字节串」于「载荷」于（「加」于「位」于「一」）而
        有（「可有」于「算法与位」）则（
            若「换钥字节同」于（「算法与位」中「零」）于（「字符串转字节串」于『ssh-ed25519』）
            则鉴「读取长度字节串」于「载荷」于（「算法与位」中「一」）而
                有（「可有」于「公钥与位」）则（
                    鉴「读取Ed25519主钥」于（「公钥与位」中「零」）而
                        有（「可有」于「公钥」）则（
                            若「换钥字节同」于「公钥」于「获准公钥」
                            则「验Ed25519认证签」于「载荷」于「会话号」于「公钥」于（「公钥与位」中「一」）
                            否则「阴」）
                        或有「可无」则「阴」）
                或有「可无」则「阴」
            否则「阴」）
        或有「可无」则「阴」
也。

「：文言：验五十号公钥认证，须用户、服务、获准公钥与 Ed25519 签皆合。汉语：服务端验证完整 Ed25519 公钥认证请求，绑定用户名、服务、会话标识和获授权公钥。：」
「验Ed25519认证请求」乃化「字节串」而化「字节串」而化「字节串」而化「字节串」而化「字节串」而「爻」也。
「验Ed25519认证请求」者会「载荷」而会「用户」而会「服务名」而会「会话号」而会「获准公钥」而
    若「不等于」于（「字节串长度」于「会话号」）于「三二」则「阴」
    否则若「不等于」于（「字节串长度」于「获准公钥」）于「三二」则「阴」
    否则鉴「读取认证共头」于「载荷」而
        有（「可有」于「共头」）则（
            若「换钥字节同」于（「共头」中「零」）于「用户」
            则（若「换钥字节同」于（「共头」中「一」）于「服务名」
                则（若「换钥字节同」于（「共头」中「二」）于（「字符串转字节串」于『publickey』）
                    则「验Ed25519认证续」于「载荷」于「会话号」于「获准公钥」于（「共头」中「三」）
                    否则「阴」）
                否则「阴」）
            否则「阴」）
        或有「可无」则「阴」
也。

「：文言：五十一号失败消息还可续法列及部分成功位，拒非布尔及尾字。汉语：解析 USERAUTH_FAILURE，验证名称列、布尔位和完整长度。：」
「读取认证失败」乃化「字节串」而（「或可有」于（「字节串」合「爻」））也。
「读取认证失败」者会「载荷」而
    若「小于」于（「字节串长度」于「载荷」）于「六」则「可无」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「五一」则「可无」
    否则鉴「读取长度字节串」于「载荷」于「一」而
        有（「可有」于「列与位」）则（
            若「不等于」于（「加」于（「列与位」中「一」）于「一」）于（「字节串长度」于「载荷」）则「可无」
            否则若「名称列可用吗」于（「列与位」中「零」）于「阳」
                则（虑「部分」者「字节串取字节」于「载荷」于（「列与位」中「一」）而
                    若「等于」于「部分」于「零」则「可有」于（（「列与位」中「零」）与「阴」）
                    否则若「等于」于「部分」于「一」则「可有」于（（「列与位」中「零」）与「阳」）
                    否则「可无」）
                否则「可无」）
        或有「可无」则「可无」
也。

「：文言：五十一号失败载可续法列与部分成功位。汉语：服务端编码 USERAUTH_FAILURE；名称列必须合法，partial success 仅作布尔值。：」
「编码认证失败载荷」乃化「字节串」而化「爻」而「字节串」也。
「编码认证失败载荷」者会「方法列」而会「部分成功」而
    若「名称列可用吗」于「方法列」于「阳」
    则「拼接字节串」于（「单字节串」于「五一」）于（「拼接字节串」于（「编码长度字节串」于「方法列」）于（「单字节串」于（若「部分成功」则「一」否则「零」）））
    否则「发生事故」授以「字节串」于『认证方法名称列非法』
也。

「：文言：五十二号成功载惟一字。汉语：服务端编码 USERAUTH_SUCCESS。：」
「编码认证成功载荷」乃化「有」而「字节串」也。
「编码认证成功载荷」者会「无」而「单字节串」于「五二」也。

「：文言：六十号待签消息须算法与公钥载物皆同所试，且无余字。汉语：验证 SSH_MSG_USERAUTH_PK_OK 完整回显所提交的 Ed25519 公钥。：」
「Ed25519公钥待签可用吗」乃化「字节串」而化「字节串」而「爻」也。
「Ed25519公钥待签可用吗」者会「载荷」而会「已试公钥载物」而
    若「小于」于（「字节串长度」于「载荷」）于「九」则「阴」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「六零」则「阴」
    否则鉴「读取Ed25519主钥」于「已试公钥载物」而
        有（「可有」于「公钥」）则（
            鉴「读取长度字节串」于「载荷」于「一」而
                有（「可有」于「算法与位」）则（
                    若「换钥字节同」于（「算法与位」中「零」）于（「字符串转字节串」于『ssh-ed25519』）
                    则鉴「读取长度字节串」于「载荷」于（「算法与位」中「一」）而
                        有（「可有」于「载物与位」）则（
                            若「不等于」于（「载物与位」中「一」）于（「字节串长度」于「载荷」）则「阴」
                            否则「换钥字节同」于（「载物与位」中「零」）于「已试公钥载物」）
                        或有「可无」则「阴」
                    否则「阴」）
                或有「可无」则「阴」）
        或有「可无」则「阴」
也。

「：文言：成功消息惟五十二号一字。汉语：验证 USERAUTH_SUCCESS 无尾随数据。：」
「认证成功载荷可用吗」乃化「字节串」而「爻」也。
「认证成功载荷可用吗」者会「载荷」而
    若「不等于」于（「字节串长度」于「载荷」）于「一」则「阴」
    否则「等于」于（「字节串取字节」于「载荷」于「零」）于「五二」
也。
