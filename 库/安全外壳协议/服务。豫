寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数据结构」之「整数操作」之书。
寻观「标准库」之「数据结构」之「可选值」之书。
寻观「标准库」之「数据结构」之「字节串」之书。
寻观「拓展库」之「安全外壳」之「二进制编码」之书。

「服务逐字同」乃化「字节串」而化「字节串」而化「整数」而「爻」也。
「服务逐字同」者会「甲」而会「乙」而会「位」而
    若「等于」于「位」于（「字节串长度」于「甲」）则「阳」
    否则若「等于」于（「字节串取字节」于「甲」于「位」）于（「字节串取字节」于「乙」于「位」）
        则「服务逐字同」于「甲」于「乙」于（「加」于「位」于「一」）
        否则「阴」
也。

「服务字节同」乃化「字节串」而化「字节串」而「爻」也。
「服务字节同」者会「甲」而会「乙」而
    若「不等于」于（「字节串长度」于「甲」）于（「字节串长度」于「乙」）则「阴」
    否则「服务逐字同」于「甲」于「乙」于「零」
也。

「：文言：五号消息以 SSH string 载服务名。汉语：编码 SSH_MSG_SERVICE_REQUEST 载荷，拒绝空服务名。：」
「编码服务请求载荷」乃化「字节串」而「字节串」也。
「编码服务请求载荷」者会「服务名」而
    若「等于」于（「字节串长度」于「服务名」）于「零」则「发生事故」授以「字节串」于『服务名不可为空』
    否则「拼接字节串」于（「单字节串」于「五」）于（「编码长度字节串」于「服务名」）
也。

「：文言：六号消息报已受服务名。汉语：服务端编码 SSH_MSG_SERVICE_ACCEPT，服务名不可为空。：」
「编码服务接受载荷」乃化「字节串」而「字节串」也。
「编码服务接受载荷」者会「服务名」而
    若「等于」于（「字节串长度」于「服务名」）于「零」则「发生事故」授以「字节串」于『服务名不可为空』
    否则「拼接字节串」于（「单字节串」于「六」）于（「编码长度字节串」于「服务名」）
也。

「：文言：五号请求须全载服务名，无余字。汉语：服务端验证 SSH_MSG_SERVICE_REQUEST 与指定服务名完全一致。：」
「服务请求载荷可用吗」乃化「字节串」而化「字节串」而「爻」也。
「服务请求载荷可用吗」者会「载荷」而会「服务名」而
    若「小于」于（「字节串长度」于「载荷」）于「五」则「阴」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「五」则「阴」
    否则鉴「读取长度字节串」于「载荷」于「一」而
        有（「可有」于「名称与位」）则（
            若「不等于」于（「名称与位」中「一」）于（「字节串长度」于「载荷」）则「阴」
            否则「服务字节同」于（「名称与位」中「零」）于「服务名」）
        或有「可无」则「阴」
也。

「：文言：六号接受消息须全载服务名，无余字。汉语：验证 SSH_MSG_SERVICE_ACCEPT 与请求的服务名完全一致。：」
「服务接受载荷可用吗」乃化「字节串」而化「字节串」而「爻」也。
「服务接受载荷可用吗」者会「载荷」而会「服务名」而
    若「小于」于（「字节串长度」于「载荷」）于「五」则「阴」
    否则若「不等于」于（「字节串取字节」于「载荷」于「零」）于「六」则「阴」
    否则鉴「读取长度字节串」于「载荷」于「一」而
        有（「可有」于「名称与位」）则（
            若「不等于」于（「名称与位」中「一」）于（「字节串长度」于「载荷」）则「阴」
            否则「服务字节同」于（「名称与位」中「零」）于「服务名」）
        或有「可无」则「阴」
也。
