寻观「标准库」之书。
寻观「构建基础」之「包上下文」之书。
寻观「构建基础」之「包定位」之书。
寻观「编译器核心」之「编译数据」之「编译配置」之「全局配置」之书。

寻观「编译器核心」之「编译步骤」之「总体过程」之「编译过程工具」之书。

「确保可移植自举包目录」乃化「字符串」而「有」也。
「确保可移植自举包目录」者会「路径」而
  虑「无」者「操作系统」之「子进程」之「成功运行并打印结果」于『mkdir』
    于（（「列」【『-p』，「路径」】））
  而
  「元」
也。

「复制可移植自举包文本文件」乃化「字符串」而化「字符串」而「有」也。
「复制可移植自举包文本文件」者会「源路径」而会「目标路径」而
  虑「无」者「确保可移植自举包目录」于（「路径」之「获取文件夹名」于「目标路径」）而
  虑「无」者「操作系统」之「子进程」之「成功运行并打印结果」于『cp』
    于（（「列」【「源路径」，「目标路径」】））而
  「元」
也。

「：文言：唯取此次所编之文，不遍取旧缓存。汉语：按当前编译闭包导出 LLVM，避免双种子共享缓存时混入另一入口。：」
「复制可移植自举包LLVM文件」乃化「字符串」而化「字符串」而「有」也。
「复制可移植自举包LLVM文件」者会「输出目录」而会「主文件」而
  虑「模块们」者「结尾多」授以「字符串」于（「读引用」授以（「列」于「字符串」）于「文件编译顺序」）于「主文件」而
  虑「无」者「从左折叠」授以「字符串」授以「整数」于（会「模块」而会「序」而
    虑「无」者「复制可移植自举包文本文件」于（「获取输出代码文件路径」于「模块」）于
      （「路径」之「路径合并」于「输出目录」于（『llvm/』附（「整数表示」于「序」）附『.ll』））而「加」于「序」于「一」）于「零」于「模块们」而「元」也。

「复制可移植自举包运行时目录」乃化「字符串」而化「字符串」而「有」也。
「复制可移植自举包运行时目录」者会「输出目录」而会「运行时根路径」而
  虑「运行时输出目录」者「路径」之「路径合并」于「输出目录」于『运行时/原生』而
  虑「无」者「确保可移植自举包目录」于「运行时输出目录」而
  虑「复制脚本」者『set -eu
cp -R "$1/." "$2/"
』而
  虑「参数们」者（（「列」于「字符串」）也（（「列」【『-c』，「复制脚本」，『yuyan-bootstrap-bundle-runtime-copy』，「运行时根路径」，「运行时输出目录」】）））
  而
  虑「无」者「操作系统」之「子进程」之「成功运行并打印结果」于『sh』于「参数们」而
  「元」
也。

「生成可移植自举包」乃化「字符串」 「：输出目录：」而化「字符串」而
  「有」也。
「生成可移植自举包」者会「输出目录」而会「主文件」而
  虑「无」者若「文件系统」之「路径存在」于「输出目录」则「发生事故」授以「有」于『自举包输出目录已存在，拒绝混入旧模块』否则「元」而
  虑「当前路径」者「文件系统」之「获取当前路径」于「元」而
  虑「运行时根路径」者「路径」之「路径合并」于「当前路径」于『运行时支持库/原生』而
  虑「无」者若（「文件系统」之「路径是文件夹」于「运行时根路径」）
  则
    「元」
  否则
    （「发生事故」于『找不到运行时源码目录：』附「运行时根路径」）而
  虑「无」者「确保可移植自举包目录」于「输出目录」而
  虑「无」者「复制可移植自举包LLVM文件」于「输出目录」于「主文件」而
  虑「无」者「复制可移植自举包运行时目录」于「输出目录」于「运行时根路径」而
  「：文言：桥接随包带正文，不复依原仓。汉语：导出包以实际实现替换兼容包含文件，离开源码仓库也能构建。：」
  虑「无」者「复制可移植自举包文本文件」于（「路径」之「路径合并」于（「包目录」于（「查包」于（「读引用」授以「包索引」于「包上下文索引」）于（「选择包身份」于（「读引用」授以「包索引」于「包上下文索引」）于（『』与『构建基础』））））于『包上下文.c』）于
    （「路径」之「路径合并」于「输出目录」于『运行时/原生/包上下文.c』）而
  虑「构建脚本」者
    『#!/bin/sh
set -eu
cd "$(dirname "$0")"
clang -O3 -DNDEBUG -Wno-override-module -Wno-error -I运行时/原生 $(find llvm -type f -name '*.ll') $(find 运行时/原生 -type f \( -name '*.c' -o -name '*.cpp' -o -name '*.ll' \)) -pthread -o yy种子
』而
  虑「构建脚本路径」者「路径」之「路径合并」于「输出目录」于『构建.sh』而
  虑「无」者「文件系统」之「写文件」于「构建脚本路径」于「构建脚本」而
  虑「无」者「操作系统」之「子进程」之「成功运行并打印结果」于『chmod』
    于（（「列」【『u+x』，「构建脚本路径」】））而
  虑「自述路径」者「路径」之「路径合并」于「输出目录」于『自述.md』而
  虑「无」者「文件系统」之「写文件」于「自述路径」于
    『# 豫言可移植自举包

此目录包含与目标机器无关的 LLVM 中间表示，以及完整的豫言原生运行时源码。

构建只需要 `clang`；垃圾回收器已经包含在运行时源码中。

可在任意工作目录执行：

```sh
./构建.sh
```

生成的编译器名为 `yy种子`。
』而
  虑「清单路径」者「路径」之「路径合并」于「输出目录」于『清单.txt』而
  虑「无」者「文件系统」之「写文件」于「清单路径」于
    『LLVM 模块：
llvm/**/*.ll

运行时源码：
运行时/原生/
』而
  虑「许可源路径」者「路径」之「路径合并」于「当前路径」于『LICENSE』而
  虑「无」者若（「文件系统」之「路径是普通文件」于「许可源路径」）
  则
    （「复制可移植自举包文本文件」于「许可源路径」于（「路径」之「路径合并」于「输出目录」于『LICENSE』））
  否则
    「元」而
  虑「无」者「打印行」于（『已生成可移植自举包：』附「输出目录」）而
  「元」
也。
