寻观「标准库」之书。
寻观「编译器核心」之「编译步骤」之「求值正则变换」之「统一正则数据」之书。

「：文言：阳支唯加一，阴支还其本，乃可合算；本须另证为裸整。汉语：仅识别真分支单条整数加一、假分支直接返回同一局部值的形状。调用者必须另证基值和结果均为裸整数；条件仍按原规则求值，归一化为零或一。拒绝额外操作、不同返回值及所有可能有作用的分支。：」
「析条件累加」乃化「正则区域」而化「正则区域」而（「或可有」于「整数」）也。
「析条件累加」者会「阳体」而会「阴体」而鉴「阳体」而
 有（「正则段」于（「列」【（「正则运算」于「号」于「类」于（「正则原语」于『豫言_整数加』）于（「列」【（「正则局部」于「基」），（「正则整数」于「增」）】））】）于（「正则产出」于（「正则局部」于「果」）））则
  若「且」于（「等于」于「增」于「一」）于（「等于」于「号」于「果」）则鉴「阴体」而
   有（「正则段」于「罄」于（「正则产出」于（「正则局部」于「还」）））则若「等于」于「基」于「还」则「可有」于「基」否则「可无」
   或有「阴余」则「可无」
  否则「可无」
 或有「阳余」则「可无」也。
