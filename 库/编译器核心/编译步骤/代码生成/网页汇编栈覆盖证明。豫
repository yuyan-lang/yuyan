寻观「标准库」之书。
寻观「编译器核心」之「编译步骤」之「求值正则变换」之「统一正则数据」之书。
寻观「编译器核心」之「编译步骤」之「代码生成」之「网页汇编栈表示证明」之书。
寻观「编译器核心」之「编译步骤」之「代码生成」之「网页汇编栈填充证明」之书。
「：文言：新组未读，首调自零遍写至长，乃可免其先填。汉语：输入及所有被调函数须已通过 SSA、表示和封闭调用图验证，入口仍须检查整数参数与容量。这里只证明根数组创建后紧接的直接调用完整覆盖数组，且无读取、未知调用或可陷阱算术。不能单独证明栈资格；容量失败仍走原 GC 后备。长度按同一原子匹配，不猜测数值相等。：」
「栈覆盖同原子」乃化「正则原子」而化「正则原子」而「爻」也。
「栈覆盖同原子」者会「甲」而会「乙」而鉴「甲」而
 有（「正则局部」于「甲号」）则（鉴「乙」而有（「正则局部」于「乙号」）则「等于」于「甲号」于「乙号」或有「其他」则「阴」）
 或有（「正则整数」于「甲数」）则（鉴「乙」而有（「正则整数」于「乙数」）则「等于」于「甲数」于「乙数」或有「其他」则「阴」）
 或有「其他」则「阴」也。
「验栈首调覆盖」乃化（化「字符串」而「或可有」于（「栈表示签」合「正则函数」））而化「正则函数」而「爻」也。
「验栈首调覆盖」者会「查」而会「函」而鉴（「函」中「三」）而有（「正则段」于「诸令」于「终」）则（鉴「诸令」而
 有（「缀」于（「正则运算」于「根」于「根类」于（「正则原语」于『豫言_新建引用数组』）于（「列」【「初」，「长」】））于（「缀」于（「正则运算」于「果」于「果类」于「正则调用」于（「缀」于（「正则函数引用」于「名」）于「实」））于「余」））则（
  鉴（「查」于「名」）而有「可无」则「阴」或有（「可有」于（「签」与「子」））则（
   若「非」于（「等于」于（「长度」授以「正则原子」于「实」）于（「长度」授以「整数」于（「子」中「二」）））则「阴」否则
   鉴（「验栈填充」于「签」于「子」）而有「可无」则「阴」或有（「可有」于「证」）则（
    递归虑「实参」其化「整数」而化（「列」于「整数」）而化（「列」于「正则原子」）而「正则原子」者会「号」而会「形」而会「值」而
     鉴「形」而有（「缀」于「首」于「尾」）则（鉴「值」而有（「缀」于「先」于「后」）则
      若「等于」于「号」于「首」则「先」否则「实参」于「号」于「尾」于「后」或有「其他」则「正则单元」）或有「其他」则「正则单元」而
    虑「映」者（化「整数」而「正则原子」也会「号」而「实参」于「号」于（「子」中「二」）于「实」）而
    「且」于（「栈覆盖同原子」于（「映」于（「证」中「零」））于（「正则局部」于「根」））于（「且」于（「栈覆盖同原子」于（「映」于（「证」中「一」））于「长」）于（「栈覆盖同原子」于（「映」于（「证」中「二」））于（「正则整数」于「零」））））））
 或有「其他」则「阴」）也。
