寻观「标准库」之书。

「：文言：各阶另起一程，两路至后端同。汉语：逐阶段重启编译器，对比 JSON 输入和豫言输入的前端与纯正则视图；旧 CPS 和手动栈后端已移除；输出目录须已存在。：」
「阶段们」乃「列」于（「字符串」合「字符串」）也。
「阶段们」者「列」【
  『源码』与『词法形式』，『词法形式』与『抽象语法』，『抽象语法』与『名称解析后形式』，
  『名称解析后形式』与『检查后定义』，『检查后对象』与『模式展开形式』，
  『模式展开形式』与『类型擦除形式』，『类型擦除形式』与『续延调用展开形式』，『续延调用展开形式』与『擦除后形式』，
  『擦除后形式』与『优化后形式』，『优化后形式』与『闭包前优化形式』，『闭包前优化形式』与『递归转换形式』，
  『递归转换形式』与『闭包环境形式』，『闭包环境形式』与『全局存储形式』，『全局存储形式』与『闭包形式』，
  『闭包形式』与『纯求值正则形式』，『纯求值正则形式』与『正则消冗形式』】也。

「可用依赖」乃化「字符串」而化「字符串」而化「字符串」而（「列」于「字符串」）也。
「可用依赖」者会「源」而会「前缀」而会「阶段」而
  虑「文件」者「前缀」附『.』附「阶段」附『.json』而
  若「文件系统」之「路径存在」于「文件」则「列」【「源」，「阶段」，「文件」】否则「罄」也。

「运行阶段列」乃化「字符串」而化「字符串」而化「字符串」而化「爻」而化（「列」于（「字符串」合「字符串」））而「有」也。
「运行阶段列」者会「编译器」而会「源」而会「前缀」而会「用文本」而会「阶段列」而鉴「阶段列」而
  有「罄」则「元」
  或有「缀」于（「入阶」与「出阶」）于「余」则（
    虑「输入」者（若「串相等」于「入阶」于『源码』则「源」否则「前缀」附『.』附「入阶」附（若「用文本」则『。豫』否则『.json』））而
    虑「依赖」者「附加」授以「字符串」于（「列」【「源」，『源码』，「源」】）于
      （「附加」授以「字符串」于（「可用依赖」于「源」于「前缀」于『名称解析资料』）于（「可用依赖」于「源」于「前缀」于『检查后定义』））而
    虑「状态」者「子进程」之「运行并传递输出」于「编译器」于（「附加」授以「字符串」于（「列」【『stage』，「入阶」，「出阶」，「输入」，「前缀」，「源」】）于「依赖」）而
    若「等」于「状态」于「零」则「运行阶段列」于「编译器」于「源」于「前缀」于「用文本」于「余」否则「发生事故」于（『阶段失败：』附「出阶」））也。

「比较阶段文件」乃化「字符串」而化「字符串」而「有」也。
「比较阶段文件」者会「目录」而会「后缀」而
  虑「甲」者「文件系统」之「读文件」于（「目录」附『/JSON.』附「后缀」）而
  虑「乙」者「文件系统」之「读文件」于（「目录」附『/文本.』附「后缀」）而
  若「串相等」于「甲」于「乙」则「元」否则「发生事故」于（『阶段结果不一致：』附「后缀」）也。

鉴「命令行参数」之「获取参数」于「元」而
  有（「列」【「编译器」，「源」，「目录」】）则（
    虑「无」者「运行阶段列」于「编译器」于「源」于（「目录」附『/JSON』）于「阴」于「阶段们」而
    虑「无」者「运行阶段列」于「编译器」于「源」于（「目录」附『/文本』）于「阳」于「阶段们」而
    虑「无」者「态射」授以（「字符串」合「字符串」）授以「有」于「阶段们」于（会「阶」而
      「比较阶段文件」于「目录」于（（「阶」中「一」）附『.json』））而
    虑「无」者「比较阶段文件」于「目录」于『检查后对象.json』而
    虑「无」者「比较阶段文件」于「目录」于『名称解析资料.json』而
    「打印行」于『逐阶段新进程：JSON 与豫言前端阶段结果一致』）
  或有「其他」则「发生事故」授以「有」于『用法：yy中间阶段集成 编译器绝对路径 样例绝对路径 已存在的输出目录』。
