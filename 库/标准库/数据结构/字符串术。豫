寻观「标准库」之「数据结构」之「爻术」之书。
寻观「标准库」之「数据结构」之「多态列」之书。
寻观「标准库」之「数据结构」之「多态列工具」之书。

寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「续延」之书。
寻观「标准库」之「语言核心」之「异常」之书。

「： 注 string list。 ：」

寻观「标准库」之「数据结构」之「整数操作」之书。

寻观「标准库」之「数据结构」之「可选值」之书。


寻观「标准库」之「数学运算」之「数组转换」之书。


「：
现代汉语：按运行时的通字符切分规则将字符串转成字符字符串列表，保持顺序。
文言：循通字符之法分串为列，序不易。
诗曰：长笺拆作字成行，一字一珠各有光。次第分明留旧序，千言依旧在囊藏。
：」
「串转通字符」乃化「字符串」而「列」于「字符串」也。
「串转通字符」者会「串」而（「数组转多态列」授以「字符串」）于（《《C调用》》名『豫言_获取通字符』传「串」）也。

「：
现代汉语：串转通字符的别名，将字符串拆成通字符列表。
文言：串转通字符之异名也。
诗曰：拆开长卷认珠玑，一字分来一字齐。别号仍循同一道，千言次第有归依。
：」
「获取字符」者「串转通字符」也。

「：
现代汉语：顺序拼接字符串列表的各项，空列表得到空字符串。
文言：循序联诸串，空列得空串。
诗曰：散落珠玑聚一绳，前珠后玉次相承。空盘未有丝头起，也守无言一盏灯。
：」
「通字符转串」乃化（「列」于「字符串」）而「字符串」也。
「通字符转串」者会「列」而
    虑「后列」者（（「多态列转数组」授以「字符串」）于「列」）而
    《《C调用》》名『豫言_通字符拼接』传「后列」也。

「：
现代汉语：通字符转串的别名，顺序拼接字符串列表。
文言：通字符转串之异名也，循列联文。
诗曰：断简重连墨尚新，千言次第复相亲。同源别号传文意，合得长笺寄远人。
：」
「合并」者「通字符转串」也。
「：
现代汉语：通字符转串的别名，将字符串列表连接为一个字符串。
文言：亦名通字符转串，联众文为一。
诗曰：诸篇合卷到书楼，旧字依然次第留。不向行间添一笔，连成文脉共长流。
：」
「串组合并」者「通字符转串」也。


「：
现代汉语：返回左字符串后接右字符串的结果。
文言：续右文于左文之后。
诗曰：左岸歌声右岸风，两行接作一笺通。先言未改后来续，共写长情到卷终。
：」
「字符串拼接」乃化「字符串」而化「字符串」而「字符串」也。
「字符串拼接」者会「串甲」而会「串乙」而《《C调用》》名『豫言_字符串拼接』传「串甲」与「串乙」也。

「：
现代汉语：接收分隔符和字符串列表，只在相邻字符串之间插入分隔符并拼接；空列表返回空串。
文言：受隔文及列，邻串之间置隔而联之，空则空串。
诗曰：句间留白字相连，不向篇头尾后连。一线穿珠分远近，空笺依旧待新篇。
：」
「间合」乃化「字符串」而化（「列」于「字符串」）而「字符串」也。
「间合」者会「分隔符」而会「字符串列」而
    「：
现代汉语：间合的局部递归函数；在相邻字符串间插入分隔符，单元素和空列表不额外追加分隔符。
文言：局部间合之术，邻串置隔，独物与空皆不加尾隔。
诗曰：邻珠之间系短绳，末珠独立自分明。空盘也莫添闲线，串罢依然首尾清。
：」
递归虑「处理」其化（「列」于「字符串」）而（「列」于「字符串」）者
        会「剩余」而
            鉴「剩余」而
                有「罄」则「罄」
                或有（「列」【「甲」】）则（「列」【「甲」】）
                或有「缀」于「甲」于「乙」则「缀」于「甲」于（「缀」于「分隔符」于（「处理」于「乙」））
    而
        虑「结果」者「处理」于「字符串列」而
        「串组合并」于「结果」
    也。

「：
现代汉语：比较两个字符串的完整内容，相等返回阳。
文言：校二串全体，同则阳。
诗曰：两纸临窗细校文，字同还要始终真。莫凭相似轻言合，尽处无差始可亲。
：」
「字符串相等吗」乃化「字符串」而化「字符串」而「爻」也。
「字符串相等吗」者会「串甲」而会「串乙」而《《C调用》》名『豫言_字符串相等』传「串甲」与「串乙」也。


「：
现代汉语：字符串相等吗的别名，比较完整字符串内容。
文言：字符串相等吗之异名也。
诗曰：旧尺新名验两笺，从头到尾不容偏。同文尽合方称是，莫认微差作并肩。
：」
「串相等」者「字符串相等吗」也。
「：
现代汉语：字符串相等吗的别名，返回两个字符串是否相等。
文言：校二串之同，与字符串相等吗同术。
诗曰：双笺逐字照晴窗，首尾无差始作双。换得题名衡未改，旧规依然守此章。
：」
「字符串相等」者「字符串相等吗」也。

「：
现代汉语：判断两个字符串列表长度及每个对应字符串是否相等，顺序参与比较。
文言：校串列之长与同位之文，序亦在校。
诗曰：两队珠光照夜深，逐行逐位验文真。长短先后皆须合，莫把相逢误作亲。
：」
「串组相等」乃化（「列」于「字符串」）而化（「列」于「字符串」）而「爻」也。
「串组相等」者会「甲」而会「乙」而
    鉴「甲」与「乙」而
        有「罄」与「罄」则「阳」
        或有（「缀」于「甲头」于「甲尾」）与（「缀」于「乙头」于「乙尾」）则（若「串相等」于「甲头」于「乙头」则「串组相等」于「甲尾」于「乙尾」否则「阴」）
        或有「其他」则「阴」也。


「：
现代汉语：判断第一个字符串是否出现在第二个字符串中，匹配连续内容。
文言：以前串求于后串，文须相连。
诗曰：短句长篇细细寻，连文相接始知音。散珠纵在千行内，未作同篇一寸心。
：」
「是子字符串吗」乃化「字符串」而化「字符串」而「爻」也。
「是子字符串吗」者会「串甲」而会「串乙」而《《C调用》》名『豫言_是子字符串』传「串甲」与「串乙」也。

「：文言：验首尾，唯比所需之字节，勿尽拆长串。汉语：前后缀判断不构造字符链表；另验 UTF-8 后续字节边界，保持原拆字比较语义。：」
「字节是后续」乃化「整数」而「爻」也。
「字节是后续」者会「字」而「且」于（「非」于（「小于」于「字」于「一二八」））于（「小于」于「字」于「一九二」）也。
「： 注：串结尾是它吗 1 2 测试1是否为2的结尾。 ：」
「：
现代汉语：判断第二个字符串是否以第一个字符串结尾，空后缀返回阳。
文言：验后串末是否前串；前空则阳。
诗曰：末笔回看旧落款，相连字里有余香。空题也许依长卷，同到篇终认此章。
：」
「串结尾是它吗」乃化「字符串」而化「字符串」而「爻」也。
「串结尾是它吗」者会「串一」而会「串二」而
 虑「前长」者（「整数」也《《C调用》》名『豫言_字符串字节长度』传「串一」）而
 虑「总长」者（「整数」也《《C调用》》名『豫言_字符串字节长度』传「串二」）而
 若「大于」于「前长」于「总长」则「阴」否则
 虑「始」者「减」于「总长」于「前长」而
 若「非」于（《《C调用》》名『豫言_字符串匹配』传「串二」与「始」与「串一」）则「阴」否则
 若「或者」于（「等于」于「始」于「零」）于（「等于」于「前长」于「零」）则「阳」否则
 「非」于（「字节是后续」于（《《C调用》》名『豫言_字符转整数』传「串二」与「始」））也。

「：
现代汉语：判断第二个字符串是否以第一个字符串开头，空前缀返回阳。
文言：验后串首是否前串；前空则阳。
诗曰：开卷先寻首一行，题辞契合见文章。空文无字皆堪接，不待篇终已可量。
：」
「串开头是它吗」乃化「字符串」而化「字符串」而「爻」也。
「串开头是它吗」者会「串一」而会「串二」而
 虑「前长」者（「整数」也《《C调用》》名『豫言_字符串字节长度』传「串一」）而
 虑「总长」者（「整数」也《《C调用》》名『豫言_字符串字节长度』传「串二」）而
 若「大于」于「前长」于「总长」则「阴」否则
 虑「始」者「零」而
 若「非」于（《《C调用》》名『豫言_字符串匹配』传「串二」与「始」与「串一」）则「阴」否则
 若「或者」于（「等于」于「前长」于「零」）于（「等于」于「前长」于「总长」）则「阳」否则
 「非」于（「字节是后续」于（《《C调用》》名『豫言_字符转整数』传「串二」与「前长」））也。

「：从第二个参数结尾删除长度为第一个长度的字符串：」
「：
现代汉语：按第一个字符串的字符数删除第二个字符串的末尾，不比较内容；删除长度超过目标时抛出事故。
文言：按前串之长裁后串之末，不校其文；过长则告事故。
诗曰：量得前笺几字长，便裁后卷末文章。不须落款同颜色，越界先闻警语扬。
：」
「去除串结尾」乃化「字符串」而化「字符串」而「字符串」也。
「去除串结尾」者会「串一」而会「串二」而
    「通字符转串」于
        （（（「多态列」之「结尾放」）授以「字符串」）
        于（（（「多态列」之「长度」）授以「字符串」）于（「串转通字符」于（「串一」）））
        于（「串转通字符」于（「串二」）））也。

「：从第二个参数开头删除长度为第一个长度的字符串：」
「：
现代汉语：按第一个字符串的字符数删除第二个字符串的开头，不检查前缀内容；长度超过目标时抛出事故。
文言：按前串长裁后串首，不校前缀；过长则告事故。
诗曰：借来短尺量篇头，裁去前行后字留。只问长宽非字样，逾涯莫向卷中求。
：」
「去除串开头」乃化「字符串」而化「字符串」而「字符串」也。
「去除串开头」者会「串一」而会「串二」而
    「通字符转串」于
        （（（「多态列」之「放」）授以「字符串」）
        于（（（「多态列」之「长度」）授以「字符串」）于（「串转通字符」于（「串一」）））
        于（「串转通字符」于（「串二」）））也。

「：
现代汉语：生成带豫言字符串引号的表示，并转义换行、制表和会影响字符串语法的符号。
文言：裹文以串引，遇换行制表及乱语法者则转书之。
诗曰：文字装成引号衣，换行藏作墨中机。重书不使原形乱，读者循章识所归。
：」
「表示串」乃化「字符串」而「字符串」也。
「表示串」者会「串」而「串组合并」于（「列」【『『』，（「：
现代汉语：表示串的局部递归函数；逐个检查通字符，转义换行、制表、结束引号和注释起始组合，其他字符原样保留。
文言：局部转书之术，逐字符验，换行制表及乱串界之符皆转书，余存。
诗曰：逐字巡来护语疆，遇逢界记换衣裳。寻常文字依然在，重写无伤旧意藏。
：」
递归虑「处理」其化（「列」于「字符串」）而（「列」于「字符串」）者
        会「列」而
            鉴「列」而
            有「缀」于『「：换行：」』于「尾」则「缀」于『「：「：：」换行：」』于（「处理」于「尾」）
            或有「缀」于『「：制表：」』于「尾」则「缀」于『「：「：：」制表：」』于（「处理」于「尾」）
            或有「缀」于『「：』：」』于「尾」则「缀」于『「：「：：」「：』：」：」』于（「处理」于「尾」）
            或有「缀」于『「』于（「缀」于『：』于「尾」）则「缀」于『「：「：：」「：「：：」：」』于（「处理」于「尾」）
            或有「缀」于「其他」于「尾」则「缀」于「其他」于（「处理」于「尾」）
            或有「罄」则「罄」
    而
        虑「结果」者「串组合并」于（「处理」于（「获取字符」于「串」））而
        「结果」
    ），『「：』：」』】）也。

「：
现代汉语：接收始、止、字符串，按通字符取左闭右开片段；应满足零不大于始、始不大于止，始不得越界，止超过长度会截至串尾。
文言：以始止裁通字符，含始不含止；始须在界内且不负，止毋先始，过末则取至末。
诗曰：始字登舟止字停，中间风物入新屏。终标不载随行去，越远惟收岸上青。
：」
「获取子字符串」乃化「整数」而化「整数」而化「字符串」而「字符串」也。
「获取子字符串」者会「始」而会「止」而会「串」而
    「串组合并」于（（「取」授以「字符串」）于（「减」于「止」于「始」）于（（「放」授以「字符串」）于「始」于（「获取字符」于「串」）））也。

「：
现代汉语：返回通字符数量，不是字节数。
文言：数通字符之多寡，非数字节。
诗曰：一字临风各占行，不因笔画计多长。通文自有分明界，逐个收来入数囊。
：」
「串长度」乃化「字符串」而「整数」也。
「串长度」者会「串」而（（「长度」授以「字符串」）于（「获取字符」于「串」））也。

「：第一个爻为从左或从右：」
「：
现代汉语：接收方向爻、搜索串和目标串；阳从左、阴从右，返回首个命中的可选字符索引。只检查零至目标长度减一的起点，空目标返回可无。
文言：阳自左而阴自右，求所授文于目标，得首中之零起序；不验末外之位，空目标则无。
诗曰：左右寻章各有程，初逢便记此中名。从零起步循文界，寻尽无踪报一声。
：」
「搜索单个」乃化「爻」而化「字符串」而化「字符串」而（「或可有」于「整数」）也。
「搜索单个」者会「从左」而会「搜索」而会「串」而
虑「字符们」者「获取字符」于「串」而
    （「以当前续延调用」于（
        会「返回」而
            （（「列」于「有」也（「态射」授以「整数」授以「有」）于（（化（「列」于「整数」）而（「列」于「整数」）也若「从左」则（会「甲」而「甲」）否则（会「整数列」而「反转」授以「整数」于「整数列」））
                    于（「整数范围」于「零」于（（「长度」授以「字符串」）于「字符们」））
                ）于（会「当前数值」而
                    若（「以起否」授以「字符串」）于「串相等」于（「获取字符」于「搜索」）于（（「放」授以「字符串」）于「当前数值」于「字符们」）
                    则（「返回」于（「可有」于「当前数值」））
                    否则「元」
                ））；
                「可无」
            ）
    ））也。

「：
现代汉语：从左查找搜索串在目标串中的第一次出现，返回可选字符索引，规则同搜索单个。
文言：自左求文，首中则记序，其法同搜索单个。
诗曰：开卷循行向后寻，初逢旧句便知音。留将路号传来者，不再追踪到远林。
：」
「串从左搜索单个」乃化「字符串」而化「字符串」而（「或可有」于「整数」）也。
「串从左搜索单个」者会「搜索」而会「串」而「搜索单个」于「阳」于「搜索」于「串」也。
「：
现代汉语：从右查找搜索串在目标串中的最后一次出现，返回可选字符索引，规则同搜索单个。
文言：自右求文，首中则记序，其法同搜索单个。
诗曰：回首长篇倒步寻，末逢旧句报知音。原来位置依然记，不把归途另作吟。
：」
「串从右搜索单个」乃化「字符串」而化「字符串」而（「或可有」于「整数」）也。
「串从右搜索单个」者会「搜索」而会「串」而「搜索单个」于「阴」于「搜索」于「串」也。

「：
现代汉语：返回字符串显式存储的字节数，不按通字符计数。
文言：计串所存字节之数，非通字符数。
诗曰：不数花名数细纹，一丝一缕各分明。长文自有藏身量，字外还须按粒衡。
：」
「字节长度」乃化「字符串」而「整数」也。
「字节长度」者会「串」而《《C调用》》名『豫言_字符串字节长度』传「串」也。

「：字符转整数于串于序数

将返回串的第序数个byte的整数值，使用C字符串的表示方式。
序数必须小于等于字节长度，如果序数等于字节长度，将返回零：」
「：
现代汉语：按从零开始的字节索引取零至二百五十五的值；索引等于字节长度时返回零。索引须非负，过长会抛出事故。
文言：自零按字节取数，至末界则零；序毋负，过长则告事故。
诗曰：拆字寻微按位量，末端零记守边疆。若将一步行过界，便有警声止远航。
：」
「字符转整数」乃化「字符串」而化「整数」而「整数」也。
「字符转整数」者会「串」而会「序数」而
    若（「大于」于「序数」于（「字节长度」于「串」））则「发生事故」于『序数超出范围』
    否则（《《C调用》》名『豫言_字符转整数』传「串」与「序数」）也。

「：
现代汉语：将一至二百五十五之间的整数构造成单字节字符串，零或越界值会报错中止；不保证构成完整通字符。
文言：以一字节成串，值限一至二百五十五，零及越界则止，未必成通字符。
诗曰：一粒微光入字囊，单凭一点未成章。须知字节非全字，留待同群共发扬。
：」
「字节转字符串」乃化「整数」而「字符串」也。
「字节转字符串」者会「字节」而《《C调用》》名『豫言_字节转字符串』传「字节」也。

「：
现代汉语：调用原生拼音转换，将文本转换为不带声调的拼音形式。
文言：以原生之术转文为无调拼音。
诗曰：汉字移声入短笺，高低声调暂搁边。只留音骨供寻读，一线相通到耳前。
：」
「字符串转无调拼音」乃化「字符串」而「字符串」也。
「字符串转无调拼音」者会「文本」而
    《《C调用》》名『豫言_字符串转无调拼音』传「文本」也。

「：
现代汉语：调用原生转换，取得文本拼音的首字母形式。
文言：转文为拼音而取其首母，循原生之法。
诗曰：音长取首记行踪，缩作微符也可通。不载全声千万转，留将眉目待相逢。
：」
「字符串转拼音首字母」乃化「字符串」而「字符串」也。
「字符串转拼音首字母」者会「文本」而
    《《C调用》》名『豫言_字符串转拼音首字母』传「文本」也。

「：
现代汉语：判断查询字符能否按原顺序在目标中找到，允许间隔但不忽略大小写；空查询为阳。
文言：循目标求查询诸字，许间而不许倒序，校字仍从相等之法；空问则阳。
诗曰：隔柳寻花一路行，前芳过后后芳迎。虽容空隙穿针去，不许回头乱次生。
：」
「串是模糊子序列吗」乃化「字符串」而化「字符串」而「爻」也。
「串是模糊子序列吗」者会「查询」而会「目标」而
    「：
现代汉语：模糊子序列匹配的局部递归函数；相等时推进查询与目标，不等时只推进目标；查询用尽为阳，目标先用尽为阴。
文言：局部比序之术，同则二者俱进，异但进目标；问尽则阳，目标先尽则阴。
诗曰：相逢双步不逢单，查询先尽报平安。目标若尽芳踪在，便道寻来未过关。
：」
递归虑「继续比较」其化（「列」于「字符串」）而化（「列」于「字符串」）而「爻」者
        会「查询字符」而会「目标字符」而
        鉴「查询字符」而
            有「罄」则「阳」
            或有（「缀」于「查询首字」于「查询其余」）则
                鉴「目标字符」而
                    有「罄」则「阴」
                    或有（「缀」于「目标首字」于「目标其余」）则
                        若「字符串相等吗」于「查询首字」于「目标首字」
                        则「继续比较」于「查询其余」于「目标其余」
                        否则「继续比较」于「查询字符」于「目标其余」
    而「继续比较」于（「获取字符」于「查询」）于（「获取字符」于「目标」）
也。

「：
现代汉语：判断文本是否含首字节大于一百二十七的通字符，空串为阴。
文言：验通字符首字节，过一百二十七则阳，空则阴。
诗曰：细检文中第一痕，高于旧界便开门。一篇若尽平常字，还报无他守本根。
：」
「串含非阿斯基字符吗」乃化「字符串」而「爻」也。
「串含非阿斯基字符吗」者会「文本」而
    （「存在吗」授以「字符串」）于
        （会「字符」而「大于」于（「字符转整数」于「字符」于「零」）于「一二七」）于
        （「获取字符」于「文本」）
也。


「：
现代汉语：依次接收原串、寻找串和替换串，按通字符替换所有不重叠匹配；寻找串必须非空。
文言：循原文以新文易所寻段，不重叠；所寻毋空。
诗曰：旧句逢时换新篇，循行不复踏前边。空文莫作追寻物，免使回环岁月迁。
：」
「串查找替换」乃化「字符串」「：目标字符串：」
          而化「字符串」「：寻找字符串：」
          而化「字符串」「：替换字符串：」
          而「字符串」也。
「串查找替换」者会「原先」而会「寻找」而会「替换」而
    「串组合并」于（（「列查找替换」授以「字符串」）
                于「串相等」
                于（「获取字符」于「原先」）
                于（「获取字符」于「寻找」）
                于（「获取字符」于「替换」））也。



「：
现代汉语：接收元素表示函数和列表，以方头括号包围并用中文逗号分隔各项。
文言：以所授术书诸物，逗而间之，方括其外。
诗曰：诸物随形各写章，逗分次第括成行。一函收尽千般色，开卷还知各所藏。
：」
「多态列表示」乃承「甲」而化（化「甲」而「字符串」）而化（「列」于「甲」）而「字符串」也。
「多态列表示」者受「甲」而会「元素表示」而会「列」而
    「串组合并」于（（「列」【『【』，（「间合」于『，』于（（「态射」）于「列」于「元素表示」）），『】』】））也。

「：
现代汉语：把字符串列表表示为括号内以中文逗号分隔的文本；元素直接使用原文，不添加字符串引号。
文言：括串列而以逗间之，物用原文，不加串引。
诗曰：原文列坐一堂中，逗号分席各相通。不向单篇添外衣，只将总卷束来风。
：」
「串列表示」乃化（「列」于「字符串」）而「字符串」也。
「串列表示」者会「列」而
    （「多态列表示」授以「字符串」）于（会「甲」而「甲」）于「列」也。

「：
现代汉语：可无表示为“可无”；可有则用元素表示函数生成“可有（内容）”。
文言：无则书可无，有则书可有而括物文。
诗曰：空匣题空有玉题，先明有无后珠玑。若藏珍物开微括，内外分明不自迷。
：」
「可选值表示」乃承「甲」而化（化「甲」而「字符串」）而化（「或可有」于「甲」）而「字符串」也。
「可选值表示」者受「甲」而会「元素表示」而会「可选」而
    鉴「可选」而
        有「可无」则『可无』
        或有（「可有」于「元素」）则
            「串组合并」于（（「列」【『可有（』，（「元素表示」于「元素」），『）』】））
    也。
