寻观「标准库」之「语言核心」之「内建类型」之书。

「：
现代汉语：按名称查询环境变量，返回是否存在及其值；未设置与已设置为空可由返回爻区分。
文言：按名察环境之值，以爻与文还；未设与设空以爻辨之。
七言绝句：
循名探得旧时音，
有字无文各有因。
空值仍能明已设，
不将未置混为邻。
：」
「获取环境变量」乃化「字符串」而（「爻」合「字符串」）也。
「获取环境变量」者会「名称」而
    《《C调用》》名『豫言_获取环境变量』传「名称」也。
