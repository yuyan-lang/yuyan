

寻观「标准库」之「语言核心」之「内建类型」之书。
寻观「标准库」之「语言核心」之「异常」之书。

寻观「标准库」之「数据结构」之「字符串术」之书。

寻观「标准库」之「数据结构」之「多态列」之书。

寻观「标准库」之「输入输出」之「笔」之书。
寻观「标准库」之「数学运算」之「数组转换」之书。

「：
现代汉语：按程序名及参数列表同步运行，返回是否零退出、标准输出、标准错误的三对子；原生启动或等待失败会中止。
文言：按名与参列同步行，返零退之爻及常出、误出；启候败则止。
七言绝句：
遣使携书赴远程，
归来三札各分明。
功成与否分头记，
两路声文不混声。
：」
「获取输出」乃化「字符串」而化（「列」于「字符串」）而（
    「爻」「：退出状态：」合
    「字符串」「：标准输出：」合
    「字符串」「：错误输出：」
）也。
「获取输出」者会「程序名」而会「参数列」而
    虑「后参数列」者（（「多态列转数组」授以「字符串」）于「参数列」）而
    《《C调用》》名『豫言_同步运行子进程并获取输出』传「程序名」与「后参数列」也。

「：
现代汉语：按程序名和参数列表同步运行，忽略标准输出与标准错误；退出码为零时返回阳，启动或等待失败返回阴。
文言：按名参同步行而略其出，零退则阳，启候败亦阴。
七言绝句：
遣去行人待复还，
不收言语只留痕。
成归一爻明如日，
失路也将阴作言。
：」
「运行」乃化「字符串」而化（「列」于「字符串」）而「爻」也。
「运行」者会「程序名」而会「参数列」而
    虑「后参数列」者（（「多态列转数组」授以「字符串」）于「参数列」）而
    《《C调用》》名『豫言_同步运行子进程』传「程序名」与「后参数列」也。

「：
现代汉语：同步运行程序，将标准输出和标准错误传给父进程，返回退出状态；原生启动或等待失败返回错误码。
文言：同步行程序，继其二出，返退码，启候败则返误码。
七言绝句：
使者行时语即传，
两途声响到灯前。
待归再记终程数，
不把长文另裹笺。
：」
「运行并传递输出」乃化「字符串」而化（「列」于「字符串」）而「整数」也。
「运行并传递输出」者会「程序名」而会「参数列」而
    虑「后参数列」者（（「多态列转数组」授以「字符串」）于「参数列」）而
    《《C调用》》名『豫言_同步运行子进程并传递输出』传「程序名」与「后参数列」也。

「：
现代汉语：同步运行程序并继承标准输入、输出和错误，返回退出码；启动失败为一百二十七，等待失败为一。
文言：同步行而承三常流，返退码；启败一百二十七，候败一。
七言绝句：
三路相通使远行，
来言去语各分明。
归时退出留其数，
同借前堂报一声。
：」
「运行并继承标准流」乃化「字符串」而化（「列」于「字符串」）而「整数」也。
「运行并继承标准流」者会「程序名」而会「参数列」而
    虑「后参数列」者（（「多态列转数组」授以「字符串」）于「参数列」）而
    《《C调用》》名『豫言_同步运行子进程并继承标准流』传「程序名」与「后参数列」也。

「：
现代汉语：依次接收前缀、程序名、参数文字、标准输出和错误输出，组合成带标签的诊断字符串；不执行命令。
文言：联前缀、程序名、参文及二出为诊断之文，不行其令。
七言绝句：
五札收来列一章，
来由名目各分行。
未曾遣使重行路，
只把前情写得详。
：」
「命令输出错误文字」乃化「字符串」而化「字符串」而化「字符串」而化「字符串」而化「字符串」而「字符串」也。
「命令输出错误文字」者会「前缀」而会「程序名」而会「参数文字」而会「标准输出」而会「错误输出」而
    「串组合并」于
        （「缀」于「前缀」于
        （「缀」于「程序名」于
        （「缀」于『 』于
        （「缀」于「参数文字」于
        （「缀」于『「：换行：」标准输出：』于
        （「缀」于「标准输出」于
        （「缀」于『「：换行：」错误输出：』于
        （（「列」【「错误输出」】）））））））））也。

「：
现代汉语：将命令及捕获的两路输出打印到标准错误；零退出返回阳，非零退出抛出含输出的事故。
文言：书令与二出于误流，零退则阳，否则附出告事故。
七言绝句：
遣使归来报始终，
双途言语付灯红。
功成一爻方归手，
败则详书告不通。
：」
「成功运行并打印结果」乃化「字符串」而化（「列」于「字符串」）而「爻」也。
「成功运行并打印结果」者会「程序名」而会「参数列」而
    虑「参数文字」者「间合」于『 』于「参数列」而
    （
    「标准错误打印行」于（「串组合并」于（（「列」【「程序名」，『 』，「参数文字」，『  』】）））；
    （
    虑「结果」者「获取输出」于「程序名」于「参数列」而
    （
        「标准错误打印行」于（「串组合并」于（（「列」【『标准输出：』，（「结果」中「一」）】）））；
        「标准错误打印行」于（「串组合并」于（（「列」【『错误输出：』，（「结果」中「二」）】）））
        ；（若「结果」中「零」则「阳」否则「发生事故」于（「命令输出错误文字」于『命令执行失败』于「程序名」于「参数文字」于（「结果」中「一」）于（「结果」中「二」）））
    ））
    ）
    也。
「：
现代汉语：捕获并等待命令结束，零退出返回阳，非零退出抛出包含程序、参数和两路输出的事故。
文言：候令而收二出，零退则阳，否则具名参与出告事故。
七言绝句：
静候行人毕此程，
成功才许一阳明。
若逢失事归途阻，
尽把来由附警声。
：」
「成功运行」乃化「字符串」而化（「列」于「字符串」）而「爻」也。
「成功运行」者会「程序名」而会「参数列」而
    虑「参数文字」者「间合」于『 』于「参数列」而
    虑「结果」者「获取输出」于「程序名」于「参数列」而
    （
        （若「结果」中「零」则「阳」否则「发生事故」于（「命令输出错误文字」于『命令执行失败：「：换行：」』于「程序名」于「参数文字」于（「结果」中「一」）于（「结果」中「二」）））
    ）
    也。
