

览标准库之书。
观标准库之书。

设整数引用为数组操作授以整数。

开整数引用则有引用类者操作而（
    虑（
        设甲为操作中创造于一于三。
        操作中写于（减于一于（操作中读取于（甲）于零））于零于甲。
        操作中写于（加于一于（操作中读取于（甲）于二））于二于甲 。
        操作中写于（加于一于（操作中读取于（甲）于二））于二于甲 。
        打印行于（表示于（操作中读取于（甲）于二））。
    ）以成元之道
）。
