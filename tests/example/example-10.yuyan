导入并打开标准库。

「：定义语法：」
参数1为〇返回〇的类型是从元类型到从元类型到元类型。
「参数1为〇返回〇」的定义是遇到了甲随后遇到了乙随后（从甲到乙）。
函数「参数1为〇返回〇」结合性无序七零零。

〇数组的类型是从元类型到元类型。
「〇数组」其实就是遇到了甲随后「数组引用类」使用于甲。
函数「〇数组」结合性无序七零零。
整数数组成员数为〇个的类型是从整数到（「整数」数组）。
「整数数组成员数为〇个」的定义是遇到了长度随后「新数组引用」使用于长度使用于长度。
函数「整数数组成员数为〇个」结合性无序七零零。
〇的第〇个成员的类型是自类到从（类数组）到从整数到类。
「〇的第〇个成员」的定义是遇到了组随后遇到了索引随后「读数组引用」使用于组使用于索引。
函数「〇的第〇个成员」结合性无序七零零。
十的定义是一零。

「：示例：」
「函数1」 的类型是 参数1为 字符串 返回 「整数」。
「函数1」 的定义是 遇到了参数1随后
    执行如下计算「
        索引 的类型是 整数。
        索引 的定义是 一。
        数组 的类型是 「整数」 数组。
        数组 的定义是  整数数组成员数为 十 个。
        变量 的定义是 「数组」的第索引个成员。
        「返回」 的定义是 二。
        返回。
    」。

打印行于『函数1的结果是』附（整数表示使用于（函数1使用于『你好』））。

索引 的类型是 整数。
索引 的定义是 一。
数组 的类型是 「整数」 数组。
数组 的定义是  整数数组成员数为 十 个。
值 的定义是 「数组」的第 索引 个成员。
「值2」 的定义是 「数组」的第 二 个成员。
