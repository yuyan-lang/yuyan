
导入并打开标准库。

对于〇的操作即会类型而（化引用类于类型而引用类于类型）也。
对于一个名为□的对象做如下操作〇即会操作函数而会对象而（虑无者操作函数于对象而对象）也。
〇的长度即会「列」而长度于（读引用于「列」）也。
〇的第〇个即会「列」而会序数而第N个于序数于（读引用于「列」）也。
让〇为□，然后〇即会参数而会函数而函数于参数也。
执行〇次以下的操作，在第□次执行〇即会次数而会操作式而打表于次数于操作式也。
交换〇中序数为〇与〇的位置即会「列引用」而会甲而会乙而
    虑本身者读引用于「列引用」而
    虑暂存者第N个于甲于本身而
    虑身2者更新第N个于甲于（会无而（第N个于乙于本身））于本身而
    虑身3者更新第N个于乙于（会无而暂存）于身2而
    虑无者写引用于「列引用」于身3而
    元也。
不做任何事即元也。
打印数列即会「列引用」而『【』附（以『，』合并（态射于（读引用于「列引用」）））附『】』也。

冒泡排序的类型是对于数列的操作。
冒泡排序的定义是对于一个名为L的对象做如下操作
    （
        （让L的长度为l，然后（
            执行l次以下的操作，在第i次执行（
                执行l-1次以下的操作，在第j次执行（
                    如果（L的第j个）大于（L的第j+1个）
                    那么（交换L中序数为j与j+1的位置）
                    否则不做任何事
                ）
            ）
            ）
        ）
    ）。

打印数列于（冒泡排序于（新引用于【2，3，1，5】））。