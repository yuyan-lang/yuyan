

寻标准库之书。
观标准库之书。

寻观标准库之数据结构之书。
寻观豫言编译器之编译步骤之语法分析之具体语法树之书。
寻观编译文件信息之书。
寻观豫言编译器之编译步骤之语法分析之函数合集之书。



编译存储类者串典于编译单元也。

存储引用者引用类于（编译存储类）也。

存储者新引用于（数据结构之字典术之空串典）也。

「： 读存储乃化有而编译存储类也。
读存储者遇有者甲而存储引用之读取于存储也。

写存储乃化编译存储类而有也。
写存储者遇编译存储类者甲而存储引用之写于甲于存储也。 ：」

「：如有重复，则无事发生：」
添加编译单元乃化字符串而有也。
添加编译单元者遇字符串者路径而（
    若（字典术之存在键于（读引用于存储）于路径）
    则元
    否则写引用于存储于（字典术之串典插入于（读引用于存储）于路径于尚未编译）
）也。

「：强制清空已有编译单元：」
清空并添加编译单元者遇字符串者路径而（
    若（字典术之存在键于（读引用于存储）于路径）
    则写引用于存储于（字典术之串典操作于（读引用于存储）于路径于（会甲而尚未编译））
    否则写引用于存储于（字典术之串典插入于（读引用于存储）于路径于尚未编译）
）也。

读取编译单元乃化字符串而编译单元也。
读取编译单元者会路径而
    鉴（字典术之串典查找于（读引用于存储）于路径）而
        有「可无」则（编译单元也发生事故于『更新于不存在的路径，必须先添加路径：』附路径）
        或有物则（ 「获取可有内容」授以（编译单元）于（或可有编译单元也物））
也。

「：更新：如不存在则添加：」
更新编译单元乃化字符串而化编译单元而有也。
更新编译单元者会路径而会单元而
    写引用于存储于（字典术之串典更新于（读引用于存储）于路径于单元）也。

「： 
读取编译信息乃化字符串而化字符串而信息也。
读取编译信息者会路径而会信息名而
    串典读取于（读取编译单元于路径）于信息名也。 ：」


存在编译单元乃化字符串而爻也。
存在编译单元者会路径而
    存在键授以编译单元于（读引用于存储）于路径也。

「：给定路径和编译信息，更新存储：」
「： 更新编译信息乃化字符串而化信息而有也。
更新编译信息者会路径而会属性而
    虑（
        当前单元者读取编译单元于路径也。
        更新后单元者串典更新于当前单元于（信息名于属性）于属性也。
        更新编译单元于路径于「更新后单元」。
    ）
也。 ：」

「： 写引用于（读取编译信息引用）于读取编译信息。 ：」
