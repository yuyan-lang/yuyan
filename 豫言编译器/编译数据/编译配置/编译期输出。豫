寻观「全局配置」之书。
寻观「标准库」之书。

寻观「标准库」之「操作系统」之「时间」之书。

「时间格式」者『[%Y-%m-%d %H:%M:%S] 』也。
「详细调试打印行」乃化（化「有」而「字符串」）而「有」也。
「详细调试打印行」者会「串函」而
  若（（「读引用」授以「爻」）于「打印详细调试信息」）
  则
    （「标准错误打印行」于（「当前日期时间格式表示」于「时间格式」）附（「串函」于「元」））
  否则
    （「元」）也。

「非常详细调试打印行」乃化（化「有」而「字符串」）而「有」也。
「非常详细调试打印行」者会「串函」而
  若（（「读引用」授以「爻」）于「打印非常详细调试信息」）
  则
    （「标准错误打印行」于（「当前日期时间格式表示」于「时间格式」）附（「串函」于「元」））
  否则
    （「元」）也。

「编译调试打印行」乃化「字符串」而化（化「有」而「字符串」）而「有」也。
「编译调试打印行」者会「打印标识」而会「串函」而
  若（（「存在吗」授以「字符串」）于（会「实际标识」而「串相等」于「打印标识」于「实际标识」）
    于（（「读引用」授以（「列」于「字符串」））于「编译调试打印标识符」））
  则
    （「标准错误打印行」于（「当前日期时间格式表示」于「时间格式」）附（「串函」于「元」））
  否则
    （「元」）
也。

「编译调试条件检查」乃化「字符串」而化（化「有」而「有」）而「有」也。
「编译调试条件检查」者会「打印标识」而会「函」而
  若（（「存在吗」授以「字符串」）于（会「实际标识」而「串相等」于「打印标识」于「实际标识」）
    于（（「读引用」授以（「列」于「字符串」））于「编译调试条件检查标识符」））
  则
    （「函」于「元」）
  否则
    （「元」）
也。

「粗略调试打印行」乃化（化「有」而「字符串」）而「有」也。
「粗略调试打印行」者会「串函」而
  若（（「读引用」授以「爻」）于「打印粗略调试信息」）
  则
    （「标准错误打印行」于（「当前日期时间格式表示」于「时间格式」）附（「串函」于「元」））
  否则
    （「元」）也。

「非常粗略调试打印行」乃化（化「有」而「字符串」）而「有」也。
「非常粗略调试打印行」者会「串函」而
  若（（「读引用」授以「爻」）于「打印非常粗略调试信息」）
  则
    （「标准错误打印行」于（「当前日期时间格式表示」于「时间格式」）附（「串函」于「元」））
  否则
    （「元」）也。
