
寻观「标准库」之书。

寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。




寻观「拓展库」之「顺序标识符」之「顺序标识符」之书。

寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。
寻观「豫言编译器」之「编译数据」之「操作符」之「操作符操作」之书。


「过期的不动点操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『循』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『以』）于（「缀」于（「元素组件」）于「罄」））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「绑定式」于（「缀」于「主体」于「罄」）则
            （
                「可有」于（「主体」）
            ）
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『循□以〇』
    也。


「拉姆达操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『会』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「绑定式」于（「缀」于「主体」于「罄」）则
            （
                「可有」于（「式节点」于（「拉姆达抽象」于「显式」）于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定式」）于「主体」）于「罄」））
            ）
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『会□而〇』
    也。

「有类型的拉姆达操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『遇』）于（「缀」于（「元素组件」）于（「缀」于（「字符串组件」于『者』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「定义域」于（「缀」于「绑定式」于（「缀」于「主体」于「罄」））则
            （
                「可有」于（「式节点」于（「拉姆达抽象带类型」于「显式」）于（「缀」于「定义域」于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定式」）于「主体」）于「罄」）））
            ）
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『遇〇者□而〇』
    也。

「隐拉姆达操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『受』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「绑定式」于（「缀」于「主体」于「罄」）则
            （
                「可有」于（「式节点」于（「拉姆达抽象」于「隐式」）于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定式」）于「主体」）于「罄」））
            ）
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『受□而〇』
    也。

「普通函数类型操作符」乃「操作符」也。
「普通函数类型操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『化』）于（「缀」于（「元素组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「定义域」于（「缀」于「值域」于「罄」）则
        （「可有」于（「式节点」于（「派抽象」于「显式」）于（「缀」于「定义域」于（「缀」于（「进行无名抽象」于「值域」）于「罄」））））
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『化〇而〇』
    也。


「派函数类型操作符」乃「操作符」也。
「派函数类型操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『化』）于（「缀」于（「元素组件」）于（「缀」于（「字符串组件」于『者』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「定义域」于（「缀」于「绑定名」于（「缀」于「值域」于「罄」））则
        （「可有」于（「式节点」于（「派抽象」于「显式」）于（「缀」于「定义域」于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定名」）于「值域」）于「罄」））））
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『化〇者□而〇』
    也。

「隐派函数类型操作符」乃「操作符」也。
「隐派函数类型操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『承』）于（「缀」于（「元素组件」）于（「缀」于（「字符串组件」于『者』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「定义域」于（「缀」于「绑定名」于（「缀」于「值域」于「罄」））则
        （「可有」于（「式节点」于（「派抽象」于「隐式」）于（「缀」于「定义域」于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定名」）于「值域」）于「罄」））））
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『承〇者□而〇』
    也。

「隐派函数无类型类型操作符」乃「操作符」也。
「隐派函数无类型类型操作符」者
    「构造操作符」于（「缀」于（「字符串组件」于『承』）于（「缀」于（「绑定组件」）于（「缀」于（「字符串组件」于『而』）于（「缀」于（「元素组件」）于「罄」））））
    于（「行」于「下个序数」）
    于（会「当前文件名」而会「式列」而鉴「式列」而
        有「缀」于「绑定名」于（「缀」于「值域」于「罄」）则
        （「可有」于（「式节点」于（「纯名派抽象」于「隐式」）于（「缀」于（「进行抽象」于（「获取绑定字符串」于「绑定名」）于「值域」）于「罄」）））
        或有「其他」则「发生事故」于『内65：操作符参数个数不匹配』
    ）于『承□而〇』
    也。
