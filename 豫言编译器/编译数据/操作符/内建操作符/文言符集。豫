寻「标准库」之书。
观「标准库」之书。








寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。

寻观「内建操作符对象定义」之书。
寻观「内建操作符类型定义」之书。


「文言符集」乃「列」于「操作符」也。
「文言符集」者
    「附加」于（
        「多」于「普通函数类型操作符」于（「多」于「函数调用操作符」于（「多」于「隐函数调用操作符」于（「多」于「模式匹配分支符」于（「多」于「模式匹配分支汇总符」于「空」））））
    ）于（「附加」于（
        「多」于「模式匹配符」于（「多」于「结构投影符」于（「多」于「爻分支符」于（「多」于「外部调用符」于（「多」于「派函数类型操作符」于「空」））））
    ）于（「附加」于（
        「多」于「隐派函数类型操作符」于（「多」于「隐派函数无类型类型操作符」于（「多」于「过期的不动点操作符」于（「多」于「类型判断操作符」于（「多」于「递归内联虑操作符」于「空」））））
    ）于（「附加」于（
        「多」于「拉姆达操作符」于（「多」于「有类型的拉姆达操作符」于（「多」于「隐拉姆达操作符」于（「多」于「内联虑操作符」于（「多」于「结构虑操作符」于「空」））））
    ）于（「附加」于（
        「多」于「对子构造操作符」于（「多」于「对子类型操作符」于（「多」于「对子解构操作符」于（「多」于「顺序执行操作符」于（「多」于「静态单例操作符」于「空」））））
    ）于（
        「多」于「字符串拼接操作符」于「空」
    ）））））
     也。

「文言优先级」乃「列」于（「操作符」合「操作符」）也。
「文言优先级」者
    「附加」于（
        「多」于（「对子解构操作符」与「结构投影符」）于（「多」于（「函数调用操作符」与「对子解构操作符」）于（「多」于（「字符串拼接操作符」与「结构投影符」）于（「多」于（「函数调用操作符」与「字符串拼接操作符」）于「空」）））
    ）于（「附加」于（
        「多」于（「对子构造操作符」与「函数调用操作符」）于（「多」于（「函数调用操作符」与「隐函数调用操作符」）于（「多」于（「隐函数调用操作符」与「函数调用操作符」）于「空」））
    ）于（「附加」于（
        「多」于（「爻分支符」与「对子构造操作符」）于（「多」于（「模式匹配分支符」与「爻分支符」）于（「多」于（「模式匹配分支汇总符」与「模式匹配分支符」）于（「多」于（「模式匹配符」与「模式匹配分支汇总符」）于「空」）））
    ）于（「附加」于（
        「多」于（「顺序执行操作符」与「模式匹配符」）于（「多」于（「内联虑操作符」与「顺序执行操作符」）于（「多」于（「顺序执行操作符」与「内联虑操作符」）于（「多」于（「结构虑操作符」与「内联虑操作符」）于「空」）））
    ）于（「附加」于（
        「多」于（「递归内联虑操作符」与「内联虑操作符」）于（「多」于（「内联虑操作符」与「递归内联虑操作符」）于「空」）
    ）于（「附加」于（
        「多」于（「拉姆达操作符」与「结构虑操作符」）于（「多」于（「拉姆达操作符」与「外部调用符」）于（「多」于（「隐拉姆达操作符」与「有类型的拉姆达操作符」）于（「多」于（「有类型的拉姆达操作符」与「拉姆达操作符」）于（「多」于（「拉姆达操作符」与「隐拉姆达操作符」）于「空」））））
    ）于（「附加」于（
        「多」于（「过期的不动点操作符」与「拉姆达操作符」）于（「多」于（「拉姆达操作符」与「过期的不动点操作符」）于（「多」于（「对子类型操作符」与「拉姆达操作符」）于（「多」于（「类型判断操作符」与「派函数类型操作符」）于（「多」于（「类型判断操作符」与「拉姆达操作符」）于「空」））））
    ）于（「附加」于（
        「多」于（「静态单例操作符」与「类型判断操作符」）于「空」
    ）于（
        「多」于（「普通函数类型操作符」与「对子类型操作符」）于（「多」于（「普通函数类型操作符」与「隐派函数无类型类型操作符」）于（「多」于（「派函数类型操作符」与「普通函数类型操作符」）于（「多」于（「隐派函数类型操作符」与「派函数类型操作符」）于（「多」于（「隐派函数无类型类型操作符」与「隐派函数类型操作符」）于「空」））））
    ））））））））也。
