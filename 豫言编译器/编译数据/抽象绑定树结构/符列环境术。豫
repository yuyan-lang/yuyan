寻观「标准库」之书。

寻观「命名环境术」之书。
寻观「抽象语法树」之「总集」之书。

寻观「豫言编译器」之「编译数据」之「操作符」之「操作符集术」之书。
寻观「豫言编译器」之「编译数据」之「操作符」之「操作符操作」之书。
寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。


「符列环境表示」乃化「符列环境」而「字符串」也。
「符列环境表示」者会「境」而
    （「操作符集表示」于（「境」中（零）））附『「：换行：」命名：』
    附（「命名环境表示」于（「境」中（一）中（零）））附『「：换行：」当前路径： 』附（「境」中（一）中（一））
    也。

「符列环境读取路径」乃化「符列环境」而「字符串」也。
「符列环境读取路径」者会「境」而「境」中（一）中（一）也。

「符列环境读取命名环境」乃化「符列环境」而「命名环境」也。
「符列环境读取命名环境」者会「境」而「境」中（一）中（零）也。

「符列环境读取所有名称」乃化「符列环境」而「字符串」列也。
「符列环境读取所有名称」者会「境」而（「命名环境获取所有名称」于（「境」中（一）中（零）））也。

「符列环境添加条目」乃化「字符串」而化「表达式」而化「符列环境」而「符列环境」也。
「符列环境添加条目」者会「目名」而会「目类」而会「境」而（「境」中（零）与（（（「本地条目」于「目名」于（「目类」））衔（「境」中（一）中（零）））与「境」中（一）中（一））与（「目名」衔（「境」中（二））））也。

「符列环境更新操作符集」乃化「符列环境」而化（「操作符集」）而「符列环境」也。
「符列环境更新操作符集」者会「境」而会「新集」而（「新集」）与（「境」中（一））与（「境」中（二））也。

「符列环境重置当前作用域」乃化「符列环境」而「符列环境」也。
「符列环境重置当前作用域」者会「境」而（「境」中（零））与（「境」中（一））与【】也。

「符列环境字符串属于当前作用域」乃化「符列环境」而化「字符串」而「爻」也。
「符列环境字符串属于当前作用域」者会「境」而会「名」而「存在吗」于（「串相等」于「名」）于（「境」中（二））也。

「符列环境读取操作符集」乃化「符列环境」而（「操作符集」）也。
「符列环境读取操作符集」者会「境」而（「境」中（零））也。

「符列环境寻找操作符」乃化「符列环境」而化「字符串」而「或可有」于（「整数」）也。
「符列环境寻找操作符」者会「境」而会「搜索名」而
    鉴（「寻找集操作符」于（「境」中（零））于「搜索名」）而
        有「可无」则（「可无」）
        或有（「可有」于「符」）则「可有」于（「获取标号」于「符」）也。


「符列环境读取操作符标号」乃化「符列环境」而化「字符串」而「整数」也。
「符列环境读取操作符标号」者会「境」而会「搜索名」而
    鉴（「符列环境寻找操作符」于「境」于「搜索名」）而
        有「可无」则「发生事故」于『操作符未找到：操作符：』附「搜索名」附『在以下环境中：「：换行：」』附（「符列环境表示」于「境」）
        或有（「可有」于「标号」）则「标号」也。

「名称存在于环境」乃化「符列环境」而化「字符串」而「爻」也。
「名称存在于环境」者会「境」而会「名」而
                鉴（「符列环境寻找操作符」于「境」于「名」）而
                    有（「可有」于「标号」）则「阳」
                    或有「可无」则（
                        鉴「解析单个名称」于（「符列环境读取命名环境」于「境」）于「名」而
                                    有「可无」则「阴」
                                    或有（「可有」于「类」）则「阳」
                    ）也。

「：返回可能进行过重命名的名称，操作符会阐述至之前名称：」
「：因为我们分离了类型检查和语法分析，语法分析期间不需要进行类型推导，所以不需要重命名：」
「添加声明及操作符」乃化「符列环境」而化「字符串」而化「表达式」而（「符列环境」）也。
「添加声明及操作符」者会「境」而会「名」而会「类型」而
            虑（
                「新集及符」者鉴「符列环境寻找操作符」于「境」于「名」而
                            有「可无」则
                                「添加字符串操作符」于（「符列环境读取操作符集」于「境」）于「名」于「名」
                            或有（「可有」于「类」）则（
                                虑「符标号」者「符列环境读取操作符标号」于「境」于「名」而
                                「： 虑符者获取集操作符于（「符列环境读取操作符集」于境）于符标号而 ：」
                                虑「集二」者「删除集操作符」于（「符列环境读取操作符集」于「境」）于「符标号」而
                                虑「新集及符」者「添加字符串操作符」于「集二」于「名」于「名」而
                                「新集及符」
                            ）也。
                「新集」者「新集及符」中（零）也。
                「环二」者 「符列环境更新操作符集」于「境」于（「新集」）也。
                「环三」者「符列环境添加条目」于「名」于「类型」于「环二」也。
                「环三」
            ）
            也。
