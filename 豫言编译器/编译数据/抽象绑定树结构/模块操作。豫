寻观「标准库」之书。

寻观「抽象语法树」之「总集」之书。

寻观「拓展库」之「顺序标识符」之「顺序标识符」之书。

寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。
寻观「豫言编译器」之「编译数据」之「编译配置」之「编译期输出」之书。

寻观「静态单例类型处理」之书。

「结构尾式替换」乃化「字符串」而化「表达式」而化「表达式」而「表达式」也。
「结构尾式替换」者会「调试信息」而会「分析式」而会「尾式」而
    鉴（「类型转定义」于「分析式」） 而
    有（「可有」于「式」）则（「式替换」于「式」于「尾式」）
    或有「可无」则「尝试运行」于（会「无」而（「解构无绑定抽象」于『解绑18：』附「调试信息」于「尾式」））于（会「事故」而「发生事故」于「事故」附『在进行结构尾式转换时』附（「表达式表示」于「尾式」））也。

「签名尾式替换」乃化「字符串」而化（「或可有」于「表达式」）而化「表达式」而化「表达式」而「表达式」也。
「签名尾式替换」者会「调试串」而会「默认替换式」而会「分析式」而会「尾式」而
    虑「无」者「非常详细调试打印行」于（会「无」而『正在进行签名尾式替换，分析式』附（「表达式表示」于「分析式」））而
    若（「是静态单例类型」于「分析式」）
    则（「式替换」于（「展开替换静态单例类型」于「分析式」）于「尾式」）
    否则（鉴「默认替换式」而
        有（「可有」于「替换式」）则（「式替换」于「替换式」于「尾式」）
        或有「可无」则「尝试运行」于（会「无」而（「解构无绑定抽象」于『解绑24』于「尾式」））于（会「事故」而「发生事故」于「事故」附『「：换行：」=》在进行签名尾式替换时』附「调试串」附（「表达式表示」于「尾式」）附『，分析式是：』附（「表达式表示」于「分析式」）））
    也。


「获取结构的最细致近似签名」乃化（「列」于「字符串」）而化「表达式」而「表达式」也。
「获取结构的最细致近似签名」者会「已有名」而会「式」而
    鉴「式」而
        有（「式节点」于「空近似结构节点」于「空」）则（（「式节点」于「空签名节点」于「空」））
        或有（「式节点」于（「近似结构节点」于（「可有」于「名」））于（「多」于（「式节点」于（「定义声明近似类型节点」于「透明性」）于（「多」于「近似类型」于（「多」于「定义」于「空」）））于（「多」于「值式」于「空」）））则
            （
                若（「存在吗」于（「串相等」于「名」）于「已有名」）
                则（「获取结构的最细致近似签名」于「已有名」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「值式」））
                否则
                （虑「尾式」者「抽象表达式操作」于「值式」于（「获取结构的最细致近似签名」于（「多」于「名」于「已有名」））而
                    （「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「近似类型」）于（「多」于（「尾式」）于「空」）））
                ）
            ）
        或有（「式节点」于（「近似结构节点」于（「可有」于「名」））于（「多」于（「式节点」于「构造声明近似类型节点」于（「多」于「近似类型」于（「多」于「定义」于「空」）））于（「多」于「值式」于「空」）））则
            （
                若（「存在吗」于（「串相等」于「名」）于「已有名」）
                则（「获取结构的最细致近似签名」于「已有名」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「值式」））
                否则
                （虑「尾式」者「抽象表达式操作」于「值式」于（「获取结构的最细致近似签名」于（「多」于「名」于「已有名」））而
                （「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「近似类型」）于（「多」于（「尾式」）于「空」）））
                ）
            ）
        或有（「式节点」于（「近似结构节点」于（「可有」于「名」））于（「多」于（「式节点」于（「类型声明近似类型节点」于「透性」）于（「多」于「近似类型」于（「多」于「定义」于「空」）））于（「多」于「值式」于「空」）））则
            （
                若（「存在吗」于（「串相等」于「名」）于「已有名」）
                则（「获取结构的最细致近似签名」于「已有名」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「值式」））
                否则
                （虑「尾式」者「抽象表达式操作」于「值式」于（「获取结构的最细致近似签名」于（「多」于「名」于「已有名」））而
                （「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「近似类型」）于（「多」于（「尾式」）于「空」）））
                ）
            ）
        或有（「式节点」于（「近似结构节点」于（「可有」于「名」））于（「多」于（「式节点」于（「递归定义声明近似节点」于「透性」）于（「多」于「近似类型」于（「多」于「递归名」于（「多」于「定义」于「空」））））于（「多」于「值式」于「空」）））则
            （
                若（「存在吗」于（「串相等」于「名」）于「已有名」）
                则（「获取结构的最细致近似签名」于「已有名」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「值式」））
                否则
                （虑「尾式」者「抽象表达式操作」于「值式」于（「获取结构的最细致近似签名」于（「多」于「名」于「已有名」））而
                （「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「近似类型」）于（「多」于（「尾式」）于「空」）））
                ）
            ）
        或有「式节点」于（「近似结构节点」于「可无」）于（「多」于「当前式」于（「多」于「尾式」于「空」））则
            「获取结构的最细致近似签名」于「已有名」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「尾式」）
        或有「其他」则「发生事故」于『最细致近似签名尚未实现：』附（「表达式表示」于「式」）
    也。


「： 获取结构最细致签名乃化表达式而表达式也。
获取结构最细致签名者循获取结构最细致签名以会式而
    「： 虑无者打印行于『正在获取结构最细致签名：』附（表达式表示于式）而 ：」
     鉴式而
        有（式节点于「空结构节点」于【】）则（（式节点于「空签名节点」于【】））
        或有（式节点于（「结构节点」于（可有名））于（式节点于签名实现节点于「当前签名」衔「当前实现」衔【】）衔「值式」衔【】）则
            「
                虑尾式者抽象表达式操作于值式于「获取结构最细致签名」而
                （式节点于（签名节点于可有名）于（当前签名）衔（尾式）衔【】）
            」
        或有（式节点于（「结构节点」于（可无））于（式节点于签名实现节点于「当前签名」衔「当前实现」衔【】）衔「值式」衔【】）则
            「
                虑尾式者抽象表达式操作于值式于「获取结构最细致签名」而
                （式节点于（签名节点于可无）于（当前签名）衔（尾式）衔【】）
            」
        或有（其他）则发生事故于『最细致签名尚未实现：』附（表达式表示于式）也。 ：」

「： 收集签名标签乃化表达式而（字符串）列也。
收集签名标签者循收集签名标签以会式而
    鉴式而
        有式节点于（空签名节点）于【】则【】
        或有式节点于（签名节点于（可有名））于（（类）衔「尾式」衔【】）则
                【名】接（收集签名标签于（（解构抽象于尾式）中一））
        或有（式节点于（签名节点于（可无））于「其他式」衔「尾式」衔【】）则
                （收集签名标签于（（解构抽象于尾式）中一））
        或有「其他」则（发生事故于『（收集签名标签：）结构不符合规范：』附（表达式表示于「其他」））也。 ：」

「收集正则签名标签」乃化「表达式」而「列」于「字符串」也。
「收集正则签名标签」者会「式」而
    鉴「式」而
        有「式节点」于（「空签名节点」）于「空」则「空」
        或有「式节点」于（「正则签名节点」于（「名」））于（「多」于（「类」）于（「多」于「尾式」于「空」））则
                「附加」于（「多」于「名」于「空」）于（「收集正则签名标签」于「尾式」）
        或有「其他」则（「发生事故」于『（收集正则签名标签：）结构不符合规范：』附（「表达式表示」于「其他」））也。

「近似结构末尾命名直接表达式」乃化「表达式」而化「字符串」而「表达式」也。
「近似结构末尾命名直接表达式」者会「式」而会「新名」而
    鉴「式」而
        有「式节点」于（「空近似结构节点」）于「空」则「发生事故」于『不可传入空结构』
        或有「式节点」于（「近似结构节点」于（「可有」于「名」））于（「多」于（「类」）于（「多」于（
                「绑定」于「可能绑定名」于（「式节点」于（「空近似结构节点」）于「空」）
            ）于「空」））则「发生事故」于『已有命名』
        或有「式节点」于（「近似结构节点」于「可无」）于（「多」于（「类」）于（「多」于（
                「绑定」于「可能绑定名」于（「式节点」于（「空近似结构节点」）于「空」）
            ）于「空」））则
                「式节点」于（「近似结构节点」于（「可有」于「新名」））于（「多」于（「类」）于（「多」于（
                    「绑定」于「可能绑定名」于（「式节点」于（「空近似结构节点」）于「空」）
                    ）于「空」））
        或有「式节点」于（「近似结构节点」于「可能名」）于（「多」于（「类」）于（「多」于（「绑定」于「可能绑定名」于「尾式」）于「空」））则
            「式节点」于（「近似结构节点」于「可能名」）于（「多」于（「类」）于（「多」于（
                「绑定」于「可能绑定名」于（「近似结构末尾命名直接表达式」于「尾式」于「新名」）
            ）于「空」））
        或有「其他」则（「发生事故」于『（命名末尾直接表达式：）结构不符合规范：』附（「表达式表示」于「其他」））也。


「收集签名近似标签」乃化「表达式」而「列」于（「字符串」合「表达式」）也。
「收集签名近似标签」者会「式」而
    鉴「式」而
        有「式节点」于（「空签名节点」）于「空」则「空」
        或有「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「类」）于（「多」于「尾式」于「空」））则
                「附加」于（「多」于（「名」与「类」）于「空」）于（「收集签名近似标签」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「尾式」））
        或有（「式节点」于（「签名节点」于「可无」）于（「多」于「其他式」于（「多」于「尾式」于「空」）））则
                （「收集签名近似标签」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「尾式」））
        或有「其他」则（「发生事故」于『（收集签名近似标签：）结构不符合规范：』附（「表达式表示」于「其他」））也。




「：
第一个表达式参数是签名所描述的物体，用于在进行无定义的绑定式替换时使用，如M : S, 那 M.l : S.l， 传入的参数是 M, S, l，输出 S.l
第二个参数是签名本身，第三个参数是投影标签

传入的序数是第一个可投影元素的返回序数值
：」
「进行签名投影」乃化「字符串」而化「整数」而化「表达式」而化「表达式」而化「字符串」而「或可有」于（「表达式」合「整数」）也。
「进行签名投影」者会「调试串」而会「当前序数」而会「物」而会「式」而会「搜索名」而
    「尝试运行」于（会「无」而
    虑「无」者「非常详细调试打印行」于（会「无」而『正在进行签名投影』附「调试串」附『，』附（「搜索名」）附『，签名：』附（「表达式表示」于「式」）附『物：』附（「表达式表示」于「物」））而
    鉴「式」而
        有（「式节点」于（「空签名节点」）于「空」）则（「可无」）
        或有「式节点」于（「签名节点」于（「可有」于「名」））于（「多」于（「类」）于（「多」于「尾式」于「空」））则
            若「串相等」于「名」于「搜索名」则（「可有」于（「类」与「当前序数」））
            否则（「进行签名投影」于「调试串」于（「加」于「当前序数」于「一」）于「物」于（「签名尾式替换」于『297』于（「可有」于（「式节点」于（「结构投影带序数」于「名」于「当前序数」）于（「多」于「物」于「空」）））于「类」于「尾式」）于「搜索名」）
        或有「式节点」于（「签名节点」于「可无」）于（「多」于「其他式」于（「多」于「尾式」于「空」））则
            （「进行签名投影」于「调试串」于「当前序数」于「物」于（「签名尾式替换」于『301』于「可无」于「其他式」于「尾式」）于「搜索名」）
        或有「其他」则（「发生事故」于『（进行签名投影：）』附「调试串」附『签名不符合规范：』附（「表达式表示」于「其他」））
    ）于（会「异常」而
    「发生事故」于「异常」附『「：换行：」在进行签名投影时』附（「表达式表示」于「式」）附『投影名：』附（「搜索名」））也。

「进行文件正则签名投影」乃化「表达式」而化「字符串」而「或可有」于（「表达式」合「整数」）也。
「进行文件正则签名投影」者会「式」而会「搜索名」而
    鉴「式」而
        有（「式节点」于（「空签名节点」）于「空」）则（「可无」）
        或有「式节点」于（「正则签名节点」于（「名」））于（「多」于（「类」）于（「多」于「尾式」于「空」））则
            若「串相等」于「名」于「搜索名」则（「可有」于（「类」与「零」））
            否则（鉴（「进行文件正则签名投影」于（「尾式」）于「搜索名」）
            而有「可无」则（「可无」）
            或有（「可有」于（「后式」与「序数」））则（「可有」于（「后式」与（「加」于「序数」于「一」））））
        或有「其他」则（「发生事故」于『（进行文件正则签名投影：）签名不符合规范：』附（「表达式表示」于「其他」））也。

「进行结构投影」乃化「表达式」而化「字符串」而「或可有」于「表达式」也。
「进行结构投影」者会「式」而会「搜索名」而
    鉴「式」而
        有（「式节点」于（「空结构节点」）于「空」）则（「可无」）
        或有「式节点」于（「结构节点」于（「可有」于「名」））于（「多」于（「类」）于（「多」于「尾式」于「空」））则
            若「串相等」于「名」于「搜索名」则（「可有」于「类」）
            否则（「进行结构投影」于（「尾式」）于「搜索名」）
        或有「式节点」于（「结构节点」于「可无」）于（「多」于「其他式」于（「多」于「尾式」于「空」））则
                （「进行结构投影」于（「尾式」）于「搜索名」）
        或有「其他」则（「发生事故」于『（进行结构投影：）』附「搜索名」附『：结构不符合规范：』附（「表达式表示」于「其他」））也。

「表达式是结构」乃化「表达式」而「爻」也。
「表达式是结构」者会「式」而
    鉴「式」而
        有（「式节点」于（「空结构节点」）于「空」）则「阳」
        或有「式节点」于（「结构节点」于「名」）于「参」则「阳」
        或有「其他」则「阴」也。

「表达式是近似结构」乃化「表达式」而「爻」也。
「表达式是近似结构」者会「式」而
    鉴「式」而
        有（「式节点」于（「空近似结构节点」）于「空」）则「阳」
        或有「式节点」于（「近似结构节点」于「名」）于「参」则「阳」
        或有「其他」则「阴」也。

「表达式是签名」乃化「表达式」而「爻」也。
「表达式是签名」者会「式」而
    鉴「式」而
        有（「式节点」于（「空签名节点」）于「空」）则「阳」
        或有「式节点」于（「签名节点」于「名」）于「参」则「阳」
        或有「其他」则「阴」也。

「：正则化后的签名是空签名或者可有标签的签名：」
「签名正则化」乃化「表达式」而「表达式」也。
「签名正则化」者会「式」而
    鉴「式」而
        有（「式节点」于（「空签名节点」）于「空」）则「式」
        或有「式节点」于（「签名节点」于（「可有」于「名」））于「参」则「式」
        或有「式节点」于（「签名节点」于「可无」）于（「多」于「类」于（「多」于「尾式」于「空」））则
            「签名正则化」于（「签名尾式替换」于『348』于「可无」于「类」于「尾式」）
        或有「其他」则「发生事故」于『不是签名！』也。

「构造正则签名」乃化「表达式」而化「表达式」而「表达式」也。
「构造正则签名」者会「结构」而会「物」而
    鉴「结构」而
        有（「式节点」于（「签名实现节点」）于（「多」于（「式节点」于（「签名递归节点」于「标签名」）于（「多」于「递归签名」于「空」））于（「多」于「实现」于「空」）））则
        （
            「式替换」于「物」于「递归签名」
        ）
        
        或有「其他」则（「发生事故」于『期待递归结构，得到了』附（「表达式表示」于「结构」））也。
 
