# 豫言编译器

解析、检查并编译豫言程序。

## 籍与用

所有者曰豫言，版本暂定 0.1.0。此包之版本，非编译器命令所报之版本。

此包为可执行文件，不供他包所依；公用诸书别在编译器核心库。程序名曰 `yy`，入口依包籍所载。

在仓库之根行 `./yy豫构 检查 豫言编译器` 以验其籍。行 `./yy豫构 构建 豫言编译器` 以成其器，不自行之。

所依与原生组成皆见[包籍](豫言编译器。包。豫)。变接口则修说明，变身份与组成则修包籍。说明不作模块。
