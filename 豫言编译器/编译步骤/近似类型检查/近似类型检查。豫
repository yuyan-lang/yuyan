寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「命名环境术」之书。
寻观「豫言编译器」之「编译数据」之「编译配置」之「编译期输出」之书。


寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。

。

寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「模块操作」之书。


「解构近似绑定」乃化「表达式」而「表达式」也。
「解构近似绑定」者会「式」而
    「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「式」也。

「进行类型近似类型检查」乃化「命名环境」而化「表达式」而「表达式」也。
「进行类型近似类型检查」者循「进行类型近似类型检查」以会「境」而会「类」而
    虑「无」者「详细调试打印行」于（会「无」而『[D]正在近似检查类型|』附（「表达式表示」于「类」）附『|是一个类型』）而
    虑「递归」者「进行类型近似类型检查」于「境」而
    鉴「类」而
        有（「式节点」于（「静态单例类型节点」）于「参」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「内建节点」于「内建类型元类型」）于「空」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「内建节点」于「内建类型整数」）于「空」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「内建节点」于「其他内建」）于「空」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「派抽象」于「派形」）于（「多」于「定义域」于（「多」于「值域」于「空」）））则
            （「式节点」于（「派抽象」于「派形」）于
                （「多」于（「递归」于「定义域」）于（「多」于
                    （「进行无名抽象」于（「递归」于（「解构近似绑定」于「值域」）））
                于「空」））
            ）
        或有（「式节点」于（「单类型签名节点」）于「参」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「近似类型原子节点」）于「空」）则
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「近似类型占位节点」）于「空」）则「：TODO：考虑是不是可以这样？：」
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「签名节点」于「可能名」）于（「多」于「当前签名」于（「多」于「后续签名」于「空」）））则
            （「式节点」于（「签名节点」于「可能名」）于
                （「多」于（「递归」于「当前签名」）于（「多」于
                    （「进行无名抽象」于（「递归」于（「解构近似绑定」于「后续签名」）））
                于「空」））
            ）
        或有（「式节点」于（「正则签名节点」于「名」）于（「多」于「当前签名」于（「多」于「后续签名」于「空」）））则
            （「式节点」于（「正则签名节点」于「名」）于
                （「多」于（「递归」于「当前签名」）于（「多」于
                    （「递归」于（「后续签名」））
                于「空」））
            ）
        或有（「式节点」于（「空签名节点」）于「空」）则
            （「式节点」于（「空签名节点」）于「空」）
        或有（「自由变量」于「名」）则
            （鉴（「解析单个名称」于「境」于「名」）而
                有「可无」则（
                        「发生事故」于『变量·』附「名」附『·未找到52，环境：』附（「命名环境表示」于「境」）
                    ）
                或有（「可有」于「名类」）则（
                    鉴「名类」而
                        有（「式节点」于（「近似类型原子节点」）于「空」）则
                            （「式节点」于（「近似类型原子节点」）于「空」）
                        或有（「式节点」于（「元变量」于「变数」）于「参」）则
                            （「式节点」于（「近似类型原子节点」）于「空」）
                        或有（「式节点」于（「签名节点」于「可能名」）于「参」）则
                            「名类」「：目前因为近似检查不能区分结构和签名的区别，所以会出现一个物体的类是结构的情况（结构的类型式签名）， TODO：修复这个bug，在定义声明时检查结构体是否是签名，并且改成签名单例节点：」
                        或有「其他」则
                            「发生事故」于『未有47』附（「表达式表示」于「类」）附『，类型：』附（「表达式表示」于「名类」）附『，不是一个类型！』附（「命名环境表示」于「境」）
                ）
            ）
        或有（「式节点」于（「近似结构节点」于「可能名」）于「参」）则
            （
                「获取结构的最细致近似签名」于「空」于「类」
            ）
        或有（「式节点」于（「纯名派抽象」于「派形」）于（「多」于「值域」于「空」））则
            （
                「式节点」于（「派抽象」于「派形」）于（
                    「多」于（「式节点」于（「近似类型占位节点」）于「空」）于（「多」于
                        （「进行无名抽象」于（「进行类型近似类型检查」于「境」于（「式替换」于（「式节点」于（「近似类型占位节点」）于「空」）于「值域」）））
                    于「空」）
                ）
            ）
        或有（「式节点」于（「函数调用」于「函形」）于「参」）则 「：应当是元类型：」
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「对子类型节点」）于（「多」于「类甲」于（「多」于「类乙」于「空」）））则
            （「式节点」于（「对子类型节点」）于（「多」于（「递归」于「类甲」）于（「多」于（「递归」于「类乙」）于「空」）））
        或有（「式节点」于（「对子类型结尾节点」）于「空」）则
            （「式节点」于（「对子类型结尾节点」）于「空」）
        「：TODO 警告：从下拷贝，如果可能请移除拷贝：」
        或有（「式节点」于（「结构投影」于「串」）于（「多」于「前式」于「空」））则
            （
                虑「前式类」者（「进行类型近似类型检查」于「境」于「前式」）而
                若（「表达式是签名」于「前式类」）
                    则（鉴（「进行签名投影」于『87』于「零」于「前式」于「前式类」于「串」）而
                        有（「可有」于（「投影后值」与「序数」））则（（「投影后值」））
                        或有「可无」则「发生事故」于『无法投影（135）！：』附「串」附『， 结构：』附（「表达式表示」于「前式类」））
                    否则（（「式节点」于（「近似类型占位节点」）于「空」））
            ）
        或有（「式节点」于「空近似结构节点」于「空」）则（（「式节点」于「空签名节点」于「空」））
        或有（「式节点」于「括号表达式」于（「多」于「内式」于「空」））则（「递归」于「内式」）
        或有（「式节点」于（「签名递归节点」于「标签名」）于（「多」于「递归式」于「空」））则（
            （「式节点」于（「签名递归节点」于「标签名」）于（「多」于（「进行无名抽象」于（「递归」于（「解构近似绑定」于「递归式」）））于「空」））
            ）
        或有「其他」则「发生事故」于『未有13：』附（「表达式表示」于「类」）附（『「：换行：」』）附（「命名环境表示」于「境」）也。


「进行对象近似类型合成」乃化「命名环境」而化「表达式」而「表达式」「：类型：」也。
「进行对象近似类型合成」者循「进行对象近似类型合成」以会「境」而会「物」而
    虑「无」者（「详细调试打印行」于（会「无」而『[D]正在进行对象近似类型合成：』附（「表达式表示」于「物」）））而
    鉴「物」而
        有（「自由变量」于「名」）则
            （鉴（「解析单个名称」于「境」于「名」）而
                有「可无」则
                            （「式节点」于（「近似类型原子节点」）于「空」）
                或有（「可有」于「名类」）则（「名类」）
            ）
        或有（「式节点」于（「函数调用」于「函形」）于（「多」于「函数」于（「多」于「参数」于「空」）））则
                （
                    虑「后函数类」者（「进行对象近似类型合成」于「境」于「函数」）而
                    鉴（「后函数类」）而有
                        （「式节点」于（「派抽象」于「派形」）于（「多」于「定义域」于（「多」于「值域」于「空」）））
                            则（
                                若（「参式相等」于「派形」于「函形」）
                                则（
                                    （「式替换」于「参数」于「值域」）
                                ）
                                否则（
                                    鉴（「函形」与「派形」）而
                                        有「显式」与「隐式」则「：TODO：去除重复计算：」
                                            （
                                                「进行对象近似类型合成」于「境」
                                                    于（「式节点」于（「函数调用」于「函形」）于
                                                        （「多」于（（「式节点」于（「函数调用」于「隐式」）于（「多」于「函数」于（「多」于（「式节点」于「空缺」于「空」）于「空」））））
                                                        于（「多」于「参数」于「空」）））
                                            ）
                                        或有「隐式」与「显式」则（「发生事故」于『函数类型是显式派，不可以以隐式调用』）
                                        或有「其他」则「发生事故」于『未有1301』）
                            ）
                        或有（「式节点」于「近似类型原子节点」于「空」）则
                            （「式节点」于「近似类型原子节点」于「空」）
                        或有（「式节点」于「近似类型占位节点」于「空」）则
                            （「式节点」于「近似类型占位节点」于「空」）
                        或有「其他」则「发生事故」于『未有135：』附（「表达式表示」于「后函数类」）附（『「：换行：」』）附（「命名环境表示」于「境」）
                ）
        或有（「式节点」于（「拉姆达抽象带类型」于「函形」）于（「多」于「参数类型」于（「多」于「体」于「空」）））则
            （
               虑「近似参数类型」者「进行类型近似类型检查」于「境」于「参数类型」而
               虑「名及新境」者「添加声明」于「境」于（「获取可能绑定名」于「体」）于「近似参数类型」而
               虑「体类型」者（「进行对象近似类型合成」于（「名及新境」中「一」）于（「式替换」于（「自由变量」于（「名及新境」中「零」））于「体」））而
               「式节点」于（「派抽象」于「函形」）于（「多」于「近似参数类型」于（「多」于（「进行抽象」于（「名及新境」中「零」）于「体类型」）于「空」））
            ）
        或有（「式节点」于（「拉姆达抽象」于「拉形」）于（「多」于「体」于「空」））则
            （
               虑「名及新境」者「添加声明」于「境」于（「获取可能绑定名」于「体」）于（「式节点」于（「近似类型占位节点」）于「空」）而
               虑「体类型」者（「进行对象近似类型合成」于（「名及新境」中「一」）于（「式替换」于（「自由变量」于（「名及新境」中「零」））于「体」））而
               「式节点」于（「派抽象」于「拉形」）于（「多」于（「式节点」于（「近似类型占位节点」）于「空」）于（「多」于（「进行抽象」于（「名及新境」中「零」）于「体类型」）于「空」））
            ）
        或有（「式节点」于（「结构投影」于「串」）于（「多」于「前式」于「空」））则
            （
                虑「前式类」者（「进行对象近似类型合成」于「境」于「前式」）而
                若（「表达式是签名」于「前式类」）
                    则（鉴（「进行签名投影」于『148』于「零」于「前式」于「前式类」于「串」）而
                        有（「可有」于（「投影后值」与「序数」））则（（「投影后值」））
                        或有「可无」则「发生事故」于『无法投影（135）！：』附「串」附『， 结构：』附（「表达式表示」于「前式类」））
                    否则（（「式节点」于（「近似类型占位节点」）于「空」））
            ）
        或有（「式节点」于（「对子解构节点」）于（「多」于「前式」于（「多」于「序数式」于「空」）））则
            （
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于（「爻分支节点」）于（「多」于「爻式」于（「多」于「阳分支」于（「多」于「阴分支」于「空」））））则
            （
                「进行对象近似类型合成」于「境」于「阳分支」
            ）
        或有（「式节点」于（「模式匹配」）于「参」）则
            （
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于（「整数节点」于「数」）于「空」）则
            （
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于（「小数节点」于「数」于「整小部」）于「空」）则
            （
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于（「字符串节点」于「串」）于「空」）则
            （
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于（「近似结构节点」于「可能名」）于「参」）则
            （
                「获取结构的最细致近似签名」于「空」于「物」
            ）
        或有（「式节点」于（「空近似结构节点」）于「空」）则
            （
                「获取结构的最细致近似签名」于「空」于「物」
            ）
        或有（「式节点」于（「对子类型节点」）于（「多」于「类甲」于（「多」于「类乙」于「空」）））则「：所有的类型的类型都是原子！：」
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于（「派抽象」于「派形」）于（「多」于「定义域」于（「多」于「值域」于「空」）））则「：所有的类型的类型都是原子！：」
            （「式节点」于（「近似类型原子节点」）于「空」）
        或有（「式节点」于「类型判断节点」于（「多」于「判断类」于（「多」于「判断式」于「空」）））则
            （
                「进行类型近似类型检查」于「境」于「判断类」
            ）
        或有（「式节点」于「结构虑」于（「多」于「参结构」于「空」））则
            （ 「：TODO 我们或许想知道具体类型：」
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于「内联虑」于（「多」于「当前」于（「多」于「后式」于「空」）））则
            （ 「：TODO 我们或许想知道具体类型：」
                 （「式节点」于（「近似类型原子节点」）于「空」）
            ）
        或有（「式节点」于「括号表达式」于（「多」于「内式」于「空」））则（「进行对象近似类型合成」于「境」于「内式」）
        或有「其他」则「发生事故」于『未有18：』附（「表达式表示」于「物」）也。

「默认近似命名环境」者「从左折叠」于（会「条目」而会「境」而
        鉴「条目」而
            有「本地条目」于（「名」）于（「类」）则
                「附加」于「境」于（「多」于（「本地条目」于「名」于（「进行类型近似类型检查」于「境」于「类」））于「空」）
    ）于「空」于「默认命名环境」也。
