


寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。

。
寻观「豫言编译器」之「编译数据」之「编译配置」之「编译期输出」之书。

寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「命名环境术」之书。





寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。



「类型擦除」乃化「命名环境」而化「表达式」
    而「表达式」「：标注后的类和物：」也。
「类型擦除」者会「境」而会「式」而
    虑「绑定对象类型擦除」者（化「命名环境」而
            化「表达式」「：对象（是一个绑定）：」而
            化「表达式」「：绑定类型：」
            而「表达式」「：检查过的对象（是一个绑定）：」也
            会「境」而
            会「绑定物」而会「绑定类型」而
            （
                虑「绑名」者「获取可能绑定名」于「绑定物」而
                虑「新绑名及新境」者「添加声明」于「境」于「绑名」于「绑定类型」而
                虑「新绑名」者（「新绑名及新境」）中「零」而
                虑「绑体」者「式替换」于（「自由变量」于（「新绑名及新境」）中「零」）于（「绑定物」）而
                虑「绑定体物」者「类型擦除」于（「新绑名及新境」中「一」）于「绑体」而
                「：TODO 验证类型擦除中没有绑定名：」
                    （「进行抽象」于「新绑名」于（「绑定体物」））
            ））而

        虑「无」者「详细调试打印行」于（会「无」而『类型擦除中』附（「表达式表示」于「式」））而
        虑「处理」者（「尝试运行」授以「表达式」）于（会「无」而
        虑「递归处理」者（遇「表达式」者「甲」而（「类型擦除」于「境」于「甲」））而
            鉴「式」而
                有（「式节点」于（「函数调用」于「函形」）于（「缀」于「函数」于（「缀」于「参数」于「罄」）））则
                    （
                        虑「后函数物」者（「类型擦除」于「境」于「函数」）而
                        虑「后参数」者（「递归处理」于「参数」）而
                        虑「后函形」者（鉴「函形」而
                            有「隐式」则「隐式」
                            或有「其他形」则「多参数形」于「一」
                        ）而
                            （「式节点」于（「函数调用」于「后函形」）于（「缀」于「后函数物」于（「缀」于「后参数」于「罄」）））
                    ）
                「： 或有（式节点于（函数调用续延于「标签名」）于「参数」衔【】）则
                    （
                        虑后参数者（「递归处理」于参数）而
                            （式节点于（函数调用续延于标签名）于（【后参数】））
                    ） ：」
                「： 或有（式节点于（结构投影带序数于串于「序数」）于「前式」衔【】）则
                    （
                        虑前式物者「类型擦除」于境于前式而
                        （式节点于（结构投影带序数于串于序数）于（【前式物】））
                    ） ：」
                或有（「自由变量」于「名」）则
                    （「式」）
                或有（「式节点」于（「文件引用节点」于「文件名」）于「罄」）则
                    （「式节点」于「空值节点」于「罄」）
                「： 或有（式节点于（展开后内建节点于量）于「参数」）则
                    （式） ：」
                或有（「式节点」于（「文件定义引用节点」于「文件名」于「投影名」于「序数」）于「罄」）则
                    「式」
                或有（「式节点」于（「结构投影带序数」于「投影名」于「序数」）于（「缀」于「结构」于「罄」））则
                    （「发生事故」于『类型擦除阶段不应再出现结构投影带序数：』附「投影名」）
                或有（「式节点」于（「展开后内建节点」于「量」）于「参数们」）则
                    （
                        虑「空式」者（「式节点」于「空值节点」于「罄」）而
                        鉴「量」而
                            有「内建类型字符串」       则「空式」
                            或有「内建类型整数」       则「空式」
                            或有「内建类型小数」       则「空式」
                            或有「内建类型动态分类值」  则「空式」
                            或有「内建类型有」         则「空式」
                            或有「内建类型爻」         则「空式」
                            或有「内建类型元类型」       则「空式」

                            或有 「其他」            则（「表达式」也
                                    虑「新实现」者（「式节点」于（「展开后内建节点」于「量」）于（（「态射」授以「表达式」授以「表达式」）于（会「参式」而「类型擦除」于「境」于「参式」）于「参数们」））而
                                    鉴「量」而
                                        有「内建爻阳」          则「新实现」
                                        或有「内建爻阴」          则「新实现」
                                        或有「内建有元」          则「新实现」
                                        或有「内建函数整数相等」    则「新实现」
                                        或有「内建函数整数大于」    则「新实现」
                                        或有「内建函数整数减」    则「新实现」
                                        或有 「其他量」            则「发生事故」于『未有213：』附（「表达式表示」于（「式」））
                                ）
                    ）
                或有（「式节点」于（「内建节点」于「量」）于「罄」）则
                    （
                        鉴「量」而
                            有「内建类型字符串」       则「式节点」于「空值节点」于「罄」
                            或有「内建类型整数」       则「式节点」于「空值节点」于「罄」
                            或有「内建类型小数」       则「式节点」于「空值节点」于「罄」
                            或有「内建类型动态分类值」  则「式节点」于「空值节点」于「罄」
                            或有「内建类型有」         则「式节点」于「空值节点」于「罄」
                            或有「内建类型爻」         则「式节点」于「空值节点」于「罄」
                            或有「内建类型元类型」     则「式节点」于「空值节点」于「罄」
                            或有「其他」则「式」
                    ）
                或有（「式节点」于（「续延调用节点」）于（「缀」于「续参」于「罄」））则
                    （虑「后体物」者「绑定对象类型擦除」于「境」于「续参」于（「式节点」于「待定节点」于「罄」）而
                            （「式节点」于（「续延调用节点」）于（「缀」于（「后体物」）于「罄」）） 
                    ）
                或有（「式节点」于（「拉姆达抽象」于「拉形」）于（「缀」于「拉参」于「罄」））则
                        （虑「后体物」者「绑定对象类型擦除」于「境」于「拉参」于（「式节点」于「待定节点」于「罄」）而
                        虑「后拉形」者（鉴「拉形」而
                            有「隐式」则「隐式」
                            或有「其他形」则「多参数形」于「一」
                        ）而
                            （「式节点」于（「拉姆达抽象」于「后拉形」）于（「缀」于（「后体物」）于「罄」））
                        ）
                或有（「式节点」于（「拉姆达抽象续延」于「标签名」）于（「缀」于「拉参」于「罄」））则
                        （虑「后体物」者「绑定对象类型擦除」于「境」于「拉参」于（「式节点」于「待定节点」于「罄」）而
                            （「式节点」于（「拉姆达抽象续延」于「标签名」）于（「缀」于（「后体物」）于「罄」））
                        ）
                或有（「式节点」于（「拉姆达抽象带类型」于「拉形」）于（「缀」于「原抽象类型」于（「缀」于「拉参」于「罄」）））则
                        「：TODO：应该有很多可能性：」
                        （虑「后体物」者「绑定对象类型擦除」于「境」于「拉参」于「原抽象类型」而
                        虑「后拉形」者（鉴「拉形」而
                            有「隐式」则「隐式」
                            或有「其他形」则「多参数形」于「一」
                        ）而
                            （「式节点」于（「拉姆达抽象」于「后拉形」）于（「缀」于（「后体物」）于「罄」））
                        ）
                或有（「式节点」于（「爻分支节点」）于（「缀」于「爻式」于（「缀」于「阳式」于（「缀」于「阴式」于「罄」））））则
                    （
                        虑「后爻式」者（「类型擦除」于「境」于「爻式」）而
                        虑「后阳式」者（「类型擦除」于「境」于「阳式」）而
                        虑「后阴式」者（「类型擦除」于「境」于「阴式」）而
                        （「式节点」于（「爻分支节点」）于（「缀」于「后爻式」于（「缀」于（「后阳式」）于（「缀」于「后阴式」于「罄」））））
                    ）
                或有（「式节点」于（「整数节点」于「数」）于「罄」）则
                    （「式节点」于（「整数节点」于「数」）于「罄」）
                或有（「式节点」于（「小数节点」于「数」于「整小部」）于「罄」）则
                    （「式节点」于（「小数节点」于「数」于「整小部」）于「罄」）
                或有（「式节点」于（「字符串节点」于「串」）于「罄」）则
                    （「式节点」于（「字符串节点」于「串」）于「罄」）
                或有（「式节点」于（「元组类型节点」）于「类型们」）则
                    （「式节点」于（「擦除后类型节点」）于「罄」）
                或有（「式节点」于「内联虑」于（「缀」于「定义式」于（「缀」于「尾式」于「罄」）））则
                    （
                        虑「后定义式」者（「类型擦除」于「境」于「定义式」）而
                        虑「后尾式物」者「绑定对象类型擦除」于「境」于「尾式」于（「式节点」于「待定节点」于「罄」）而
                        （「式节点」于「内联虑」于（「缀」于（「后定义式」）于（「缀」于（「后尾式物」）于「罄」）））
                    ）
                或有（「式节点」于「内联虑带类型」于（「缀」于「定义类」于（「缀」于「定义式」于（「缀」于「尾式」于「罄」））））则
                    （
                        虑「后定义式」者（「类型擦除」于「境」于「定义式」）而
                        虑「后尾式物」者「绑定对象类型擦除」于「境」于「尾式」于「定义类」而
                        （「式节点」于「内联虑」于（「缀」于（「后定义式」）于（「缀」于（「后尾式物」）于「罄」）））
                    ）
                或有（「式节点」于（「递归节点」）于（「缀」于「递归定义域」于（「缀」于「递归值域」于「罄」）））则
                    （
                        虑「后递归值域」者（「绑定对象类型擦除」于「境」于「递归值域」于「递归定义域」）而
                        （「式节点」于（「递归节点」）于（「缀」于（「式节点」于「擦除后类型节点」于「罄」）于（「缀」于「后递归值域」于「罄」）））
                    ）
                或有（「式节点」于「模式匹配」于（「缀」于「分析式」于「分支们」））则
                    （
                        虑「后分析式物」者「类型擦除」于「境」于「分析式」而
                        虑「后分支们式」者（「态射」授以「表达式」授以「表达式」）于（会「分支」而（「类型擦除」于「境」于「分支」））于「分支们」而
                        （「式节点」于（「模式匹配」）于（「缀」于（「后分析式物」）于（「后分支们式」）））
                    ）
                「： 或有（式节点于（模式匹配分支）于「模式」衔「结果」衔【】）则
                    （
                        虑新境者「添加模式匹配声明」于境于模式而
                        虑分支物者（「类型擦除」于新境于结果）而
                        （（式节点于（模式匹配分支）于（模式）衔（分支物）衔【】））
                    ） ：」
                或有（「式节点」于（「常量模式匹配分支」）于（「缀」于「常量」于（「缀」于「结果」于「罄」）））则
                    （「式节点」于（「常量模式匹配分支」）于（「缀」于「常量」于（「缀」于（「类型擦除」于「境」于「结果」）于「罄」）））
                或有（「式节点」于（「变量模式匹配分支」）于（「缀」于「结果」于「罄」））则
                    （「式节点」于（「变量模式匹配分支」）于（「缀」于（「绑定对象类型擦除」于「境」于「结果」于（「式节点」于「待定节点」于「罄」））于「罄」））
                或有（「式节点」于（「唯一构造器模式匹配分支」于「序数」）于（「缀」于「结果」于「罄」））则
                    （「式节点」于（「唯一构造器模式匹配分支」于「序数」）于（「缀」于（「绑定对象类型擦除」于「境」于「结果」于（「式节点」于「待定节点」于「罄」））于「罄」））
                或有（「式节点」于（「元组构造节点」）于「元参」）则
                    （
                        虑「后式」者（「式节点」于（「元组构造节点」）于（（「态射」授以「表达式」授以「表达式」）于（「类型擦除」于「境」）于「元参」））而
                                「后式」
                    ）
                或有（「式节点」于（「元组解构节点」于「序数」）于（「缀」于「元组」于「罄」））则
                    （
                        虑「后元组式」者「类型擦除」于「境」于「元组」而
                            （「式节点」于（「元组解构节点」于「序数」）于（「缀」于「后元组式」于「罄」））
                    ）
                或有（「式节点」于「顺序执行节点」于（「缀」于（「式节点」于（「拉姆达抽象续延」于「标签名」）于（「缀」于「体」于「罄」））于（「缀」于「尾式」于「罄」）））则
                （
                    虑「后体」者（「绑定对象类型擦除」于「境」于「体」于（「式节点」于「待定节点」于「罄」））而
                    虑「后尾式」者（「类型擦除」于「境」于「尾式」）而
                    （「式节点」于「顺序执行节点」于（「缀」于（「式节点」于（「拉姆达抽象续延」于「标签名」）于（「缀」于（「后体」）于「罄」））于（「缀」于「后尾式」于「罄」）））
                ）
                或有（「式节点」于「顺序执行节点」于（「缀」于「式甲」于（「缀」于「式乙」于「罄」）））则
                    （
                        虑「后式甲式」者「类型擦除」于「境」于「式甲」而
                        虑「后式乙式」者「类型擦除」于「境」于「式乙」而
                            （「式节点」于「内联虑」于（「缀」于（「后式甲式」）于（「缀」于（「进行无名抽象」于（「后式乙式」））于「罄」））） 
                    ）
                或有（「式节点」于（「唯一构造器序数元组节点」于「序数」于「元组长」）于（「缀」于「元组式」于「罄」））则（
                    （「式节点」于（「唯一构造器序数元组节点」于「序数」于「元组长」）于（「缀」于（「递归处理」于「元组式」）于「罄」））
                ）
                或有（「式节点」于（「唯一构造器序数元组投影序数节点」）于（「缀」于「构造式」于「罄」））则（
                    （「式节点」于（「唯一构造器序数元组投影序数节点」）于（「缀」于（「递归处理」于「构造式」）于「罄」））
                ）
                或有（「式节点」于（「唯一构造器序数元组投影元组节点」）于（「缀」于「构造式」于「罄」））则（
                    （「式节点」于（「唯一构造器序数元组投影元组节点」）于（「缀」于（「递归处理」于「构造式」）于「罄」））
                ）
                或有（「式节点」于（「文件引用节点」于「新文件名」）于「罄」）则
                    （
                            （「式节点」于（「文件引用节点」于「新文件名」）于「罄」）
                    ）
                或有（「式节点」于（「解析后外部调用节点无类型」于「串」）于「参」）则
                    （
                       （「式节点」于（「解析后外部调用节点无类型」于「串」）于（（「态射」授以「表达式」授以「表达式」）于「递归处理」于「参」）） 
                    ）
                或有（「式节点」于（「解析后外部调用节点带类型」于「串」）于（「缀」于「类」于「参」））则
                    （
                       （「式节点」于（「解析后外部调用节点无类型」于「串」）于（（「态射」授以「表达式」授以「表达式」）于「递归处理」于「参」）） 
                    ）
                或有（「式节点」于「箭头类型节点」于「参」）则
                    （
                        「式节点」于（「擦除后类型节点」）于「罄」
                    ）
                或有（「式节点」于「隐式派类型节点」于「参」）则
                    （
                        「式节点」于（「擦除后类型节点」）于「罄」
                    ）
        或有「其他」则「发生事故」于『未有类型擦除126：』附（「表达式表示」于「式」）
        ）
        于（会「异常」而
            「发生事故」于「异常」附『「：换行：」在类型擦除』附（「表达式表示」于「式」）附『时』
    ）
        而

        「处理」
也。

「类型擦除顶层」乃化「表达式」而「表达式」也。
「类型擦除顶层」者会「式」而
    「类型擦除」于「罄」于「式」也。
