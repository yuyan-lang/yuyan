寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。

寻「函数合集」之书。
寻观「抽象语法分析工具」之书。
寻观「导入解析」之书。
寻观「词法解析」之书。

「寻找可能导入文件」乃化「字符串」「：路径：」而化（「列」于「表达式」）「：具体语法：」而「列」于「字符串」也。
「寻找可能导入文件」者会「路径」而会「输入」而
    虑「解析表达式」者（（「读引用」授以（「函数合集」之「解表达式默认操作符类型」））于（「函数合集」之「解表达式默认操作符引用」））于「路径」而
    （「串联」授以「字符串」）于（（「打表态射」授以（「列」于「表达式」）授以（「列」于「字符串」））于（「切分顶层语句」于「输入」）于（会「语句」而
        若（「等」于（（「长度」授以「表达式」）于「语句」）于「零」）
        则「罄」
        否则（
            鉴（「解析表达式」于「语句」）而
                有（「式节点」于（「文件操作近似类型节点」于「作」）于（「缀」于「结构名式」于「罄」））则
                    若「需要导入」于「作」
                    则（
                        虑「结构名」者「表达式结构名称转字符串列」于「结构名式」而
                        （「缀」授以「字符串」）于（「解析导入文件名」于「路径」于「结构名」）于「罄」
                    ）
                    否则「罄」
                或有「其他」则「罄」
        ）
    ））也。

「寻找可能导入文件顶层」乃化「字符串」「：路径：」而「列」于「字符串」也。
「寻找可能导入文件顶层」者会「路径」而
    （「移除重复」授以「字符串」）于「串相等」于（「寻找可能导入文件」于「路径」于（「词法解析顶层」于（「文件系统」之「读文件」于「路径」）））也。
