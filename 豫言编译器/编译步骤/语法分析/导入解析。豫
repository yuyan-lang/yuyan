
寻观「标准库」之书。




「标准搜索路径」者（「缀」授以「字符串」）于『./藏书阁』于（（「缀」授以「字符串」）于『.』于「罄」）也。
「文件拓展名」者（「缀」授以「字符串」）于『。豫』于（（「缀」授以「字符串」）于『.yuyan』于「罄」）也。



「当前路径」者（「行」授以「字符串」）于「文件系统」之「获取当前路径」也。

「标准搜索绝对路径」者（「态射」授以「字符串」授以「字符串」）于（会「甲」而「路径」之「路径合并」于「当前路径」于「甲」）于「标准搜索路径」也。

「：在路径中找到第一个匹配的文件：」
「寻找文件」乃化（「列」于「字符串」）而化「字符串」而「字符串」也。
「寻找文件」者会「路径们」而会「文件名」而（
        虑「所有可能路径」者（（「串联」授以「字符串」）于（（「态射」授以「字符串」授以（「列」于「字符串」））于（会「路径」而
            （「滤态射」授以「字符串」授以「字符串」）于（会「拓展名」而
                虑「新文件名」者「路径」附「操作系统」之「路径」之「路径分隔符」附「文件名」附「拓展名」而
                    若（「文件系统」之「路径存在」于「新文件名」）
                        则（若（「文件系统」之「路径是普通文件」于「新文件名」）
                            则（「可有」于「新文件名」）否则「可无」）
                        否则「可无」
            ）于「文件拓展名」
        ）于「路径们」））而
        鉴「所有可能路径」而
            有「罄」则「发生事故」于『文件未找到：』附（「文件名」）附『在路径：』附（「间合」于『，』于「路径们」）
            或有「缀」于「头」于「尾」则「头」
）也。

「：在路径中找到第一个匹配的文件夹：」
「寻找文件夹」乃化（「列」于「字符串」）而化「字符串」而「字符串」也。
「寻找文件夹」者会「路径们」而会「文件名」而（
        虑「所有可能路径」者（（「滤态射」授以「字符串」授以「字符串」）于（会「路径」而
                虑「新文件名」者「路径」附「操作系统」之「路径」之「路径分隔符」附「文件名」而
                    若（「文件系统」之「路径存在」于「新文件名」）
                        则（若（「文件系统」之「路径是文件夹」于「新文件名」）
                            则（「可有」于「新文件名」）否则「可无」）
                        否则「可无」
        ）于「路径们」）而
        鉴「所有可能路径」而
            有「罄」则「发生事故」于『文件夹未找到』
            或有「缀」于「头」于「尾」则「头」
    
）也。

「：参数文件夹名及结构名：」
「解析文件夹中导入文件」乃化「字符串」而化（「列」于「字符串」）而「字符串」也。
「解析文件夹中导入文件」者会「路径」而会「结构名」而
    鉴「结构名」而
        有「罄」则「发生事故」于『不可为空』
        或有「缀」于「单个文件名」于「罄」则「寻找文件」于（（「缀」授以「字符串」）于「路径」于「罄」）于「单个文件名」
        或有「缀」于「文件夹名」于「后续结构」则
            「解析文件夹中导入文件」
                于（「寻找文件夹」于（（「缀」授以「字符串」）于「路径」于「罄」）于「文件夹名」）
                于「后续结构」
            也。


「：输入：当前文件路径，寻找的结构名
    输出：找到的文件路径：」
「解析导入文件名」乃化「字符串」而化（「列」于「字符串」）而「字符串」也。
「解析导入文件名」者会「当前文件路径」而会「结构名」而
    鉴「结构名」而
        有「罄」则「发生事故」于『不可为空』
        或有「缀」于「单个文件名」于「罄」则「寻找文件」于（（「缀」授以「字符串」）于（（「路径」之「获取文件夹名」）于「当前文件路径」）于「标准搜索绝对路径」）于「单个文件名」
        或有「缀」于「文件夹名」于「后续结构」则
                「解析文件夹中导入文件」
                    于（「寻找文件夹」于（（「缀」授以「字符串」）于（（「路径」之「获取文件夹名」）于「当前文件路径」）于「标准搜索绝对路径」）于「文件夹名」）
                    于「后续结构」
            也。
