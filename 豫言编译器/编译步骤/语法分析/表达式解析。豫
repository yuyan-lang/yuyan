
寻观「标准库」之书。

寻观「具体语法树」之书。

寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。




寻观「豫言编译器」之「编译数据」之「操作符」之「操作符操作」之书。

寻「函数合集」之书。

寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「符列环境术」之书。

寻观「豫言编译器」之「编译数据」之「操作符」之「操作符集术」之书。

寻观「豫言编译器」之「编译数据」之「操作符」之「操作符数据结构」之书。
寻观「豫言编译器」之「编译数据」之「操作符」之「内建操作符」之「总集」之书。

寻观「豫言编译器」之「编译数据」之「编译配置」之「编译期输出」之书。

寻观「表达式解析输入分析」之书。



「：给定组件的操作符标号，栈（缩减且，反转过的）和组件，其中栈和组件必须是相同长度，
    若匹配成功，返回元素或绑定组件的参数，
    若匹配失败，返回可无：」
「匹配组件获取参数」乃化「整数」而化（「列」于「栈素」）而化（「列」于「组件」）而「或可有」于（「列」于「表达式」）也。
「匹配组件获取参数」者（会「符标号」而会「栈素们」而会「组件们」而
    若（「不等于」于（「长度」于「栈素们」）于（「长度」于「组件们」））
    则「发生事故」于『栈与组件长度必须相等』
    否则（
        虑「匹配字符串操作符」者
            （化（「列」于（「操作符」合「整数」））而化「字符串」而「爻」也
                （会「符序们」而会「组符名」而
                    「存在吗」于（会「符序」而
                            虑「栈整符」者「二对子取零」于「符序」而
                            虑「栈组序号」者「二对子取一」于「符序」而
                                「且」于（「串相等」于（「获取操作符组件字符串」于「栈整符」于「栈组序号」）于「组符名」）
                                                于（「等」于（「获取标号」于「栈整符」）于「符标号」）
                    ）于「符序们」）
            ）
            而
        「从左折叠」于（
                会「素组」而遇（「或可有」于（「列」于「表达式」））者「当前参数」而
                鉴「当前参数」而
                有「可无」则「可无」
                或有（「可有」于「当前参数内容」）则（
                    鉴（「栈素」合「组件」也「素组」）而
                        有（「栈操作符」于「先境」于「符序们」）与（「字符串组件」于「组符名」）则
                            （若（「匹配字符串操作符」于「符序们」于「组符名」）
                                则「可有」授以（「列」于「表达式」）于（「当前参数内容」）
                                否则「可无」 ）
                        或有（「栈操作符」于「先境」于「符序们」）与（「特殊字符串组件」于「组符名」于「转」）则
                            （若（「匹配字符串操作符」于「符序们」于「组符名」）
                                则「可有」授以（「列」于「表达式」）于（「当前参数内容」）
                                否则「可无」 ）
                        或有「其他」与（「字符串组件」于「名」）则「可无」
                        或有「其他」与（「特殊字符串组件」于「名」于「转」）则「可无」
                        或有「素」与「绑定组件」则
                            （
                                鉴「素」而
                                有「栈绑定」于「串」则（「可有」于（「附加」于「当前参数内容」于（「多」于（「自由变量」于「串」）于「空」）））
                                「：要做：这两项我们应该支持，但目前可能会产生多种唯一解
                                或许我们应该比较解析是否生成了结构相同的抽象语法树，
                                    如果是的话则可不报错：」
                                或有「其他」则「可无」「：注：是否要propogate原因？：」
                            ）
                        或有「素」与（「元素组件」于「组优先」）则
                            （「：COPY Paste：」
                                鉴「素」而
                                有「栈绑定」于「串」则（「可有」于（「附加」于「当前参数内容」于（「多」于（「自由变量」于「串」）于「空」）））
                                或有「栈式」于「式」则（「可有」于（「附加」于「当前参数内容」于（「多」于「式」于「空」）））
                                或有「其他」则「可无」「：注：是否要propogate原因？：」
                            ）
                        或有「其他」则（「发生事故」于『表192？』）
                ）
            ）于（「可有」授以（「列」于「表达式」）于「空」）于（「拉链合并」于「栈素们」于「组件们」）
    ））也。

「：获取参数栈素列中首个字符串组件记住的环境，必须传入反转过的栈：」
「获取规约前环境」乃化（「列」于「栈素」）而「或可有」于「符列环境」也。
「获取规约前环境」者（会「栈」而
    虑「可能先境们」者「滤态射」于（会「素」而鉴「素」而有
        （「栈操作符」于「先境」于「符序们」）则（「可有」于「先境」）
        或有「其他」则「可无」）于「栈」而
    鉴「可能先境们」而
        有「空」则「可无」
        或有（「多」于「头」于「尾」）则（「可有」于「头」））也。

「进行一次栈尾规约」乃化「符列环境」而化（「列」于「栈素」）而「列」于（「符列环境」合（「列」于「栈素」））也。
「进行一次栈尾规约」者会「境」而会「栈」而
    虑「顶栈符序们」者
            鉴「栈」而
                有（「多」于（「栈操作符」于「顶先境」于「顶栈符序们」）于「余下栈」）则
                    「：如果栈顶不可能是prefix operator，：」
                    「顶栈符序们」
                或有（「多」于（「栈式」于「顶式」）于（「多」于（「栈操作符」于「顶先境」于「顶栈符序们」）于「余下栈」））则
                    「顶栈符序们」
                或有「其他」则
                    （「列」于（「操作符」合「整数」）也「发生事故」于『405无法进行栈尾规约』）而

        「串联」于（「打表态射」于（「顶栈符序们」）于（
            会「顶栈符序」而
            虑「顶栈整符」者「顶栈符序」中「零」而
                
                虑「符标号」者（「获取标号」于「顶栈整符」）而
                虑「组件们」者「获取所有组件」于「顶栈整符」而
                若（「大于」于（「长度」于「组件们」）于（「长度」于「栈」））
                    则（「空」）
                    否则（
                            虑「栈素们」者「反转」于（「取」于（「长度」于「组件们」）于「栈」）而
                            虑「余下栈素」者「放」于（「长度」于「组件们」）于「栈」而
                            虑「参数」者（（「或可有」于（「列」于「表达式」））也（「匹配组件获取参数」于「符标号」于「栈素们」于「组件们」））而

                            虑「先境」者（鉴（「获取规约前环境」于「栈素们」）而
                                有「可无」则「境」
                                或有（「可有」于「先境」）则「先境」）而

                            虑「结果」者（「列」于（「符列环境」合（「列」于「栈素」））也
                                鉴「参数」而
                                有「可无」则（
                                        「非常详细调试打印行」于（会「无」而『参数获取失败』）；
                                    「空」）
                                或有（「可有」于「参数列」）则（
                                        「非常详细调试打印行」于（会「无」而『参数获取成功』）；
                                    （虑「可能阐述」者（
                                                「尝试运行」于（会「无」而
                                                    「获取阐述」于「顶栈整符」于「参数列」
                                                ）于（会「错误信息」而
                                                    「发生事故」于（
                                                        「错误信息」
                                                            附『「：换行：」当前栈是：』附（「栈表示」于「栈」）
                                                            附『「：换行：」操作符是：』附（「操作符表示」于「顶栈整符」）
                                                            附『「：换行：」环境：』附（「符列环境表示」于「境」）
                                                    ）
                                                ）
                                            ）而
                                        鉴「可能阐述」而
                                            有（「可有」于「阐述式」）则（「多」于（「先境」与（「多」于（「栈式」于「阐述式」）于「余下栈素」））于「空」）
                                            或有「可无」则「空」
                                    ）
                                ））而

                            「结果」
                    ）
        ））
    也。
        
「：若规约成功，返回有，若规约失败，返回无：」
「：我们规约时会碰到一种情况
A 式 | B，AB没有优先级关系，这种情况下就应该是 A先吃式，还是B先吃式呢？
我们规约会选后者，首先检查 A的优先级没有比B高，
那么B会不会被移位是一个关键问题！如果我们发现B的优先级也没有A高，那么我们应该报错！
但是在这里，为了减少报错，
我们选择移位！之后在总体规约时就会变成B先吃式。

本函数只进行可能规约并返回结果，不进行移位

我们要返回更新的栈操作符，因为规约与否取决于是什么样的栈操作符
：」
「进行操作符规约」乃化「符列环境」而化（「列」于「栈素」）而化「栈素」而「列」于（「符列环境」合（「列」于「栈素」）合「栈素」）也。
「进行操作符规约」者会「境」而会「栈」而会「符」而
    虑「递归」者「进行操作符规约」而
    虑「无」者「非常详细调试打印行」于（会「无」而『进行操作符规约中：』附（「栈素表示」于「符」）附『，栈顶：』附（若（「大于」于（「长度」于「栈」）于「三」）
                                                                则（「栈表示」于（「取」于「三」于「栈」））
                                                                否则（「栈表示」于「栈」）））而
    鉴「符」而
        有（「栈操作符」于「后后境」于「符序们」）则
            若（「小于」于（「长度」于「栈」）于「二」）
            则（「多」于（「境」与「栈」与「符」「：如果栈只有一个或没有元素，原样返回：」）于「空」）
            否则
            （
                鉴「栈」而
                    有（「多」于（「栈操作符」于「顶先境」于「顶符序们」）于「余下栈」）则
                        「：如果栈顶不可能是postfix operator，因为移位时会规约：」
                        （「多」于（「境」与「栈」与「符」）于「空」）
                    或有（「多」于（「栈绑定」于「字」）于「余下栈」）则
                        （「多」于（「境」与「栈」与「符」）于「空」）
                    或有（「多」于（「栈式」于「顶式」）于（「多」于（「栈操作符」于「顶先境」于「顶栈符序们」）于「余下栈」））则
                    （
                        虑「可以规约条件」者（化（（「操作符」合「整数」）合（「操作符」合「整数」）合「有」）而「爻」也（会「组合」而
                                鉴（「组合」）而
                                    有（「整符」与「组件序号」）与（「顶栈整符」与「顶组件序号」）与（「元式」）则
                                        若（「等」于「组件序号」于「零」）
                                            则（「发生事故」于『后续组件序号不应当等于零』）
                                            否则（
                                                若（「不等于」于（「加」于「顶组件序号」于「二」）于（「长度」于（「获取所有组件」于「顶栈整符」）））「：如果没到结尾，不规约：」
                                                    则（「阴」）
                                                    否则（
                                                        虑「可以规约」者
                                                            （（「存在吗」于（会「甲」而「等」于「甲」于（「获取标号」于「顶栈整符」））于（
                                                                「：获得下个组件前的组件：」
                                                                鉴（「第N个」于（「减」于「组件序号」于「一」）于（「获取所有组件」于「整符」））
                                                                    而有「元素组件」于「同等优先级」则
                                                                        「获取所有同等或更高优先级的开放操作符」于（「境」中「零」）于（「获取标号」于「整符」）
                                                                    或有「元素组件」于「更高优先级」则「获取所有更高优先级的开放操作符」于（「境」中「零」）于（「获取标号」于「整符」）
                                                                    或有「元素组件」于「重置优先级」则（（「获取集所有开放操作符」于（「境」中「零」）））
                                                                    或有「其他」则「发生事故」于（『不可能的组件：299：』附（「组件表示」于「其他」））
                                                            ）））而
                                                        虑「无」者「非常详细调试打印行」于（会「无」而『检查可以规约：』附（「爻表示」于「可以规约」）附『，栈：』附（「栈表示」于（「多」于「符」于「栈」）））而
                                                        「可以规约」
                                                ）
                                            ）
                            ））而
                        虑「所有组合」者「串联」于（「打表态射」于「符序们」于
                            （会「后续符序」而
                                （「打表态射」于「顶栈符序们」于（会「顶栈符序」而
                                    「后续符序」与「顶栈符序」与「元」
                                ））
                            ））而
                        虑「可以规约与不可规约组合」者
                            「条件分类」于「所有组合」于「可以规约条件」而
                            「：如果全部都可以规约，或者全部都不可以规约，可以采用快速处理：」
                        若（「等」于（「长度」于（「可以规约与不可规约组合」中「零」））于「零」）
                            则
                            （
                                「：全部不可以规约，原样返回：」
                                「多」于（「境」与「栈」与「符」「：如果栈只有一个或没有元素，原样返回：」）于「空」
                            ）
                            否则
                            （
                                若（「等」于（「长度」于（「可以规约与不可规约组合」中「一」））于「零」）
                                则（
                                    「：全部都可以规约，进行一次规约：」
                                    「串联」于（「打表态射」
                                                于（「进行一次栈尾规约」于「顶先境」于「栈」）
                                                于（会「后先境及后栈」而
                                                        （「递归」于（「后先境及后栈」中「零」）于（「后先境及后栈」中「一」）于「符」）
                                    ））
                                ）
                                否则（
                                    「：不一致，全部打散：」
                                    「串联」于（「打表态射」于「所有组合」于（会「组合」而
                                        鉴（（「操作符」合「整数」）合（「操作符」合「整数」）合「有」也「组合」）而
                                            有（「整符」与「组件序号」）与（「顶栈整符」与「顶组件序号」）与（「元式」）则
                                                「递归」于「境」
                                                    于（「多」于（「栈式」于「顶式」）于（「多」于（「栈操作符」于「顶先境」于（「多」于（「顶栈整符」与「顶组件序号」）于「空」））于「余下栈」））
                                                    于（「栈操作符」于「后后境」于（「多」于（「整符」与「组件序号」）于「空」））
                                        ））

                                ）
                            ）
                    ）
                    或有「其他栈」则「发生事故」于『栈的格式有问题』
            ）
        或有「其他」则「发生事故」于『不存在』也。

「移位或规约一步」乃化「符列环境」而化（「列」于「栈素」）而化（「列」于「栈素」）而「列」于（「符列环境」合（「列」于「栈素」））也。
「移位或规约一步」者会「境」而会「已有」而会「次字们」而
    （「：我们更新了次字匹配，匹配的次字永远可以移位：」
        虑「符列」者「符列环境读取操作符集」于「境」而
        虑「无」者（「详细调试打印行」于（会「无」而『移位或规约一步：』附（「栈表示」于（「反转」于「次字们」））））而
        虑「递归」者「移位或规约一步」而
        鉴（「已有」与「次字们」）而
        有「全栈」与「空」则（「多」于（「境」与「全栈」）于「空」）
        「：绑定：」
        或有（「栈」）与（「多」于（「栈绑定」于「甲」）于「后次字们」）则
            （「递归」于「境」「：绑定在操作符中更新：」于（「多」于（「栈绑定」于「甲」）于（「栈」））于「后次字们」）
        「：表达式：」
        或有（「栈」）与（「多」于（「栈式」于「式」）于「后次字们」）则
            （「递归」于「境」于（「多」于（「栈式」于「式」）于（「栈」））于「后次字们」）
        「：普通操作符：」
        「：TODO进行规约后更新绑定信息？？？：」
        或有（「栈」）与（「多」于（「栈操作符」于「先先境」于「先符序们」）于「后次字们」）则「：TODO 进行多次移位规约：」
            （「串联」于（
            「打表态射」于（「进行操作符规约」于「境」于「栈」于（「栈操作符」于「先先境」于「先符序们」））
                于（会「规约后境及规约后栈」而
                    鉴（「规约后境及规约后栈」）而
                        有「规约后境」与「规约后栈」与（「栈操作符」于「先境」于「符序们」）则
                        （ 
                            虑「无」者「非常详细调试打印行」于（会「无」而『进行操作符规约后栈之一：』附（「栈表示」于「规约后栈」））而
                            虑「末尾符序们及非末尾符序们」者
                                （「条件分类」于「符序们」于（会「符序」而
                                鉴「符序」而
                                    有「整符」与「组件序号」则
                                        （「等」于「组件序号」于（「减」于（「长度」于（「获取所有组件」于（「整符」）））于「一」））））
                                而
                            「：分开处理末尾符序与非末尾符序：」
                            虑「末尾符序们」者「末尾符序们及非末尾符序们」中「零」而
                            虑「非末尾符序们」者「末尾符序们及非末尾符序们」中「一」而
                                「附加」于（若（「大于」于（「长度」于「末尾符序们」）于「零」）
                                    则（
                                        「串联」于（「打表态射」于（「进行一次栈尾规约」于「规约后境」于（「多」于（「栈操作符」于「先境」于「末尾符序们」）于「规约后栈」））
                                            于（会「后先境及后栈」而
                                                （「递归」于（「后先境及后栈」中「零」）于（「后先境及后栈」中「一」）于「后次字们」）
                                            ）
                                        ）
                                    ）
                                    否则「空」）
                                于（
                                    若（「等」于（「长度」于「非末尾符序们」）于「零」）
                                        则（「空」）
                                        否则（
                                            虑「剩余符序」者（「引用类」于（「列」于（「操作符」合「整数」））也「新引用」于「空」）而
                                            虑「特殊结果」者（「串联」于（「打表态射」于「非末尾符序们」于（会「符序」而
                                                    鉴「符序」而
                                                        有「整符」与「组件序号」则
                                                        （
                                                            鉴（「获取组件」于「整符」于「组件序号」）而
                                                                有（「特殊字符串组件」于「名」于「转」）则（
                                                                    虑「后境及转后栈」者「转」于（「规约后境」与「规约后栈」）而
                                                                    虑「后境」者「后境及转后栈」中「零」而
                                                                    虑「转后栈」者「后境及转后栈」中「一」而
                                                                    「递归」于「后境」于（「多」于（「栈操作符」于「规约后境」于（「多」于（「整符」与「组件序号」）于「空」））于「转后栈」）于「后次字们」）
                                                                或有「其他」则（
                                                                    「写引用」于「剩余符序」于（「附加」于（「读引用」于「剩余符序」）于（「多」于「符序」于「空」））；「空」
                                                                ）
                                                        ）
                                                ）））而
                                                「附加」于「特殊结果」于
                                                （
                                                    虑「剩余」者（「读引用」于「剩余符序」）而
                                                    若（「大于」于（「长度」于「剩余」）于「零」）
                                                        则（「递归」于「规约后境」于（「多」于（「栈操作符」于「规约后境」于「剩余」）于「规约后栈」）于「后次字们」）
                                                        否则「空」
                                                ）
                                                
                                                
                                            ）
                                ）
                        ）
                ）
            ））
        「：结束：」
        或有「栈」与（「多」于「栈文件结尾」于「空」）则
            （
                若（「大于」于（「长度」于「栈」）于「一」）
                则（
                    「串联」于（「打表态射」于（「进行一次栈尾规约」于「境」于「栈」）于（会
                        「后先境及后栈」而（「递归」于（「后先境及后栈」中「零」）于（「后先境及后栈」中「一」）于「次字们」）
                    ））
                ）否则
                「多」于（「境」与（「多」于「栈文件结尾」于「栈」））于「空」
            ）
        「：其他故障：」
        或有「其他」与「输入」则「发生事故」于（『豫言编译器/编译步骤/语法分析/表达式解析。豫:210 表达式解析遇到故障，在输入』
                    附（「栈表示」于（「反转」于「输入」））附『「：换行：」当前栈是：』附（「栈表示」于「其他」））
        或有「其他」则「发生事故」于『解表91』
    ）也。

「：一定会消耗输入：」
「单一步骤」乃化「符列环境」而化（「列」于「栈素」）而化（「列」于「表达式」）而化（化「字符串」而化（「列」于「栈素」）而「有」）而「列」于（「符列环境」合（「列」于「栈素」）合（「列」于「表达式」））也。
「单一步骤」者会「符列」而会「已有」而会「输入」而会「调试」而
    鉴「输入」而
    有「空」则「发生事故」于『输入不可为空105』
        或有「其他」则（
        虑「无」者「详细调试打印行」于（会「无」而『正在执行单一步骤，输入是：』附（「表达式列表示」于「输入」）附『， 栈是』附（「栈表示」于「已有」））而
            虑「输入解析」者「分析输入」于「符列」于「已有」于「输入」于「调试」而

            虑「结果」者「串联」于（「态射」于（会「甲」而
                    鉴（（（「列」于「栈素」）合（「列」于「栈素」）合（「列」于「表达式」））也「甲」）而
                    有「更新已有」与「次字们」与「剩余输入」则
                                    （
                                        鉴（「移位或规约一步」于（「符列」）于「更新已有」于「次字们」）而
                                        有「空」则（「调试」于『无规约：』于（「附加」于（「反转」于「次字们」）于「已有」「：栈永远是反着的，打印时顺序会颠倒：」）
                                                    ；「空」）
                                        或有「新已有列」则（
                                            「打表态射」于「新已有列」于（会「新已有」而
                                                （「新已有」中「零」）与（「新已有」中「一」）与「剩余输入」）
                                            ）
                                    ）
                    或有「其他2」则（「发生事故」于『匹配失误116』）
                ）
                于「输入解析」）而

            「结果」
        ）也。

「：返回可能的解：」
「所有步骤」乃化（「列」于「表达式」）「：已完成的：」而
        化（「列」于（「符列环境」合（「列」于「栈素」）合（「列」于「表达式」）））「：待探索的：」而
        化（化「字符串」而化（「列」于「栈素」）而「有」）「：调试：」而「列」于「表达式」也。
「所有步骤」者会「已有结果」而会「待处理理解」而会「调试」而
    鉴「待处理理解」而
        有「空」则「已有结果」
        或有（「多」于（「符列」与「已有」与「输入」）于「待处理尾」）则
        （
            鉴「输入」而
            有「空」则
            （
                虑「最终结果」者
                （「列」于「表达式」也
                    「串联」于（「打表态射」于（「移位或规约一步」于（「符列」）于「已有」于（「多」于「栈文件结尾」于「空」））于
                        （会「结果」而
                            鉴「结果」而
                                有（「符」与「全栈」）则（
                                    鉴「全栈」而
                                        有（「多」于「栈文件结尾」于「空」）则（「多」于（「式节点」于「空缺」于「空」）于「空」）
                                        或有（「多」于「栈文件结尾」于（「多」于（「栈式」于「式」）于「空」））则（「多」于「式」于「空」）
                                        或有「其他」则（「调试」于『全栈非单一表达式：』于「其他」；「空」）「：解析失败：」
                                ）
                                「： 或有「可无」则（调试于『无尾规约』于「已有」；空） ：」
                        ））
                ）而
                    「所有步骤」于（「附加」于「已有结果」于「最终结果」）于「待处理尾」于「调试」
            ）
            或有（「多」于（「式节点」于「具体分隔符节点」于「空」）于「输入尾」）
                则（「所有步骤」于「已有结果」于（「多」于（「符列」与「已有」与「输入尾」）于「待处理尾」）于「调试」）
            或有「其他」则（
                「： 粗略调试打印行于（会无而『表达式解析调试信息：当前已有表达式数：』附（整数表示于（长度于已有结果））附『，待处理理解个数：』附（整数表示于（长度于待处理理解））
                    附『，当前剩余输入长度：』附（整数表示于（长度于输入））
                    附『，当前输入头：』附（若（长度于输入）大于二
                                            则（「表达式列表示」于（取于二于输入））
                                            否则（「表达式列表示」于（输入）））
                    附『，当前栈头：』附（若（长度于已有）大于二
                                            则（「栈表示」于（取于二于已有））
                                            否则（「栈表示」于（已有））））。 ：」
                虑「下步」者「单一步骤」于「符列」于「已有」于「输入」于「调试」而
                虑「结果」者（「所有步骤」于「已有结果」于（「附加」于「待处理尾」于「下步」）于「调试」）而
                「结果」
            ）
        ）也。
    
「：注：可能要加入背景操作符列：」
「进行解析」乃化「符列环境」而化（「列」于「表达式」）「：输入：」而「表达式」也。
「进行解析」者会「符列」而会「输入」而
    「： 打印行于（『表达式解析：进行解析：』附（「表达式列表示」于（输入）））。 ：」
    虑「失败解析」者（（「引用类」于（「列」于（「列」于「栈素」）））也「新引用」于「空」）而
    虑「调试」者（（化「字符串」而化（「列」于「栈素」）而「有」）也（会「信息」而会「列」而
        （虑「无」者（「详细调试打印行」于（会「无」而『分支解析失败，』附「信息」附『，栈：』附（「栈表示」于「列」）））而
        「写引用」于「失败解析」于（「多」于「列」于（「读引用」于「失败解析」）
        ））））
    而
    虑「结果」者（「表达式」也「尝试运行」于（会「不要」而鉴（「所有步骤」于「空」于（「多」于（「符列」与「空」与「输入」）于「空」）于「调试」）而
                    有「空」则
                        「发生事故」于（『无法解析，可能的理解：「：换行：」』附
                                （「间合」于『，「：换行：」』于（「态射」于「栈表示」于（「读引用」于「失败解析」））））
                        附『「：换行：」输入是：』附（「表达式列表示」于「输入」）
                        附『「：换行：」环境：』附（「符列环境表示」于「符列」）
                    或有（「多」于「唯一解」于「空」）则
                        （「唯一解」）
                    或有「多种解法」则
                        （
                            
                            （
                                「发生事故」于『多于一个解法：「：换行：」』附
                                （「间合」于『「：换行：」』于（「态射」于（「表达式表示」）于（「多种解法」）））
                                附『「：换行：」输入是：』附（「表达式列表示」于「输入」）
                                附『「：换行：」环境：』附（「符列环境表示」于「符列」）
                            ）
        ））
        于（会「错误信息」而
            「发生事故」于（「错误信息」）
        ）
    ）
    而
    「结果」
也。

「解表达式」乃化「符列环境」而化（「列」于「表达式」）而「表达式」也。
「解表达式」者会「符列」而会「树列」而
    （「进行解析」于「符列」于「树列」）
也。

「解表达式默认操作符」乃化「字符串」而化（「列」于「表达式」）而「表达式」也。
「解表达式默认操作符」者会「当前文件名」而会「树列」而
    「解表达式」于（（「操作符集」也「默认操作符」）与（「空」与「当前文件名」）与「空」）于「树列」
也。

「写引用」于「函数合集」之「解表达式引用」于「解表达式」。
「写引用」于「函数合集」之「解表达式默认操作符引用」于「解表达式默认操作符」。
