寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。



「： 文件路径，（开始行，开始列），（结束行表，结束列）。 ：」
「源码信息」即（「整数」合「整数」）合（「整数」合「整数」）合「字符串」也。「：TODO for bootstrap, no way to distinguish A & B & C and A & (B & C)：」

「：若匹配成功，则返回剩余输入：」
「顶层匹配」乃化（「列」于「表达式」）而化（「列」于「字符串」）「：单个字符：」而「或可有」于（「列」于「表达式」）也。
「顶层匹配」者会「输入」而会「字列」而
    鉴「字列」而
        有「罄」则「可有」于（「输入」）
        或有「缀」于「字列首」于「字列尾」则
            （
                鉴「输入」而
                有「缀」于（「式节点」于（「具体字符节点」于「输入字」）于「罄」）于「输入尾」则（
                    若「字符串相等」于「字列首」于「输入字」
                    则「顶层匹配」于「输入尾」于「字列尾」
                    否则「可无」
                    ）
                或有「其他」则「可无」
            ）也。
