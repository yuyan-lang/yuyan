
寻标准库之书。
观标准库之书。

寻具体语法树之书。
观具体语法树之书。

观抽象语法之书。
「： 表达式者抽象语法之表达式也。 ：」

寻命名环境术之书。
观命名环境术之书。




操作符立元类型也。



组件立元类型也。
    字符串组件立化字符串而化整数而组件也。「：（暂时）precedence必须等于前序组件的precedence：」
    元素组件立化整数而组件也。「：两端必须大于等于组件的precedence，中间可为min：」
    绑定组件立组件也。

构造操作符立化组件列「：name components：」
            而化整数「：precedence：」
            而化（化表达式列而表达式）「：elaborator，参数以正常顺序传入：」
            而操作符也。

符列环境者操作符列合条目列也。

获取优先级乃化操作符而整数也。
获取优先级者会符而
    鉴符而有（构造操作符于组于级于阐述）则级也。

获取组件乃化操作符而组件列也。
获取组件者会符而
    鉴符而有（构造操作符于组于级于阐述）则组也。
获取阐述乃化操作符而（化表达式列而表达式）也。
获取阐述者会符而
    鉴符而有（构造操作符于组于级于阐述）则阐述也。

「：获取操作符中的字符部分：」
获取操作符的关键字乃化操作符而（字符串合整数合操作符）列也。
获取操作符的关键字者会符而
    鉴符而
    有构造操作符于件们于优先级于阐述则
        （滤态射于
            （会件而
                鉴件而
                    有元素组件于「其他」则「可无」
                    或有绑定组件则「可无」
                    或有字符串组件于串于串优先级则可有（
                                                  串与串优先级与符）
            ）于件们
        ）也。


获取所有关键词乃化操作符列而（字符串合整数合操作符）列也。
获取所有关键词者会操列而
    （（串联于（态射于获取操作符的关键字于「操列」）））也。

P1者一零零也。
P2者二零零也。
P3者三零零也。
P4者四零零也。
P5者五零零也。
P6者六零零也。
P7者七零零也。
P8者八零零也。
P9者九零零也。
PMax者一零零零零也。

「：给定当前的组件（名称与优先级），检查下一组件是否是绑定组件，
    如果是，返回绑定的结尾组件，如果不是绑定组件，返回空。：」
获取随后的绑定信息乃化字符串而化整数而化操作符而或可有（字符串）也。
获取随后的绑定信息者会测试名而会测试优先级而会符而
    鉴符而
    有（构造操作符于组于级于阐述）则
        （虑「
            分析组乃化组件列而或可有（字符串）也。
            分析组者循分析组以会甲而
                鉴甲而
                有【】 则「可无」
                或有（字符串组件于串于先）衔绑定组件衔（字符串组件于后串于后先）衔「其他」则
                    （若（「串相等」于串于测试名）且（先等于测试优先级）
                    则可有（后串）
                    否则分析组于（（字符串组件于后串于后先）衔「其他」）
                    ）
                或有（首衔尾）则（分析组于尾）
                也。

            结果者分析组于组也。
        」以成结果之道）
    也。

    
函数声明操作符者
    构造操作符于【（字符串组件于『会』于PMax），（绑定组件），
                （字符串组件于『而』于PMax），（元素组件于P1）】
    于P1
    于（会式列而鉴「式列」而
        有绑定式衔主体衔【】则
            （鉴绑定式而
            有自由变量于名则式节点于拉姆达抽象于【进行抽象于名于主体】
            或有「其他」则发生事故于『绑定的主体不是一个变量』
            ）
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。


普通函数操作符乃操作符也。
普通函数操作符者
    构造操作符于【（字符串组件于『化』于PMax），（元素组件于P1），（字符串组件于『而』于P1），（元素组件于P1）】
    于P1
    于（会式列而鉴「式列」而
        有定义域衔值域衔【】则
        式节点于（派抽象于显式）于【定义域，（进行无名抽象于值域）】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

结构投影符乃操作符也。
结构投影符者
    构造操作符于【（元素组件于P9），（字符串组件于『之』于P9），（元素组件于（P9加一））】
    于P8
    于（会式列而鉴「式列」而
        有体衔（自由变量于名）衔【】则
        式节点于（结构投影于（名））于【体】
        或有体衔「它」衔【】则发生事故于『内65：之的参数必须是自有变量』
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

函数调用操作符乃操作符也。
函数调用操作符者
    构造操作符于【（元素组件于P8），（字符串组件于『于』于P8），（元素组件于（P8加一））】
    于P8
    于（会式列而鉴「式列」而
        有函衔参衔【】则
        式节点于（函数调用于显式）于【函，参】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

模式匹配分支符乃操作符也。
模式匹配分支符者
    构造操作符于【（字符串组件于『有』于PMax），（元素组件于P5加一），（字符串组件于『则』于P5），（元素组件于（P5加一））】
    于P5
    于（会式列而鉴「式列」而
        有模式衔体衔【】则
        式节点于（模式匹配分支）于【模式，体】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）也。

模式匹配分支汇总符乃操作符也。
模式匹配分支汇总符者
    构造操作符于【（元素组件于（P4加一）），（字符串组件于『或』于（P4加一）），（元素组件于（P4））】
    于P4
    于（会式列而鉴「式列」而
        有（式节点于（模式匹配分支）于分）衔
            （式节点于（模式匹配）于已有分支）衔【】则
            式节点于（模式匹配）于（（式节点于（模式匹配分支）于分）衔已有分支）
        或有（式节点于（模式匹配分支）于分）衔
            （式节点于（模式匹配分支）于分二）衔【】则
            式节点于（模式匹配）于（（式节点于（模式匹配分支）于分）衔（式节点于（模式匹配分支）于分二）衔【】）
        或有「其他」则发生事故于『内148：期待模式匹配分支』
    ）也。

模式匹配符乃操作符也。
模式匹配符者
    构造操作符于【（字符串组件于『鉴』于（PMax）），（元素组件于（P3加一）），（字符串组件于『而』于（P3加一）），（元素组件于（P3加一））】
    于P3
    于（会式列而鉴「式列」而
        有对象衔（式节点于（模式匹配）于已有分支）衔【】则
            式节点于（模式匹配）于（对象衔已有分支）
        或有对象衔
            （式节点于（模式匹配分支）于分二）衔【】则
            式节点于（模式匹配）于（对象衔（式节点于（模式匹配分支）于分二）衔【】）
        或有「其他」则发生事故于『内161：期待模式匹配分支』
    ）也。


内建符者态射于（会量而
    构造操作符于【字符串组件于（内建常量表示于量）于PMax】
    于PMax于（会式列而内建于（量））
）于所有内建常量
    也。

所有操作符者【
    函数声明操作符
    ，普通函数操作符
    ，函数调用操作符
    ，模式匹配分支符
    ，模式匹配分支汇总符
    】接【
    模式匹配符
    ，结构投影符
    】接
    
    内建符也。


「： 所有关键字者「串组合并」于（态射于获取操作符的关键字于所有操作符）也。 ：」