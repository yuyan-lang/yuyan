
寻标准库之书。
观标准库之书。

寻具体语法树之书。
观具体语法树之书。

观抽象语法之书。
「： 表达式者抽象语法之表达式也。 ：」

寻顺序标识符之顺序标识符之书。
观顺序标识符之顺序标识符之书。


操作符立元类型也。


优先级关系立元类型也。
    同等优先级立优先级关系也。
    更高优先级立优先级关系也。
    重置优先级立优先级关系也。

组件立元类型也。
    字符串组件立化字符串而组件也。
    元素组件立化优先级关系而组件也。「：元素组件是否允许同等优先级的元素？若为阳则允许，为阴则不允许，默认：阴：」
    绑定组件立组件也。

构造操作符立化组件列「：name components：」
            而化整数「：uid：」
            而化（化表达式列而表达式）「：elaborator，参数以正常顺序传入：」
            而操作符也。

表示优先级关系乃化优先级关系而字符串也。
表示优先级关系者会关系而鉴关系而
    有同等优先级则『⊝』
    或有更高优先级则『(↑)』
    或有重置优先级则『⊥』也。

表示组件乃化组件而字符串也。
表示组件者会组件而鉴组件而
    有字符串组件于串则串
    或有绑定组件则『□』
    或有元素组件于关系则表示优先级关系于关系也。


表示操作符乃化操作符而字符串也。
表示操作符者会符而鉴符而
    有构造操作符于组于号于举则（（以『』合并（态射于表示组件于组））附『(』附（表示于号）附『)』）也。

获取标号乃化操作符而整数也。
获取标号者会符而
    鉴符而有（构造操作符于组于级于阐述）则级也。

获取所有组件乃化操作符而组件列也。
获取所有组件者会符而
    鉴符而有（构造操作符于组于级于阐述）则组也。

获取组件乃化操作符而化整数而组件也。
获取组件者会符而会序数而
    鉴符而有（构造操作符于组于级于阐述）则
    （若（（长度于组）大于序数）则第N个于序数于组否则（发生事故于『获取组件序数超出范围！』））也。

获取组件字符串乃化操作符而化整数而字符串也。
获取组件字符串者会符而会序数而
    鉴（获取组件于符于序数）而
        有字符串组件于串则串
        或有「其他」则发生事故于『不是字符串』也。

获取阐述乃化操作符而（化表达式列而表达式）也。
获取阐述者会符而
    鉴符而有（构造操作符于组于级于阐述）则阐述也。

「：获取操作符中的字符部分：」
获取操作符的关键字乃化操作符而（（操作符合整数））列也。
获取操作符的关键字者会符而
    鉴符而
    有构造操作符于件们于标号于阐述则
        （滤态射序号于
            （会当前索引而会件而
                鉴件而
                    有元素组件于「其他」则「可无」
                    或有绑定组件则「可无」
                    或有字符串组件于串则可有（（符与当前索引））
            ）于件们
        ）也。


获取所有关键词乃化操作符列而（操作符合整数）列也。
获取所有关键词者会操列而
    （（串联于（态射于（会甲而获取操作符的关键字于甲）于「操列」）））也。
「： 
P1者一零零也。
P2者二零零也。
P3者三零零也。
P4者四零零也。
P5者五零零也。
P6者六零零也。
P7者七零零也。
P8者八零零也。
P9者九零零也。
PMax者一零零零零也。 ：」

「：给定当前的组件（名称与优先级），检查下一组件是否是绑定组件，
    如果是，返回绑定的结尾组件，如果不是绑定组件，返回空。：」
获取随后的绑定信息乃化操作符而化整数而或可有（字符串）也。
获取随后的绑定信息者会符而会组序数而
    鉴符而
    有（构造操作符于组于级于阐述）则
        若（组序数加二）小于（长度于组）
        则（
            虑「
            分析组乃化组件列而或可有（字符串）也。
            分析组者循分析组以会甲而
                鉴甲而
                有（字符串组件于串）衔绑定组件衔（字符串组件于后串）衔「其他」则可有（后串）
                或有「其他」则「可无」
                也。

            结果者分析组于（放于组序数于组）也。
        」以成结果之道）否则「可无」
    也。

    
拉姆达操作符者
    构造操作符于【（字符串组件于『会』），（绑定组件），
                （字符串组件于『而』），（元素组件于「同等优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有绑定式衔主体衔【】则
            （鉴绑定式而
            有自由变量于名则式节点于拉姆达抽象于【进行抽象于名于主体】
            或有「其他」则发生事故于『绑定的主体不是一个变量』
            ）
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。


普通函数类型操作符乃操作符也。
普通函数类型操作符者
    构造操作符于【（字符串组件于『化』），（元素组件于重置优先级），（字符串组件于『而』），（元素组件于「同等优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有定义域衔值域衔【】则
        式节点于（派抽象于显式）于【定义域，（进行无名抽象于值域）】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

结构投影符乃操作符也。
结构投影符者
    构造操作符于【（元素组件于「同等优先级」），（字符串组件于『之』），（元素组件于「更高优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有体衔（自由变量于名）衔【】则
        式节点于（结构投影于（名））于【体】
        或有体衔「它」衔【】则发生事故于『内65：之的参数必须是自有变量』
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

函数调用操作符乃操作符也。
函数调用操作符者
    构造操作符于【（元素组件于「同等优先级」），（字符串组件于『于』），（元素组件于「更高优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有函衔参衔【】则
        式节点于（函数调用于显式）于【函，参】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）
    也。

模式匹配分支符乃操作符也。
模式匹配分支符者
    构造操作符于【（字符串组件于『有』），（元素组件于「重置优先级」），（字符串组件于『则』），（元素组件于更高优先级）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有模式衔体衔【】则
        式节点于（模式匹配分支）于【模式，体】
        或有「其他」则发生事故于『内65：操作符参数个数不匹配』
    ）也。

模式匹配分支汇总符乃操作符也。
模式匹配分支汇总符者
    构造操作符于【（元素组件于「更高优先级」），（字符串组件于『或』），（元素组件于「更高优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有（式节点于（模式匹配分支）于分）衔
            （式节点于（模式匹配）于已有分支）衔【】则
            式节点于（模式匹配）于（（式节点于（模式匹配分支）于分）衔已有分支）
        或有（式节点于（模式匹配分支）于分）衔
            （式节点于（模式匹配分支）于分二）衔【】则
            式节点于（模式匹配）于（（式节点于（模式匹配分支）于分）衔（式节点于（模式匹配分支）于分二）衔【】）
        或有「其他」则发生事故于『内148：期待模式匹配分支』
    ）也。

模式匹配符乃操作符也。
模式匹配符者
    构造操作符于【（字符串组件于『鉴』），（元素组件于「重置优先级」），（字符串组件于『而』），（元素组件于「更高优先级」）】
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有对象衔（式节点于（模式匹配）于已有分支）衔【】则
            式节点于（模式匹配）于（对象衔已有分支）
        或有对象衔
            （式节点于（模式匹配分支）于分二）衔【】则
            式节点于（模式匹配）于（对象衔（式节点于（模式匹配分支）于分二）衔【】）
        或有「其他」则发生事故于『内161：期待模式匹配分支』
    ）也。

爻分支符乃操作符也。
爻分支符者
    构造操作符于（【（字符串组件于『若』），（元素组件于重置优先级），（字符串组件于『则』），（元素组件于「重置优先级」）】
    接【（字符串组件于『否则』）， （元素组件于「更高优先级」）】）
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有爻式衔阳分支衔阴分支衔【】则
            式节点于（爻分支节点）于【爻式，阳分支，阴分支】
        或有「其他」则发生事故于『内187：应该是3个』
    ）也。

外部调用符乃操作符也。
外部调用符者
    构造操作符于（【（字符串组件于『《《C调用》》名』），（元素组件于重置优先级），（字符串组件于『传』），（元素组件于「更高优先级」）】）
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有名称衔参数衔【】则
            式节点于（外部调用节点）于【名称，参数】
        或有「其他」则发生事故于『内2187：应该是2个』
    ）也。

「： 顶层操作符乃操作符也。
顶层操作符者
    构造操作符于（【（元素组件于更高优先级）】）
    于（行于下个序数）
    于（会式列而鉴「式列」而
        有式衔【】则式
        或有「其他」则发生事故于『内187：应该是3个』
    ）也。 ：」

内建符者态射于（会量而
    构造操作符于【字符串组件于（内建常量表示于量）】
    于（行于下个序数）于（会式列而内建于（量））
）于所有内建常量
    也。

