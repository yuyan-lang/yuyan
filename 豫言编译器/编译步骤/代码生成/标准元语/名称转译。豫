
寻观标准库之书。


寻观拓展库之顺序标识符之顺序标识符之书。


查询目录乃引用类于（串典于字符串）也。
查询目录者新引用于（空串典）也。


「串典引用更新」于「查询目录」于『yy_豫言不安全转换』于『yy_unsafe_cast』。

「串典引用更新」于「查询目录」于
『yy_豫言字符串获取字节数组』于
『yy_string_get_byte_array』。
「串典引用更新」于「查询目录」于
『yy_豫言字符转整数』于
『yy_string_byte_to_int』。
「串典引用更新」于「查询目录」于
『yy_豫言子字符串从字节序数开始』于
『yy_substring_from_byte_index』。
「串典引用更新」于「查询目录」于
『yy_豫言字符串匹配』于
『yy_string_match』。
「串典引用更新」于「查询目录」于
『yy_豫言字符串获取字节序数当前字符』于
『yy_string_get_unicode_char_at_index』。
「串典引用更新」于「查询目录」于
『yy_豫言字符串获取JSON字符串』于
『yy_string_get_json_string』。
「串典引用更新」于「查询目录」于
『获取当前异常处理器』于
『yy_get_current_exception_handler』。
「串典引用更新」于「查询目录」于
『设置当前异常处理器』于
『yy_set_current_exception_handler』。 

名称转译乃化字符串而字符串也。
名称转译者会名而
    若（存在键于（读引用于「查询目录」）于名）
    则（「串典读取」于（读引用于「查询目录」）于名）
    否则（虑新名者
    「串组合并」于（态射于（会字符而
        虑字符数者字符转整数于字符于零而
        若（「且」于（字符数大于三一）于（一二八大于字符数））
        则字符
        否则（「串组合并」于（打表于（字节长度于「字符」）于（会序数而
            「整数表示」于（字符转整数于「字符」于序数）
        ）））
    ）于（获取字符于名））而
    （
        虑首字符数者字符转整数于名于零而
        若（「且」于（首字符数大于三一）于（一二八大于首字符数））
        则新名
        否则『mlname_』附新名
    ）
    ）也。
