「：
函数应用形状分析

现状：
这个文件先保留为架构说明。实际代码暂时还在「类型检查工具」中，因为它同时需要调用对象合成、
对象检查、类型检查和模式匹配检查；这些函数目前用引用打结。如果贸然把实现搬到这里，会形成
更隐蔽的 import 环。后续要迁移时，应先把这些引用单独拆成一个很小的“类型检查递归接口”文件，
再把纯 head-spine 规划代码搬到本文件。

目标：
在真正检查函数应用参数之前，先只判断“当前应用形状”是否可能成立。
这里不创建全局求解状态；隐式类型参数只作为本次应用问题的局部求解变量。
求解结果是一个替换表：类型参数名 -> ground 类型。

输入：
一，函数头的类型。
二，已经写出的应用参数脊。
三，检查目标类型。

算法：
一，收集函数应用为 head-spine 形态：头式 与 参数脊。
二，分析函数头类型，剥下最前缀的隐式类型参数。
    这些前缀参数构成本次局部求解域。
    非前缀位置不允许引入新的待求类型变量。
三，将剥去前缀后的函数类型看作一串显式/隐式参数需求和一个末尾结果类型。
四，对比“已写出的应用参数数量”和“函数类型可接受的参数数量”。
    如果写出的参数过多，立即报错；这种应用一定不能成立。
五，按已写出的参数数量，从函数类型前面丢弃同样数量的参数层。
    丢弃时只记录每个实参应该检查的定义域，不立即递归检查实参。
六，显式实参只消费对应的函数定义域，并把“实参、定义域”记录为待检查项。
    这一步不检查实参，也不根据实参形状或实参类型反推前缀类型参数。
七，只有一个额外的受限求解规则：
    如果当前应用链的第一个显式实参是普通变量，并且该变量在环境中的类型是“构造器应用到参数脊”
    的形态，例如 列 甲、结果 甲 乙 或 元组类型[甲；乙]，则允许用该变量类型约束当前定义域。
    这条规则不递归检查实参，不合成任意实参类型，也不适用于头为类型变量的形态，例如 甲 乙。
八，把丢弃参数后的尾类型与检查目标类型做单向局部合一。
    合一左边可以含前缀变量；右边必须是 ground 类型。
    如果同一变量被推出两个不同类型，报错。
    如果尾类型和目标类型不匹配，报错。
    如果尾类型或目标类型中仍有未 ground 的未知，报错。
九，应用替换表到所有实参定义域、尾类型和需要插入的隐式参数。
    此时不应再有任何本次应用局部未知量。
十，如果写出的参数不足，但尾类型与检查目标类型匹配，则做 eta 展开。
    形状分析只在检查模式发生，因为只有检查模式有目标类型作为参考。
    展开出的参数使用已经 ground 的定义域类型。
十一，在替换和 eta 之后，重新检查所有实参并重建普通嵌套函数调用表达式。

限制：
一，第一版只支持前缀隐式类型参数的局部求解。
二，返回类型可以是泛型函数，但它只是尾类型的一部分，不会被本次应用继续消费。
    例如 {甲}. 甲 -> ({乙}. 乙 -> 丙) 在本次应用中只有一个显式函数参数。
    应用掉第一个参数后，剩余的 {乙}. 乙 -> 丙 原样作为尾类型与检查目标类型匹配。
三，eta 展开前必须先确认尾类型与目标类型匹配，并且所有局部变量已经 ground。

实现边界：
一，「局部类型替换」只负责局部替换、occurs check、单向合一和“全解”确认。
二，head-spine 规划只负责产生：检查后的头式、待检查参数列、最终局部替换、结果类型。
三，参数真正的对象检查统一放在规划之后；这样拉姆达参数可以等定义域 ground 后再检查。
四，对象检查遇到“隐式函数头检查为非隐式目标类型”时，才转入 head-spine 检查。
   普通子类型检查不自动补隐式参数。
：」
