
寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「抽象绑定树结构」之「抽象语法树」之「总集」之书。
。



寻观「豫言编译器」之「编译步骤」之「总体过程」之「编译过程工具」之书。
寻观「拓展库」之「癸象工具」之「总集」之书。

寻观「豫言编译器」之「编译步骤」之「求值正则变换」之「续延传递变换」之书。

「用法说明」者『用法：./yy debug [cmd] [Path]
其中[cmd]是 showchecked 』也。

「打印检查后定义」乃化「字符串」而「有」也。
「打印检查后定义」者会「路径」而
    虑「缓存文件路径」者（「获取输出文件路径」于「路径」）附『.检查后定义.json』而
    虑「检查后定义」者（「抽象语法反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「缓存文件路径」）））而
    「打印行」于（「表达式表示」于（「检查后定义」））也。

「友好打印正则语法」乃化「字符串」而「有」也。
「友好打印正则语法」者会「路径」而
    虑「缓存文件路径」者（「获取输出文件路径」于「路径」）附『.求值正则形式.json』而
    虑「新正则形式」者（「抽象语法反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「缓存文件路径」）））而
    「打印行」于（「抽象语法友好表示顶层」于（「新正则形式」））也。

「打印语法」乃化「字符串」而「有」也。
「打印语法」者会「路径」而
    虑「缓存语法」者（「抽象语法反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「路径」）））而
    「打印行」于（「表达式表示」于（「缓存语法」））也。

「打印语法树组」乃化「字符串」而「有」也。
「打印语法树组」者会「路径」而
    虑「缓存语法」者（「表达式树组反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「路径」）））而
    「打印行」于（「树组表示」于（「缓存语法」））也。

「友好打印语法」乃化「字符串」而「有」也。
「友好打印语法」者会「路径」而
    虑「缓存语法」者（「抽象语法反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「路径」）））而
    「打印行」于（「抽象语法友好表示顶层」于（「缓存语法」））也。

「友好打印语法树组」乃化「字符串」而「有」也。
「友好打印语法树组」者会「路径」而
    虑「缓存语法」者（「表达式树组反序列化」于（「癸象解析」于（「文件系统」之「读文件」于「路径」）））而
    「打印行」于（「表达式树组友好表示」于「零」于（「缓存语法」））也。


「进行调试」乃化（「列」于「字符串」）而「有」也。
「进行调试」者会「参数」而
    鉴「参数」而
     有（「缀」于『showchecked』于（「缀」于「检查后定义路径」于「罄」））则（「打印检查后定义」于「检查后定义路径」）
     或有（「缀」于『showanf』于（「缀」于「正则路径」于「罄」））则（「友好打印正则语法」于「正则路径」）
     或有（「缀」于『dumptree』于（「缀」于「文件路径」于「罄」））则（「打印语法」于「文件路径」）
     或有（「缀」于『dumptrees』于（「缀」于「文件路径」于「罄」））则（「打印语法树组」于「文件路径」）
     或有（「缀」于『showtree』于（「缀」于「文件路径」于「罄」））则（「友好打印语法」于「文件路径」）
     或有（「缀」于『showtrees』于（「缀」于「正则路径」于「罄」））则（「友好打印语法树组」于「正则路径」）
     或有「其他」则（「发生事故」于「用法说明」）也。
