寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「编译配置」之「全局配置」之书。




「处理命令行参数」乃化「字符串」列而「有」也。
「处理命令行参数」者循「处理命令行参数」以会「串们」而
    鉴「串们」而
        有【】则「元」
        或有『--』衔「尾」则「写引用」于「运行时命令行参数」于「尾」
        或有『-v』衔「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有『-vv』衔「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；（「写引用」于「打印详细调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有『-vvv』衔「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；（「写引用」于「打印详细调试信息」于「阳」）；
                                                （「写引用」于「打印非常详细调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有『--debug-print-add』衔「串」衔「尾」则（（「写引用」于「编译调试打印标识符」于（「串」衔（「读引用」于「编译调试打印标识符」）））；「处理命令行参数」于「尾」）
        或有『--debug-check-add』衔「串」衔「尾」则（（「写引用」于「编译调试条件检查标识符」于（「串」衔（「读引用」于「编译调试条件检查标识符」）））；「处理命令行参数」于「尾」）
        或有『--debug-skip-opt-name』衔「串」衔「尾」则（（「写引用」于「跳过编译优化诸名称」于（「串」衔（「读引用」于「跳过编译优化诸名称」）））；「处理命令行参数」于「尾」）
        或有『--parse-only』衔「尾」则（（「写引用」于「仅执行语法分析」于「阳」）；「处理命令行参数」于「尾」）
        或有『--type-check-only』衔「尾」则（（「写引用」于「仅执行类型检查」于「阳」）；「处理命令行参数」于「尾」）
        或有『--type-check-and-erase-only』衔「尾」则（「处理命令行参数」于「尾」）
        或有『--compile-only』衔「尾」则（（「写引用」于「仅执行编译不运行」于「阳」）；「处理命令行参数」于「尾」）
        或有『--self-integrity-check』衔「尾」则（（「写引用」于「执行自我完整性检查」于「阳」）；「处理命令行参数」于「尾」）
        或有『--no-llvm-target-triple』衔「尾」则（（「写引用」于「不写目标三元组」于「阳」）；「处理命令行参数」于「尾」）
        或有『--do-not-load-cache』衔「尾」则（（「写引用」于「不加载缓存」于「阳」）；「处理命令行参数」于「尾」）
        或有『--do-not-save-cache』衔「尾」则（（「写引用」于「不进行缓存」于「阳」）；「处理命令行参数」于「尾」）
        或有『--no-debug-print』衔「尾」则（（「写引用」于「打印非常粗略调试信息」于「阴」）；「处理命令行参数」于「尾」）
        或有『--print-llvm-commands』衔「尾」则（（「写引用」于「打印执行的外部指令」于「阳」）；「处理命令行参数」于「尾」）
        或有『--no-comments-in-llvm』衔「尾」则（（「写引用」于「字节码打印调试信息」于「阴」）；「处理命令行参数」于「尾」）
        或有『--print-opt-info』衔「尾」则（（「写引用」于「打印优化调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有『--override-llvm-target-triple』衔「串」衔「尾」则（（「写引用」于「覆写目标三元组」于（「可有」于「串」））；「处理命令行参数」于「尾」）
        或有『--no-dependency-check』衔「尾」则（（「写引用」于「不检查依赖更新」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-c』衔「尾」则（（「写引用」于「仅执行编译不运行」于「阳」）；「处理命令行参数」于「尾」）
        或有『-o』衔「输出路径」衔「尾」则（（「写引用」于「输出可执行文件路径」于（「可有」于「输出路径」））；「处理命令行参数」于「尾」）
        或有『-h』衔「尾」则（（「写引用」于「需要显式帮助」于「阳」）；「处理命令行参数」于「尾」）
        或有『--help』衔「尾」则（（「写引用」于「需要显式帮助」于「阳」）；「处理命令行参数」于「尾」）
        或有『--version』衔「尾」则（（「写引用」于「需要显式版本」于「阳」）；「处理命令行参数」于「尾」）
        或有『--about』衔「尾」则（（「写引用」于「需要显式关于」于「阳」）；「处理命令行参数」于「尾」）
        「：优化相关：」
        或有『--do-not-optimize』衔「尾」则（（「写引用」于「不进行优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--optimize』衔「尾」则（（「写引用」于「进行优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--static-linking』衔「尾」则（（「写引用」于「使用静态链接」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--whole-program-opt』衔「尾」则（（「写引用」于「使用全局编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--optimize-intermediate-files』衔「尾」则（（「写引用」于「优化单一中间文件」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--target=js』衔「尾」则（（「写引用」于「目标平台」于（『js』））；「处理命令行参数」于「尾」）
        或有『--target=mlton』衔「尾」则（（「写引用」于「目标平台」于（『mlton』））；「处理命令行参数」于「尾」）
        或有『--target=polyml』衔「尾」则（（「写引用」于「目标平台」于（『polyml』））；「处理命令行参数」于「尾」）
        或有『--mode=worker』衔「尾」则（（「写引用」于「当前模式」于（『worker』））；「处理命令行参数」于「尾」）
        或有『--parallel』衔「尾」则（（「写引用」于「使用并行编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--very-parallel』衔「尾」则（（「写引用」于「使用并行编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--debug』衔「尾」则（（「写引用」于「进入调试模式」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--save-build-artifact』衔「尾」则（（「写引用」于「保存编译期中间产物」于（「阳」））；「处理命令行参数」于「尾」）
        或有『--override-add-file-dependency』衔「串」衔「尾」则（（「写引用」于「文件编译顺序」于（（「读引用」于「文件编译顺序」）接【「串」】））；「处理命令行参数」于「尾」）
        或有『-O0』衔「尾」则（（「写引用」于「执行优化总轮数」于（「零」））；「处理命令行参数」于「尾」）
        或有『--no-debug-symbol』衔「尾」则（（「写引用」于「写二进制文件调试标签」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-fdead-code-elim』衔「尾」则（（「写引用」于「执行未使用式消除优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-dead-code-elim』衔「尾」则（（「写引用」于「执行未使用式消除优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-fconst-prop』衔「尾」则（（「写引用」于「执行常量展开优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-const-prop』衔「尾」则（（「写引用」于「执行常量展开优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-ffunc-uncurry』衔「尾」则（（「写引用」于「执行去柯里化优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-func-uncurry』衔「尾」则（（「写引用」于「执行去柯里化优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-ffunc-inline』衔「尾」则（（「写引用」于「执行函数内联优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-func-inline』衔「尾」则（（「写引用」于「执行函数内联优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-finline-limit=500』衔「尾」则（（「写引用」于「函数内联优化限制」于（「五零零」））；「处理命令行参数」于「尾」）
        或有『-ftco』衔「尾」则（（「写引用」于「执行尾调用优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-tco』衔「尾」则（（「写引用」于「执行尾调用优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-fstack-alloc』衔「尾」则（（「写引用」于「执行栈分配优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-stack-alloc』衔「尾」则（（「写引用」于「执行栈分配优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有『-ffunc-arg-opt』衔「尾」则（（「写引用」于「执行函数参数优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有『-fno-func-arg-opt』衔「尾」则（（「写引用」于「执行函数参数优化」于（「阴」））；「处理命令行参数」于「尾」）
        
        或有「其他参数」衔「尾」则（
            若（『-D』是「其他参数」的开头）且（「是子字符串吗」于『=』于「其他参数」）
            则（
                鉴「搜索单个」于「阳」于『=』于「其他参数」而
                    有（「可有」于「等号序数」）则（
                        虑「无」者「列引用前衔」于「编译期属性」于（（「获取子字符串」于「二」于「等号序数」于「其他参数」）与（「获取子字符串」于（「等号序数」加「一」）于（「串长度」于「其他参数」）于「其他参数」））而
                        「处理命令行参数」于「尾」
                    ）
                    或有「可无」则（
                        「发生事故」于『不认识的参数：』附「其他参数」
                    ）
            ）否则（
                若（『--worker-task=』是「其他参数」的开头）则（
                    虑「工作任务」者（「获取子字符串」于（「串长度」于『--worker-task=』）于（「串长度」于「其他参数」）于「其他参数」）而
                    虑「无」者（「写引用」于「单一工作模式任务」于（「工作任务」））而
                    「处理命令行参数」于「尾」
                ）否则（
                    若（『-』是「其他参数」的开头）
                    则（「发生事故」于『不识别的命令行选项：』附「其他参数」）
                    否则（「写引用」于「要编译的文件」于（（「读引用」于「要编译的文件」）接【「其他参数」】）；「处理命令行参数」于「尾」）
                ）
            ）
            ）
        也。
    
    
