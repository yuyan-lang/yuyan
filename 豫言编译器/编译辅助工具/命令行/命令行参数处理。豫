寻观「标准库」之书。
寻观「豫言编译器」之「编译数据」之「编译配置」之「全局配置」之书。




「处理命令行参数」乃化（「列」于「字符串」）而「有」也。
「处理命令行参数」者会「串们」而
    鉴「串们」而
        有「罄」则「元」
        或有「缀」于『--』于「尾」则「写引用」于「运行时命令行参数」于「尾」
        或有「缀」于『-v』于「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『-vv』于「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；（「写引用」于「打印详细调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『-vvv』于「尾」则（（「写引用」于「打印粗略调试信息」于「阳」）；（「写引用」于「打印详细调试信息」于「阳」）；
                                                （「写引用」于「打印非常详细调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--debug-print-add』于（「缀」于「串」于「尾」）则（（「写引用」于「编译调试打印标识符」于（「缀」于「串」于（「读引用」于「编译调试打印标识符」）））；「处理命令行参数」于「尾」）
        或有「缀」于『--debug-check-add』于（「缀」于「串」于「尾」）则（（「写引用」于「编译调试条件检查标识符」于（「缀」于「串」于（「读引用」于「编译调试条件检查标识符」）））；「处理命令行参数」于「尾」）
        或有「缀」于『--debug-skip-opt-name』于（「缀」于「串」于「尾」）则（（「写引用」于「跳过编译优化诸名称」于（「缀」于「串」于（「读引用」于「跳过编译优化诸名称」）））；「处理命令行参数」于「尾」）
        或有「缀」于『--parse-only』于「尾」则（（「写引用」于「仅执行语法分析」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--type-check-only』于「尾」则（（「写引用」于「仅执行类型检查」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--type-check-and-erase-only』于「尾」则（「处理命令行参数」于「尾」）
        或有「缀」于『--compile-only』于「尾」则（（「写引用」于「仅执行编译不运行」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--self-integrity-check』于「尾」则（（「写引用」于「执行自我完整性检查」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--no-llvm-target-triple』于「尾」则（（「写引用」于「不写目标三元组」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--do-not-load-cache』于「尾」则（（「写引用」于「不加载缓存」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--do-not-save-cache』于「尾」则（（「写引用」于「不进行缓存」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--no-debug-print』于「尾」则（（「写引用」于「打印非常粗略调试信息」于「阴」）；「处理命令行参数」于「尾」）
        或有「缀」于『--print-llvm-commands』于「尾」则（（「写引用」于「打印执行的外部指令」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--no-comments-in-llvm』于「尾」则（（「写引用」于「字节码打印调试信息」于「阴」）；「处理命令行参数」于「尾」）
        或有「缀」于『--print-opt-info』于「尾」则（（「写引用」于「打印优化调试信息」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--override-llvm-target-triple』于（「缀」于「串」于「尾」）则（（「写引用」于「覆写目标三元组」于（「可有」于「串」））；「处理命令行参数」于「尾」）
        或有「缀」于『--no-dependency-check』于「尾」则（（「写引用」于「不检查依赖更新」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-c』于「尾」则（（「写引用」于「仅执行编译不运行」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『-o』于（「缀」于「输出路径」于「尾」）则（（「写引用」于「输出可执行文件路径」于（「可有」于「输出路径」））；「处理命令行参数」于「尾」）
        或有「缀」于『-h』于「尾」则（（「写引用」于「需要显式帮助」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--help』于「尾」则（（「写引用」于「需要显式帮助」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--version』于「尾」则（（「写引用」于「需要显式版本」于「阳」）；「处理命令行参数」于「尾」）
        或有「缀」于『--about』于「尾」则（（「写引用」于「需要显式关于」于「阳」）；「处理命令行参数」于「尾」）
        「：优化相关：」
        或有「缀」于『--do-not-optimize』于「尾」则（（「写引用」于「不进行优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--optimize』于「尾」则（（「写引用」于「进行优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--static-linking』于「尾」则（（「写引用」于「使用静态链接」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--whole-program-opt』于「尾」则（（「写引用」于「使用全局编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--optimize-intermediate-files』于「尾」则（（「写引用」于「优化单一中间文件」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--target=js』于「尾」则（（「写引用」于「目标平台」于（『js』））；「处理命令行参数」于「尾」）
        或有「缀」于『--target=mlton』于「尾」则（（「写引用」于「目标平台」于（『mlton』））；「处理命令行参数」于「尾」）
        或有「缀」于『--target=polyml』于「尾」则（（「写引用」于「目标平台」于（『polyml』））；「处理命令行参数」于「尾」）
        或有「缀」于『--mode=worker』于「尾」则（（「写引用」于「当前模式」于（『worker』））；「处理命令行参数」于「尾」）
        或有「缀」于『--parallel』于「尾」则（（「写引用」于「使用并行编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--very-parallel』于「尾」则（（「写引用」于「使用并行编译」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--debug』于「尾」则（（「写引用」于「进入调试模式」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--save-build-artifact』于「尾」则（（「写引用」于「保存编译期中间产物」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『--override-add-file-dependency』于（「缀」于「串」于「尾」）则（（「写引用」于「文件编译顺序」于（「附加」于（「读引用」于「文件编译顺序」）于（「缀」于「串」于「罄」）））；「处理命令行参数」于「尾」）
        或有「缀」于『-O0』于「尾」则（（「写引用」于「执行优化总轮数」于「零」）；「处理命令行参数」于「尾」）
        或有「缀」于『--no-debug-symbol』于「尾」则（（「写引用」于「写二进制文件调试标签」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fdead-code-elim』于「尾」则（（「写引用」于「执行未使用式消除优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-dead-code-elim』于「尾」则（（「写引用」于「执行未使用式消除优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fconst-prop』于「尾」则（（「写引用」于「执行常量展开优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-const-prop』于「尾」则（（「写引用」于「执行常量展开优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-ffunc-uncurry』于「尾」则（（「写引用」于「执行去柯里化优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-func-uncurry』于「尾」则（（「写引用」于「执行去柯里化优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-ffunc-inline』于「尾」则（（「写引用」于「执行函数内联优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-func-inline』于「尾」则（（「写引用」于「执行函数内联优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-finline-limit=500』于「尾」则（（「写引用」于「函数内联优化限制」于「五零零」）；「处理命令行参数」于「尾」）
        或有「缀」于『-ftco』于「尾」则（（「写引用」于「执行尾调用优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-tco』于「尾」则（（「写引用」于「执行尾调用优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fstack-alloc』于「尾」则（（「写引用」于「执行栈分配优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-stack-alloc』于「尾」则（（「写引用」于「执行栈分配优化」于（「阴」））；「处理命令行参数」于「尾」）
        或有「缀」于『-ffunc-arg-opt』于「尾」则（（「写引用」于「执行函数参数优化」于（「阳」））；「处理命令行参数」于「尾」）
        或有「缀」于『-fno-func-arg-opt』于「尾」则（（「写引用」于「执行函数参数优化」于（「阴」））；「处理命令行参数」于「尾」）
        
        或有「缀」于「其他参数」于「尾」则（
            若「且」于（「串开头是它吗」于『-D』于「其他参数」）于（「是子字符串吗」于『=』于「其他参数」）
            则（
                鉴「搜索单个」于「阳」于『=』于「其他参数」而
                    有（「可有」于「等号序数」）则（
                        虑「无」者「列引用前衔」于「编译期属性」于（（「获取子字符串」于「二」于「等号序数」于「其他参数」）与（「获取子字符串」于（「加」于「等号序数」于「一」）于（「串长度」于「其他参数」）于「其他参数」））而
                        「处理命令行参数」于「尾」
                    ）
                    或有「可无」则（
                        「发生事故」于『不认识的参数：』附「其他参数」
                    ）
            ）否则（
                若（「串开头是它吗」于『--worker-task=』于「其他参数」）则（
                    虑「工作任务」者（「获取子字符串」于（「串长度」于『--worker-task=』）于（「串长度」于「其他参数」）于「其他参数」）而
                    虑「无」者（「写引用」于「单一工作模式任务」于（「工作任务」））而
                    「处理命令行参数」于「尾」
                ）否则（
                    若（「串开头是它吗」于『-』于「其他参数」）
                    则（「发生事故」于『不识别的命令行选项：』附「其他参数」）
                    否则（「写引用」于「要编译的文件」于（「附加」于（「读引用」于「要编译的文件」）于（「缀」于「其他参数」于「罄」））；「处理命令行参数」于「尾」）
                ）
            ）
            ）
        也。
    
    
