寻观标准库之书。
寻观豫言编译器之编译数据之编译配置之全局配置之书。

「：一些，可有被设置：」
需要显式帮助者新引用于「阴」也。
需要显式版本者新引用于「阴」也。
需要显式关于者新引用于「阴」也。
要编译的文件乃引用类于字符串列也。
要编译的文件者新引用于「【】」也。
运行时命令行参数乃引用类于字符串列也。
运行时命令行参数者新引用于「【】」也。



处理命令行参数乃化字符串列而有也。
处理命令行参数者循处理命令行参数以会串们而
    鉴串们而
        有【】则元
        或有『--』衔尾则写引用于运行时命令行参数于尾
        或有『-v』衔尾则（（写引用于打印粗略调试信息于阳）；处理命令行参数于尾）
        或有『-vv』衔尾则（（写引用于打印粗略调试信息于阳）；（写引用于打印详细调试信息于阳）；处理命令行参数于尾）
        或有『-h』衔尾则（（写引用于需要显式帮助于阳）；处理命令行参数于尾）
        或有『--help』衔尾则（（写引用于需要显式帮助于阳）；处理命令行参数于尾）
        或有『--version』衔尾则（（写引用于需要显式版本于阳）；处理命令行参数于尾）
        或有『--about』衔尾则（（写引用于「需要显式关于」于阳）；处理命令行参数于尾）
        或有其他参数衔尾则（
            若（『-』是其他参数的开头）
            则（发生事故于『不识别的命令行选项：』附其他参数）
            否则（写引用于要编译的文件于（（读引用于要编译的文件）接【其他参数】）；处理命令行参数于尾）
            ）
        也。
    