# 安全外壳重换钥验证

本测试用两轮不同的 X25519 共密和交换哈希模拟首次会话与重协商。它验证再次派钥仍使用首次 `session_id`，`NEWKEYS` 报文用旧钥封装，发送与接收方向分别切换，重复切换被拒绝，随后新钥能互相解密数据。它还验证自动重换钥的双向报文阈值、IV 低 16 位回绕、完整 64 位计数器将尽时的提前触发，以及单调时钟恰好达到一秒的时间阈值。半包测试核对未收齐时保留密文和 IV，整帧到齐后才认证并推进 IV。此包是本地状态测试；豫言客户端互通探针另已与 OpenSSH 和豫言服务端分别完成认证后网络重协商。
