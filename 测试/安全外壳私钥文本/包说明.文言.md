# 安全外壳私钥文本验证

以 `ssh-keygen` 各造无口令、有口令 Ed25519 私钥，置 `/tmp/yyssh私钥文本/未加密` 与 `/tmp/yyssh私钥文本/加密`；前者公钥 `.pub` 同置。此验核无口令钥之种所生公钥同于彼文，亦拒加密钥、断甲胄、坏首、坏 Base64 字及坏尾填。

析者惟收 OpenSSH `openssh-key-v1` 未加密单把 Ed25519 私钥；严核 Base64、字段界、双检整数、公私同及尾填。有口令钥今不纳。
