# 安全外壳私钥文本验证

使用 `ssh-keygen` 各生成一把无口令和一把有口令的 Ed25519 私钥，分别保存为 `/tmp/yyssh私钥文本/未加密` 与 `/tmp/yyssh私钥文本/加密`。无口令私钥对应的 `.pub` 文件放在同一目录。本测试验证无口令文件解析出的种子可导出相同公钥，并拒绝加密文件、截断外壳、损坏的开头、非法 Base64 字符和错误填充。

解析器仅支持 OpenSSH `openssh-key-v1` 未加密、单把 Ed25519 私钥。它严格检查 Base64、字段边界、重复校验整数、公钥一致性与填充；有口令私钥不在支持范围内。
