# 安全外壳密码验证

本可执行包对照 RFC 7748 的 X25519 向量、RFC 8032 的 Ed25519 向量、NIST 的 GCM-AES256 向量及 SHA-256 空消息摘要，验证 `豫言/安全外壳密码` 的原生绑定。另核密文、附加数据与标签遭篡改时拒绝返回明文。构建时需要 OpenSSL；Homebrew 环境若缺少头文件搜索路径，设置 `CPATH=/opt/homebrew/opt/openssl@3/include`。验证时使用 `-- --do-not-load-cache`，确保当前源码进入产物。
