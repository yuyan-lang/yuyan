# 底层示例

底层模式（不用垃圾回收）之回归示例：以常豫言书之，声明 `「编译模式」者『底层』也。`，唯依 `底层库`。所成者 Wasm 模块：

```sh
./yy豫构 构建 底层示例 --输出 /tmp/yy底层示例.wasm
```

复以 `工具/底层测试` 于宿主之整数执行器逐项校其导出之函数（尾递归、递归、分派、内联循环、可变格、位运算、多参数、导出签名之缀）。涉内存与静态数据之导出，唯验模块，待客体执行器等系统之码于裸机实测。
