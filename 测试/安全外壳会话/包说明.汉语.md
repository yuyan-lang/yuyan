# 安全外壳会话验证

验证 `豫言/安全外壳协议` 的 session channel 开启与确认、共用流量窗口、普通与 stderr 扩展数据、PTY 模式和尺寸、窗口变化、shell、exec、env 与 signal 请求、exit-status、exit-signal，以及全局请求与请求应答。通道寿命测试验证双向 EOF/CLOSE 的顺序和重复消息拒绝。测试拒绝截断、尾随数据、非法布尔与模式和越过窗口的数据；signal 还验证禁止要求应答，exit-signal 验证 core dumped 布尔位。
