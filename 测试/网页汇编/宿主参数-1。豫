寻观「标准库」之书。
「：文言：空参不失，符文不妄执行。汉语：参数保留空串、空格、中文与 shell 元字符，验证不经过 shell 拼接。：」
「结果」者「操作系统」之「子进程」之「获取输出」于『/usr/bin/printf』于
  （「列」【『%s|%s|%s』，『』，『含 空格』，『$(exit 9)』】）也。
若「且」于（「结果」中「零」）于（「串相等」于（「结果」中「一」）于『|含 空格|$(exit 9)』）
  则「打印行」于『宿主参数测试通过』否则「发生事故」授以「有」于『宿主参数边界错误』。
