寻观「标准库」之书。

「：文言：递归之后尚须相加，栈帧不可先弃。汉语：非尾递归在小栈、大堆下触发独立栈检查，不能依赖 GC 才发现溢出。：」
「深加」乃化「整数」而「整数」也。
「深加」者会「数」而若「等于」于「数」于「零」则「零」
  否则「加」于「数」于（「深加」于（「减」于「数」于「一」））也。
「输入」者若「等于」于（「长度」授以「字符串」于（「命令行参数」之「获取参数」于「元」））于「零」则「一零零零」否则「整数操作」之「理解整数」于（「第N个」授以「字符串」于「零」于（「命令行参数」之「获取参数」于「元」））也。
「打印行」于（「整数表示」于（「深加」于「输入」））。
