寻观「标准库」之书。
寻观「标准库」之「控制」之「异步」之书。
「：文言：空候有期，坏参见拒，异符各报。汉语：Node 构建宿主只等待自己启动的子进程，覆盖空列表超时、非法参数和未知句柄的位置对应。：」
「空候」者「输入输出多路等待」于（「罄」授以（「整数」合「整数」））于「一」也。
若「且」于（「等于」于（「空候」中「零」）于「零」）于（「等于」于（「长度」授以「整数」于（「空候」中「一」））于「零」）则「元」否则「发生事故」授以「有」于『空等待错误』。
「坏时」者「输入输出多路等待」于（「罄」授以（「整数」合「整数」））于（「减」于「零」于「二」）也。
若「小于」于（「坏时」中「零」）于「零」则「元」否则「发生事故」授以「有」于『非法超时未拒绝』。
「坏掩码」者「输入输出多路等待」于（「列」【「零」与「零」】）于「零」也。
若「小于」于（「坏掩码」中「零」）于「零」则「元」否则「发生事故」授以「有」于『非法事件未拒绝』。
「未知」者「输入输出多路等待」于（「列」【「零」与「一」，「一」与「二」】）于「零」也。
若「且」于（「等于」于（「长度」授以「整数」于（「未知」中「一」））于「二」）于
  （「且」于（「等于」于（「第N个」授以「整数」于「零」于（「未知」中「一」））于「四」）于
    （「等于」于（「第N个」授以「整数」于「一」于（「未知」中「一」））于「四」））
则「元」否则「发生事故」授以「有」于『未知句柄事件错误』。
「打印行」于『进程等待边界通过』。
