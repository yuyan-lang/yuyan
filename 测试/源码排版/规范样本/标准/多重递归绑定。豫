寻观「标准库」之书。

「对嵌套整数列表逐层求和并记录元素总数」乃
  化（「列」于（「列」于「整数」））而
  （「整数」合「整数」）
也。
「对嵌套整数列表逐层求和并记录元素总数」者会「整数列表们」而
  递归虑「单列求和」其化（「列」于「整数」）而「整数」者会「数们」而
    鉴「数们」而
      有「罄」则「零」
      或有（「缀」于「当前数」于「剩余数」）则
        「加」于「当前数」于（「单列求和」于「剩余数」）
  而
  递归虑「单列计数」其化（「列」于「整数」）而「整数」者会「数们」而
    鉴「数们」而
      有「罄」则「零」
      或有（「缀」于「忽略当前数」于「剩余数」）则
        「加」于「一」于（「单列计数」于「剩余数」）
  而
  递归虑「逐列汇总」其化（「列」于（「列」于「整数」））而（「整数」合「整数」）者会「剩余列」而
    鉴「剩余列」而
      有「罄」则「零」与「零」
      或有（「缀」于「当前列」于「后续列」）则
        虑「后续结果」者「逐列汇总」于「后续列」而
        虑「当前总和」者「单列求和」于「当前列」而
        虑「当前个数」者「单列计数」于「当前列」而
        「加」于「当前总和」于（「后续结果」中「零」）
          与「加」于「当前个数」于（「后续结果」中「一」）
  而
  「逐列汇总」于「整数列表们」
也。
