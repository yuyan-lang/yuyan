# 操作系统接口装载包

客器既成，行 `yy豫言操作系统装载包 输出目录 绑定目录...`。先以公约源核第四式客需之簿，复依绑定时所记 `宿主清单来源.txt`，核实际适配侧独立所藏宿供之簿，各合而存。同一公约之浏览器与云工适配可各守其簿；二载器于客启前验二簿。

宿供之簿不可抄客需，适配若变须人审而更新。今编器以一 `yuyan:gc-host/v1.call` 通桥投诸源术，载器仅可验通桥与 `_start` 之形；逐术贯通，仍须实跑而验。
