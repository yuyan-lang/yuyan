# 导出内存与实例同封

增“网页外部实例”，合声明与已装配之状态。装配者成其封，调用者行导出之函，取存者按导出名得该实例实有之线性区，不复制，不重造实例。毋须分传模块与实例。

宿主可循旧守界接口取舍与增长。重取同一导出，所观仍同；增长之后，旧引用亦见新页与新字。以函数名求内存，或名不存在，皆明报错。

二〇二六年九月二十二日，独立 C 夹具增取置字节二导出。原生及网页目标之豫言参考执行皆验：宿写客可读，客写宿可见；重取导出不失更新，别实例同址仍零；宿增一页，客可取新零并置字，旧引用可见，而别实例仍二页。相同标准模块经 V8，结果同。误导出类别及不存在之名皆拒，诸型合。

复验见外部模块测试及汉语篇所链流程。此接口属宿主参考执行，未增内存导入、跨进程共享或内核通信，亦未谓客体动态装载已成；全 OS 之目标仍待续行。

更新夹具后，完整驱动复造输入，双架构一百一十回归皆过，产物在 `/tmp/yy裸机验证.Dgiam9`。裸机仍验启动实算与负例，本篇新接口由原生、网页参考执行及 V8 验之。散列与界见 `导出内存验证记录.json`。
