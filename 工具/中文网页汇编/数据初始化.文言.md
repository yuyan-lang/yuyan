# 数据初始化语义

增被动数据实例、弃段、初始化及填充，以备模块数据与用户态字符串之用。今唯存储操作，未接指令解码与模块实例化。

诸实例可共不变字节，弃段之记各有其属。弃则视为空，重复弃之无害，已写之内存不失。弃后唯源零、长零可初始化，目标仍须守界。

先验源与目的全界，后写；偏移不回绕。填充取值之低八位，跨页亦然，不以一中文字符为一字节。

原生与网页目标诸试皆过，标准引擎覆验亦合。验跨页、源偏移、越界不半写、负位形、重弃、实例独立及零长之界。命令详汉语篇。

仍借宿主存储，未接内存数据段、装配与裸机，不得称内核无回收页分配与命令壳已成。
