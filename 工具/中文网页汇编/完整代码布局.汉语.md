# 完整函数代码布局

命令 `yy中文网页汇编 检查代码布局 模块.wasm` 在类型段和函数接口检查之后读取代码段。检查定义函数数与代码体数相同，逐函数保留完整签名、压缩局部声明分组、指令起止偏移及参数加局部总数。函数编号已计入前置导入，不会将第一个定义函数误当作函数零。

每组局部类型的具体引用编号均检查作用域，包括数量为零的分组。分组保持压缩形式，不按恶意巨大数量立即创建数组；总数通过减法界限检查，拒绝超过 u32 的值。函数体和代码段各守边界，缺少代码段、代码数量不符、局部编码截断和末尾缺少结束字节均拒绝。

11 条测试通过，涵盖 i64 与 eqref 局部保留、指令边界、数量溢出、零数量组中的非法引用、截断和缺失代码。库与命令类型检查、测试构建和运行已通过；命令构建与真实输入结果另录。

这一层只确定声明和边界。末尾存在结束字节并不证明其是正确的指令终结；非空局部的确定赋值、控制流、操作数类型及各条指令仍需验证。命令不执行模块，不能据此授予执行许可，默认裸机镜像不变。

命令构建与真实输入检查已通过：优化器三代含 4474 个定义函数，栈向量排序含 841 个，GC 重复索引边界含 793 个，均成功读取布局。对应日志为 `/tmp/yy代码布局优化器日志`、`/tmp/yy代码布局排序日志`、`/tmp/yy代码布局引用日志`。这些输入是工作区现有产物，本轮未重新编译。
