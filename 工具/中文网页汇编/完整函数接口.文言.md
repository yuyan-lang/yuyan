# 完整函数接口

命令 `yy中文网页汇编 检查函数连接 提供模块.wasm 使用模块.wasm 导入序号` 以共享表读二模块，循使用者所指函数导入之名求提供者导出，再验完整函数型。提供文件须显式指定，不循模块名擅载文件。

验导入定义所引皆函数，以导入先、定义后立索引，验函数导出之界，并拒诸类别间重名。非函数导入未受，非函数导出之资源号未验。函数体、初始化与实例运行皆未验未行，成功文亦明示。

八试皆成，覆盖子型提供、移号、错签名、越界、非函数、重名及未受内存导入；一途可含数条件。三个正反例模块经标准引擎验为有效，异型于连接时拒。

库与命令验型已成，命令构建调用结果后录。默认裸机镜像未改；完整函数体验证与实例绑定尚待，不得称裸机双向调用已成。

命令构建既成，相容连接退出零，错签名退出一而报类型不相容。日志存 `/tmp/yy接口连接成功日志`、`/tmp/yy接口连接拒绝日志`。
