# 中文网页汇编

以豫言立模块之类，读写中文，验而双向编解标准二进制。今版本曰整数核心一，具三十二位整数常量、局部读写暂存、加减乘、调用、弃值、空操作；类型、导入、函数与导出各成表。参数及局部皆三十二位整数，结果可多。

索引自零起，导入先于定义。验栈、索引、常量与导出之名，未备指令及版本明拒。文本读而不执行，字节直接生，不借外汇编器。今具整数参考执行与可信宿主绑定，未具控制、内存、浮点及回收，旧后端亦未迁入；工具自身尚依旧编译器与运行库。用法详同名汉语稿。

文本数字用中文，负数冠负；名称含零则以字节列存之。读入各守段界，验变长整数、类型与通字符，自定义及未备之段明拒。输入限一兆字节，向量与局部等数量限六万五千五百三十六，此为实现之限，非标准之限。规范化不保原注与逐字节编码。

参考执行计函数及指令之费，调用深度有界；不能凭预算抢占宿主自身。未具进程隔离，不以此冒安全沙箱。往返、恶例、回绕、多返、宿主调用、长列及含零名称皆有测试，性能之成另验。

整数实例装配已立，按名系函数，先验其型，共预算与深度。详[模块装配](模块装配.文言.md)。客体用户态加载与完整实例尚未备。

增三十二位比较十一令，详[整数比较](整数比较.文言.md)。

结构控制之表示、验证与编解码已立，执行尚待接入，详[结构控制](结构控制.文言.md)。

执行与装配执行已通整数访存，详[内存指令](内存指令.文言.md)；中文存储文本尚待成。
