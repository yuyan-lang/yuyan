# 完整函数接口检查

命令 `yy中文网页汇编 检查函数连接 提供模块.wasm 使用模块.wasm 导入序号` 在同一共享类型表中读取两个模块，按使用方指定函数导入的字段名寻找提供方导出，再检查完整函数类型。提供文件由调用者显式指定；命令不会根据导入模块名自动寻找或加载文件。

读取器核验函数导入与定义所引用的类型确为函数，按导入在前、定义在后建立函数索引，检查函数导出边界，并检查所有导出名称重复，包括跨类别重名。非函数导入目前明确拒绝；非函数导出的资源索引未在此层检查。函数体、初始化和实例运行均未验证或执行，成功消息会明确说明这一限制。

8 条测试已通过，涵盖子类型提供接口、使用方类型编号平移、错误签名、导入序号越界、非函数声明、类型越界、导出越界、跨类别重名及不支持的内存导入路径（部分条件在同一测试路径中覆盖）。三个正反例模块均经标准引擎确认二进制有效，类型不合由连接阶段拒绝。

库与命令类型检查通过；命令构建与调用验证结果另录。默认裸机镜像未改变。下一步仍需连接完整函数体验证与实际实例绑定，不能把此检查当作已完成裸机双向调用。

命令构建已完成：有效连接退出 0，错误签名退出 1，并输出“函数接口类型不相容”。日志为 `/tmp/yy接口连接成功日志`、`/tmp/yy接口连接拒绝日志`。
