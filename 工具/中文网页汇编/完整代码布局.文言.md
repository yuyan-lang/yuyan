# 完整函数代码布局

命令 `yy中文网页汇编 检查代码布局 模块.wasm` 先验类型与接口，次读代码。定义数须合体数，存完整签名、压缩局部组、指令起止及参数局部总数。编号先计导入，不误首定义为函数零。

局部具体型号皆验作用域，虽零数量亦验。组不预展，巨数不得诱其先分配；总数以余量验 u32 之界。函数体、代码段各守其界，无代码、数异、截断与末字节非结束者皆拒。

十一试皆成，含宽整、eq 引用、边界、溢出、零组错号与缺失。库及命令验型、测试构建运行皆成；命令与实输入结果后录。

此唯定声明与边界，末字节为结束不证指令正确。非空局部确定赋值、控制、操作数与各指令尚待验。不运行模块，不授执行权，默认镜像不变。

命令既成，实输入布局皆读成：优化器三代四千四百七十四函数，排序八百四十一，GC 重复索引边界七百九十三。日志存 `/tmp/yy代码布局优化器日志`、`/tmp/yy代码布局排序日志`、`/tmp/yy代码布局引用日志`。皆用既有产物，今未重编。
