# 模块全局之值

后续已接双架构裸机，详[裸机全局](../豫言裸机/全局状态.文言.md)。下存初验之界。

于旧函数、内存、数据之外增状态层，记自有三十二位全局之可变与常初值。标准字节可读写全局段及取置指令。验初值、索引、栈与只读之禁；旧无全局上下文者明拒其令。

每装配各生引用，调用更新可续存。跨模函数守提供者实例，同号不借调用者之值。全局与内存、数据段共用实例执行接口。

命令入口之执行与装配执行今接此层，旧无全局模块仍可用。中文文本仍为函数核心，完整状态声明未入。诸复验令详汉语篇，末二命令得十一、一千零一十一。

二〇二六年九月二十二日，持续更新、独立实例、只读、跨模归属、内存共存及字节往返经原生参考执行与 V8 皆过，诸型合，旧内存命令亦过。

旧双架构九十八验皆过，产物在 `/tmp/yy裸机验证.sOqjsT`。全局尚未接裸机，勿以回归冒称其裸机已验。今只具自有三十二位及常量初值，未具全局导入导出、引用全局及余初式。用户态执行器裸机接入、完整垃圾回收、调度、文件系统与默认壳仍待之。

复验同源码网页目标亦过；原生与网页皆拒非法可变标志、误型、误初式与段内余字四类。散列及验界见 `全局状态验证记录.json`。
