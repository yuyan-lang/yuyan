寻观「标准库」之书。
寻观「标准库」之「操作系统」之「文件系统」之书。
寻观「标准库」之「操作系统」之「路径」之书。
寻观「标准库」之「操作系统」之「命令行参数」之书。
寻观「构建基础」之「包定位」之书。
寻观「包配置」之「包依赖」之书。
寻观「包配置」之「配置数据」之书。
寻观「包配置」之「配置解析」之书。

「确认种子原生范围」乃化「已解析包图」而「有」也。
「确认种子原生范围」者会「图」而
  虑「无」者「态射」授以「包标识」授以「有」于（「图」中「一」）于（会「标识」而
    虑「包值」者「查包」于（「图」中「零」）于「标识」而
    虑「原生」者「包原生」于（「解析包文件」于（「包值」中「二」））而
    若「等于」于（「长度」授以「字符串」于（「原生」中「零」））于「零」则「元」否则
      若「且」于（「包标识相等」于「标识」于（『』与『构建基础』））于
        （「且」于（「串组相等」于（「原生」中「零」）于（「列」【『包上下文.c』】））于
          （「等于」于（「长度」授以「字符串」于（「原生」中「一」））于「零」））
      则「元」否则「发生事故」授以「有」于『双种子导出发现额外原生组成，需扩展导出协议，不能忽略它们』）而「元」也。

「：文言：两种同出，初举不假旧途。汉语：发布包同时携带编译器和豫构的可移植源码，导出仍通过普通包构建入口。：」
「导出双种子包」乃化「字符串」而化「字符串」而「整数」也。
「导出双种子包」者会「编译器」而会「输出」而
  若「路径存在」于「输出」则「发生事故」授以「整数」于『自举包输出目录已存在，请指定新目录以保留旧产物』否则
  虑「自己」者「真实路径」于（「获取程序名」于「元」）而
  虑「编译器参数」者若「串相等」于「编译器」于『』则「罄」授以「字符串」否则「列」【『--编译器』，「编译器」】而
  虑「无」者「态射」授以（「字符串」合「字符串」）授以「有」于
    （「列」【（『豫言编译器』与『编译器』），（『豫构』与『豫构』）】）于（会「项」而
      虑「状态」者「操作系统」之「子进程」之「运行并传递输出」于「自己」于
        （「附加」授以「字符串」于（「列」【『构建』，（「项」中「零」）】）于
          （「附加」授以「字符串」于「编译器参数」于（「列」【『--』，『--emit-bootstrap-bundle』，（「路径合并」于「输出」于（「项」中「一」））】）））而
      若「等于」于「状态」于「零」则「元」否则「发生事故」授以「有」于（『导出稳定种子失败：』附（「项」中「零」）））而
  虑「脚本」者「路径合并」于「输出」于『构建.sh』而
  虑「无」者「写文件」于「脚本」于『#!/bin/sh
set -eu
cd "$(dirname "$0")"
./编译器/构建.sh
./豫构/构建.sh
cp 编译器/yy种子 yy_bs_stable
cp 豫构/yy种子 yy豫构_stable
./yy_bs_stable --version
./yy豫构_stable -h
』而
  虑「无」者「写文件」于（「路径合并」于「输出」于『种子格式』）于『豫言双种子一「：换行：」』而
  虑「无」者「写文件」于（「路径合并」于「输出」于『自述.汉语.md』）于『# 豫言双种子发布包

运行 `./构建.sh`，生成 `yy_bs_stable` 和 `yy豫构_stable`。将两者安装在源码仓库根目录，随后运行 `make 连续自举 豫构`。整个源码构建链均通过豫构；不支持旧的单种子引导。
』而
  虑「无」者「写文件」于（「路径合并」于「输出」于『自述.文言.md』）于『# 豫言双种

行 `./构建.sh`，成编译器与豫构两种，置源码之根。继行 `make 连续自举 豫构`，诸级皆循包制，不复旧途。
』而
  虑「状态」者「操作系统」之「子进程」之「运行并传递输出」于『chmod』于（「列」【『u+x』，「脚本」】）而「状态」也。
