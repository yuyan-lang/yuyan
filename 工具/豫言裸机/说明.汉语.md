# 豫言双架构裸机原型

## 当前系统总览（先看这里）

豫言操作系统现在是一个可以启动、可以交互、有持久文件系统的单核系统，ARM64 与 AMD64 都在 QEMU 里通过自动验收：

| 层 | 内容 | 文档 |
|---|---|---|
| 内核 | 手写机器码（由豫言源生成）：页权限隔离、定时抢占、两个任务槽的创建／等待／终止／回收／复制、单条整数消息邮箱与阻塞接收、动态内存块（线性内存可增长）、设备窗口与 IOMMU 隔离、Virtio 设备的用户态访问、实时钟（[墙钟服务](墙钟服务.汉语.md)：ARM64 读 PL031、AMD64 读 CMOS） | 本目录各专题文档 |
| 用户态执行器 | `客体二号`：用底层模式的普通豫言写成的 WasmGC 解释器（栈机、保守标记清扫回收、异常、尾调用、软浮点）；在用户态运行普通豫言程序 | [客体二号](客体二号.汉语.md) |
| 文件系统 | 用户态 Virtio 块驱动 + 双区追加日志的持久文件系统（多层目录、崩溃安全的提交扇区、紧缩），离线造盘工具 `yy造盘` | [客体二号](客体二号.汉语.md)、[文件系统提交日志](文件系统提交日志.汉语.md) |
| 壳 | 普通豫言写的默认命令壳（`壳/壳。豫`）：目录、切换、列、读、写、追加、复制、移动、建、删、运行、脚本、加、回显、设、取消、环境；命令输出可用 `>`、`>>` 写入文件，`运行` 可用 `< 文件` 重定向标准输入、`甲 \| 乙 \| 丙` 串成顺序管道；子进程里也能读写文件（由壳代办）| [客体二号](客体二号.汉语.md) 的验收一节 |
| 进程 | 同步子进程：复制任务，子任务把磁盘上的程序换进执行器，父任务等待退出码；标准输入重定向、环境变量随复制交给子任务 | 同上“子进程”“标准输入重定向、环境变量与顺序管道”两节 |
| 外来程序 | 别的编译器（wasi-sdk 的 C 等）产出的命令行程序不经转换就能在壳里 `运行`：执行器实现了 WASI preview1 的大半（命令行、环境变量、标准流、文件与目录、时钟、随机数、睡眠、`proc_exit`），二十二个示例（你好、猫、字数、大写、排序、找、日历……）在两个架构的 QEMU 里逐字验证 | [WASI示例](WASI示例.汉语.md)、[客体二号](客体二号.汉语.md) 的“WASI preview1 子集”一节 |
| 动态装载 | 多实例：运行中从盘上装载 Wasm 模块，跨模块导入导出与双向回调（含延迟链接） | 同上“多实例”一节 |
| 数值 | 小数运算走 IEEE 754 软浮点（加减乘除、转换、定点六位转串、串转小数）；Wasm 的 f32/f64 全部指令（比较、算术、取整、开方、转换、饱和截断）同样由软浮点实现，逐位对照 V8 | 同上“软浮点”“Wasm 浮点指令”两节 |

**构建并运行**（在仓库根目录、已安装 QEMU 时）：

```sh
./yy豫构 文件 工具/豫言裸机/豫言系统。豫 --编译器 ./yy4_bs --输出 yy豫言系统
YY_GC_INITIAL_HEAP_SIZE_MB=1024 ./yy豫言系统 运行 --架构 兼容六十四      # 交互；壳里输入“退出”结束
YY_GC_INITIAL_HEAP_SIZE_MB=1024 ./yy豫言系统 构建 --预置 某目录树       # 只造镜像与盘并打印 QEMU 命令
```

`--预置` 指向的目录树整棵放进盘里；树里以 `。豫` 结尾的文件先编成程序（`/程序/你好。豫` 成 `/程序/你好.wasm`），以 `.低阶库` 结尾的标记文件构建同名底层包（`/库/库甲.低阶库` 成 `/库/库甲.wasm`），壳里用 `运行 /程序/你好.wasm 参数…` 启动，普通豫言程序可用 `豫言_加载模块`／`豫言_调用导出` 装载并调用库。用法见 `工具/豫言裸机/豫言系统。豫`；可运行的示例程序（你好、回声、质数、斐波那契、统计、复制文件、行数、环境变量）见 [示例](示例.汉语.md)，用 C 写成的 WASI 程序见 [WASI示例](WASI示例.汉语.md)。

**验收**：

```sh
./yy豫构 文件 工具/豫言裸机/普通豫言验证。豫 --编译器 ./yy4_bs --输出 yy普通豫言验证
YY_GC_INITIAL_HEAP_SIZE_MB=1024 ./yy普通豫言验证                       # 42 项 × 双架构，约 10 分钟；--只验 普通-壳- 只跑一类
```

覆盖：普通豫言程序（打印、列表、闭包、异常、回收压力、小数、浮点指令、表指令）、壳（基本、文件命令、运行子进程、子进程的文件、输出重定向、脚本、示例程序、环境变量与管道、WASI 外来程序（含文件与目录）、子程序崩溃隔离、磁盘上的大程序）、语言测试集 `测试/网页汇编` 的十二例、磁盘（跨重启读写、离线造盘）、动态装载（含回收压力）、内核服务探针。内核本身另有一套 `yy裸机验证`（下文，三百余项）。

**当前限制**：内核只有两个任务槽，所以一次只能有一个子进程、子进程不能再起子进程，管道也只能是顺序的（各段一个接一个运行，前一段必须自己结束）；子进程的文件操作由初始任务经邮箱代办（每条消息三个字节，量大很慢）；实时钟只有秒的精度与 UTC；小数精确表示未实现；跨实例只传数值；没有 SIMD。

已实现 ARM64、AMD64 的 QEMU 裸机启动验证。标准 Wasm 整数核心模块经过自有提前编译后，在真实用户态执行；最小特权核心提供页权限、同步异常入口、定时中断和受控服务。机器字节、重定位、启动头和页表全部由豫言生成，没有汇编源文件，也不调用外部汇编器或链接器。

内核是单核微内核：两任务池的运行时创建、等待、终止、回收、复制及消息通信，见[双任务调度](双任务调度.汉语.md)；不指定输入模块时，生成的镜像默认启动早期的整数命令壳（[用户命令壳](用户命令壳.汉语.md)）。以下各节描述内核的最小验证器 `yy裸机验证`：直接把标准 Wasm 整数模块提前编译成机器码运行，不经用户态执行器。普通豫言程序（完整 WasmGC）、持久文件系统、以普通豫言写的壳、子进程与动态装载都在用户态执行器 `客体二号` 上，见上文总览，各阶段实测证据见验证记录。

## 一条命令验证

在仓库根目录、安装 QEMU 后运行：

```sh
./yy3_bs 工具/豫言裸机/验证。豫 -o yy裸机验证
./yy裸机验证
```

验证器使用豫言异步子进程，每项约三十秒等待预算；超时按本轮独立进程号文件终止模拟器。全部镜像、输入模块和串口日志保存到新建临时目录，最后打印路径。测试用到宿主的 `mktemp`、`dd` 和 `/bin/kill`；`dd` 只用于负对照改动页表或中断标志，不参与正常镜像生成。

最初的六项基础检查如下；当前验证器另包含定时器、抢占、结构控制、存储、算术及外部模块等扩展用例，具体数量由运行结果计算：

| 检查 | 客体必须发生的行为 |
|---|---|
| 正常 | 在用户态完成函数调用、局部更新、溢出回绕及乘法核验，然后正常退出 |
| 隔离 | 完成同样核验后，读取内核私有页，产生权限异常；核心核对异常来源及地址 |
| 错误结果 | 将预期四十二改为四十三，核心必须报失败 |
| 放开权限 | 只改受测页表的用户访问位；探测不再陷入，验证必须报失败 |
| 操作数栈越界 | 保持 Wasm 类型正确，压入超过客体栈限额的值；运行时必须报失败 |
| 调用栈越界 | 有效模块进入递归，调用帧越界前必须报失败 |

检查同时匹配退出码和串口文字；负对照禁止出现成功标记。AMD64 使用模拟退出设备：通过退出码为三十三，拒绝为三十五。ARM64 使用半主机退出：通过为零，拒绝为一。串口输出都来自客体实际访问设备；退出机制仅用于结束模拟，不承担程序计算或打印。

## 单独生成镜像

```sh
./yy3_bs 工具/豫言裸机/入口。豫 -o yy豫言裸机
./yy中文网页汇编 编码 工具/豫言裸机/启动验证.网页汇编。豫 yy裸机客体.wasm
./yy豫言裸机 臂六十四 yy裸机客体.wasm yy臂正常.elf 正常
./yy豫言裸机 兼容六十四 yy裸机客体.wasm yy兼容正常.img 正常
```

末参数可改为「隔离」，生成附带用户态内核页读取探针的验证镜像。探针是平台验证桩，不是私有 Wasm 指令。正常镜像不包含该探针。生成器只编译输入，不预先运行输入程序；计算结果在模拟的 CPU 上形成。

ARM64：

```sh
qemu-system-aarch64 -machine virt,gic-version=2,virtualization=off,secure=off \
  -cpu cortex-a53 -accel tcg -m 128M -smp 1 -display none -monitor none \
  -serial stdio -no-reboot -semihosting-config enable=on,target=native \
  -kernel yy臂正常.elf
```

AMD64：

```sh
qemu-system-x86_64 -machine pc -accel tcg -m 128M -smp 1 \
  -display none -monitor none -serial stdio -no-reboot \
  -device isa-debug-exit,iobase=0xf4,iosize=0x04 -kernel yy兼容正常.img
```

正常串口输出：

```text
豫言核心启动
用户程序已运行
用户态计算验证通过
```

## 模块契约与实现边界

输入必须导出无参数、无结果的「启动」。只允许以下函数导入，名字和签名均检查：

| 模块与名称 | 参数、结果 | 实现 |
|---|---|---|
| 平台／写字节 | 一个三十二位整数，无结果 | 经系统调用访问串口，检查字节范围 |
| 平台／核验 | 两个三十二位整数，无结果 | 比较实值与期值，失败立即停止验证 |
| 平台／用户态 | 无参数，一个三十二位结果 | 核心先检查保存的硬件用户态状态，才返回一 |
| 平台／读字节 | 无参数，一个三十二位结果 | 非阻塞读取串口，无数据返回负一 |
| 平台／时钟滴答 | 无参数，一个三十二位结果 | 返回已处理的定时中断计数 |
| 平台／任务编号 | 无参数，一个三十二位结果 | 返回当前静态任务编号 |
| 平台／任务状态 | 一个三十二位参数、一个三十二位结果 | 查询运行状态位，无效编号返回负一 |

当前启动验证协议要求正常结束前恰好完成四次核验；这属于测试协议，不是通用系统调用标准。不能将这些宿主接口误称为 WAT 规定的操作系统接口：模块导入及类型是标准的，平台服务语义由本项目定义。

函数参数、结果置于独立的非 GC 操作数栈，局部变量置于真实调用帧。两套后端均支持整数核心的所有现有指令。操作数栈限一百二十八千字节，调用栈约一百二十八千字节，二者有间隔；压值和分配调用帧时检查边界，超限进入失败路径。函数参数与附加局部合计至多一百二十八项；用户代码至多一兆字节。尚未做寄存器分配或跨函数栈检查消除，不据此宣称接近 C 性能。

ARM64 以自有 ELF64 头装载，在一级异常层安装页表和异常向量，通过异常返回进入零级用户态。AMD64 以多重引导首版地址头装载，从三十二位保护模式切换到六十四位长模式，安装页表、全局描述符、任务状态段及中断门，再进入第三特权环。QEMU 提供镜像装载入口；尚无磁盘启动扇区或实机固件移植。

两端采用静态两兆字节页映射。AMD64 核心位于低两兆，用户域位于二至四兆；ARM64 核心位于 `0x40000000` 区域，用户域位于 `0x40200000`，另有 `0x40400000` 私有受测页。串口只由特权核心访问。用户代码与用户栈尚在同一可读写用户映射中，未实现页级写入／执行互斥；这些限制不影响当前内核页隔离检查，但还不足以成为不可信程序的生产沙箱。

镜像生成阶段可以在无工具搜索路径时运行。生成器自身目前仍由既有豫言编译器和运行库构建；通过 Wasm 执行生成器也仍借助现有 Wasmtime 宿主。不能把客体无 LLVM/libc 依赖等同于整个自举工具链已经独立。

## 代码与规范

- `库/编译器核心/裸机后端/机器编码。豫`：字节序、两遍标号解析和重定位。
- `整数降级。豫`：标准整数模块验证、函数提前编译和平台导入桩。
- `臂核心。豫`、`兼容核心。豫`：平台启动、保护状态、异常和镜像装配。
- `任务上下文。豫`、`任务调度。豫`：统一整数帧和静态任务切换。
- `验证。豫`：双架构正常及负对照回归。

规范依据：[QEMU 臂虚拟平台](https://www.qemu.org/docs/master/system/arm/virt.html)、[多重引导地址头](https://www.gnu.org/software/grub/manual/multiboot/html_node/Header-address-fields.html)、[Intel 系统与指令手册](https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html)、[Arm 异常层切换说明](https://developer.arm.com/community/arm-community-blogs/b/tools-software-ides-blog/posts/changing-exception-level-and-security-state-with-an-armv8a-fixed-virtual-platform)。本次环境版本及验证记录见同目录验证稿。

已新增用户态结构化分支、循环、跳转和返回，双架构二十项裸机检查通过，详[结构控制](结构控制.汉语.md)。后续定时中断和抢占进展见[定时中断](定时中断.汉语.md)与[双任务调度](双任务调度.汉语.md)。

新增非阻塞控制台读字节，双架构二十四项验证通过，详[控制台输入](控制台输入.汉语.md)。

最新已接入双任务的用户故障退出与退出码查询，见[故障隔离](故障隔离.汉语.md)。当前双架构验收为 138 项（包含可选外部 C 模块）；该报告亦记录了避免旧宿主编译缓存误验的冷构建方法。

阻塞等待和退出唤醒已接入，最新双架构 148 项通过，见[阻塞等待](阻塞等待.汉语.md)。

新增受所有权约束的任务终止，双架构 154 项通过，详[主动终止](主动终止.汉语.md)；动态创建和资源回收仍待实现。

已接入运行时任务复制，双架构 160 项通过，详[运行时创建](运行时创建.汉语.md)。当前是单槽静态内存池，尚无动态模块加载和资源复用。

当前已完成固定任务池中的创建、等待、终止、回收与安全复用，双架构 172 项通过，详[任务回收](任务回收.汉语.md)。通用多任务分配、IPC、动态程序加载、Shell 和持久化文件系统继续按总目标推进。

已加入受所有权和代际句柄约束的双向整数消息邮箱，双架构 180 项通过，详[任务通信](任务通信.汉语.md)。接收暂为非阻塞，后续继续扩展用户态服务所需能力。

阻塞消息接收、唤醒与对端退出处理已通过验收，双架构共 192 项，详[阻塞通信](阻塞通信.汉语.md)。通用调度、字节传输和用户态服务仍继续推进。

**默认镜像生成入口现为常规运行。** 省略第四参数或使用“运行”即可运行不含测试核验的程序；“正常”“隔离”仍保留旧验收含义。常规退出与完整退出码传递详[常规运行](常规运行.汉语.md)，双架构 210 项通过。这不是完整高级豫言或 WasmGC 的运行支持。
当前 IOMMU 工作已扩展到用户线性内存的连续页映射：首部、末部、上界、只读保护和用户态可见性均通过两架构验收，完整回归共 262 项。详[用户设备内存](用户设备内存.汉语.md)。默认用户态 Shell 已具备任务交互；生产 Virtio 块驱动、文件持久化、动态执行器和 WasmGC 仍未完成。
用户态 Virtio 寄存器访问和“设备驱动”构建方式已接入，两架构完整回归共 268 项通过，详[用户设备寄存器](用户设备寄存器.汉语.md)。设备初始化与内存授权由内核控制，用户程序已能协商特性并配置队列；磁盘队列传输、退出清理和文件持久化尚未完成。
用户态 Virtio 块读写、显式刷新与跨 QEMU 启动的数据保留已通过两架构验收，完整回归共 280 项，详[用户磁盘读写](用户磁盘读写.汉语.md)。文件目录、Shell 文件命令和设备服务进程化仍待实现。
文件系统的用户态提交日志已通过两架构恢复、未完成尾部、损坏拒绝和容量边界验证，完整回归共 294 项，详[文件系统提交日志](文件系统提交日志.汉语.md)。目录、文件名、删除、空间回收和 Shell 文件命令尚未完成。
用户态文件目录已支持创建、嵌套目录、查找枚举、内容替换、分段读取和删除，跨启动保持句柄代际，完整回归共 308 项。详[用户文件目录](用户文件目录.汉语.md)。Shell 文件命令、独立文件服务与日志磁盘空间回收仍待实现。
默认用户态 Shell 现已接入文件、目录、路径与显式格式化命令，同时保留任务及输入处理，完整回归共 314 项。详[命令壳文件操作](命令壳文件操作.汉语.md)。日志空间回收、独立文件服务、动态程序加载和 WasmGC 仍待完成。
设备持有者的自然、显式、陷阱和硬件缺页退出现已执行内核授权撤销、复位与 IOMMU 清理；子任务结束不误撤父设备。完整回归共 326 项，详[设备退出清理](设备退出清理.汉语.md)。随机在途压力、一般资源池、磁盘空间回收、动态加载和 WasmGC 仍待完成。

客体用户态的有界字节读取与三十二位变长整数解码已通过两架构验证，完整回归共 328 项；另以独立标准 Wasm 引擎验证并执行新增用例。详[客体字节读取](客体字节读取.汉语.md)。这只是动态加载的读取基础，尚未接入默认 Shell，不代表模块验证、动态执行或 WasmGC 已完成。

客体模块头、区段顺序与边界、UTF-8 名字和选段隔离已通过两架构测试，完整回归共 330 项。详[客体模块区段](客体模块区段.汉语.md)。索引仅描述区段位置，段内容、指令和实例化仍待验证与实现，尚不能加载执行外部程序。

客体整数函数索引已支持类型、函数导入、定义、局部声明、导出和启动签名检查，完整回归共 332 项。详[客体函数索引](客体函数索引.汉语.md)。代码仍待指令验证，非函数资源也尚未解析完整；索引成功不会授予执行许可。

客体内存、全局与数据段读取及独立工作区初置已通过两架构验收，完整回归共 334 项。详[客体资源初置](客体资源初置.汉语.md)。初置检查全部主动段后才复制数据；尚不执行函数，完整指令验证、实例化与动态执行仍待实现。

客体 i32 函数体已可解码为指令记录，检查结构与立即数并回填条件出口，完整回归共 336 项。详[客体指令解码](客体指令解码.汉语.md)。操作数栈验证与执行循环尚未实现，解码完成不授予执行许可。

客体 i32 函数体的操作数与控制栈验证已通过两架构验收，完整回归共 338 项。详[客体操作数栈](客体操作数栈.汉语.md)。四十四个模块预期与标准验证器一致；执行循环、运行时陷阱、模块全验和动态运行仍待完成。

用户态整数解释器已真正运行标准模块的控制、算术和直接调用，循环求和与递归阶乘均由客体算出，完整回归共 340 项。详[客体整数执行](客体整数执行.汉语.md)。内存执行、导入连接、默认 Shell 动态加载、跨模块互调与 WasmGC 仍待完成。

用户态解释器现可执行内存读写、增长、复制、填充、数据初始化与丢弃，完整回归共 342 项。详[客体内存执行](客体内存执行.汉语.md)。越界检查覆盖回绕、空操作和无部分写入；导入、默认 Shell 动态加载、链接与 WasmGC 仍待完成。

客体解释器已支持固定平台函数导入、按导出名查找、启动一次及默认空函数引用表；独立 C Wasm 原始字节实际运行并完成四次回调核验，完整回归共 348 项。详[客体导入与启动](客体导入与启动.汉语.md)。默认 Shell 文件加载、通用跨模块链接、WASI、间接调用和 WasmGC 仍待完成。

默认用户态 Shell 现支持从磁盘运行 Wasm 子任务、等待、运行中终止和退出码报告；重启后可再次加载，包括独立 C 模块。父 Shell 在子程序运行时仍可读写文件。完整回归共 352 项，四个默认镜像已更新，详[命令壳程序运行](命令壳程序运行.汉语.md)。通用任务与内存池、模块链接、WASI、间接调用、磁盘空间回收和 WasmGC 仍待完成。

2026-09-25：独立宽除函数使 ARM64 命令壳代码超出一 MiB 代码预算，凡以文件命令壳为基础的用例都将失败。ARM64 常量装载现在只在高十六位非零时才发 `MOVK`，文件命令壳缩小约七万字节，余量转正；新的[代码量诊断。豫](代码量诊断。豫)可随时核对两架构余量。两架构不带外部模块共 360 项通过；带外部模块的 368 项运行时观察到父子任务同写串口的间歇交错失败，详[臂常量装载](臂常量装载.汉语.md)。

控制台输出现由内核按行缓冲：每个任务在内核私有页蓄一行，遇换行、缓冲写满、读输入、任务退出或被终止时才一次写串口，一行输出不再被另一个任务的字节截断。两任务各写 3000 行的 `控制台行原子` 场景及 `--只验 名称` 选项已加入验证器，两架构共 362 项通过，带外部模块的 370 项连续通过，详[控制台按行原子输出](控制台行缓冲.汉语.md)。

2026-09-25：降级后端改为寄存器式：虚拟操作数栈映射到寄存器，常量与比较同后继指令融合，函数入口一次性检查调用栈。ARM64 命令壳代码从 978 KB 缩到 162,900 字节，AMD64 从 798,873 缩到 184,000 字节，为继续扩展执行器留出预算；两架构共 362 项（带外部模块 370 项）QEMU 验收全部通过。详[寄存器降级](寄存器降级.汉语.md)。

2026-09-25：内核新增内存块管理：ARM64／AMD64 各有 32 块×2 MiB 的物理池，服务二二 `内存块(n)` 为任务按需增加块，服务二三返回块数；`memory.grow` 超过八页时把线性内存迁入动态块并继续增长，任务退出、故障、被终止时归还，复制任务复制全部块，池尽时干净地失败。新增四个场景，两架构共 378 项（带外部模块）QEMU 验收全部通过。详[内存块管理](内存块管理.汉语.md)。
