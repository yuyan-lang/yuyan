寻观「标准库」之书。
「：文言：借宿主之外编译器造试件，以豫言验之。汉语：唯一参数是支持 wasm32 的 Clang 路径；此工具只构造互操作测试输入，再运行已构建的豫言参考执行和裸机验证器。：」
「执行」乃化「字符串」而化（「列」于「字符串」）而「有」也。
「执行」者会「程序」而会「参数」而虑「果」者「操作系统」之「子进程」之「获取输出」于「程序」于「参数」而
 若「果」中「零」则「打印行」于（「果」中「一」）否则「发生事故」于（「程序」附『：』附（「果」中「二」）附（「果」中「一」））也。
鉴「命令行参数」之「获取参数」于「元」而
 有（「列」【「编译器」】）则（
  虑「共参」者（（「列」于「字符串」）也（「列」【『--target=wasm32-unknown-unknown』，『-O1』，『-fno-builtin』，『-nostdlib』，『-Wl,--no-entry』，『-Wl,--strip-all』，『-Wl,--initial-memory=131072』，『-Wl,--max-memory=262144』，『工具/豫言裸机/外部模块夹具.c』】））而
  虑「正」者「执行」于「编译器」于（「附加」授以「字符串」于「共参」于（「列」【『-o』，『yy外部C模块.wasm』】））而
  虑「负」者「执行」于「编译器」于（「附加」授以「字符串」于「共参」于（「列」【『-D错误期望=1』，『-o』，『yy外部C错误.wasm』】））而
  虑「解释」者「执行」于『./yy外部模块测试』于「罄」而
  「执行」于『./yy裸机验证』于（「列」【『--外部模块』，『yy外部C模块.wasm』，『yy外部C错误.wasm』】））
 或有「其他」则「发生事故」授以「有」于『用法：提供支持网页汇编目标的外部编译器路径』。
