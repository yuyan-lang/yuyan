# 完整类型栈

验栈存完整型与验时未知型，兼记高度、帧底、不可达。唯不可达而至本帧底者可取未知；已有实值仍须合型。未知无二进制值型编码，非运行对象。

诸值逆序取、顺序压。入块先于父取参，立新底，压声明型；新块不承父之不可达记。出块验父子底、果型及余值，复父态而压果。断流唯弃本帧之上，不损下层。

十二运行试、验型、构建皆成；另五标准模块之不可达与帧界分类同 Node。日志存 `/tmp/yy完整类型栈检查日志`、`/tmp/yy完整类型栈构建日志`、`/tmp/yy完整类型栈运行日志`。

此为指令验型之基，诸指令、标签、异常与确定赋值全验未备，未接裸机执行，默认镜像不变。
