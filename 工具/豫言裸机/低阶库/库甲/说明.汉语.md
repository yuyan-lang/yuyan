# 库甲

动态装载的验用库（底层模式）。由客体执行器二号在运行时按路径装载，实例名取装载时给的名字（验收里用 `库甲`）。

- 导出：`加(甲, 乙)`、`阶乘(数)`、`经乙求和(数)`、`回调二倍(数)`；
- 导入：`库乙.累加`。库甲先于库乙装载，所以这个导入在装载时找不到，首次调用 `经乙求和` 时才链接（延迟链接）；库乙的 `累加` 又回调库甲的 `回调二倍`，构成双向调用。

构建：`./yy豫构 构建 库甲 --输出 /tmp/yy库甲.wasm`。验收见 `工具/豫言裸机/普通探针/盘/动态-一。豫`。
