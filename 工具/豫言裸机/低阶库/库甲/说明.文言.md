# 库甲

动态装载之验用库（底层模式）。客体执行器二号于运行时依路径装之，实例之名取装载时所授（验收用 `库甲`）。

- 导出：`加(甲, 乙)`、`阶乘(数)`、`经乙求和(数)`、`回调二倍(数)`；
- 导入：`库乙.累加`。库甲先于库乙而装，故此导入于装载时无所得，至初召 `经乙求和` 乃链（延迟之链）；库乙之 `累加` 复回调库甲之 `回调二倍`，成双向之调。

构：`./yy豫构 构建 库甲 --输出 /tmp/yy库甲.wasm`。验见 `工具/豫言裸机/普通探针/盘/动态-一。豫`。
