寻观「标准库」之书。
「：文言：表指令之验：装载十二个自生之客模块，各行十余条 table.init、elem.drop、table.copy、table.grow、table.fill、table.get/set/size，而返其表之校验和，与 V8 所算者相较。汉语：表指令端到端验收（普通豫言驱动）：装载 /库/表0.wasm … /库/表11.wasm（由 工具/豫言裸机/客体二号/表指令对照.cjs --造探针 生成，每个模块两张 funcref 表、五个元素段、导出 main，顺序执行 14–21 条随机表指令后遍历两张表用 call_indirect 累积校验和），逐个调用 main 并打印“序号 返回值”；期望取自在 V8 里运行同一模块，所以两个架构上执行器的表指令必须与 V8 语义一致才会通过。实例总数至多 16，故只有 12 个模块。：」
「加载模块」乃化「字符串」而化「字符串」而「整数」也。
「加载模块」者会「路径」而会「名」而《《C调用》》名『豫言_加载模块』传「路径」与「名」也。
「调用零」乃化「整数」而化「字符串」而「整数」也。
「调用零」者会「实例」而会「名」而《《C调用》》名『豫言_调用导出』传「实例」与「名」也。
「一个」乃化「整数」而「有」也。
「一个」者会「序」而
 虑「路」者『/库/表』附（「整数表示」于「序」）附『.wasm』而
 虑「实例」者「加载模块」于「路」于（『表』附（「整数表示」于「序」））而
 「打印行」于（（「整数表示」于「序」）附『 』附（「整数表示」于（「调用零」于「实例」于『main』）））也。
「逐个」乃化「整数」而「有」也。
「逐个」者会「序」而
 若「小于」于「序」于「一二」
 则（（「一个」于「序」）；（「逐个」于（「加」于「序」于「一」）））
 否则「元」也。
「逐个」于「零」。
