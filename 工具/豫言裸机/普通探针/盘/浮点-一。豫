寻观「标准库」之书。
「：文言：浮点指令之验：装载自生成之客模块，其六十六导出各遍历一千二十四项输入而累其果之位为校验和，与 V8 所算者相较。汉语：浮点指令端到端验收（普通豫言驱动）：装载 /库/浮点.wasm（由 工具/豫言裸机/客体二号/造浮点模块.cjs 生成，导出 op0…op65 对应 f32、f64 的全部比较、算术、取整、开方、转换与饱和截断指令，序号见 造浮点模块.cjs --名单），对每个导出遍历 1024 项输入表（f64、i64、f32、i32 的位型），打印“序号 校验和”；期望文件来自在 V8 里运行同一模块，所以只有两个架构上执行器的软浮点与 V8 逐位一致才会通过。：」
「加载模块」乃化「字符串」而化「字符串」而「整数」也。
「加载模块」者会「路径」而会「名」而《《C调用》》名『豫言_加载模块』传「路径」与「名」也。
「调用一」乃化「整数」而化「字符串」而化「整数」而「整数」也。
「调用一」者会「实例」而会「名」而会「甲」而《《C调用》》名『豫言_调用导出』传「实例」与「名」与「甲」也。
「逐个」乃化「整数」而化「整数」而「有」也。
「逐个」者会「实例」而会「序」而
 若「小于」于「序」于「六六」
 则（（「打印行」于（（「整数表示」于「序」）附『 』附（「整数表示」于（「调用一」于「实例」于（『op』附（「整数表示」于「序」））于「一零二四」））））；（「逐个」于「实例」于（「加」于「序」于「一」）））
 否则「元」也。
虑「实例」者「加载模块」于『/库/浮点.wasm』于『浮点』而
「逐个」于「实例」于「零」。
