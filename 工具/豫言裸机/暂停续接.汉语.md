# 暂停续接

当前进度（2026-09-23）：用户已恢复 OS 目标，正在持续开发。程序播种镜像改用主动数据段；客体索引已分别记录函数参数中的 `eqref` 和 `funcref`，保留 `eqref` 结果编码，仍以 `-2` 拒绝未支持的 GC 执行。普通豫言最小模块的 297 字节真实类型段现已在双架构裸机中完整扫描并记录 33 个类型；368 项 QEMU 验收重新通过。3,186 字节模块可经 Shell 文件路径装入，但尚不能执行。另有 `i31` 内部编码函数通过豫言及标准 Wasm 引擎测试，未接客体指令。详见[程序镜像主动数据](程序镜像主动数据.汉语.md)、[客体等引类型记录](客体等引类型记录.汉语.md)、[客体复合类型索引](客体复合类型索引.汉语.md)、[客体小整编码](客体小整编码.汉语.md)及验证记录.json。下一项应对已保存的字段、宽值和具体引用做完整类型验证，补递归组与子类型，再接操作数、局部、堆及 `i31` 指令；不得把现有 368 项验收解释为普通豫言 WasmGC 已运行。

2026-09-23 用户已要求恢复操作系统开发。本页以下暂停状态保留为历史快照；恢复后的第一项工作见[启动与内存声明](启动与内存声明.汉语.md)。

2026-09-23 按用户要求暂停，未完成操作系统总目标。暂停后不自动继续；待用户恢复。

当前最后完成数据段与 GC 数组数据指令静态验证。20 个数据数组测试、20 个捕获回归通过，类型检查与命令构建通过。yy优化器三代.wasm 与 yy栈默认回归GC重复索引边界.wasm 的当前支持范围内全部函数指令验证通过；模块未执行，资源未全验。这不能作为普通豫言已在裸机运行的证明。

下一步原拟完整内存声明、读写指令及启动函数约束，尚未修改代码。仍需元素段、主动数据、完整资源验证，以及客体完整类型执行器、异常传播、真实 GC 回收和普通豫言 Shell 接入。不得仅靠继续添加静态测试宣称 OS 完成。

最后裸机验收记录是调用帧容量扩展的 368 场景，见验证记录.json；最近静态验证未改变默认镜像。日志目录 /tmp/yy裸机验证.SiUBMH。构建使用 ./yy设备配置编译器，避免使用陈旧 ./yy。新静态验证入口为工具/中文网页汇编/入口。豫。

本次提交不纳入已有的多态列过滤改动、对应长列测试和性能对照报告。临时日志可能消失，正式源码、测试及双语阶段文档随提交保存。恢复后先核查工作树和验证记录，不将历史通过当成新修改的验收。
