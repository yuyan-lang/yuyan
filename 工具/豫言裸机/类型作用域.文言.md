# 类型作用域

递归组先立诸名，同组可相引、自引；异组唯见此前与本组。检函数参数、结果、结构域、数组域中具体堆号。

父必先于子，且至多一父；终型不可继承，父子复合类别须同。空组不增编号。此仅全验之前提，字段方差、函数参数逆变、结果协变与递归等价尚待，不得独授执行之权。

十五类型段由二进制读而验，含同组相引、异组前引、参数结果越界、终型为父、自继承、向后继承、异类与多父。验型、构建、运行皆成，与 Node 标准引擎分类亦同。

日志存 `/tmp/yy类型作用域检查日志`、`/tmp/yy类型作用域构建日志`、`/tmp/yy类型作用域运行日志`。尚未接裸机类型段加载，默认镜像不变。
