# 双架构用户态结构控制

两后端今接块、循环、条件、否则、结束、跳转、条件跳转、返回及陷阱。豫言自定标签、自书机器字节，不借汇编源与外汇编器。

编译时记帧与栈高，循环标其始，余块标其终，返回归共同收尾。条件先出栈，非零取真。不可达仍验其法，降级可省其基本令。

跳转若须弃中间值，先移所留之果，后下栈指。移自低址至高址，可容重叠及多果；不置私缓冲，不易函数接口，旧栈限仍守。

新增控制正负试，两架构共二十试。用户态累加一至万得五千万五千；跨块弃九而留四十二，与外值七相加得四十九；子函数弃九十九而返一二，相减得负一；嵌套条件避未选之陷阱而得四十二。负试故改期值，核心必报败。

此仍单用户域原型。解释器预算不自及提前编译之机器码；裸机未具定时抢占及故障任务回收，死循环犹可独占处理器。默认用户态壳、输入、文件及完整回收尚待成。

二十试皆过，产物与串口录存 `/tmp/yy裸机验证.qxw30e`。标准引擎四核验亦合，误期值则拒；机器编码、重定位与诸型检查皆过。

二〇二六年九月二十五日：本文所述以内存栈为本之降，已为[寄存器降级](寄存器降级.文言.md)所代；旧之内存编码及其试皆已除。所述行为之规与 QEMU 之验不易，两机共三百六十二项（并外部模块三百七十项）悉过。
