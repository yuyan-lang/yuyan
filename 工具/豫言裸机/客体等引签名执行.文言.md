# 客体等引函数之署

今容函数之参、独果为等引。署之参高十六位记其型；验栈于调用、归果、取参、置参、复置参皆据之。行时仍循旧引值与调用帧；平台整数之约未改。

`等引签名正.wasm` 于既有外来模块之启动中增等引恒等函数，令小整引与空等引各经一调用，终仍行旧平台核验。此模块已由 `WebAssembly.Module` 验其合式。双架构裸机试令见同名汉语文档。

是役止于函数之署。等引局部声明、宽数型及普通豫言最小模块所需余指令，犹待续治。
