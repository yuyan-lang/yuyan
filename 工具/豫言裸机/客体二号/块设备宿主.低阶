; 块设备（调试变体）：由宿主（Node）提供 平台/块容量、块读、块写；线性内存偏移直接交给宿主
(导入 块容量宿 "平台" "块容量" 0 1)
(导入 块读宿 "平台" "块读" 2 1)
(导入 块写宿 "平台" "块写" 2 1)
(函数 块就绪 () (返回 (> (块容量宿) 0)))
(函数 块容量 () (返回 (块容量宿)))
(函数 块读 (扇区 偏) (返回 (块读宿 扇区 偏)))
(函数 块写 (扇区 偏) (返回 (块写宿 扇区 偏)))
(函数 块刷新 () (返回 0))
