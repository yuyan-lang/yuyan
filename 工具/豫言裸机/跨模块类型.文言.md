# 跨模块函数类型

提供、使用各循己境。先验共享表前缀同，再验所指皆函数，后比身份而循提供者父链，以合使用者之求。不以彼号索此表。

表可为同一扩展链之异时快照；独立表同号异构则拒，分叉不合亦拒，当重登于共表。此层唯匹配类型，未绑定函数实现，未运行跨模块调用。

七试验移号等价、子供父求、逆向拒绝、异签名、非函数、独立表碰撞与旧快照，验型构建运行皆成。另以 Node 实例化提供者与使用者，子型可绑，i64/i32 参数异则链接败；此为规范对照，非豫言裸机之验收。

日志存 `/tmp/yy跨模块类型检查日志`、`/tmp/yy跨模块类型构建日志`、`/tmp/yy跨模块类型运行日志`。完整接口描述、实例绑定及客体加载尚待接入，默认镜像不变。
