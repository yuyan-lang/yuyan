寻观「标准库」之书。
「：文言：环境变量：依名而查其值。汉语：示例程序 环境变量：对每个命令行参数当作环境变量名，打印它的值（没设置则说明未设置）；环境变量由壳的 设 命令给出。用法：设 问候 你好，再 运行 /程序/环境变量.wasm 问候。：」
「查一个」乃化「字符串」而「有」也。
「查一个」者会「名」而
 虑「果」者「环境变量」之「获取环境变量」于「名」而
 若「果」中「零」则「打印行」于（「名」附『 = 』附（「果」中「一」））否则「打印行」于（「名」附『 未设置』）也。
「逐个」乃化（「列」于「字符串」）而「有」也。
「逐个」者会「们」而鉴「们」而
 有「罄」则「元」
 或有「缀」于「首」于「尾」则（虑「无」者「查一个」于「首」而「逐个」于「尾」）也。
「逐个」于（「命令行参数」之「获取参数」于「元」）。
