寻观「标准库」之书。
「：文言：行数：读标准入至终，逐行印其序与文，末印总数。汉语：示例程序 行数：从标准输入逐行读到文件结束，打印每行的序号与内容，最后打印总行数；输入由 < 重定向或管道提供（在控制台上则要一直读，没有结束）。用法：运行 /程序/行数.wasm < /数据/文本.txt。：」
「读逐行」乃化「整数」而「有」也。
「读逐行」者会「序」而
 虑「果」者「尝试读取标准输入行」于「元」而
 若「果」中「零」
 则（虑「无」者「打印行」于（（「整数表示」于（「加」于「序」于「一」））附『：』附（「果」中「一」））而「读逐行」于（「加」于「序」于「一」））
 否则「打印行」于（『共 』附（「整数表示」于「序」）附『 行』）也。
「读逐行」于「零」。
