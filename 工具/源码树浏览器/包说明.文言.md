# 源码树浏览器

发布可用“构建源码 源码目录 输出目录”，惟览所命之根，饰文取于当前目录。语法不合者转义为纯文，仍可读。发布者先依包界备副本，不混所依；旧“构建”之令不改。

生成可浏览的源码静态网站。

## 籍与用

所有者曰豫言，版本暂定 0.1.0。此包之版本，非编译器命令所报之版本。

此包不供他包所依，包内诸书仍可相引。程序名曰 `yy源码树浏览器`，入口依包籍所载。

在仓库之根行 `./yy豫构 检查 源码树浏览器` 以验其籍。行 `./yy豫构 构建 源码树浏览器` 以成其器，不自行之。

所依与原生组成皆见[包籍](源码树浏览器。包。豫)。变接口则修说明，变身份与组成则修包籍。说明不作模块。
