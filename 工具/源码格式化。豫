「：
用法：yy格式化 最大行宽 文件路径【文件路径……】

所有文件会先完成带注释语法解析与排版；全部成功后才会原地写回。
最大行宽按统一码字符数计算，单个不可拆分的标识符、字符串或注释可以超过此值。
：」

寻观「标准库」之书。
寻观「标准库」之「操作系统」之「命令行参数」之书。
寻观「标准库」之「操作系统」之「文件系统」之书。
寻观「标准库」之「操作系统」之「进程」之书。
寻观「工具」之「源码排版」之书。


「退出格式化」乃化「字符串」而化「整数」而「有」也。
「退出格式化」者会「消息」而会「状态」而
    「标准错误打印行」于「消息」；
    「退出」于「状态」；
    「发生事故」于「消息」
也。

「行宽文字有效」乃化「字符串」而「爻」也。
「行宽文字有效」者会「文字」而
    「且」于
        （「非」于（「字符串相等吗」于「文字」于『』））
        于（「全部符合」授以「字符串」于（会「字符」而「是子字符串吗」于「字符」于『0123456789』）于（「获取字符」于「文字」））
也。

「待写文件」即「字符串」合「字符串」也。

「生成待写文件」乃化「整数」而化（「列」于「字符串」）而「列」于「待写文件」也。
「生成待写文件」者会「最大行宽」而会「路径们」而
    鉴「路径们」而
        有「罄」则「罄」
        或有（「缀」于「文件路径」于「尾」）则
            虑「源码」者「读文件」于「文件路径」而
            虑「结果」者「格式化源码」于「文件路径」于「最大行宽」于「源码」而
            「缀」于（「文件路径」与「结果」）于（「生成待写文件」于「最大行宽」于「尾」）
    也。

「写回格式化文件」乃化（「列」于「待写文件」）而「有」也。
「写回格式化文件」者会「文件们」而
    鉴「文件们」而
        有「罄」则「元」
        或有（「缀」于「当前」于「尾」）则
            虑「无」者「写文件」于（「当前」中「零」）于（「当前」中「一」）而
            「写回格式化文件」于「尾」
    也。


鉴「获取参数」于「元」而
    有（「缀」于「行宽文字」于（「缀」于「首路径」于「尾路径」））则
        若「行宽文字有效」于「行宽文字」则
            虑「最大行宽」者「理解整数」于「行宽文字」而
            若「小于」于「最大行宽」于「一零」
            则「退出格式化」于『最大行宽不可小于十』于「二」
            否则「写回格式化文件」于（「生成待写文件」于「最大行宽」于（「缀」于「首路径」于「尾路径」））
        否则「退出格式化」于『最大行宽必须是十进制正整数』于「二」
    或有「其他」则「退出格式化」于『用法：yy格式化 最大行宽 文件路径【文件路径……】』于「二」
。
