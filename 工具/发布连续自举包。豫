「：
生成、验证并发布供 GitHub Actions 使用的连续自举包。

本工具只允许从已推送且无已跟踪改动的 yybs 分支发布。它会：

一、使用现有稳定编译器完成三阶段自举并检查固定点；
二、生成并在本机重新构建可移植自举包；
三、下载当前远端资产作为回滚备份；
四、替换 continuous-bootstrap release 中的同名资产；
五、重新下载已发布资产并逐字节核验。

用法：

    ./yy_bs_stable 工具/发布连续自举包。豫 -o 发布连续自举包
    ./发布连续自举包 --publish

临时目录不会自动删除，其中保留发布前的远端资产，可用于手工回滚。
：」

寻观「标准库」之书。
寻观「标准库」之「语言核心」之「异常」之书。
寻观「标准库」之「数据结构」之「字符串术」之书。
寻观「标准库」之「数据结构」之「多态列」之书。
寻观「标准库」之「操作系统」之「命令行参数」之书。
寻观「标准库」之「操作系统」之「文件系统」之书。
寻观「标准库」之「操作系统」之「路径」之书。
寻观「标准库」之「操作系统」之「进程」之书。
寻观「标准库」之「操作系统」之「子进程」之书。
寻观「标准库」之「输入输出」之「笔」之书。


「去除末尾换行」乃化「字符串」而「字符串」也。
「去除末尾换行」者会「内容」而
    若「串结尾是它吗」于『「：换行：」』于「内容」
    则「获取子字符串」于「零」于（「减」于（「串长度」于「内容」）于「一」）于「内容」
    否则「内容」
也。

「命令失败文字」乃化「字符串」而化（
    「爻」「：退出状态：」合
    「字符串」「：标准输出：」合
    「字符串」「：错误输出：」
）而「字符串」也。
「命令失败文字」者会「说明」而会「结果」而
    「说明」附『「：换行：」标准输出：』附（「结果」中「一」）
        附『「：换行：」错误输出：』附（「结果」中「二」）
也。

「取得命令输出」乃化「字符串」而化（「列」于「字符串」）而「字符串」也。
「取得命令输出」者会「程序」而会「参数」而
    虑「结果」者「获取输出」于「程序」于「参数」而
    若「结果」中「零」
    则「去除末尾换行」于（「结果」中「一」）
    否则「发生事故」于（「命令失败文字」于（『命令执行失败：』附「程序」）于「结果」）
也。

「运行发布步骤」乃化「字符串」而化「字符串」而化（「列」于「字符串」）而「有」也。
「运行发布步骤」者会「说明」而会「程序」而会「参数」而
    （
        「打印行」于（『==> 』附「说明」）；
        「成功运行并打印结果」于「程序」于「参数」；
        「元」
    ）
也。

「要求仓库状态适合发布」乃化「有」而「字符串」也。
「要求仓库状态适合发布」者会「无」而
    虑「无」者若「且」于
        （「路径是普通文件」于『豫言编译器/入口。豫』）
        于（「路径是文件夹」于『.git』）
    则「元」
    否则「发生事故」于『请从豫言仓库根目录运行本工具』而
    虑「分支」者「取得命令输出」于『git』于
        （「缀」于『branch』于（「缀」于『--show-current』于「罄」））而
    虑「无」者若「串相等」于「分支」于『yybs』
    则「元」
    否则「发生事故」于（『只允许从 yybs 分支发布，当前分支为：』附「分支」）而
    虑「无」者「运行发布步骤」于『更新远端 yybs 引用』于『git』于
        （「缀」于『fetch』于（「缀」于『origin』于（「缀」于『yybs』于「罄」）））而
    虑「当前提交」者「取得命令输出」于『git』于
        （「缀」于『rev-parse』于（「缀」于『HEAD』于「罄」））而
    虑「远端提交」者「取得命令输出」于『git』于
        （「缀」于『rev-parse』于（「缀」于『origin/yybs』于「罄」））而
    虑「无」者若「串相等」于「当前提交」于「远端提交」
    则「元」
    否则「发生事故」于
        （『当前提交尚未推送到 origin/yybs：「：换行：」当前：』附「当前提交」附
         『「：换行：」远端：』附「远端提交」）而
    虑「已跟踪改动」者「取得命令输出」于『git』于
        （「缀」于『status』于
        （「缀」于『--porcelain』于
        （「缀」于『--untracked-files=no』于「罄」）））而
    虑「无」者若「串相等」于「已跟踪改动」于『』
    则「元」
    否则「发生事故」于（『存在尚未提交的已跟踪改动，拒绝发布：「：换行：」』附「已跟踪改动」）而
    「当前提交」
也。

「写兼容构建入口」乃化「字符串」而「有」也。
「写兼容构建入口」者会「自举包目录」而
    虑「兼容入口路径」者「路径合并」于「自举包目录」于『build.sh』而
    虑「无」者「写文件」于「兼容入口路径」于
        『#!/bin/sh
set -eu
cd "$(dirname "$0")"
./构建.sh
cp -f 豫言自举编译器 yy_bundled_bs
』而
    虑「无」者「运行发布步骤」于『赋予兼容构建入口执行权限』于『chmod』于
        （「缀」于『u+x』于（「缀」于「兼容入口路径」于「罄」））而
    「元」
也。

「上传失败并尝试回滚」乃化「字符串」而化（
    「爻」「：退出状态：」合
    「字符串」「：标准输出：」合
    「字符串」「：错误输出：」
）而「字符串」也。
「上传失败并尝试回滚」者会「备份资产路径」而会「上传结果」而
    虑「回滚结果」者「获取输出」于『gh』于
        （「缀」于『release』于
        （「缀」于『upload』于
        （「缀」于『continuous-bootstrap』于
        （「缀」于「备份资产路径」于
        （「缀」于『--clobber』于
        （「缀」于『--repo』于
        （「缀」于『yuyan-lang/yuyan』于「罄」）））））））而
    若「回滚结果」中「零」
    则「命令失败文字」于『发布新资产失败；旧资产已经自动恢复』于「上传结果」
    否则（「命令失败文字」于『发布新资产失败』于「上传结果」）附
        『「：换行：」自动恢复旧资产也失败：』附
        （「命令失败文字」于『回滚失败』于「回滚结果」）
也。

「发布连续自举包」乃化「有」而「有」也。
「发布连续自举包」者会「无」而
    虑「提交」者「要求仓库状态适合发布」于「元」而
    虑「临时根」者「取得命令输出」于『mktemp』于
        （「缀」于『-d』于（「缀」于『/tmp/yuyan-continuous-bootstrap.XXXXXX』于「罄」））而
    虑「二级编译器」者「路径合并」于「临时根」于『yy2_bs』而
    虑「三级编译器」者「路径合并」于「临时根」于『yy3_bs』而
    虑「四级编译器」者「路径合并」于「临时根」于『yy4_bs』而
    虑「自举包目录」者「路径合并」于「临时根」于『yy-bootstrap』而
    虑「压缩包路径」者「路径合并」于「临时根」于『yuyan-bootstrap-bundle.tar.gz』而
    虑「备份目录」者「路径合并」于「临时根」于『发布前备份』而
    虑「备份资产路径」者「路径合并」于「备份目录」于『yuyan-bootstrap-bundle.tar.gz』而
    虑「核验目录」者「路径合并」于「临时根」于『发布后核验』而
    虑「核验资产路径」者「路径合并」于「核验目录」于『yuyan-bootstrap-bundle.tar.gz』而

    虑「无」者「打印行」于（『将发布提交：』附「提交」）而
    虑「无」者「打印行」于（『临时及回滚目录：』附「临时根」）而
    虑「无」者「运行发布步骤」于『检查 GitHub 登录状态』于『gh』于
        （「缀」于『auth』于（「缀」于『status』于「罄」））而

    虑「无」者「运行发布步骤」于『构建二级编译器』于『./yy_bs_stable』于
        （「缀」于『豫言编译器/入口。豫』于
        （「缀」于『-o』于
        （「缀」于「二级编译器」于
        （「缀」于『--parallel』于「罄」））））而
    虑「无」者「运行发布步骤」于『构建三级编译器』于「二级编译器」于
        （「缀」于『豫言编译器/入口。豫』于
        （「缀」于『-o』于
        （「缀」于「三级编译器」于
        （「缀」于『--parallel』于「罄」））））而
    虑「无」者「运行发布步骤」于『构建四级编译器』于「三级编译器」于
        （「缀」于『豫言编译器/入口。豫』于
        （「缀」于『-o』于
        （「缀」于「四级编译器」于
        （「缀」于『--parallel』于「罄」））））而
    虑「无」者「运行发布步骤」于『检查三级与四级编译器固定点』于『cmp』于
        （「缀」于「三级编译器」于（「缀」于「四级编译器」于「罄」））而

    虑「无」者「运行发布步骤」于『生成可移植自举包』于「三级编译器」于
        （「缀」于『豫言编译器/入口。豫』于
        （「缀」于『--emit-bootstrap-bundle』于
        （「缀」于「自举包目录」于
        （「缀」于『--parallel』于「罄」））））而
    虑「无」者「写兼容构建入口」于「自举包目录」而
    虑「无」者「运行发布步骤」于『从可移植包重新构建稳定编译器』于
        （「路径合并」于「自举包目录」于『build.sh』）于「罄」而
    虑「无」者「运行发布步骤」于『检查可移植包编译器版本』于
        （「路径合并」于「自举包目录」于『yy_bundled_bs』）于
        （「缀」于『--version』于「罄」）而
    虑「无」者「运行发布步骤」于『压缩可移植自举包』于『tar』于
        （「缀」于『-czf』于
        （「缀」于「压缩包路径」于
        （「缀」于『-C』于
        （「缀」于「临时根」于
        （「缀」于『yy-bootstrap』于「罄」）））））而

    虑「无」者「运行发布步骤」于『创建远端资产备份目录』于『mkdir』于
        （「缀」于『-p』于（「缀」于「备份目录」于「罄」））而
    虑「无」者「运行发布步骤」于『下载当前 continuous-bootstrap 资产作为回滚备份』于『gh』于
        （「缀」于『release』于
        （「缀」于『download』于
        （「缀」于『continuous-bootstrap』于
        （「缀」于『--pattern』于
        （「缀」于『yuyan-bootstrap-bundle.tar.gz』于
        （「缀」于『--dir』于
        （「缀」于「备份目录」于
        （「缀」于『--clobber』于
        （「缀」于『--repo』于
        （「缀」于『yuyan-lang/yuyan』于「罄」））））））））））而

    虑「无」者「打印行」于『==> 发布新的 continuous-bootstrap 资产』而
    虑「上传结果」者「获取输出」于『gh』于
        （「缀」于『release』于
        （「缀」于『upload』于
        （「缀」于『continuous-bootstrap』于
        （「缀」于「压缩包路径」于
        （「缀」于『--clobber』于
        （「缀」于『--repo』于
        （「缀」于『yuyan-lang/yuyan』于「罄」）））））））而
    虑「无」者若「上传结果」中「零」
    则（
        「打印行」于（「上传结果」中「一」）；
        「标准错误打印行」于（「上传结果」中「二」）；
        「元」
    ）
    否则「发生事故」于（「上传失败并尝试回滚」于「备份资产路径」于「上传结果」）而

    虑「无」者「运行发布步骤」于『创建发布后核验目录』于『mkdir』于
        （「缀」于『-p』于（「缀」于「核验目录」于「罄」））而
    虑「无」者「运行发布步骤」于『重新下载已发布资产』于『gh』于
        （「缀」于『release』于
        （「缀」于『download』于
        （「缀」于『continuous-bootstrap』于
        （「缀」于『--pattern』于
        （「缀」于『yuyan-bootstrap-bundle.tar.gz』于
        （「缀」于『--dir』于
        （「缀」于「核验目录」于
        （「缀」于『--clobber』于
        （「缀」于『--repo』于
        （「缀」于『yuyan-lang/yuyan』于「罄」））））））））））而
    虑「无」者「运行发布步骤」于『逐字节核验远端资产』于『cmp』于
        （「缀」于「压缩包路径」于（「缀」于「核验资产路径」于「罄」））而

    虑「无」者「打印行」于『continuous-bootstrap 资产发布并核验成功。』而
    虑「无」者「打印行」于（『如需回滚，旧资产保存在：』附「备份资产路径」）而
    「元」
也。

「打印用法」乃化「有」而「有」也。
「打印用法」者会「无」而
    「标准错误打印行」于
        『用法：
  ./yy_bs_stable 工具/发布连续自举包。豫 -o 发布连续自举包
  ./发布连续自举包 --publish
』
也。


鉴「获取参数」于「元」而
    有（「缀」于『--publish』于「罄」）则「发布连续自举包」于「元」
    或有「其他」则（「打印用法」于「元」；「退出」于「二」）。
